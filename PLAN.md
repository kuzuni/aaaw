# 꼬마기사 키우기 — 구현 인수인계 문서 (PLAN.md)

> 이 문서는 다른 세션/계정이 **이 문서만 보고** 남은 작업을 그대로 이어서 완성할 수 있도록 쓴 완전한 스펙 + 작업 지시서다.
> 유저와 이미 합의된 사항이므로 **여기 적힌 내용을 임의로 바꾸지 말 것.** 새로운 아이디어(신규 특전, 신규 시스템)를 추가하려면 반드시 유저에게 먼저 목록/계획을 보여주고 승인받은 뒤 구현할 것. (유저가 여러 번 강하게 요구한 작업 방식임)

---

## 0. 프로젝트 개요

- **게임**: 세로형(9:19) 캐주얼 자동전투 방치형 RPG. 단일 HTML 파일(`index.html`)로 구현. 외부 라이브러리 없음, 그래픽은 Canvas로 직접 그림(**참고 스크린샷: 레포의 `docs/ref/*.jpg`** — 주인이 2026-09-02 원본 이름 그대로 등록. Legend of Slime 류 카툰 스타일. 구 경로 `C:\Users\user\Desktop\올빼미\` 는 로컬 전용이라 폐기), 오디오는 Web Audio 합성.
- **레포**: `C:\Users\user\Documents\GitHub\aaaw` (main 브랜치, origin 원격 있음)
- **배포된 아티팩트**: https://claude.ai/code/artifact/228cba1b-db0b-4aef-b5ef-7aa344890524
  - 다른 대화에서 업데이트하려면 Artifact 도구에 `url`로 이 주소를 넘겨서 republish (url 없이 publish하면 새 아티팩트가 생기니 주의).
- **파일 현황**:
  - `index.html` — 게임 본체. **현재 중간 상태**: 특전 65종 버전(메테오 포함, 구버전 목록)이 들어있음. 아래 최종 스펙으로 **재작성 필요**. (⚑ T96·T100 으로 이식이 끝나 이 «중간 상태» 문면은 이력이다 — 현행은 특전 10종 고정·챕터 300 이고 `expNeed` 는 **주인 확정(2026-09-03 · T100) `5*lv+1`** 이다. §2.4)
  - `sim.js` — 밸런스 시뮬레이터 (Node로 실행). 최종 특전 102종/챕터 생성 로직이 이미 구현되어 있음. **게임과 수식을 공유하는 기준 코드**로 쓸 것.
  - `PLAN.md` — 이 문서.

## 1. 남은 작업 체크리스트 (순서대로)

1. [ ] `node sim.js 1` / `node sim.js 2` / `node sim.js 3` 실행해서 밸런스 검증 (섹션 7)
2. [ ] 시뮬 결과로 `sim.js`의 `TUNE` 값과 특전 수치 조정 (섹션 7의 합격 기준 충족까지 반복)
3. [ ] 조정 끝난 최종 수치/특전을 `index.html`에 이식 (섹션 2~6 스펙대로 전면 재작성)
4. [ ] 브라우저에서 실제 플레이 확인 (챕터1 클리어, 특전 획득, 이벤트 3종, 보스, 사망/클리어 흐름)
5. [ ] 아티팩트 republish (위 URL로)
6. [ ] git commit + push, GitHub Pages 활성화 (섹션 9)

---

## 2. 확정된 게임 스펙 (유저와 합의 완료 — 변경 금지)

### 2.1 화면/기본
- 프레임: **9:19 세로**, 화면 중앙 배치, `aspect-ratio:9/19`.
- **⚑ 주인 지시(2026-09-02): 모바일 크롬에서 글씨·UI가 축소돼 보이는 문제 수정.** 원인 = viewport 메타 부재(모바일이 데스크톱 폭으로 렌더 후 축소). `<head>` 에 `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, viewport-fit=cover">` 필수(핀치줌 금지는 게임이라 기본값 — 위임). 모바일에서는 9:19 프레임이 화면을 꽉 채우고 데스크톱에서는 중앙 배치 유지. **T3 검증에 «모바일 폭(390px급)에서 글씨/UI 크기 정상» 항목 추가.**
- 폰트: Google Fonts **Jua**. 카툰/캐주얼 스타일, 다크 패널 + 골드 액센트 (현 index.html 스타일 유지).
- 오디오: **BGM 없음. 효과음만** (Web Audio 합성; click/hit/crit/hurt/arrow/bolt/throw/coin/poof/shield/evade/levelup/perk/heal/devil/angel/boss/win/lose). 음소거 토글은 로비/게임 양쪽 🔊 버튼, localStorage 저장.
- 저장(localStorage, key `kkoma-knight-v1`): `{gold, maxChapter, selChapter, muted, up:{dmg,hp,aspd,crit}}`. try/catch 필수.

### 2.2 로비 (참고: 메인로비.jpg)
- 다크그린 배경 + ⚔ 워터마크, 상단: 아바타(캔버스로 기사 그림) + ⚔️전투력 + 🪙골드 + 🔊.
- 중앙: `CHAPTER N` + 밑줄 + **챕터 디오라마 카드**(챕터별 시드로 나무/바위/수풀 배치가 달라지는 캔버스) + ◀▶ 화살표(해금 범위 내 이동).
- 주황 **START** 버튼.
- 하단 5탭: 상점(🔒잠금 토스트), **강화**(영구 강화 팝업), 전투(현재), 카드(🔒), 도전(🔒).
  - **⚑ 주인 지시(2026-09-02, §11 장비 시스템 도입): 아래 «영구 강화 4종» 은 통째로 폐지.** 성장 축은 §11 의 장비 + 슬롯 강화로 대체된다. 탭 재구성(위임 규약 기본값): **강화 탭 → 장비 탭**(장착/합성/슬롯 강화), **상점 탭 해금**(다이아 구매 + 장비 뽑기), 카드/도전은 🔒 유지. 아래 4종 수치·비용·R05 성장률 기록과 «강화 상한 티어 시스템» 지시(같은 날 07:4X)는 **대상 소멸로 함께 폐기** — 골고루 유도 취지는 §11.4 슬롯 균등 보너스가 계승한다. 저장 포맷의 `up:{}` 도 장비/슬롯/다이아 저장으로 교체(§11).
- ~~영구 강화 4종 (골드 소모, 죽어도 유지):~~ (폐지 — 위 지시. 아래 기록은 되돌림용 보존)
  - 공격력: `30+8*Lv`, 비용 `floor(40*1.32^Lv)`
  - 체력: `300+60*Lv`, 비용 `floor(40*1.30^Lv)`
  - 공격속도: `1+0.03*Lv`/s, 비용 `floor(60*1.38^Lv)`
  - 치명타: `5+Lv`%, 비용 `floor(55*1.36^Lv)`
  - ※ 시뮬 결과에 따라 비용 성장률 조정 가능 (섹션 7)
  - **⚑ 주인 지시(2026-09-02): 강화 상한 티어 시스템 (골고루 강화 유도).** 각 강화의 최대 레벨은 **10렙에서 시작**하고, **4종이 전부 현재 상한에 도달하면 상한이 +10** 된다 (전부 10렙 → max 20렙, 전부 20렙 → max 30렙, … 무한 반복). 수식(위임 규약 기본값): `각 강화의 상한 = 10 * (1 + floor(min(4종 레벨)/10))` — 하나라도 뒤처지면 나머지가 그 구간에 묶이므로 골고루 올릴 수밖에 없다. UI: 상한에 걸린 강화 버튼은 «MAX — 모든 강화 N렙 달성 시 해제» 표시(위임 규약 기본값). sim.js 실험3의 자동 강화 구매도 이 상한을 따라야 함 — **밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «강화상한 반영» 표시.**
  - **R05 확정 비용 성장률** (수치식은 그대로, 성장률만 조정 — 근거 `docs/balance/round-05/`):
    공격력 `floor(40*1.055^Lv)` · 체력 `floor(40*1.055^Lv)` · 공격속도 `floor(60*1.075^Lv)` · 치명타 `floor(55*1.075^Lv)`
  - **R05 확정 골드 드랍**: 처치 `round((0.6+0.10*챕터)*rand(1,1.8)*goldMul)`, 클리어 보너스 `3*챕터`
    (초기값 `(2+챕터)` / `40*챕터` 에서 하향 — 비용이 싸진 만큼 수입을 줄여 강화 레벨 곡선을 유지)

### 2.3 인게임 (참고: 메인 게임화면*.jpg)
- 상단: 💀처치수 + 🪙골드 + 🔊 + ☰(일시정지: 이어하기/포기). 중앙 상단: CHAPTER N + 진행도 바(처치수/전체).
- 좌하단 배속 버튼: x1 → x1.5 → x2.
- 하단 다크 패널: `Lv/EXP바(초록)`, `HP바(빨강)`, `실드바(파랑)` + 스탯 그리드 8종(공격력/방어력/공격속도/반격확률/치명타확률/회피/치명타배율/흡혈). 특전으로 기본값보다 오른 스탯은 **초록색**. 버프 반영된 **실효값(eff)** 표시.
- 우하단 **📘 Info 버튼** → 이번 런에서 얻은 특전 목록 팝업 (등급 테두리 카드, "화면을 탭하면 닫힘"). 천사의 축복도 기록됨.
- **⚑ 주인 지시(2026-09-02): 발동 중 버프 아이콘 표시.** 시간제 버프(예: «3초간 회피 +10%»)가 발동 중일 때 그게 켜져 있는지 보이도록, **화면 왼쪽 위·챕터 표시보다 아래** 위치에 해당 특전의 아이콘을 표시한다. **등급색 테두리 포함.** 중첩 중이면 **아이콘 오른쪽 위에 중첩 수**(예: 3)를 뱃지로. 버프가 끝나면 아이콘 제거. 여러 버프 동시 발동 시 세로로 쌓는다(위임 규약 기본값 — 오래된 것이 위).
- **⚑ 주인 지시(2026-09-02): 얻은 특전 미리보기 줄.** 인포를 펼치지 않아도 뭘 얻었는지 보이게, **📘 Info 버튼이 있는 행의 왼쪽 끝부터** 이번 런에서 얻은 특전 아이콘을 **획득 순서대로** 나열한다. **등급색 포함.** 같은 특전 중복 획득은 아이콘 1개 + 오른쪽 위 개수 뱃지, 한 줄을 넘치면 최신 것들이 보이게 하고 «+N» 으로 합침(위임 규약 기본값 — 전체 목록은 인포 팝업).
- **⚑ 플레이어 기본 스탯 (주인 확정 2026-09-03 — 노브 아님).** 아무 장비·특전도 없는 상태의 시작값이다. 종전엔 치명타 배율·방어력·반격 확률·회피 넷이 엔진 `mkPlayer` 에 리터럴로만 있고 여기 값이 없었다(T27 «미문서 상수 4종» · 승인 대기 22번) — 주인이 값을 확정하면서 그 안건이 종결됐다. 공/체/실 기본치는 §11.7 노템 25/150/250 을 그대로 쓴다.

  | 스탯 | 확정값 | 엔진 상수 |
  |---|---|---|
  | 공격속도 | 1.0 /s | `TUNE.pAspd0` |
  | 치명타 확률 | 20% | `TUNE.pCrit0` |
  | 치명타 데미지 | 150% | `TUNE.pCritF0` |
  | 반격 확률 | 20% | `TUNE.pCounter0` |
  | 방어력 | 20 | `TUNE.pDef0` |
  | 회피 | 20 | `TUNE.pEvade0` |

  기존 «치확 5 · 치배 200 · 반격 10 · 방어 5 · 회피 8» 을 전면 교체한 값이라 **밸런스 영향이 크다 — 반영 전 측정치는 무효**이고, T1 회차 로그에 «기본스탯 반영» 을 표시한다. `sim.js`·`index.html` 동시 반영이며 `tools/verifyCombatConst.js` ① 이 이 표와 두 엔진을 상시 대조한다.
- **⚑ 밸런스 기준점 (주인 확정 2026-09-03).** 실험 하니스의 표준 장비는 **희귀 풀셋**(공 100 / 체 500 / 실 800)이다. 특전 실험(§7 실험1·2)은 «위 기본 스탯 + 희귀 풀셋» 플레이어로 재는 것이 정본이고, 측정 챕터는 §7 «변별점 규칙» 대로 고른다.
- 전투 규칙:
  - 플레이어는 자동으로 우측 전진(속도 `132*walkMul`), 가장 가까운 적이 74px 안이면 멈추고 공격(공속 = 실효 aspd, 공격당 1.0 쿨).
  - **적은 절대 이동하지 않음. 스폰 위치에 고정.** 근접 적은 플레이어가 105px 안에 오면 그 자리에서 공격(쿨 1.3s). 원거리 적(40% 확률, 웨이브 첫 마리는 제외)은 440px 안에서 화살 발사(쿨 2.1s, 화살 속도 330). **⚑ 챕터별 고정 — 원거리 «자리» 는 챕터 시드로 굴린다(T105, 주인 확정 2026-09-03 17:0X «같은 챕터 같은 원거리 자리»)**: 종전에는 판을 시작할 때마다 `Math.random()` 으로 새로 굴려 같은 챕터라도 판마다 원거리 자리·마릿수가 달랐다. 이제 `chapterLayout(c)` 안에서 챕터 시드 RNG 로 굴려 웨이브 노드에 `ranged[]` 로 싣고, 두 엔진의 웨이브 생성부는 그것을 읽기만 한다. **확률 40%·첫 마리 제외는 그대로**(바뀐 것은 자리의 고정이지 비율이 아니다) · 보스는 원거리 아님. 게이트 `verifyChapterFixed` ⓔ~ⓙ.
  - 적 간격 88px. HP바(숫자 포함)를 적 발밑에 표시. 플레이어는 HP바+실드바 2단.
  - 보스: 크기 1.7배, 뿔 달린 스킨, 3번째 공격마다 강타(x2.2, 화면 흔들림+경고 텍스트). 접근 시 "⚠️ BOSS ⚠️" 경고 + 효과음.
  - 실드: ~~`maxSh = maxHp*0.8`(기본).~~ **⚑ 주인 확정(2026-09-02 스탯 사다리 개편): 실드는 최대체력 파생값이 아니라 «독립 스탯»이다.** 장비가 공격력·체력·**실드** 3스탯을 각각 기여하고 기본 실드는 노템 250 (§11.5-a). 피해는 실드 먼저 소모. ~~**웨이브(보스 제외) 전멸 시 실드 완전 충전.**~~ **⚑ 주인 지시(2026-09-02): 웨이브 전멸 실드 충전 폐지.** 실드는 공짜로 재충전되지 않는다 — 충전 수단은 **특전뿐**(🔰 처치 시 충전, 🔋 피격 시 충전, 💠 초과 회복 등). 시작 시 가득 찬 실드가 닳으면 특전 없이는 그대로 끝. sim.js 에도 웨이브 전멸 충전이 있으면 제거 — **밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «실드무료충전폐지 반영» 표시.**
  - 회피/방어/반격/흡혈/치명타 모두 스크린샷 게임과 동일 개념.
  - **⚑ 주인 확정(2026-09-02 15:4X): 적 전원 회피율 10% 고정.** 모든 적(보스 포함 — 위임 기본값)은 플레이어의 타격(기본공격·소환·반격 전부 — 위임 기본값)을 10% 확률로 회피한다. 회피 시 데미지 0 + «MISS» 텍스트 팝(위임). **적중률(명중률) 스탯/옵션은 이 게임에 존재하지 않는다** — 적 회피 10% 를 뚫는 수단(특전·장비 옵션·버프) 추가 금지(흡혈 금지와 같은 축). 수치 10% 는 주인 확정 상수라 튜닝 노브 아님. sim.js·index.html 동시 반영, 반영 전 측정치 무효(«적회피 반영» 표시).
- 연출(이미 구현됨, 유지): 타격 스파크, 크리 화면흔들림, 처치 연기+코인 텍스트, 데미지 숫자 팝(크리는 크고 금색), 캐릭터 런지/스쿼시, 레벨업 팝업 회전광선+배너드롭+카드 순차등장.

### 2.4 챕터 구조 (1~20)
- 챕터 수: **⚑⚑⚑ 주인 정정(2026-09-03 · T103): 420개로 확장** (종전 20 → 100 → 300 → 500 → 600 → 420. 이 줄에 옛 값을 남기지 말 것 — 아래 웨이브 줄과 같은 이유로 게이트가 이 문장에서 숫자를 읽는다). 클리어 시 다음 챕터 해금(순차), 420 클리어 후 "모든 챕터 클리어" 처리. 벽: 기존 10·15·**90 대형 벽** 유지 + **420 최종 벽 위치**. **⚑ 주인 정정 — 사다리 8점 맨 아랫줄 «신화9강+슬롯100» 이 600 → 420 으로 내려왔고**(주인 원문 «신화9강+슬롯50 -> 380, 신화9강+슬롯100 -> 420 정도로 하자 챕터 밸런스»), T102(500)·T104(600) 선례대로 **«맨 아랫줄 과녁 = 챕터 수 = 최종 벽 위치»** 라 챕터 상한·최종 벽 위치도 420 이다(위임 기본값 — 주인이 600 유지를 원하면 한 줄이면 되돌린다). 10·15 벽은 위치 고정. **⚑ T103 재적합 결과 최종 벽 배수는 ×1.00/×1.00(꺼짐)** — 아래 §6 참조.
- 레이아웃은 **챕터 번호 시드 기반 결정적 생성** (`sim.js`의 `chapterLayout(c)` 그대로 사용). **⚑⚑⚑ 주인 확정(2026-09-03 17:0X · T105) — 여기에 «적의 원거리 자리» 도 포함된다**: 주인 «쉼터, 악마, 천사, 적들 배치가 전부 챕터별로 고정이어야 함». 쉼터·악마·천사의 배치 순서, 웨이브 수·마릿수, 적 간격은 이미 챕터 시드로 고정이었고, **매판 굴리던 원거리 여부만 남아 있던 것을 T105 가 챕터 시드로 옮겼다**(§2.3). 시드 스트림 소비 순서는 «이벤트 셔플 → 원거리» 로 못 박는다 — 그래야 챕터마다 이미 정해져 있던 이벤트 자리가 한 챕터도 안 바뀐다(게이트 ⓙ 골든 지문이 그 불변을 지킨다). 스킨·첫 공격 타이머·전투 난수(치명·회피·소환)는 «배치» 가 아니라 연출·전투 난수라 계속 매판 굴린다(위임 기본값). **T105 실측(챕터 1~420 · 상한은 T103 확정값)**: 원거리 비율 **37.45%**(11,797/31,500) · 챕터당 원거리 **최소 14 · 최대 41 · 평균 28.09마리**. ⚠ 챕터당 14~41 은 3배 폭이라 **원거리 축의 인접 챕터 난이도 역전이 실재한다**(과녁 ch5 는 30 인데 바로 옆 ch6 은 14 = 전 챕터 최소). 패턴·비율은 임의로 손대지 말 것(주인 지시) — 밸런스 작업은 대표 챕터 표에 «원거리 마릿수» 열을 두고 보고한다.
  - **⚑⚑⚑ T107 확정: 웨이브 5개 · 챕터별 적 수 곡선 — 1~5챕터는 17마리, 6챕터부터 챕터당 +1, 상한 50마리 (보스 포함 총 수).** 일반 적 = 총 수 − 1 을 웨이브 5개에 균등 분배하고 나머지는 앞 웨이브부터 준다(16 → 4·3·3·3·3 · 49 → 10·10·10·10·9 — 위임 기본값). — 이 줄에 옛 값을 취소선으로 남겨 두지 말 것: 게이트(`verifyCombatConst` ①)가 이 문장에서 숫자를 읽어 엔진과 대조하므로, 같은 모양의 옛 숫자가 앞에 남아 있으면 그쪽을 먼저 읽는다.
  - **⚑⚑⚑ 주인 확정 «챕터별 적 수 곡선» (2026-09-03 17:3X · T107 — T100 의 «전 챕터 76마리» 고정 구성을 폐기한다). 주인 원문: «그 적수를 1챕터~5까지 17마리, 6챕터부터 1 올라갈 때마다 1개씩 추가, 그리고 적 개수 50개 되면 더이상 안 올리기» (처음 76 이라 했다가 «50마리 넘으면 피로하다» 로 50 확정).** 공식은 `N(c) = c ≤ 5 ? 17 : min(50, 17 + (c − 5))` — 챕터 6 = 18 · 7 = 19 · … · **38 챕터부터 50 고정**이고, 이 수는 **보스를 포함한 총 수**다(위임 기본값). 나머지 구성은 전 챕터 동일하다: 웨이브 **5개** · 보스 **1**(항상 마지막) · 쉼터 **2 (고정)** · 악마 **1** · 천사 **1** — 적 총 수는 전 챕터 **상한 50 이내**이고, 제비뽑기는 «이벤트 배치 순서» 에만 남는다. 일반 적 N−1 은 웨이브 5개에 **최대한 균등 분배·나머지는 앞 웨이브부터** 나눈다(위임 기본값 — 16 → 4·3·3·3·3 · 49 → 10·10·10·10·9). **⚑ 이 곡선은 챕터 1~420 전 구간에 적용된다** (후반 난이도는 마릿수가 아니라 적 스탯으로 낸다 — 주인 보강 조항은 그대로). 게이트 `tools/verifyChapterFixed.js` 가 챕터 전수로 공식 일치·단조 비감소·상한·구성·특전 개수를 실측한다. **T107 실측**: 적 총 수 챕터 1~5 = 17 · 6 = 18 · 15 = 27 · 28 = 40 · 38 이후 = 50(최대 50 · 단조 비감소) · 이벤트 배치 순서 12종.
  - **⚑ 특전은 «그 챕터의 경험치가 주는 만큼» 이다 (⚑⚑⚑ 주인 확정 2026-09-03 17:3X · T107 — 주인 «특전은 걍 되는 만큼으로 하셈. 3개 할 정도였으면 순서대로 3개 정도만. 특전 사실 꼭 10까지 안 떠도 되긴 할듯»).** T100 의 «완주 = 정확히 특전 10개» 항등식은 이 지시로 폐기됐다. 경험치 상수는 전부 불변이다 — 처치 **3** · 보스 **9** · 쉼터 **26**×2 · `expNeed = 5·Lv + 1`. 레벨업마다 §3.1 순서대로 한 장, 악마는 다음 순번을 앞당기고, 챕터를 끝내는 보스 처치 레벨업은 특전을 주지 않는다(규칙 전부 그대로). 그래서 한 판에 얻는 수는 **보스 전 공급 = (N−1)×3 + 52** 가 정하고, 실측은 **챕터 1~5 = 6개 · 15 = 7개 · 28 = 8개 · 38 이후 = 9개(최대)** 다. `PERK_PICKS = 10`·풀 10종은 그대로 두되 **10번(방어력 +10%)은 상한 50 에서 어느 챕터에서도 안 나온다**(49×3 + 52 = 199 < 9레벨 누적 234) — 주인이 «꼭 10까지 안 떠도 된다» 로 승인했다(빼거나 순서를 바꾸는 것은 주인 몫).
  - 웨이브 사이 이벤트: ~~rest(쉼터)/devil(악마)/angel(천사)를 가중치(45/30/25%)로 배치하되 **쉼터 최소 1회 보장** (2~3회 나올 수도 있고, 악마나 천사 중 하나만 나올 수도 있음)~~ → **위 확정 제약으로 대체(2026-09-02)**: 악마 1·천사 1 을 먼저 배치하고 남는 슬롯은 전부 쉼터(1~4 클램프), 슬롯 내 순서만 시드 셔플(위임 규약 기본값).
  - 마지막은 보스 1마리.
- 난이도: `sim.js`의 `enemyStats(c,w)` 사용. **챕터 10부터 벽(배수 점프)이 있어 강화 노가다 없이는 클리어가 빡세야 함.** 15에도 2차 점프. 정확한 수치는 시뮬 튜닝 결과를 따름 (섹션 7).
- **⚑ 주인 확정(2026-09-02 스탯 사다리 개편): 챕터당 성장률 단일상수(`eHpG 1.185`/`eDmgG 1.08`) 폐기 → «구간별 성장률».** 주인이 확정한 등급별 절대 스탯 사다리(§11.7)에서 역산한다. 현행 단일 18.5%/챕터는 챕터 5→260 에서 적을 ×1.3e20 으로 키우는데 주인 스탯표는 ×104 라 **17자리가 어긋나** 양립 불가였다. 구간표는 §11.7.
- **⚑ 불변 규약(주인 확정 2026-09-03): 이벤트·팝업이 열려 있는 동안 게임 시간은 완전히 멈춘다.** 쉼터·악마·천사·레벨업 특전 선택·일시정지 등 팝업이 떠 있는 사이에는 시계(`G.t`)·체력·실드·쿨다운·적 이동이 **한 틱도 흐르지 않는다** — 쉼터에서 팝업을 열어 둔 채 기다려 회복이나 쿨다운을 공짜로 버는 악용을 원천 차단한다. 구현은 «팝업을 여는 자리는 `openOverlay` 하나뿐이고 그 안에서 정지, 메인 루프는 정지 중 `update` 를 아예 안 부른다» 이며, 화면 그리기는 계속한다. 회귀하면 게이트가 빨개진다(`verifyT2` ㊴ 정적 · T3 `battle.js` 실측).
- 이벤트 상세:
  - **쉼터 🏕️**: ~~"❤️ 최대체력의 40% 회복" vs "🌟 경험치 +10"~~ **⚑ 주인 확정(2026-09-02 17:1X): "❤️ 체력 260 회복" (고정값 — 최대체력 비율 아님) vs "🌟 경험치 +26"** 둘 중 선택. sim.js·index.html 동시 반영 (시뮬 정책은 그대로 항상 경험치 선택 = +26). 회차 로그 «쉼터260/26 반영» 표시.
  - **악마 😈**: **지불하기 전에 어떤 특전인지 카드로 먼저 보여줌.** **⚑⚑ 주는 것 = 다음 순번 특전 하나를 앞당겨 준다** (2026-09-03 T96 — 특전이 순서 고정이라 «전설 확정» 은 의미를 잃었다. 위임 기본값). 줄 특전이 남아 있지 않으면 거래 자체가 성립하지 않는다(비용도 안 낸다 — 위임). 선택지: "최대 체력의 30% 지불(-N)하고 위 특전 획득" vs "지나간다". 수락 시 특전 획득 연출(광선). **⚑ 주인 확정(2026-09-03): 지불은 현재체력이 아니라 «최대체력에서» 깎는다** — 그 판 동안 최대체력 자체가 30% 줄어든 채 진행(현재체력이 새 최대치를 넘으면 클램프 — 위임). **시뮬 정책: 가상 플레이어는 악마 항상 수락** (승인 32번 종결).
  - **천사 😇**: "공격력 +5% (무료)" vs "📺 광고 보고 +15%" (광고는 3초 카운트다운 모의 광고). 버프는 해당 런 한정. **⚑ 시뮬 정책(주인 확정 2026-09-03): 가상 플레이어는 항상 왼쪽(무료 +5%) 선택.**
- 경험치: **일반 적 처치 +3, 보스 +9.** 레벨업 필요량: ~~`4+2*Lv`~~ ~~`4+4*Lv`~~ ~~`4+3*Lv`~~ → **⚑⚑ 주인 확정(2026-09-03 · T100): `5*Lv+1`** (Lv1→**6**, Lv2→11, Lv3→16 …, **레벨마다 +5씩 증가**). 주인 원문: «경험치 6으로 시작해야 하고 레벨업당 필요 경험치가 5씩 증가. 적 죽이면 경험치 3씩.» 10레벨까지 누적 Σ(5·L+1) = **285** 다. ⚑ **T107 로 «완주 = 특전 정확히 10개» 항등식은 폐기**됐다 — 챕터마다 적 수가 다르므로(위 곡선) 보스 전 공급 **(N−1)×3 + 52** 가 그 챕터의 특전 수를 정하고, 실측은 **1~5 = 6개 · 15 = 7 · 28 = 8 · 38 이후 = 9(최대)** 다. **적 처치 경험치 3 은 주인 지시대로 불변.** sim.js·index.html 동시 반영.
- **⚑ 주인 지시(2026-09-03): 전투 HUD 경험치 바에 «레벨» 을 쓰지 않는다.** 캡 표기는 참고 스크린샷(`docs/ref/메인 게임화면.jpg`)대로 **«EXP»** 고, `Lv«레벨»` 표기는 폐지다. 레벨 자체는 그대로 산다(필요 경험치 `5*Lv+1` · 레벨업 특전 지급 · 레벨업 팝업 문구) — 없애는 것은 **경험치 바의 표시**뿐이다. 캡이 ❤️·🛡️ 한 글자보다 넓어졌으므로 경험치 바는 «절대배치 캡 + 가운데 숫자» 대신 가로 flex(캡 → 남은 폭 가운데 숫자)로 깔고, 글자는 T54 선례대로 프레임 폭에 연동해 좁은 기기에서 줄인다. 게이트: `verifyT2` ㉓(3-b) 정적 · T3 `boot.js` ④ 실측(3자리 레벨 «395/400» 에서 겹침·잘림 0).
- **⚑ 주인 지시(2026-09-02): 레벨업의 효과는 특전 선택지 하나뿐이다.** 레벨업 시 체력/실드 회복 금지, 최대 체력/최대 실드 증가도 금지 — 어떤 스탯 보정도 없이 **특전 하나만** 준다 (⚑ 2026-09-03 T96 — 3택 선택창은 폐지, §3.1 순서대로 자동 획득). (현 index.html 이 레벨업마다 풀충전되는 구버전 동작 — 주인 관측. 제거 대상. 특전 자체의 회복/최대치 증가 효과 ❤️/🔷/🦾 등은 무관하게 유지.) sim.js 에 같은 동작이 있는지 확인해 있으면 제거 — **밸런스에 유의미한 영향이므로 반영 전 회차 점수는 무효, 회차 로그에 «레벨업무보정 반영» 표시.**
- **⚑ 주인 지시(2026-09-02): 챕터를 끝내는 보스 처치 시에는 특전을 주지 않는다.** 보스 경험치(+9)는 그대로 받되, 그걸로 레벨업이 됐어도 레벨업 팝업 없이 **바로 클리어 화면으로** 간다 (어차피 런이 끝나므로). sim.js 도 동일하게(클리어 확정 후 특전 지급 없음).
- 골드: 처치 `round((0.6+0.10*챕터)*1.22^(챕터-1)*rand(1,1.8)*goldMul)`, 클리어 보너스 `3*챕터*1.22^(챕터-1)`
  (**T6 신설 `goldGrowth`, 현행값 1.22** — 챕터 300 확장 + 슬롯 강화 비용이 1.6^렙 으로 오르므로 수입도 지수로 따라가야 한다). 사망/포기해도 번 골드는 가져감.
  (**⚑ T16**: 이 두 숫자는 T1 회차마다 바뀌는 **경제 노브**다 — 이력은 T6 1.185 → R07 1.22, 슬롯 비용 T6 6.9^렙 → R06 5.5^렙 → R07 4.2^렙. 변경 시 §6·§11.5-a 와 함께 여기도 고칠 것. 게이트: `node tools/verifyPlanConst.js`)
  (초기 스펙은 `(2+챕터)` / `40*챕터` — R02 튜닝에서 하향, 근거 `docs/balance/round-02/`. §7 «골드 드랍» 노브)
- **⚑⚑⚑ 레벨업 → 특전을 «순서대로 하나» 자동 획득 (2026-09-03 주인 확정 · T96).** 선택창·등급 굴림·무료 새로고침·전지의 눈은 **전부 폐지**됐다. 순서와 효과는 §3.1 표가 정본이고, 10개를 다 얻은 뒤의 레벨업은 특전을 주지 않는다(위임 기본값).

---

## 3. 특전 시스템 최종 스펙 — **고정 10종 · 순서 획득** (⚑⚑⚑ 주인 확정 2026-09-03 · T96)

> **⚑ 이 절은 T96 에서 «132종(일반44·희귀46·전설42) 체제» 를 통째로 버리고 주인 확정본으로 교체한 것이다.**
> 주인 원문: «특전 싹 다 폐지하고, 아래 10개를 순서대로 선택하는 걸로 고정». 등급·선택창·무료 새로고침·
> 전지의 눈은 **전부 사라졌다**. 구 132종 목록의 정본이던 `docs/perk-redesign.md` 는 이력 문서로만 남는다.

### 3.0 공통 규칙

- **특전은 아래 10종이 전부다. 등급이 없다.** (특전의 일반/희귀/전설 3단, 등장 확률 45/35/20, 등급 색은 폐지.
  **장비의 등급 5단은 별개 — 그대로 유지**된다. §11 스탯 사다리·뽑기·합성은 이 절과 무관하다.)
- **획득 방식 = 레벨업할 때마다 표의 순서대로 하나씩 자동 획득.** 선택창·등급 굴림·새로고침 없음.
  **10개를 다 얻은 뒤의 레벨업은 특전을 주지 않는다**(위임 기본값 — 반복 획득을 원하시면 주인이 한 줄로 정정).
- **악마의 거래 = 다음 순번 특전을 하나 앞당겨 준다** (위임 기본값 — 순서가 고정이라 «전설 확정» 이 의미를 잃었다).
  비용(최대 체력의 30% 를 최대치에서 차감)과 시뮬 정책(항상 수락)은 §2.4 그대로다.
  줄 특전이 남아 있지 않으면 거래 자체가 성립하지 않는다(비용도 안 낸다 — 위임).
- **한 특전은 한 번만 얻는다** — 순서 획득이라 중복이 구조적으로 불가능하다.
- **버프 중첩 상한 전부 삭제 — 무한 중첩** (주인 확정). 시간제 버프는 발동될 때마다 계속 쌓이고 각자 자기 시간이 끝나면 빠진다.
  **방어막(ward) 장수 상한도 없다.** 따라서 표시 텍스트에 «최대 N중첩» 을 쓰지 않는다.
  ⚑ T96 — 새 특전 10종에는 버프도 방어막도 없지만 **장비 계열 옵션에는 그대로 있어** 이 규칙은 유효하다.
- **소환 데미지 = 주인 확정 상수** (튜닝으로 계수 변경 금지): 도끼 **30%** · 화살 **50%**(발당) ·
  검기 **50%** · 번개 **75%**(회당) · 창 **100%**(마리당, 일직선 최대 **8마리** 관통).
  아래 8·9·10 의 소환은 **기존 엔진 그대로 «1개 = 1발», 쿨다운 없음**이다.
- **소환 적중도 «공격» 이다**(§4) — 다만 새 10종의 소환 3종은 트리거가 «피격/회피/반격» 이라
  **소환 적중이 새 소환을 낳지 않는다**: 소환 연쇄 기대값 **B = 0** (임계 0.8 을 구조적으로 만족).
- **금지축 (특전·장비 옵션 공통, 게이트 감시)**: 골드·경험치 등 경제류 · 이동속도 ·
  최대 체력/최대 실드 증가 · 적중률(명중) · 부활 · 분신 · 주기형 회복(«N초마다 체력 회복/실드 수리»).
  **⚑ «흡혈 금지» 는 폐기됐다** (주인 확정 2026-09-03 ⑥ — 아래 7번 «생명 흡수» 가 최신 확정이다).
  시간형 **공격·버프**는 허용축이지만, 새 10종에는 주기형이 하나도 없다.
- **기절 지속은 3초 또는 6초만** (1/1.5/2/2.5초류 금지). 보스는 지속 `STUN_BOSS_MUL`(1/3) 배 — 위임 기본값.
  ⚑ T96 — 새 특전 10종에는 기절이 없지만 **장비 계열 옵션에는 그대로 있어** 이 규칙은 유효하다.
- **방어력·회피는 % 표기·% 의미**: 방어력 20% = 받는 피해 20% 감소(상한 80), 회피도 % (상한 90).
- 트리거 축: 공격 / 치명타 / 처치 / 피격 / 원거리 피격 / 회피 / 반격 / 빗맞음 / 회복 / 방어막 방어 / 이벤트 / 조건부 패시브.
- 버프 엔진: `buffs = {atk, aspd, critR, critF, def, evade}` 배열, 각 항목 `{t:남은시간, amt}`.
  실효 스탯 = 기본 + 버프 합(atk/aspd는 곱연산 `*(1+합)`, 나머지는 덧셈). **상한 없음(무한 중첩).**
- `sim.js` 의 특전 id/엔진 키가 기준이고 `index.html` 이 같은 키를 쓴다(게이트가 두 파일을 대조).

### 3.1 특전 10종 (이 순서가 곧 획득 순서다 — ⚑⚑⚑ T104 로 순서 재정렬 + 1번 효과 교체)

| # | sim id | 특전 | 효과 | 구현 |
|---|---|---|---|---|
| 1 | p_evadeHeal | 회피 시 회복 | 회피 시 **10%** 확률로 최대 체력 **6%** 회복 | `hitPlayer`(회피 분기) → `pkk(0.10)` 시 `heal(p, p.maxHp*0.06, true)` |
| 2 | p_atk | 공격력 증가 | 공격력 **+20%** | 획득 시 `p.dmg *= 1.20` (장비 합산 뒤 곱연산) |
| 3 | p_evade | 회피율 증가 | 회피율 **+10** | 획득 시 `p.evade += 10` (기본 20 → 30) |
| 4 | p_arrowEv | 회피 시 화살 | 회피 시 화살 **1개** | `hitPlayer`(회피 분기) → `fireArrows(p,1)` (⚑ T108 — 확정 발동) |
| 5 | p_axeHit | 피격 시 도끼 | 피격 시 도끼 **1개** | `hitPlayer` → `fireAxe(p,1)` (⚑ T108 — 확정 발동) |
| 6 | p_counter | 반격률 증가 | 반격률 **+10** | 획득 시 `p.counter += 10` (기본 20 → 30) |
| 7 | p_spearCt | 반격 시 창 | 반격 시 창 **1개** | `doCounter` → `fireSpear(p,1)` (⚑ T108 — 확정 발동) |
| 8 | p_critR | 치명타 확률 증가 | 치명타 확률 **+10** | 획득 시 `p.critR += 10` (기본 20 → 30) |
| 9 | p_critF | 치명타 피해 증가 | 치명타 피해 **+50** | 획득 시 `p.critF += 50` (기본 150 → 200) |
| 10 | p_def | 방어력 증가 | 방어력 **+10%** | 획득 시 `p.def *= 1.10` (장비 합산 뒤 곱연산) |

**⚑ T104 (주인 확정 2026-09-03) — 1번 특전 효과가 «생명 흡수(준 피해 10% 회복)» 에서 «회피 시 회복(10% 확률로 최대 체력 6% 회복)» 로 바뀌었다. 나머지 9종은 «효과·수치» 는 한 글자도 안 바뀌고 순서만 재정렬됐다** (종전 순서 «공격력→회피→반격→치확→치배→방어→흡수→도끼→화살→창» 폐기). 부수 효과로 **특전에서 흡혈(steal) 축이 완전히 사라진다** — 엔진의 `steal` 스탯은 남지만 특전이 쓰지 않으므로 게이트 «흡혈» 단언은 이 특전 기준으로 갈아끼웠다.

**수치 해석 (주인 위임 기본값 ⑦ — 다르게 뜻하셨으면 한 줄로 정정해 주십시오)**
- **회피 시 회복 (1번 특전)** = 트리거는 «내가 적 공격을 회피한 순간»(4번 «회피 시 화살» 과 같은 자리) · **회피 성공마다 독립 10% 굴림** · 회복량 6% 는 **최대 체력 기준(현재 체력 아님)** · **최대치 초과분은 버림 · 실드는 안 채움**. 구현은 `heal(..., noBoost=true)` 라 회복 증폭(장비 «회복량 +n%»)·오버킬 수리 분기를 타지 않는다.
- 확률형(회피·반격·치명타 확률)은 **+10**(기본 20 → 30), 치명타 피해는 **+50**(기본 150 → 200).
- 공격력 +20% · 방어력 +10% 는 **기본치에 곱연산**이며 **장비 합산 뒤**에 걸린다
  (`mkPlayer` 가 장비 옵션을 먼저 다 적용한 뒤 특전이 붙으므로 획득 시점 곱이 곧 «장비 합산 뒤» 다).
- 3·4·5 의 소환은 기존 도끼·화살·창 소환 엔진 그대로(1개 = 1발, 쿨다운 없음).

**엔진 상수** — `PERK_ATK_M 1.20 · PERK_DEF_M 1.10 · PERK_EVADE_A 10 · PERK_COUNTER_A 10 ·
`PERK_CRITR_A 10 · PERK_CRITF_A 50 · PERK_EVHEAL_CH 0.10 · PERK_EVHEAL_F 0.06 · PERK_SUMMON_CH 1.00` (두 엔진 같은 이름·같은 값 · ⚑ T104 로 `PERK_STEAL` 폐기 + `PERK_EVHEAL_CH`·`PERK_EVHEAL_F` 신설).


## 4. 엔진 구현 세부 (sim.js와 동일하게)

- `pk(chance)` — 발동 판정. **모든 "n% 확률" 발동형은 이 헬퍼를 통해서만 굴릴 것.** (⚑ 확률을 1.22배로 부풀리던 신화 🌪️ `m_procX2` 는 신화 폐지와 함께 삭제됐다 — 엔진의 `pkk()` 는 배수 자리를 남겨 두되 특전으로는 도달하지 않는다.)
- 실효 스탯: `effDmg`(rage 포함), `effAspd`, `effCritR`, `effCritF`, `effDef`(상한 80), `effEvade`(lastStand 포함, 상한 90). HUD에도 실효값 표시.
- `healPlayer(amt)` — healAmp 적용 → healBoost2 → 회복시 트리거(healDef/healShield3/healShield5/healAtk) → 초과분은 overheal/overBolt 처리. 무한루프 방지: 특전 적용시 초기회복 등은 `noBoost=true`로 호출.
- `dealPlayerDamage(e, ratio, fromBasic)` — 치명 판정(fullHpCrit/nextCrit) → firstHit/execute/backDmg 배수 → 흡혈 → 치명타시 트리거들 → execKill → onKill. **기본공격에서만** nextCrit/nextAtk 소모, 분신/추가타 발동.
- `applyStun(e, sec)` — **스턴 (T48)**. 더 긴 쪽만 남기고(합산 금지), 보스는 지속 배수를 곱한다. 스턴 중인 적은 그 틱의 근접 타격·화살 발사를 건너뛰고 공격 타이머도 멈춘다. 게임 쪽은 여기서 «기절» 연출을 띄운다.
- `procOnMiss(e)` — **빗맞음 (T48)**. 적 회피로 내 타격이 빗나간 «그 자리» 에서만 부른다: `dealDmg` 의 회피 분기와 `doCounter` 의 회피 분기 둘뿐(«MISS» 팝이 뜨는 자리와 동일). 빗맞음 스택은 여기서 쌓고 `dealDmg` 의 가산 보너스 풀에서 적중 한 타당 한 장씩 쓴다.
- `gainWard(p, 확률)` — **횟수형 방어막**. 획득은 공격·피격·회피·처치 트리거에서, 소모는 `hitPlayer` 에서 수치형 실드보다 **먼저**. **장수 상한 없음(주인 확정 «무한»)** — `wardCap` 폐지. 방어막이 한 장 소모될 때 «방어막 방어» 트리거(회복·반사·창)가 굴러간다.
- `procOnRanged(src)` — **원거리 피격 (T48)**. `hitPlayer` 의 `!isMelee` 경로에서, 일반 피격 트리거를 전부 굴린 뒤에 부른다. 회피 분기가 앞에서 반환하므로 «빗맞은» 화살은 굴리지 않는다.
- `doCounter(src)` — 반격 데미지 `effDmg*0.7*(1+counterX)` + 반격시 트리거들 + counterChain(1회 제한).
- **소환 데미지 계수 (주인 확정 상수 — §3.0 과 같은 값). 튜닝으로 이 다섯 값을 바꾸지 말 것.**
  줄을 계열마다 나눠 둔 이유: 한 줄에 몰아 쓰면 `verifyOptText` ④ 의 대조 풀이 다섯 함수의 합집합이 되어
  «한 계수만 어긋난 드리프트» 가 다른 계수의 같은 숫자에 가려 통과해 버린다(자가 시험 ④ 가 이걸 잡는다).
  - `fireAxe` — 도끼 한 개당 공격력의 **0.30**
  - `fireArrows` — 화살 한 발당 **0.50**
  - `fireWave` — 검기 한 개당 **0.50**
  - `fireBolts` — 번개 한 회당 **0.75**
  - `fireSpear` — 창에 뚫린 적 한 마리당 **1.00**
- 소환 발사 함수 `fireAxe/fireArrows/fireBolts/fireWave/fireSpear` — **발수를 인자로 받는다**(특전 텍스트의 «도끼 2개/화살 2발/번개 2회» 가 그대로 인자다). 데미지 계수는 §3.0 주인 확정 상수 고정. 소환 개조 특전(🪞 도끼 튕김 · 🔗 연쇄 번개 · 🏹² 화살 2배 · 🏹→🔱 창 변환 · 🌊🔱 거대 검기 · 🪓🌪️ 3회전)은 이 함수들 안에서만 갈린다. arsenal(장비 옵션)은 이 5개 중 랜덤 호출. 투사체는 게임에선 그려서 날아가고(도끼 회전/화살/창/검기 초승달, 번개는 하늘에서 지그재그), 시뮬에선 번개만 즉발.
- **⚑ 주인 확정(2026-09-02 15:3X): 소환 적중도 «공격» 으로 친다 — 트리거 연쇄 허용.** 소환(창/도끼/화살/번개/검기)이 적을 맞히면 «공격 시 n%» 트리거를 굴리고, 그 타격이 치명타면 «치명타 시» 트리거도 굴린다 (예: «공격 시 50% 창» 보유 시 창 적중마다 또 50% 굴림 → 창이 창을 부르는 연쇄 허용). 특전·장비 옵션 공통. 종전 «기본공격에서만» 규칙은 **분신·추가타·nextCrit/nextAtk 소모에만 잔존**(위임 기본값). 확률·연쇄 인위 제한 금지 — 세면 밸런스 수치로 잡는다. 성능 가드(동시 투사체 상한 등)만 허용(위임).
  - **구현 (T45 — 두 파일 같은 동사)**: 소환 적중은 전부 `summonHit()` 을 거친다 — 데미지 함수(`dealDmg`/`dealPlayerDamage`) 직후 `procOnAttack()` 을 굴린다. «치명타 시» 트리거는 원래부터 데미지 함수 안에 있어 기본공격 전용이 아니었으므로 소환 적중에도 이미 걸렸다 — 이 개정으로 새로 붙는 것은 «공격 시» 쪽이다. 소환 투사체 생성은 전부 `pushProj()` 를 거친다.
  - **성능 가드 (주인 위임 기본값 — 밸런스 노브 아님)**: 동시 활성 투사체 상한 `PROJ_CAP` = **200** (초과분은 «즉발 판정» 으로 대체해 데미지가 사라지지 않는다) · 한 틱에 굴리는 소환 적중 트리거 수 상한 `PROC_TICK_CAP` = **200** (초과해도 데미지는 그대로 들어가고 트리거만 멈춘다 — 즉발로 꼬리를 무는 연쇄가 한 틱 안에서 무한히 자라는 것을 막는다). 두 상수는 `sim.js` 와 `index.html` 에서 같아야 한다.
  - 감시: `verifyCombatConst` ③ (엔진을 실제로 굴려 «소환 적중 → 트리거» · 기본공격 전용 자원 미소모 · 가드 2종 동작을 단언) + `verifyT2` ⑳ (두 파일 대조).
  - **⚑⚑ 주인 확정(2026-09-03) — 소환 연쇄 임계 규칙 (T78).** 연쇄 메커니즘은 위 그대로 유지하되, **«소환 1발의 적중이 낳는 신규 소환 기대값 B» 를 어떤 특전 조합에서도 임계 0.8 이하로 지켜야 한다.** `B = Σ(보유 소환 특전의 발동확률 × 발수)` 이고, B 가 1 이상이면 한 타격의 총 소환 수가 등비급수로 발산해 `PROC_TICK_CAP` 이 끊을 때까지 부푼다 — 주인이 지목한 «신화 뜨면 게임 끝» 의 원인이 이것이다(개정 전 최악 조합 실측 B = 12.108).
    - **개정 후 (T78 실측 B = 0.769)**: 「공격 시」 소환 특전의 발동 확률을 전부 같은 값으로 통일하고 발수를 내렸다 — 값은 §3.2~§3.4 특전 표와 §11.6 옵션 표가 정본이다. **⚑ P1(T83) 특전 교체 후에는 발수 특전이 전부 장비 옵션 쪽에만 남는다**(도끼 3개·화살 3발·번개 2회 — §11.6). 새 132종에서 「공격 시」 축의 소환은 일반 🌊 10% 1개 · 희귀 🏹 15% 2발 · 전설 ⚡🪓🏹🌊 각 20% 2개(2발) 이고, 화살 2배(🏹²)·도끼 3회전(🪓🌪️)·연쇄 번개(🔗)가 곱해진다 — **B 재측정과 필요 시 재조정은 P3 몫**이다.
    - **감시: `node tools/verifySummonChain.js`** — 두 파일 확률·발수 대조 · 최악 조합(모든 소환 특전 + 모든 발수 특전 + 폭풍의 힘 동시 보유)의 분석값 · 같은 조합을 엔진에서 실제로 굴려 잰 실측값 · `procOnAttack` 에 게이트가 모르는 소환이 새로 생겼는가. **소환을 새로 추가하거나 확률·발수를 올리면 이 게이트가 빨개진다.**
- **⚑ 주인 지시(2026-09-02): 다연발 소환은 순차 연사 (다다다닥).** 한 발동에서 여러 발이 나가는 소환(화살 N발·번개 N회·도끼 3개 등, 특전이 모여 10발이 됐어도)은 **같은 프레임에 겹쳐 쏘지 말고 한 발씩 순차 발사하되, 아주 빠르게(다다다닥)** — 주인 보강: **발마다 구별되어 보이는 게 목적**이므로 «순차 + 존나 빠르게» 둘 다 지킬 것. 간격 50~70ms(눈으로 낱발이 구별되는 최소 수준, 배속 반영), 발마다 사운드·타겟 선정·데미지 판정도 그 발 시점에(위임 규약 기본값). 현 index.html 은 `for` 루프 동시 스폰이라 T2 에서 수정 대상. sim.js 는 즉발 유지(총합 데미지 동일 — 밸런스 영향 무시 가능, T1 회차 무효 아님).
- 랜덤 타겟: 플레이어 앞 540px 이내 생존 적 중 랜덤 (이벤트 노드 뒤 다음 웨이브는 사거리 밖이라 안전).
- 적 처치 중복 방지: `e.dead` 가드 필수 (반격+가시 동시 처치 등).
- 악마/레벨업 모두 `perkChances` 증가 → `perkHp` 소급 로직.

## 5. UI 추가 요구 (기존 index.html에 이미 있음 — 유지)

- 악마 거래 특전 미리보기 카드(섹션 2.4), 📘 Info 팝업, 레벨업 연출, 전설 카드 글로우. (⚑ 신화 등급 폐지 — 특전 카드 색은 3색)
- 특전 카드: 등급 태그 + 아이콘 + 설명, `<b>` 강조는 초록색.

## 6. 챕터 20개 + 벽 (시뮬 튜닝 대상)

`sim.js`의 `TUNE` 초기값 (2026-09-01 튜닝 전):
```
적 HP  = 40 * 1.35^(c-1) * (1+0.22*웨이브) * (c>=10: ×1.6) * (c>=15: ×1.5)
적 DMG =  8 * 1.26^(c-1) * (1+0.10*웨이브) * (c>=10: ×1.35) * (c>=15: ×1.25)
보스: HP ×12, DMG ×1.8, 5의 배수 챕터는 HP ×1.5 추가
```

**현재 확정값 (⚑⚑⚑ T107 2단계 — T106 의 적합을 **T107 1단계(챕터별 적 수 곡선) · T108(소환 3종 확정 발동) · T109(특전 순서 2·3 ↔ 6·7) 가 모두 합류한 트리에서 다시 잡은 값**. 챕터당 성장은 단일 상수가 아니라 §11.7 «구간별 성장률» 표를 따른다):**
```
적 HP  = 46.780464 * §11.7 구간별 HP 성장 누적(1→c) * (1+0.15*웨이브) * (c>=10: ×1.50) * (c>=15: ×1.00)
적 DMG =  8.699327 * §11.7 구간별 DMG 성장 누적(1→c) * (1+0.08*웨이브) * (c>=10: ×1.25) * (c>=15: ×1.00)
보스: HP ×8, DMG ×1.8 (주인 확정 상수 — 아래)
90 대형 벽  : c>=90  → HP ×2.00 · DMG ×1.50   (wall3Hp / wall3Dmg)
420 최종 벽 : c>=420 → HP ×1.00 · DMG ×1.00   (wall4Hp / wall4Dmg / wall4At)
```
- **⚑⚑⚑ T103 — 최종 벽이 챕터 600 → 420 으로 내려왔고, 배수는 ×1.00(꺼짐)이 됐다** (주인 정정 «신화9강+슬롯100 -> 420»). 10·15·90 벽은 위치·배수 모두 그대로다. **왜 껐나** — 주인 지시 ④가 «380→420 구간률은 150→380 률을 그대로 잇고, 과녁 420 이 10% 가 되게 하는 잔차를 `wall4` 로 둔다. 잔차가 1 아래로 나오면 벽을 끄고 률만으로 잇는다» 이고, 실측 잔차가 HP 0.74 · DMG 0.76 으로 **1 아래**였다. 즉 380→420 에 필요한 난이도 증가는 150→380 률 40챕터분(HP ×2.50)보다 작다(실제 필요 ×1.86). 벽을 되살리고 싶으면 380→420 구간률을 그만큼 내려야 한다 — 그 재배치는 §6 «벽 예산» 항등식과 같은 종류다. 임계는 엔진에서 `TUNE.wall4At` 한 곳에만 있고 두 엔진이 그 값을 같이 본다.
- **⚑ R02 «벽 예산» 항등식 — 벽을 켤 때 사다리를 지키는 방법 (T35·R01 이 «벽을 켜면 사다리가 깨진다» 로 남긴 문제의 해답).**
  사다리 과녁 중 **5 만 벽 밖(c<10)이고 15·30·50·70·120·260 은 전부 벽 안(c≥10)** 이다. 그래서 두 식만 고정하면 7점이 통째로 보존된다:
  `D(5) = 기저 · g0^4` 과 `D(15) = 기저 · g0^14 · W`. 두 식을 나누면 **`g0^10 · W = 상수`(HP 2.000 · DMG 1.624)** —
  즉 **10챕터 벽의 배수는 «구간0 성장률 10챕터분» 에서 빌려 온다.** 벽을 W 만큼 켜면 구간0 성장률을 그만큼 낮추고 기저를 올려 되갚는다.
  R02 적용값: HP `1.0718 → 1.0292` + 기저 `22.8 → 26.82` + `W 1.5` · DMG `1.0497 → 1.0265` + 기저 `4.56 → 4.986` + `W 1.25`.
  결과 — **실험5 사다리 7/7 유지**(5.5/9.0/4.1/5.5/3.5/5.5/5.0%. 희귀·신화는 1000판 재측정값) · 챕터 9→10 난이도 계단 **×1.54**.
- **⚑ R03 — 같은 항등식으로 90·300 벽을 켰다.**
  - **90 대형 벽**: 과녁 70 과 120 사이에 있으므로 예산식은 `g70^50 · W3 = D(120)/D(70)`(HP 5.068 · DMG 5.270). `W3 = HP 2.0 / DMG 1.5` 를 켜고 구간 70→120 률을 `3.30/3.38% → 1.88/2.54%` 로 내려 되갚았다. **D(70)·D(120) 둘 다 불변** — 90 은 과녁이 아니라 벽이므로 자유롭게 세울 수 있다.
  - **300 최종 벽**: 사다리 최상단 과녁이 260 이고 **그 위에는 과녁이 없다** — 즉 300 벽만은 예산 제약이 전혀 없다. `W4 = HP 3.2 / DMG 1.8`. **이것은 `slotCostG` 의 짝 노브다** — 슬롯을 싸게 하면 계정이 부유해져 최종 벽이 12~19회로 무너지므로, 예산이 자유로운 `wall4` 로 그만큼 되돌린다(12시드 실측: 300 벽 적합 1/12 → **9/12**).
  - **260 → 300 구간 신설** (`[260, 1.0056]` HP·DMG 공통): §11.7 산문이 «260→300 은 슬롯 강화 몫, 필요 ×1.25» 라고 적어 두었는데 엔진은 «120→260 률을 뒤로 연장» 해 **×1.84** 가 되어 있었다(T53 ⑤ⓐ 지적). `1.0056^40 = 1.250` 으로 PLAN 문장을 이행한 것이며, 구간 시작이 260 이라 **과녁 260 은 불변**이다.
  - 실측 — **실험5 사다리 7/7 유지**(1000판: 4.6/4.8/4.3/6.4/4.8/4.2/4.8%) · 실험3 챕터 90 시도 **2/5 → 4/5 적합**(목표 30~400회) · 챕터 300 **0/5 → 1/5**(도달 자체가 0/5 였다).
  **⚠ 예산이 유한하다**: `W` 를 올리면 `g0` 가 내려가 챕터 1~9 가 평평해지고(6~9 «각 2~5회» 목표와 반대), 기저가 올라가 챕터 1~4 가 무거워진다.
  실측 — `W 1.5` = §7 1~20 적합 셀 **36/100** · `W 1.7` = **27/100**(오히려 악화) · 벽 끄기 = **29/100**. **1.5 가 실측 최적**이다.
  같은 이유로 **15 벽(`wall2`)은 켤 수 없다** — 그것은 `D(15)` 자체에 곱해져 사다리 과녁 «일반=15» 를 직접 깨고, 같은 예산을 10 벽과 나눠 쓴다.

**R06 튜닝 기록 (T35 이전 — 되돌림용. 시뮬 근거: `docs/balance/round-06/`):**
```
적 HP  = 40 * 1.185^(c-1) * (1+0.15*웨이브) * (c>=10: ×1.95) * (c>=15: ×0.82)
적 DMG =  8 * 1.08^(c-1)  * (1+0.08*웨이브) * (c>=10: ×1.25) * (c>=15: ×1.00)
보스: HP ×8, DMG ×1.8 (주인 확정 상수 — 아래)
```
(R05 값은 `1.15 / 1.10 / ×2.6 / ×1.06 / ×1.15` 였다 — 되돌림용 기록.
 R06 의도: **eHpG 를 올려 챕터6~9 의 무저항 구간(평균 1.3~2.7회)을 조이고**, 그만큼 `wallHp` 를 내려
 챕터10 벽의 절대 난도를 유지했다. 후반 상한 초과(챕터15~19 평균 12~18회)는 `wall2Hp` 1.06→0.82 와
 `eDmgG` 1.10→1.08 로 완화하고, 줄어든 압박은 `wallDmg` 1.15→1.25 로 되돌렸다.
 스윕 8종을 20회 반복 비교해 고른 조합이다.
 **⚠ 주의: 2026-09-02 07:5X 주인 지시로 영구강화 4종이 폐지되고 장비 시스템(§11)이 신설됐다.
 위 적 수치는 «영구강화 경제» 를 전제로 튜닝된 값이므로, T6 통합 후 반드시 재튜닝해야 한다.**)
- **⚑ T6 (§11 장비 시스템 통합) 추가분 — R06 적 수치(`eHpG 1.185` 등)는 한 자리도 바꾸지 않았다.** 챕터 확장에 필요한 항만 신설 (**벽 배수는 T35 가 위 확정값 블록으로 이관·임시 비활성**):
```
90 대형 벽  : c>=90  → HP ×3.6 · DMG ×1.30   (T35 이전 값 — 되돌림용 기록)
300 최종 벽 : c>=300 → HP ×3.6 · DMG ×1.30   (T35 이전 값 — 되돌림용 기록)
골드        : 처치·클리어 보너스에 챕터당 성장 배수 goldGrowth = 1.185 (= eHpG)   ← T6 값
```
- **⚑ R07 (T1) 경제 노브 재조정 — 적 수치(`eHpG` 등)는 여전히 한 자리도 안 바꿨다.** 바꾼 것은 경제 3개뿐:
```
goldGrowth  : 1.185 → 1.22
슬롯 비용   : 150 * 5.5^L → 600 * 4.2^L → 600 * 3.5^L → 600 * 1.6^L   (slotCostBase / slotCostG · 마지막이 ⚑ T1 R03 확정값 — T53 해소)
```
  근거(실험4 실측): T6 값(1.185 / 5.5)은 **챕터 90 에서 F2P·과금 둘 다 영구 정체**했고(슬롯 13 에서 멈춤, 앵커 A 는 15 필요),
  `goldGrowth` 만 1.22 로 올리면 90 은 뚫리지만 이번엔 **챕터 118 에서 40일 정체**했다.
  **T6 이 세운 «slotCostG < goldGrowth^6(=6.93)» 규칙은 틀렸다.** 올바른 점근 조건은
  «슬롯 1렙이 벌어 주는 챕터 수 = ln(slotG)/ln(eHpG) ≈ 5.8챕터, 그 동안의 골드 증가 `goldGrowth^5.8` ≥ `slotCostG`» 이다
  (1.22^5.8 = 3.2). 이 조건을 어기면 «레벨당 비용 ×5.5 vs 5.8챕터당 수입 ×3.0» 이라 회차가 갈수록 벌어져 **어떤 챕터에선가 반드시 멈춘다.**
  실측 확인: `slotCostG` 3.2 = 폭주(60일차 챕터 911) · 4.2 = 정상(30/60/90/120/180일차 = 31/125/155/173/179, 정체 0) · 4.8 = 챕터 90 정체 · 5.5 = 챕터 118 정체.
  `slotCostBase` 를 150 → 600 으로 올린 것은 비용 성장률을 낮춘 만큼 초반 슬롯이 너무 빨리 오르는 것을 되돌리기 위한 평행이동이다.
- **⚑ R09 (T1) 정정 — 위 R07 의 «4.2 = 정상» 은 관측 범위가 짧아서 생긴 오판이다 (T13·T15).**
```
슬롯 비용   : 600 * 4.2^L → 600 * 3.5^L   (slotCostG 만 변경, base 는 유지)
```
  R07 은 조건식 `goldGrowth^5.808 ≥ slotCostG` 를 **옳게 세워 놓고 `1.22^5.808 = 3.174 ≥ 4.2` 를 «만족» 이라 적었다** — 부등호가 반대인 계산 착오다.
  실제로는 매 챕터 **3.4% 씩 적에게 뒤처져**(적대비 0.9674) 실험3 이 챕터 120 부터 무너지고 실험4 가 **챕터 212 에서 영구 정체**했다.
  `slotCostG` 3.5 는 적대비 **0.990** 으로 적자가 1/3.5 로 줄어 챕터 300 완주가 된다.
  실측(워커 A T13 3런 + R09 6런 = **9런 독립 재현**): **91~299 구간 적합 100%** (R09 6런 전부) · 챕터 300 최종 벽 81~350회.
  유효 구간은 **3.4~3.6** — 3.3 은 폭주(91~300 전부 시도 1회), 3.8 은 열화 시점이 챕터 160 으로 밀릴 뿐 곡선이 같다.
  **⚠ 이 변경의 대가 — 챕터 90 대형 벽이 무너진다(R09 6런 0/6, 시도 4~28회 vs 목표 30~400회).**
  원인은 난이도가 아니라 **도달 시점 빌드**다: 챕터 90 도달 시 슬롯이 `4.2` 에서는 **14~15렙(= 앵커 A 스펙 «신화 0강·슬롯 균등 15렙» 과 정확히 일치)** 인데 `3.5` 에서는 **16렙**이 된다.
  `wall3Hp` 로는 못 막는다 — 그건 앵커 A 의 측정 대상 자체를 어렵게 만들어 실험5 앵커 A(현 3.3%, 목표 2~10%)를 2% 아래로 떨어뜨린다.
  `slotCostBase` 로도 못 막는다 — R09 스윕(각 3런, 챕터 95까지): base **1200** 은 슬롯이 아직 16렙 · **2400** 은 3런 **전부** 챕터 10~12 에서 400회 상한에 막힘 · **3000** 은 슬롯 15렙 복원에 성공하나 3런 중 1런이 챕터 18 에서 막힘.
  즉 **«앵커 A 정렬»과 «초반(1~20) 통과»가 같은 노브에서 정면충돌**하며, 이는 승인 대기 9번(초반 무성장 구간)의 직접 귀결이다 → **승인 대기 14번**.
  두 벽 배수와 §11 장비 계수는 **§11.7 앵커 3점에서 역산**했다. 실험5 실측(각 120판):
  **C 챕터30 = 13.3%(7.5회) · A 챕터90 = 5.0%(20회) · B 챕터300 = 7.5%(13.3회)** — 세 과녁 모두 «수십 회 끝에 겨우» 구간.
  근접 챕터: 28=64% / 32=1.7% · 88~89=100% / 92=0% · 298~299=100% / 301=1.7%.
  원시 출력 `docs/balance/T6/e5.txt`.
(R02 중간값은 waveHp 0.50 / waveDmg 0.26 / wallHp 2.6 이었다 — 주인 지시 «웨이브 전멸 실드 충전 폐지»
 반영으로 체감 난이도가 올라 R03 에서 웨이브 증가율을 되돌리고 벽만 2.9 로 올렸다.
 **R04 에서 보스 ×8·강화 상한 티어·하니스 재보정을 반영하고 eHpG 1.11→1.15 / eDmgG 1.14→1.10 으로 재조정했다**
 — 보스가 ×12(5배수 ×18)에서 ×8 로 약해진 만큼 챕터 성장률을 올리고, 후반 즉사 압박을 줄이려 DMG 성장률은 내렸다.)
- **⚑ 주인 확정 상수(2026-09-02, 07:3X 정정): 보스 HP = 일반몹 ×8 고정 · 보스 DMG = ×1.8 고정.** (앞서 07:2X 의 ×3 은 주인이 ×8 로 정정.) 기존 «HP ×12 · 5의 배수 챕터 ×1.5 추가» 폐기 — `bossHp = 8`, `bossDmg = 1.8`, `bigBoss` 배수 삭제, 챕터 무관 항상 동일 (기준 일반몹 = 해당 챕터 보스 웨이브 시점의 `enemyStats`, 위임 규약 기본값). **두 값 모두 주인 확정 상수라 튜닝 노브가 아니다** — 이후 밸런스는 다른 노브로 맞출 것. PROGRESS «주인 승인 대기» 1번 안건은 이 지시로 종결.
- 되돌림용 기록: 초기값 → R02 값은 위 두 블록 비교로 즉시 복원 가능.
- 근거: 초기값은 챕터10에서 400회 재도전으로도 못 뚫는 **완전 차단**이었음(비평가 2인 6.3/10).
  강화 비용이 지수(1.32^Lv)로 오르는데 골드 수입은 챕터에 선형이라 400판에 강화 +4레벨에 그쳐
  «반복하면 뚫린다»가 성립하지 않았다. 비용 성장률을 낮추고 적 챕터 성장률을 내리는 대신
  **웨이브 내 증가율(waveHp/waveDmg)을 올려** 한 판 안의 난이도를 유지했다.

## 7. 밸런스 시뮬레이션 절차 (필수 수행)

> **⚑⚑⚑ 2026-09-03 주인 확정 — 이 절의 «기준» 은 전부 폐기됐다. 유효한 밸런스 기준은 아래 2점뿐이다.**
>
> | 과녁 | 조건 | 목표 |
> |---|---|---|
> | **A** | 표준 장비(희귀 풀셋 6부위 · 슬롯 0렙) + 특전 10종을 순서대로 획득 | **챕터 15 클리어율 10%** |
> | **B** | 노장비(장비 0 · 슬롯 0) + 특전 10종을 순서대로 획득 | **챕터 4 클리어율 30%** |
>
> 허용 오차 **±2%p** · 측정은 **과녁당 1,000판 이상 · 고정 시드 3벌**(측정 위생 규약만 그대로 유지) · 노브는 **구간별 성장률 · 벽 배수 · 적 수**.
>
> **폐기된 기준 (판정에 쓰지 말 것 — 아래 본문은 이력 보존용)**: 스탯 사다리 7점(5·15·30·50·70·120·260) · 채점표 v1~v5 와 실험1~5 배점 · **«비평가 2인 각 ≥8/10» 게이트** · 실험1 등급 과녁(10/20/80%) · 실험2 등급 내 폭(<25%p) · 실험3 진행 곡선 목표(챕터별 시도 횟수) · 실험4 기준 ①②③ · 앵커 A·B·C(이미 폐기). 등급이 사라졌으므로 실험1·2 는 측정 자체가 불가능하고, 나머지는 주인이 «방금 말한 게 기준이고 나머지 기준은 다 폐기» 로 명시적으로 내렸다.
>
> `node sim.js 1~5` 러너와 `tools/regress.js` 는 **진단 도구로만** 남긴다(점수 없음). 이 절과 §11.7 의 재작성, 그리고 `tools/verifyScoreCriteria.js` 를 새 과녁 2점 기준으로 갈아엎는 것은 **T97 의 첫 번째 할 일**이다.


실행: 레포 루트에서 `node sim.js 1` (과녁 판정) · 진단은 `node sim.js 3/4/5` (전부: `node sim.js`)

> **⚑⚑⚑ T97 이행 완료 (2026-09-03 / 워커 C)** — 주인 지시대로 이 절을 **과녁 2점 기준으로 재작성**했다. 옛 채점표(v1~v5)·등급 과녁·실험2 폭·실험3/4/5 합격 기준·«비평가 2인 각 ≥8/10» 게이트의 **문면을 전부 걷어냈다**(전문은 커밋 `fd6d3a3` 이전 이력에 있다 — 되살릴 일이 있으면 거기서 꺼낼 것). 게이트 `tools/verifyScoreCriteria.js` 도 같은 회차에 **새 과녁 2점의 문면·구현·실측 게이트로 갈아엎었다**(29항목 · 음성 14/14).

### 7.1 유효한 기준 — 난이도 사다리 8점 (이것이 전부다)

> **⚑⚑⚑ 주인 확정 2026-09-03 (T103)** — 종전 과녁 2점(표준 ch15 = 10% · 노장비 ch4 = 30%)은 이 표에 흡수됐다. 여덟 칸 **전부 같은 목표(클리어율 10%)** 이고, 장비 등급과 **슬롯 레벨까지 함께** 못박혀 있다.

| # | 빌드 (특전은 §3.1 순서대로 «그 챕터가 주는 만큼» — ⚑ T107) | 목표 챕터 | 목표 클리어율 |
|---|---|---|---|
| 1 | 노템 (장비 0 · 슬롯 0) | **5** | **10%** |
| 2 | 일반 풀셋 · 슬롯 0 | **15** | **10%** |
| 3 | 희귀 풀셋 · 전 부위 슬롯 5 | **28** | **10%** |
| 4 | 영웅 풀셋 · 전 부위 슬롯 10 | **40** | **10%** |
| 5 | 전설 풀셋 · 전 부위 슬롯 15 | **70** | **10%** |
| 6 | 신화 풀셋 · 전 부위 슬롯 25 | **150** | **10%** |
| 7 | 신화 +9강 풀셋 · 전 부위 슬롯 50 | **380** | **10%** |
| 8 | 신화 +9강 풀셋 · 전 부위 슬롯 100 | **420** | **10%** |

- 허용 오차 **±2%p** · 측정 **과녁당 1,000판 이상** · **고정 시드 3벌**.
- ⚑ **측정 조건의 특전 수는 «10종 전부» 가 아니다 (T107 주인 지시 ④)** — 그 챕터의 경험치가 주는 만큼이라 칸마다 다르다: 1칸(ch5) 6개 · 2칸(ch15) 7 · 3칸(ch28) 8 · 4~8칸(ch40 이상) 9(최대). 순서는 §3.1 그대로이고, 10번(방어력 +10%)은 적 수 상한 50 에서 어느 챕터에서도 안 나온다. 실측·근거는 `docs/balance/T107/result.md` §3.
- ⚑ 8칸은 **주인 정정으로 600 → 420** 이다 (주인 원문 «신화9강+슬롯50 -> 380, 신화9강+슬롯100 -> 420 정도로 하자 챕터 밸런스»). «정도» 라 하셨지만 과녁은 420 정확히로 두고 허용 오차만 ±2%p 를 쓴다 — 자를 흔들지 않기 위해서다.
- 구현 정본은 `sim.js` 의 `EXP1_TARGETS`·`EXP1_TOL=2`·`EXP1_SCORE_N=1000` 이고 `node sim.js 1` 이 판정표를 직접 찍는다. **실험5(사다리 진단)도 같은 표를 본다** — T103 이 두 표를 하나로 합쳤다(종전엔 실험5 쪽 스탯이 T102 이전 값이라 낡아 있었다).
- **여덟 칸이 서로 다른 노브를 갖는다** — §11.7 의 구간 경계를 **과녁 챕터에 맞춰**(5·15·28·40·70·150·380·420) 잘라 두었기 때문이다. 구간 하나가 과녁 하나만 담당하므로 앞에서부터 순서대로 자를 수 있고, 뒤 구간이 앞 과녁을 되돌리지 않는다. 상세 §11.7.
- **하니스는 자가 고정이다** — 주인이 장비·슬롯·챕터를 직접 못박았으므로 «변별 구간에 앉도록 하니스를 재선정한다»(구 정본 «변별점 규칙» ①~⑤)는 절차가 성립하지 않는다. 하니스를 옮기는 것은 **자 조작**이다.
- ⚠ 이 축은 예민하다 — 적 HP·DMG 가 **정수 반올림**이라 «한 대 더 때려야 하나» 가 계단으로 나타난다. 초반 칸일수록 계단이 크다: 2칸(챕터 15)은 적 HP 51 → 52 한 칸이 클리어율 **14.5% → 5.5%** 를 옮긴다(웨이브 배수 덕에 그 사이가 10.2% 로 한 번 더 잘린다). **0.5% 단위보다 잘게 움직여도 의미가 없다.**

### 7.2 측정 규약 (자를 지키는 규칙 — 그대로 유효)

- **⚑⚑ 회귀 측정 규약 (T87)**: 회차 간 비교에 쓰는 수치는
  ① **고정 시드 3벌 이상**으로 재고 회차 로그에 **쓴 시드를 적는다**,
  ② 판수는 상수 기본값(`EXP1_SCORE_N` · `EXP5_SCORE_N`)을 그대로 쓴다 — 탐색용으로 낮춘 런은 근거로 인정하지 않는다,
  ③ 해석 문서(`docs/balance/**`)의 숫자는 **원시 출력에서 그대로 복사**한다 (다시 돌린 런의 값을 섞지 않는다).
  실행 도구 = `node tools/regress.js` (기본 시드 11·12·13 고정 · `REGRESS_RAW=<디렉터리>` 로 원시 출력 저장).
  게이트 `tools/verifySeedProtocol.js` 가 ⓐ 같은 시드 = 같은 결과 ⓑ 다른 시드 = 다른 결과 ⓒ 판수 상수 배선 ⓓ 러너의 고정 시드 3벌 이상을 본다.
- **⚑ 시드 하니스 (R11)**: `SEED=<정수>` 를 주면 전 실험이 결정적으로 돈다(미설정 시 무시드). 전투·특전과 뽑기를 **별도 스트림**으로 분리해 두어 같은 시드의 F2P/과금 두 런은 **k번째 뽑기 결과가 서로 같다**. 스윕은 `SWEEP_SEED=<정수> node tools/sweep.js …` (공통난수 비교).
- **⚑ 주인 확정(2026-09-02 16:4X) 시뮬 공통 정책: 가상 플레이어는 쉼터에서 항상 «🌟 경험치» 선택** (체력 회복 금지 — 전 실험 공통 측정 조건. 실제 게임은 유저 자유 선택). **엔진 반영 = T46**, 게이트 = `tools/verifyRestPolicy.js`(양방향 — 시뮬에 회복이 되살아나도, 그 정책을 게임에 잘못 이식해 회복 버튼을 지워도 빨개진다).
- **⚑ 주인 확정(2026-09-03) 시뮬 공통 정책: 가상 플레이어는 «😈 악마 = 항상 수락 · 😇 천사 = 항상 왼쪽(무료 공격력 +5%)»** (쉼터 «항상 경험치» 와 같은 축의 측정 조건 통일 — 전 실험 공통. 실제 게임은 유저 자유 선택이라 두 이벤트 모두 2택 그대로다). 함께 확정된 **거래 비용 = «최대체력의 30% 를 최대치에서 차감»**(§2.4)이라 악마를 수락한 판은 그 뒤 최대체력이 줄어든 채 측정된다 — **밸런스 영향 있음**. **엔진 반영 = T90**(공통 상수 `DEVIL_COST` · 공통 동사 `payDevilCost`), 게이트 = `tools/verifyDevilPolicy.js`. **⚑ T96 이후 악마가 주는 것은 «다음 순번 특전 앞당김»** 이라 **총 개수를 늘리지 않는다** — 앞당길 것이 없으면 아무것도 주지 않는다. (⚑ T107 — 한 판의 특전 수는 그 챕터의 경험치가 정하므로 **6~9개**다. 종전 문면 «총 개수는 10 그대로» 는 «완주 = 10개» 항등식 시절 것이라 폐기.)
- **하루 플레이 판수 = 30판**(`GT.runsPerDay`) — 다이아 적립을 판 단위로 환산하는 기준이라 실험3 과 실험4 가 같은 경제 코어를 쓴다.

### 7.3 조정 노브

- `TUNE` 의 **구간별 성장률**(`eHpSeg`/`eDmgSeg` · §11.7) · **벽 배수**(`wallHp/wallDmg` · 90·300 벽 포함) · **기저**(`eBaseHp`/`eBaseDmg`) · 골드 드랍 · 장비 공/체 계수 · 슬롯 강화 비용/스탯 곡선(§11).
- **적 «수» 는 노브가 아니다 — 주인 확정 «곡선» 으로 고정이다** (⚑ 주인 확정 2026-09-03 · T96 → 구성은 T100 → **곡선은 T107 에서 갱신**) — 챕터별 적 수는 `N(c) = c ≤ 5 ? 17 : min(50, 17 + (c − 5))` 이고 웨이브 5·쉼터 2·악마 1·천사 1·보스 1 은 전 챕터 동일이다. 이 곡선·구성을 건드리면 챕터별 특전 개수(1~5 = 6 · 15 = 7 · 28 = 8 · 38+ = 9)가 함께 움직여 `verifyChapterFixed` 가 빨개진다. 난이도는 **적 스탯으로만** 만든다.
- 보스 ×8/×1.8, 뽑기 확률·천장, 다이아 수급량, 플레이어 기본 스탯, 특전 10종 수치는 **주인 확정 상수라 노브가 아니다**.
- ⚠ **`slotCostG` 는 `goldGrowth^(ln slotG / ln eHpG)` 이하여야 한다** (슬롯 1렙이 벌어 주는 챕터 수 동안의 골드 증가분이 다음 레벨 비용을 감당해야 한다). 현행 값 기준 = `1.22^5.808` = **3.174**. 확정값은 **`1.6`** — «정상 상태 슬롯 레벨» 의 닫힌 식 `L ≈ c · ln(goldGrowth) / ln(slotCostG)` 에서 챕터 300 이 상한 150 에 닿게 고른 값이다(`3.5` 는 챕터 431 에서야 닿아 사문이었다). 반드시 **`wall4` 와 짝으로** 움직일 것 — 슬롯을 싸게 하면 계정이 부유해져 최종 벽이 무너진다.

### 7.4 진단 도구 (점수 없음 — 판정에 쓰지 않는다)

주인 확정: «러너는 남긴다 — `node sim.js 1~5` 와 `tools/regress.js` 는 **점수 없는 진단 도구**로만 쓴다.»

| 모드 | 무엇을 보나 | 상태 |
|---|---|---|
| `node sim.js 1` | **난이도 사다리 8점 판정**(§7.1 · 종전 «과녁 2점» 은 T103 으로 흡수) | **유일한 판정 자** |
| `node sim.js 2` | — | 폐지 (T96 — 등급이 사라져 «등급 내 폭» 은 측정 대상이 없다) |
| `node sim.js 3` | 챕터 1→N 진행 시뮬 (챕터당 시도 횟수 · 슬롯·장비 성장) | 진단 |
| `node sim.js 4` | F2P/과금 일 단위 장비 진행 (도달 챕터·정체 구간) | 진단 |
| `node sim.js 5` | 장비 등급별 사다리 (등급 풀셋이 어느 챕터에서 몇 %인가) | 진단 |
| `node sim.js fit` / `table` | 요구 전투력 역산 · 장비 옵션표 덤프 | 진단 |

`tools/scoreExp3.js`·`tools/score3.js` 는 **폐기된 실험3 채점표의 환산기**다 — 숫자를 보는 용도로만 남기고 그 점수로 회차를 판정하지 않는다.

### 7.5 폐기 목록 (⚑ 주인 확정 2026-09-03 «방금 말한 게 기준이고 나머지 기준은 다 폐기»)

**판정에 쓰지 말 것**: 스탯 사다리 **7점**(5·15·30·50·70·120·260) · **채점표 v1~v5 전부**와 실험1~5 배점 · **«비평가 2인 각 ≥8/10» 합격 게이트** · 실험1 등급 과녁(10/20/80%) · 실험2 등급 내 폭(<25%p) · 실험3 진행 곡선 목표(챕터별 시도 횟수) · 실험4 기준 ①②③ · 앵커 A·B·C · 하니스 «변별점 규칙» ①~⑤.

- **조정한 수치는 반드시 이 문서(§6 · §11.7)에도 갱신해서 기록할 것.** 게이트 `tools/verifyPlanConst.js` 가 PLAN ↔ 엔진 80항목을 대조한다.
- 시뮬 조정 후 최종 수치를 index.html 에 이식. (`sim.js` 와 `index.html` 의 수식이 항상 일치해야 함)

## 8. index.html 이식 시 주의

- 현재 index.html은 특전 65종 구버전 + 메테오가 들어있음 → 특전 목록/엔진 부분을 이 문서 스펙으로 교체. 메테오 완전 제거.
- ~~쉼터 선택지가 구버전("체력 50%" vs "즉시 레벨업")임 → 섹션 2.4로 교체.~~ → **✅ 교체 완료 (T46, 2026-09-02): «❤️ 체력 N 회복(최대체력 40%·실제 숫자 표시)» vs «🌟 경험치 +10». 게이트 `tools/verifyRestPolicy.js` 가 구버전 복귀를 감시한다.**
- 챕터가 무한 + 웨이브 5마리 고정 구조임 → `chapterLayout(c)` 기반 20챕터로 교체.
- `expNeed` 는 **주인 확정(2026-09-03 · T100) `5*lv+1`** 로 반영돼 있음 (§2.4 — 종전 ~~`4+2*lv`~~·~~`4+4*lv`~~·~~`4+3*lv`~~ 폐기).
- 이벤트/보스/전투 루프/렌더러/로비/사운드는 재사용 가능.

## 9. 배포 (유저 요청)

1. `git add -A && git commit` (메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`) → `git push origin main`
2. GitHub Pages 활성화: 이 PC에는 gh CLI가 **없음**. 유저에게 https://github.com/kuzuni/aaaw/settings/pages 에서 Branch=main, 폴더=/(root) 로 저장해달라고 안내할 것 (1회만 하면 됨). 활성화되면 https://kuzuni.github.io/aaaw/ 로 서빙됨. (gh를 설치했다면 `gh api repos/kuzuni/aaaw/pages -X POST -f "source[branch]=main" -f "source[path]=/"` 도 가능)
3. 아티팩트 republish: Artifact 도구, `file_path=index.html`, `url=https://claude.ai/code/artifact/228cba1b-db0b-4aef-b5ef-7aa344890524`

## 10. 작업 방식 (유저가 강하게 요구)

- **새로운 콘텐츠(특전/시스템/수치 체계)를 임의로 추가하지 말 것.** 추가/변경하고 싶으면 먼저 목록·계획을 유저에게 보여주고 승인받을 것.
- 이미 승인된 것(이 문서 전체)은 그대로 구현하면 됨.
- 밸런스 튜닝(숫자 조정)은 섹션 7 기준 내에서 자율 수행 OK — 결과 요약해서 보고할 것.
- 유저는 한국어 사용. 보고도 한국어로.

---

## 11. 장비 시스템 (⚑ 주인 지시 2026-09-02 — 전체 신설)

> «(위임)» 표시는 주인이 세부를 정하지 않아 기본값으로 채운 것 — 주인이 뒤집으면 그때 되돌린다. 나머지는 주인 원문.

### 11.1 구조
- **등급 5개**: 일반 / 희귀 / 영웅 / 전설 / 신화 (이름·순서는 위임. 색: 특전 4색 재사용 + 영웅 보라 `#B96CE8` (위임)).
- **부위 6개**: 무기 / 투구 / 갑옷 / 장갑 / 신발 / 목걸이 (위임).
- **부위당 종류 3가지** (예: 신발 = 부츠·샌들·장화) → 총 18종 × 5등급.
- 모든 장비는 장착 시 **공격력·체력 상승** (수치 = 등급·강화 스케일, T1/T6 튜닝 몫).
- **계열 옵션**: 같은 부위의 3종류는 각각 **다른 계열** (주인 예시: 샌들=회피 계열 · 부츠=반격 계열 · 장화=체력 계열). 같은 종류의 상위 등급은 **하위 등급 옵션을 포함하고 하나 더 얹는다** (주인 예시: 희귀 샌들 «회피 +7» → 영웅 샌들 «회피 +7, 회피 시 10% 확률로 도끼 1개 랜덤 적 발사»).
- **옵션 개수**: 일반 0개(옵션 없음) · 희귀 1 · 영웅 2 · 전설 3 · 신화 4 (등급당 +1, 위임으로 일반화).
- **⚑ 신화 강화 옵션**: 신화 장비는 **+3강 · +6강 · +9강**에서 옵션이 1개씩 더 생긴다 (신화 +9강 = 옵션 7개, **+9강이 옵션의 끝**). 강화 자체는 무한이지만 옵션 추가는 +9강까지.
- **18계열 옵션표**: T6 이 초안을 짜서 이 자리(§11.6)에 등재 — **기존 엔진 동사만 재사용**(회피/반격/치명타/처치/피격/회복/실드/골드/소환 5종. 신규 메커니즘 발명 금지), PROGRESS «주인 검토» 에 표로 올릴 것.

### 11.2 뽑기 (가챠)
- 1회 **400 다이아** (상점에서).
- 확률: **신화 0.1% / 전설 2% / 영웅 10% / 희귀 30% / 일반 57.9%** (나머지).
- **50회 천장**: 누적 50회째에 신화 확정. 신화가 나오면(자연 획득이든 천장이든) **카운터 0으로 리셋** (주인 예: 29회째 자연 신화 → 다시 50회 뒤 확정).
- **전설 10회 피티**: 10회당 1번 전설 이상 확정. **50회 천장과 겹치면 신화가 우선, 전설 확정은 다음 뽑기로 이월** (주인 명시). 전설 이상 획득 시 그 카운터 리셋(위임).
- 부위·종류는 18종 균등 랜덤 (위임). 1회/10회(4,000) 버튼, 10연 할인 없음 (위임).

### 11.3 합성·강화
- **같은 부위 + 같은 종류 + 같은 등급 3개 → 다음 등급 1개** (부위·종류 유지. 일반→희귀→영웅).
- **전설(차상위)**: 3개 합성해도 등급업이 아니라 **본체 +1강** (재료 중 최고 강화 기준 +1, 위임). **+10강 도달 시 그 대신 신화 0강 1개로 변환.**
- **신화(최고)**: 같은 방식으로 3개 합성 = +1강, **무한 강화**, 변환 없음. (옵션은 +9강까지 — §11.1)
- **장착 중인 장비는 합성 재료에서 제외** (위임).
- **⚑ 주인 확정 제약: 신화 +0강 > 전설 +9강** (공/체 기여 기준으로 반드시 성립).
  (**⚑ T29**: §11.5-a 의 기여식을 대입하면 성립 조건은 **`rarStep > 1 + plusStep*9`** — 현행 `plusStep 0.12` 기준 **하한 2.08** 이다. `rarStep`·`plusStep` 은 둘 다 §7 조정 노브라 T1 회차가 내리면 이 제약이 조용히 깨진다. 게이트: `node tools/verifyGearEcon.js`)

### 11.4 슬롯 강화 (영구강화의 대체)
- 장비를 끼우는 **슬롯 6개를 각각 골드로 강화**. **필요 골드는 레벨이 오를수록 증가** (곡선 = 튜닝 몫).
- **⚑ 주인 확정(2026-09-02): 슬롯 1레벨당 공격력·체력·실드 3스탯이 각각 +1%** — `slotMul(L) = 1 + 0.01*L` (가산). 종전 «레벨당 배수 `slotG 2.68`» 은 폐기.
- **⚑ 주인 확정: 슬롯 레벨 상한 150.** (만렙 ×2.5, 균등보너스까지 ×6.25)
- **균등 보너스 (골고루 유도)**: 6슬롯 전부 5렙 이상 → 공/체 **+5%**, 전부 10렙 이상 → **+10%**, … 이런 식으로 계속 (`+5% × floor(min(슬롯 렙)/5)`, 무한 — 위임으로 일반화).
- **기존 영구 강화 4종 폐지** (§2.2 참조).

### 11.5 경제 (주인 확정 상수)
- **다이아 무료 보급: 하루 2,500개** (시뮬 가정치이자 게임 수급 기준. 게임 내 구현은 로비 일일 보상 수령, 위임).
- **IAP: ₩110,000 = 다이아 12,000개** (모의 결제 — 실결제 없음, 상점의 모의 지급 버튼, 위임).
- 상점 탭: 다이아 구매(모의) + 장비 뽑기.

### 11.5-a 장비·슬롯 수치 (⚑ 주인 확정 2026-09-02 — 절대 스탯 지정)

> **종전의 «앵커 3점 역산 + 등비수열 `rarStep 155`» 은 전면 폐기.** 주인이 등급별 «다 꼈을 때 캐릭터 스탯» 을
> 절대값으로 확정했으므로(§11.7), 장비 계수는 역산이 아니라 **그 표에서 직접 나눈 값**이다.

```
부위 1개의 기여 = 등급별 기여값(아래 표) * (1 + 2.1111111111111111*강화) * (1 + 0.01*슬롯레벨)
플레이어 = (기본치 + 6부위 합) * 균등보너스(1 + 0.05*floor(min(슬롯)/5))
기본치(노템) = 공격력 25 · 체력 150 · 실드 250     (종전 공30/체300, 실드=체×0.8 파생 → 폐기)
plusStep 2.1111111111111111   (= 19/9. 강화 1레벨당 +211.11% → +9강 = 정확히 ×20 — 종전 0.13)
슬롯 레벨 상한 150 · 1렙당 +1% (종전 slotG 2.68 폐기)
슬롯 강화 비용 = floor(600 * 1.6^레벨)   (⚑ T1 R03 재산정 — 3.5 는 T53 «F2P 챕터 300 미도달» 의 주된 축이었다. §7 슬롯 비용 항목 참조)
```

**등급별 1부위 기여 (0강 · 슬롯 0렙)** — ⚑⚑⚑ **T102 재설계 (주인 확정 2026-09-03)**

| 등급 | 공격력 | 체력 | 실드 |
|---|---|---|---|
| 일반 | 4.167 | 16.667 | 25.000 |
| 희귀 | 12.500 | 58.333 | 91.667 |
| 영웅 | 29.167 | 141.667 | 225.000 |
| 전설 | 62.500 | 308.333 | 491.667 |
| 신화 | 395.833 | 1975.000 | 3158.333 |

**등급별 풀셋 총 스탯 (0강 · 슬롯 0렙 · 주인 확정표)** — 위 표는 이 표에서 `(풀셋 − 기본치) ÷ 6` 으로 나온 값이다(역산 금지).

> ⚠ 이 표의 첫 칸은 일부러 «…(풀셋)» 으로 적는다 — 위 기여표와 행 이름이 겹치면
> 게이트(`verifyPlanConst` §11.5-a 기여표 대조)가 어느 표를 읽는지가 문서 순서에 좌우된다.

| 등급 | 공격력 | 체력 | 실드 | 앞 등급 대비 |
|---|---|---|---|---|
| 노템(풀셋) | 25 | 150 | 250 | — |
| 일반(풀셋) | 50 | 250 | 400 | (현행 유지) |
| 희귀(풀셋) | 100 | 500 | 800 | **×2** |
| 영웅(풀셋) | 200 | 1,000 | 1,600 | **×2** |
| 전설(풀셋) | 400 | 2,000 | 3,200 | **×2** |
| 신화(풀셋) | 2,400 | 12,000 | 19,200 | **×6** |
| 신화 +9강(풀셋) | 48,000 | 240,000 | 384,000 | **×20** |

- **검산(T102 실측)**: 노템~신화 6줄은 주인 표와 **정확히 일치**한다.
- **⚠ 신화+9강만 −1% 정도 못 미친다 — 구조적이다.** 강화는 «장비 기여» 에만 걸리고 기본치(25/150/250)는 불변이라
  실제값이 **47,525 / 237,150 / 379,250** (오차 −0.99% / −1.19% / −1.24%)이다. 주인의 «×20» 은 풀셋 «총합» 에
  걸린 배수이고 엔진의 ×20 은 «장비분» 에 걸리므로 기본치의 19배만큼 어긋난다. 없애려면 «기본치도 강화된다» 를
  주인이 새로 확정해야 하므로 T102 는 손대지 않았다 — 승인 대기 등재(`docs/PROGRESS.md`).
- **⚑⚑ 주인 확정 제약 «신화 +0강 > 전설 +9강»(§11.3 · T29) 이 이 개편으로 깨졌다.** `plusStep 19/9` 에서
  **전설 +9강 = 전설 ×20 = 부위당 1,250 / 6,167 / 9,833** 인데 **신화 0강은 395.8 / 1,975 / 3,158** 이라
  세 축 모두 신화가 진다(여유 0.32배). 이것은 튜닝 실수가 아니라 **주인 확정 두 조항의 산술적 귀결**이다 —
  «전설→신화 ×6» 과 «+9강 = ×20» 을 동시에 요구하면 제약이 성립할 수 없다(성립하려면 +9강 배수 < 6, 즉
  `plusStep < 5/9 ≈ 0.556`). **파생 문제**: `legendToMythPlus`(전설 +10강 → 신화 0강 변환, §11.3)가
  **큰 손해**가 된다 — 전설 +10강 부위당 공 1,312.5 → 신화 0강 395.8. **주인 판단이 필요해 승인 대기로 등재했고,
  게이트 `verifyGearEcon` ①은 «면제(waiver) + 매 실행 경고» 로 두어 주인이 정하면 곧바로 되살아난다.**
- 슬롯 비용 `floor(600 * 3.5^레벨)` 은 «슬롯 1렙 = 5.8챕터분» 전제의 역산값이라 **이번 개편으로 근거가 소멸**했다
  (1렙 = +1%). 상한 150 레벨을 전제로 밸런스 루프에서 재산정 — 튜닝 몫.

### 11.6 계열 옵션표 (T6 초안 — **주인 검토 대상**)

> 작성: T6 (`sess-2221-26307` 워커 B). **기존 엔진 동사만 재사용**했다 — 새 메커니즘은 없다.
> 유일한 신규 조합은 `evadeAxe`(회피 트리거 + 도끼 발사)로, **주인이 §11.1 에서 직접 예시로 든 옵션**이다.
> 읽는 법: 상위 등급은 **왼쪽 옵션을 전부 포함하고 하나 더** 얹는다.
> 희귀=옵션1, 영웅=1~2, 전설=1~3, 신화=1~4, 신화 +3강=1~5, +6강=1~6, +9강=1~7 (+9강이 옵션의 끝). 일반은 옵션 없음.
> 구현 위치: `sim.js` 의 `GOPT` (덤프: `node sim.js table`).

| 부위 | 종류 | 계열 | 옵션1 · 희귀(1) | 옵션2 · 영웅(2) | 옵션3 · 전설(3) | 옵션4 · 신화(4) | 옵션5 · 신화+3(5) | 옵션6 · 신화+6(6) | 옵션7 · 신화+9(7) |
|---|---|---|---|---|---|---|---|---|---|
| 무기 | 대검 | 검기 | 공격력 +6% | 공격 시 5% 확률 검기 발사 | 공격 시 30% 확률 공격력 +14% 4초 | 검기 관통 20·사거리 1400 | 공격 시 25% 확률 공격력 +35% 5초 | 반격 시 검기 발사(확정) | 체력 50% 이하 적에게 피해 2.2배 |
| 무기 | 전투도끼 | 도끼 소환 | 공격력 +6% | 공격 시 5% 확률 도끼 발사 | 치명타 확률 +6 | 도끼 3개로 증가 | 도끼 발동 확률 +5% | 처치 시 실드 충전 | 최대 체력 적 첫 타격 피해 +20% |
| 무기 | 장궁 | 화살 소환 | 공격력 +6% | 공격 시 5% 확률 화살 2발 | 치명타 배율 +30 | 화살 3발로 증가 | 치명타 시 75% 확률 추가타 | 화살 발동 확률 +5% | 최대 체력 적에게 치명타 확률 62 |
| 투구 | 투구 | 방어 | 방어 +6 | 피격 시 방어 +3 3초(누적) | 방어 +8 | 피격 시 30% 확률 방어 +14 4초 | 피격 시 방어 +5 4초(누적) | 피격 시 20% 확률 방어 +15 4초 | 실드가 있으면 받는 피해 20% 감소 |
| 투구 | 왕관 | 치명타 확률 | 치명타 확률 +6 | 치명타 시 치명 확률 +5 3초(누적) | 치명타 확률 +8 | 처치 시 30% 확률 치명 확률 +14 4초 | 치명타 시 45% 확률 공격 즉시 재장전 | 치명타 확률 +10 | 치명타 시 공격력 +15% 4초 |
| 투구 | 두건 | 번개 소환 | 치명타 배율 +25 | 공격 시 5% 확률 번개 1회 | 치명타 시 치명 배율 +34 4초 | 번개 2회로 증가 | 3초마다 번개 자동 발사 | 치명타 시 공격속도 +25% 3초 | 공격 시 5% 확률 소환 무작위 발사 |
| 갑옷 | 판금갑옷 | 체력·피격 | 최대 체력 +8% | 피격 시 15% 확률 체력 2% 회복 | 최대 체력 +10% | 피격 시 22% 확률 회피 +14 3초 | 최대 체력 +12% | 피격 시 5% 확률 실드 5% 충전 | 사망 시 1회 부활 |
| 갑옷 | 사슬갑옷 | 가시 반사 | 방어 +5 | 피격 시 60% 확률 가시 반사 | 방어 +7 | 피격 시 방어 +3 3초(누적) | 가시 반사 확률 +60% | 최대 체력 +10% | 피격 시 30% 확률 회피 +15 3초 |
| 갑옷 | 로브 | 실드 | 최대 실드 +15% | 처치 시 실드 소량 충전 | 최대 실드 +20% | 회복 시 30% 확률 실드 8% 충전 | 처치 시 실드 충전 강화 | 최대 실드 +25% | 실드가 0일 때 공격력 1.5배 |
| 장갑 | 건틀릿 | 치명타 배율 | 치명타 배율 +30 | 치명타 시 치명 배율 +20 3초 | 치명타 배율 +40 | 치명타 시 30% 확률 체력 4% 회복 | 치명타 배율 +50 | 치명타 시 75% 확률 추가타 | 뒤쪽 적에게 피해 3.2배 |
| 장갑 | 가죽장갑 | 공격속도 | 공격속도 +8% | 공격 시 30% 확률 공속 +5% 3초 | 공격속도 +10% | 처치 시 공속 +20% 4초 | 공격속도 +12% | 처치마다 공속 영구 +1% | 공속 +35%·이동속도 +35% |
| 장갑 | 핸드랩 | 연타·분신 | 공격력 +5% | 공격 시 30% 확률 공격력 +5% 3초 | 공격력 +7% | 공격 시 10% 확률 공격력 영구 +1% | 공격력 +9% | 기본공격마다 분신 추가타 | 체력 25% 이하 적 즉사(보스 제외) |
| 신발 | 샌들 | 회피 | 회피 +7 | 회피 시 10% 확률 도끼 1개 발사 | 회피 +8 | 회피 시 공격력 +28% 5초 | 회피 시 15% 확률 체력 7% 회복 | 회피 시 회피 +8 3초(누적) | 회피 시 다음 공격 치명타 확정 |
| 신발 | 부츠 | 반격 | 반격 확률 +7 | 반격 시 공격력 +5% 3초 | 반격 확률 +8 | 반격 시 공격력 +14% 4초 | 반격 피해 +100% | 반격 시 체력 4% 회복 | 반격 시 연쇄 반격(확정) |
| 신발 | 장화 | 체력 | 최대 체력 +8% | 처치 시 체력 0.5% 회복 | 최대 체력 +10% | 회복량 +20% | 최대 체력 +12% | 처치 시 체력 +0.8% 추가 회복 | 체력 10% 이하 시 회피 +40 |
| 목걸이 | 펜던트 | 회복 | 회복량 +15% | 회복 시 30% 확률 방어 +5 3초 | 회복량 +20% | 회복 시 20% 확률 실드 3% 충전 | 회복 시 공격력 +8% 3초 | 회복 시 20% 확률 추가 회복 | 과회복분의 7배가 실드로 전환 |
| 목걸이 | 부적 | 처치 | 골드 획득 +30% | 처치 시 방어 +10 3초 | 골드 획득 +40% | 처치 시 30% 확률 치명 확률 +14 4초 | 획득 경험치 +1 | 처치 시 실드 충전 강화 | 골드 획득 2배 |
| 목걸이 | 구슬목걸이 | 창 소환 | 공격력 +5% | 공격 시 5% 확률 창 발사 | 치명타 확률 +6 | 창 피해 13.5배·관통 | 창 발동 확률 +5% | 적 화살 30% 확률 오발 | 모든 발동 확률 1.22배 |

- **위임 표기**: 부위별 종류 이름(대검/전투도끼/장궁 …)과 계열 배정은 위임 기본값이다.
  단 **신발 3종은 주인 예시 그대로** — 샌들=회피 · 부츠=반격 · 장화=체력.
- 소환 5종(도끼·화살·번개·검기·창)이 각각 한 계열의 주축이 되도록 배치했다.

### 11.7 밸런스 과녁 (⚑⚑⚑ 주인 확정 2026-09-03 · T103 — 난이도 사다리 8점)

> **⚑⚑⚑ 2026-09-03 주인 확정 — 유일한 밸런스 기준은 §7.1 의 «난이도 사다리 8점» 이다.** 여덟 칸 전부 «특전 10종 순서 획득 · 클리어율 10% ±2%p» 이고, 장비 등급뿐 아니라 **슬롯 레벨까지** 못박혀 있다: 노템 5 · 일반 15 · 희귀+슬롯5 28 · 영웅+슬롯10 40 · 전설+슬롯15 70 · 신화+슬롯25 150 · 신화9강+슬롯50 380 · **신화9강+슬롯100 420**(⚑ 주인 정정 — 종전 600 폐기).
>
> 종전의 «사다리 7점»(T35~T1) 과 그 뒤의 «과녁 2점»(T97)은 둘 다 이 표에 흡수됐다. 아래 «구간별 성장률» 표는 **사다리 8점에 재적합된 현행 값**이며, 구간 경계를 **과녁 챕터에 맞춰 새로 잘랐다**(종전 15/20/30/50/70/120/260 은 옛 7점 것이라 과녁과 어긋나 있었다). 재적합 근거·실측은 `docs/balance/T103/result.md`.

- 챕터 **420개** (⚑⚑⚑ T103 — 주인 정정으로 600 → 420). 벽: 10 · 15(비활성) · **90(대형)** · **420(최종 · 배수 ×1.00 = 꺼짐)**. **벽 배수는 주인이 튜닝에 위임**.
  **⚑ 왜 420 위를 안 두나** — 420 위는 슬롯 150(만렙, 슬롯 100 대비 파워 ×1.40)으로도 150→420 의 률에서 **≈445 가 한계**라, 445~600 은 아무도 못 깨는 죽은 콘텐츠가 된다. 주인이 600 유지를 원하면 420→600 을 «슬롯 150·과금 장기 영역» 으로 두고 률만 이으면 된다(한 줄 정정으로 되돌릴 자리).
- ~~앵커 A(신화 0강+슬롯 15렙 = 90) · 앵커 B(신화 +9강+슬롯 50렙 = 300) · 앵커 C(전설 0강+슬롯 10렙 = 30)~~ → **폐기.**
  §7.1 의 «난이도 사다리 8점» 이 유일한 과녁이다.

**⚑ 주인 확정 스탯 사다리 8점 (측정 조건 = «특전 10종 순서 획득» · 슬롯 레벨까지 주인이 못박았다)**

> **⚑⚑⚑ T103 — 이 표가 §7.1 의 사다리 8점과 같은 표다.** 종전의 «겨우 클리어하는 챕터» 열(폐기 기준)과
> «일반 특전만 뜨는 런»(승인 38번 A안 — 등급 폐지로 성립 불가)은 둘 다 사라졌다. 판정은 **클리어율 10% ±2%p** 하나뿐이다.
> 스탯 3열은 §11.5-a 주인 확정 장비표 + 슬롯·균등 보너스에서 나온 값이고, 엔진 실측이 이 값과 0.5% 안에서 같은지를
> 게이트 `verifyScoreCriteria` ⓒ 가 매번 확인한다.

| # | 상태 | 슬롯 | 과녁 챕터 (클리어율 10%) | 공격력 | 체력 | 실드 |
|---|---|---|---|---|---|---|
| 1 | 노템 | 0 | **5** | 25 | 150 | 250 |
| 2 | 일반 풀셋 | 0 | **15** | 50 | 250 | 400 |
| 3 | 희귀 풀셋 | 5 | **28** | 108.9 | 543.4 | 868.9 |
| 4 | 영웅 풀셋 | 10 | **40** | 239.3 | 1,193.5 | 1,908.5 |
| 5 | 전설 풀셋 | 15 | **70** | 524.7 | 2,619.1 | 4,188.9 |
| 6 | 신화 풀셋 | 25 | **150** | 3,742.2 | 18,703.1 | 29,921.9 |
| 7 | 신화 +9강 풀셋 | 50 | **380** | 106,912 | 533,475 | 853,125 |
| 8 | 신화 +9강 풀셋 | 100 | **420** | 190,050 | 948,300 | 1,516,500 |

- 챕터 **150 → 420** 은 슬롯 강화 몫이다. 7칸(슬롯 50) → 8칸(슬롯 100)의 파워 증가는 **×1.78** 뿐인데 챕터는 40개라, 이 구간의 챕터당 요구 성장률이 앞 구간보다 낮다 — 그래서 **최종 벽이 꺼졌다**(위 §6). 주인이 8칸을 600 이 아니라 420 으로 내린 것이 바로 이 «평평해지는 구간» 을 없애기 위한 정정이다.
- **⚑ 난이도 구간별 성장률 — 튜닝 노브 (주인 확정 2026-09-02 15:1X: «2번(성장률 표)은 필요 없고 1번(사다리) 기준으로 해야 함»).** 아래 표는 **사다리 8점에 실측 재적합한 T103 현행값**이며, 구간 경계가 과녁 챕터와 1:1 로 맞물려 있다. 게이트 `tools/verifyPlanConst.js` 가 이 표와 엔진 배열을 8줄 전수 대조한다.

| 구간 | 적 HP 성장/챕터 | 적 DMG 성장/챕터 | 비고 |
|---|---|---|---|
| 1 → 5 | 2.92% | 2.65% | ⚑ T103 — 1칸(노템 챕터 5)만 지나는 구간. 률은 옛 값 그대로 두고 **기저만** 옮겨 맞췄다(챕터 1~4 의 상대 형상 보존) |
| 5 → 15 | 1.0082% | 1.0082% | ⚑ T107 2단계 재적합(T106 3.2343/3.2343) — 2칸(일반 챕터 15)의 전용 노브. 10 벽(HP ×1.5 · DMG ×1.25)이 이 구간 안에 있어 예산을 나눠 쓴다. **전 구간 최저 률** — 기저가 2.08배로 뛰면서 챕터 5 가 이미 충분히 어려워, 5→15 는 거의 평평해도 과녁이 맞는다 |
| 15 → 28 | 6.2254% | 5.8603% | ⚑ T107 2단계 재적합(T106 6.6673/6.3007) — 3칸(희귀+슬롯5 챕터 28) |
| 28 → 40 | 8.2263% | 6.5726% | ⚑ T107 2단계 재적합(T106 9.4817/7.8089) — 4칸(영웅+슬롯10 챕터 40). **전 구간 통틀어 최대 경사** · 잔차 보정 1회(×1.005 · 아래 result.md §3) |
| 40 → 70 | 5.2733% | 2.5283% | ⚑ T107 2단계 재적합(T106 5.6146/2.8607) — 5칸(전설+슬롯15 챕터 70) |
| 70 → 150 | 2.4105% | 2.6021% | ⚑ T107 2단계 재적합(T106 2.5948/2.7867) — 6칸(신화+슬롯25 챕터 150). **90 대형 벽(HP ×2.0 · DMG ×1.5)이 이 구간 안**이라 그만큼 률이 낮다 |
| 150 → 380 | 2.3170% | 2.2640% | ⚑ T107 2단계 재적합(T103·T106 2.3303/2.2773) — 7칸(신화9강+슬롯50 챕터 380). 230챕터짜리 최장 구간이라 배수가 아주 작게만 움직인다 |
| 380 → 420 | 1.3863% | 1.3863% | ⚑ T107 2단계 재적합(T103·T106 1.4496) — 8칸(신화9강+슬롯100 챕터 420). **최종 벽이 꺼져 있어**(×1.00) 이 률이 그대로 마지막 40챕터다 |

**⚑⚑⚑ T103 재적합 (2026-09-03 / 워커 B) — 위 표가 주인 확정 «사다리 8점» 에 맞춘 현행 값이다.** 세 가지를 했다.
① **구간 경계를 과녁 챕터에 맞춰 새로 잘랐다** (5·15·28·40·70·150·380·420). 종전 경계(15/20/30/50/70/120/260)는 옛 사다리 7점 것이라 새 과녁과 어긋나 있었고, 그 상태로는 구간 하나가 과녁 둘에 걸려 «하나를 맞추면 옆이 밀리는» 구조가 남는다. 경계를 과녁에 맞추면 **구간 k 가 과녁 k+1 만 담당**하므로 앞에서부터 순서대로 자를 수 있다.
② **8칸 각각에서 «적 HP·DMG 를 같은 배수 m 으로 곱해 클리어율 10% 가 되는 m» 을 실측**하고(격자 주사 · 시드 11·12·13 × 각 1,500판), 구간률을 `((m_{k+1}/m_k) · D_old(c_{k+1})/D_old(c_k) ÷ 구간 안 벽배수)^(1/n)` 로 다시 잘랐다. 같은 m 을 HP·DMG 에 함께 곱하므로 **챕터별 HP:DMG 비가 보존**된다. 1→5 구간률만 옛 값을 그대로 두고 **기저를 40.6/7.55 → 23.1528/4.30551 로** 옮겼다(챕터 1~4 의 상대 형상 보존).
③ **최종 벽을 껐다**(×1.00) — 위 §6 의 «왜 껐나» 참조. 10·15·90 벽은 손대지 않았다(70→150 률이 1 위라 90 벽 예산이 남는다).
- **인계 기준선(재적합 전)**: 8칸 클리어율이 0·0·0·0·0·89.0·100.0·100.0% 였다 — 초반 5칸은 T102 의 장비 배수 개편(신화 ×6 · +9강 ×20) 뒤 난이도 노브를 아무도 안 만져 «못 깨는» 쪽으로, 후반 3칸은 «너무 쉬운» 쪽으로 통째로 벗어나 있었다.
- 실측·원시 출력 `docs/balance/T103/result.md`.

<details><summary>이력 — T97 재적합 (과녁 2점 시절 · 2026-09-03 / 워커 C)</summary>

**⚑⚑⚑ T97 재적합 — 당시 표가 주인 확정 과녁 2점에 맞춘 값이었다.** 두 가지를 했다.
① **구간0 을 챕터 5 에서 쪼갰다.** 종전엔 챕터 1~14 가 한 구간이라 과녁 A(챕터 15)와 B(챕터 4)의 노브가 하나뿐이었고, «A 를 맞추면 B 가 밀리는» 구조였다(T96 인계 기준선 A 100.0% · B 33.6%). 챕터 4 는 `[0,·]` 만, 챕터 15 는 `[5,·]` 를 10칸 지나므로 분할하면 **두 과녁이 서로 독립인 노브를 갖는다**(R05 의 15→20 / 20→30 분할과 같은 수법).
② **D(120~300) 보존 축척.** ①로 챕터 15 «위» 가 통째로 HP ×2.406 · DMG ×2.471 올라간다. 그대로 두면 후반이 통째로 밀려 챕터 300 이 상한 장비(신화+9·슬롯150)로도 안 깨진다. 그래서 **15 → 120 구간률의 로그만 같은 비율로 축소**했다(HP λ=0.792 · DMG λ=0.735 · 120 위 두 구간과 벽 배수는 불변) — `Σ nᵢ·ln rᵢ`(15→120)가 정확히 `ln(shift)` 만큼 줄어 **D(120) 이 보존**되고, 그 위는 구간률이 그대로라 **챕터 120~300 의 난이도가 T97 이전과 완전히 동일**하다(적 HP ch120 12,250 · ch300 416,386 불변). 난이도 증가분은 챕터 15 의 ×2.40 에서 120 의 ×1.00 으로 잦아든다(ch30 ×2.04 · ch50 ×1.62 · ch100 ×1.08).
⚠ **축척 구간을 300 까지 넓히면 오히려 나빠진다** — 그러면 챕터 120~260 이 1.2~1.4배 무거워져 F2P 정체 지점이 **챕터 202 → 193 으로 당겨진다**(실험3 시드 11 실측). 120 까지만 축척하면 **210** 으로 밀린다. 후반은 «건드리지 않는 것» 이 가장 좋았다.
⚠ 두 과녁은 15 «위» 구간률과 **독립**이므로 ②는 과녁을 흔들지 않는다. 근거 `docs/balance/T97/raw.md`.

</details>

  **⚑ T1 R01 재적합값이다** (초기값은 아래 «T35 초기값» 표 — 되돌림용으로 남긴다).
  적합 방법: 구간마다 지수 `a` 하나만 움직이고 성장률 = `1 + a*(T35 초기율 − 1)` 로 두어 HP:DMG 비율(=주인 확정 사다리의 공격력축:체력축 비율)을 보존했다. 기저는 `40/8` → **`22.8/4.56`**(×0.57).
  `a` 를 앞 구간부터 순차 이분탐색 — 구간 k 는 과녁 k+1 이후에만 영향을 주므로 뒤 구간이 앞 과녁을 되돌리지 않는다.
  **실측: 사다리 7점이 처음으로 7/7 합격** (시드 4벌 + 무시드 각 400판 = 35측정 전부 합격 구간 2~10% 안, 관측 2.8~8.5%). 근거 `docs/balance/resume-R01/raw.md`.

  **⚑ T1 R02 — 구간0(5→15)만 `7.18/4.97%` → `2.92/2.65%`, 기저 `22.8/4.56` → `26.82/4.986`.**
  난이도 총량을 내린 것이 **아니라** §6 «벽 예산» 항등식(`g0^10·W = 상수`)대로 **10챕터 벽 배수(HP ×1.50 · DMG ×1.25)로 옮겨 담은 재배치**다.
  사다리 과녁 5·15 가 항등식으로 보존되고 15 이후 구간률은 손대지 않았으므로 **7점이 그대로 유지된다**(실측 7/7).
  구간 15 이후 5줄과 지수 `a` 는 R01 값 그대로다. 근거 `docs/balance/resume-R02/raw.md`.

  **⚑ T1 R05 — 구간 `15 → 30`(5.44%) 하나를 `15 → 20`(15.00%) + `20 → 30`(0.96%) 둘로 분할.**
  «벽 예산» 과 같은 종류의 재배치다. 항등식 `x⁵ · y¹⁰ = 1.0544¹⁵ = 2.2135`(실제 `1.15⁵·1.0096¹⁰ = 2.2129`, 잔차 0.03%)
  로 **챕터 30 이후 누적이 보존**되므로 사다리 과녁 7점이 전부 그대로다 — 바뀌는 것은 **챕터 16~29 뿐**이다(실측 7/7 유지).
  근거: 실험3 의 «1~20» 소계가 0.325/1.5 로 줄곧 바닥이고 구간 11~19·20 이 «시도 1회» 에 붙어 있었다.
  40시드 짝지음(공통난수) 실측 — 1~20 소계 **+0.023 ± 0.005 (t=4.21)** · 총점 **1.274 → 1.384 (+0.110 ± 0.061)**.
  이 족(族)의 상한은 **x = 1.1724**(그 위는 `y < 1` 이라 챕터 20~30 에서 적이 약해진다).
  근거 `docs/balance/resume-R05/raw.md`.

  **T35 초기값 (되돌림용 · 표를 두 벌 두면 PLAN↔엔진 드리프트가 재발하므로 한 줄로만 남긴다 — «플레이어 힘 선형» 가정의 역산이고 이 값으로는 사다리 1/7 이었다):**
  HP `7.18 / 4.73 / 3.53 / 4.99 / 1.65 / 0.55` % · DMG `4.97 / 4.73 / 2.18 / 2.38 / 1.69 / 0.51` % (구간 순서 동일, 기저 `40 / 8`).

  (~~챕터 1~5 는 5→15 성장률을 앞으로 연장~~ → **⚑ T97 에서 `1 → 5` 가 독립 구간이 됐다** — 과녁 B 전용 노브라 5→15 와 값이 다르다. ~~260~300 은 120~260 성장률을 뒤로 연장~~ → **⚑ T1 R03 에서 `260 → 300` 을 독립 구간으로 분리**했다 — 연장하면 ×1.84 가 되어 위 «필요 ×1.25» 와 어긋난다.)
  **종전 단일 상수 `eHpG 1.185`·`eDmgG 1.08` 은 전 구간 폐기.**
- 이 개편으로 **숫자 인플레가 사라진다**: 종전 설계는 신화 풀셋 공격력이 86억이었는데 주인 확정표는 1,200 이다.
- 검증은 §7 실험4(F2P 진행)·실험5(스탯 사다리 7점) — T1 채점 루프에 통합.
