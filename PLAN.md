# 꼬마기사 키우기 — 구현 인수인계 문서 (PLAN.md)

> 이 문서는 다른 세션/계정이 **이 문서만 보고** 남은 작업을 그대로 이어서 완성할 수 있도록 쓴 완전한 스펙 + 작업 지시서다.
> 유저와 이미 합의된 사항이므로 **여기 적힌 내용을 임의로 바꾸지 말 것.** 새로운 아이디어(신규 특전, 신규 시스템)를 추가하려면 반드시 유저에게 먼저 목록/계획을 보여주고 승인받은 뒤 구현할 것. (유저가 여러 번 강하게 요구한 작업 방식임)

---

## 0. 프로젝트 개요

- **게임**: 세로형(9:19) 캐주얼 자동전투 방치형 RPG. 단일 HTML 파일(`index.html`)로 구현. 외부 라이브러리 없음, 그래픽은 Canvas로 직접 그림(**참고 스크린샷: 레포의 `docs/ref/*.jpg`** — 주인이 2026-09-02 원본 이름 그대로 등록. Legend of Slime 류 카툰 스타일. 구 경로 `C:\Users\user\Desktop\올빼미\` 는 로컬 전용이라 폐기), 오디오는 Web Audio 합성.
- **레포**: `C:\Users\user\Documents\GitHub\aaaw` (main 브랜치, origin 원격 있음)
- **배포된 아티팩트**: https://claude.ai/code/artifact/228cba1b-db0b-4aef-b5ef-7aa344890524
  - 다른 대화에서 업데이트하려면 Artifact 도구에 `url`로 이 주소를 넘겨서 republish (url 없이 publish하면 새 아티팩트가 생기니 주의).
- **파일 현황**:
  - `index.html` — 게임 본체. **현재 중간 상태**: 특전 65종 버전(메테오 포함, 구버전 목록)이 들어있음. 아래 최종 스펙(특전 102종, 챕터 20개)으로 **재작성 필요**. `expNeed`는 이미 `4+2*lv`로 수정돼 있음.
  - `sim.js` — 밸런스 시뮬레이터 (Node로 실행). 최종 특전 102종/챕터 생성 로직이 이미 구현되어 있음. **게임과 수식을 공유하는 기준 코드**로 쓸 것.
  - `PLAN.md` — 이 문서.

## 1. 남은 작업 체크리스트 (순서대로)

1. [ ] `node sim.js 1` / `node sim.js 2` / `node sim.js 3` 실행해서 밸런스 검증 (섹션 7)
2. [ ] 시뮬 결과로 `sim.js`의 `TUNE` 값과 특전 수치 조정 (섹션 7의 합격 기준 충족까지 반복)
3. [ ] 조정 끝난 최종 수치/특전을 `index.html`에 이식 (섹션 2~6 스펙대로 전면 재작성)
4. [ ] 브라우저에서 실제 플레이 확인 (챕터1 클리어, 특전 획득, 이벤트 3종, 보스, 사망/클리어 흐름)
5. [ ] 아티팩트 republish (위 URL로)
6. [ ] git commit + push, GitHub Pages 활성화 (섹션 9)

---

## 2. 확정된 게임 스펙 (유저와 합의 완료 — 변경 금지)

### 2.1 화면/기본
- 프레임: **9:19 세로**, 화면 중앙 배치, `aspect-ratio:9/19`.
- 폰트: Google Fonts **Jua**. 카툰/캐주얼 스타일, 다크 패널 + 골드 액센트 (현 index.html 스타일 유지).
- 오디오: **BGM 없음. 효과음만** (Web Audio 합성; click/hit/crit/hurt/arrow/bolt/throw/coin/poof/shield/evade/levelup/perk/heal/devil/angel/boss/win/lose). 음소거 토글은 로비/게임 양쪽 🔊 버튼, localStorage 저장.
- 저장(localStorage, key `kkoma-knight-v1`): `{gold, maxChapter, selChapter, muted, up:{dmg,hp,aspd,crit}}`. try/catch 필수.

### 2.2 로비 (참고: 메인로비.jpg)
- 다크그린 배경 + ⚔ 워터마크, 상단: 아바타(캔버스로 기사 그림) + ⚔️전투력 + 🪙골드 + 🔊.
- 중앙: `CHAPTER N` + 밑줄 + **챕터 디오라마 카드**(챕터별 시드로 나무/바위/수풀 배치가 달라지는 캔버스) + ◀▶ 화살표(해금 범위 내 이동).
- 주황 **START** 버튼.
- 하단 5탭: 상점(🔒잠금 토스트), **강화**(영구 강화 팝업), 전투(현재), 카드(🔒), 도전(🔒).
  - **⚑ 주인 지시(2026-09-02, §11 장비 시스템 도입): 아래 «영구 강화 4종» 은 통째로 폐지.** 성장 축은 §11 의 장비 + 슬롯 강화로 대체된다. 탭 재구성(위임 규약 기본값): **강화 탭 → 장비 탭**(장착/합성/슬롯 강화), **상점 탭 해금**(다이아 구매 + 장비 뽑기), 카드/도전은 🔒 유지. 아래 4종 수치·비용·R05 성장률 기록과 «강화 상한 티어 시스템» 지시(같은 날 07:4X)는 **대상 소멸로 함께 폐기** — 골고루 유도 취지는 §11.4 슬롯 균등 보너스가 계승한다. 저장 포맷의 `up:{}` 도 장비/슬롯/다이아 저장으로 교체(§11).
- ~~영구 강화 4종 (골드 소모, 죽어도 유지):~~ (폐지 — 위 지시. 아래 기록은 되돌림용 보존)
  - 공격력: `30+8*Lv`, 비용 `floor(40*1.32^Lv)`
  - 체력: `300+60*Lv`, 비용 `floor(40*1.30^Lv)`
  - 공격속도: `1+0.03*Lv`/s, 비용 `floor(60*1.38^Lv)`
  - 치명타: `5+Lv`%, 비용 `floor(55*1.36^Lv)`
  - ※ 시뮬 결과에 따라 비용 성장률 조정 가능 (섹션 7)
  - **⚑ 주인 지시(2026-09-02): 강화 상한 티어 시스템 (골고루 강화 유도).** 각 강화의 최대 레벨은 **10렙에서 시작**하고, **4종이 전부 현재 상한에 도달하면 상한이 +10** 된다 (전부 10렙 → max 20렙, 전부 20렙 → max 30렙, … 무한 반복). 수식(위임 규약 기본값): `각 강화의 상한 = 10 * (1 + floor(min(4종 레벨)/10))` — 하나라도 뒤처지면 나머지가 그 구간에 묶이므로 골고루 올릴 수밖에 없다. UI: 상한에 걸린 강화 버튼은 «MAX — 모든 강화 N렙 달성 시 해제» 표시(위임 규약 기본값). sim.js 실험3의 자동 강화 구매도 이 상한을 따라야 함 — **밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «강화상한 반영» 표시.**
  - **R05 확정 비용 성장률** (수치식은 그대로, 성장률만 조정 — 근거 `docs/balance/round-05/`):
    공격력 `floor(40*1.055^Lv)` · 체력 `floor(40*1.055^Lv)` · 공격속도 `floor(60*1.075^Lv)` · 치명타 `floor(55*1.075^Lv)`
  - **R05 확정 골드 드랍**: 처치 `round((0.6+0.10*챕터)*rand(1,1.8)*goldMul)`, 클리어 보너스 `3*챕터`
    (초기값 `(2+챕터)` / `40*챕터` 에서 하향 — 비용이 싸진 만큼 수입을 줄여 강화 레벨 곡선을 유지)

### 2.3 인게임 (참고: 메인 게임화면*.jpg)
- 상단: 💀처치수 + 🪙골드 + 🔊 + ☰(일시정지: 이어하기/포기). 중앙 상단: CHAPTER N + 진행도 바(처치수/전체).
- 좌하단 배속 버튼: x1 → x1.5 → x2.
- 하단 다크 패널: `Lv/EXP바(초록)`, `HP바(빨강)`, `실드바(파랑)` + 스탯 그리드 8종(공격력/방어력/공격속도/반격확률/치명타확률/회피/치명타배율/흡혈). 특전으로 기본값보다 오른 스탯은 **초록색**. 버프 반영된 **실효값(eff)** 표시.
- 우하단 **📘 Info 버튼** → 이번 런에서 얻은 특전 목록 팝업 (등급 테두리 카드, "화면을 탭하면 닫힘"). 천사의 축복도 기록됨.
- **⚑ 주인 지시(2026-09-02): 발동 중 버프 아이콘 표시.** 시간제 버프(예: «3초간 회피 +10%»)가 발동 중일 때 그게 켜져 있는지 보이도록, **화면 왼쪽 위·챕터 표시보다 아래** 위치에 해당 특전의 아이콘을 표시한다. **등급색 테두리 포함.** 중첩 중이면 **아이콘 오른쪽 위에 중첩 수**(예: 3)를 뱃지로. 버프가 끝나면 아이콘 제거. 여러 버프 동시 발동 시 세로로 쌓는다(위임 규약 기본값 — 오래된 것이 위).
- **⚑ 주인 지시(2026-09-02): 얻은 특전 미리보기 줄.** 인포를 펼치지 않아도 뭘 얻었는지 보이게, **📘 Info 버튼이 있는 행의 왼쪽 끝부터** 이번 런에서 얻은 특전 아이콘을 **획득 순서대로** 나열한다. **등급색 포함.** 같은 특전 중복 획득은 아이콘 1개 + 오른쪽 위 개수 뱃지, 한 줄을 넘치면 최신 것들이 보이게 하고 «+N» 으로 합침(위임 규약 기본값 — 전체 목록은 인포 팝업).
- 전투 규칙:
  - 플레이어는 자동으로 우측 전진(속도 `132*walkMul`), 가장 가까운 적이 74px 안이면 멈추고 공격(공속 = 실효 aspd, 공격당 1.0 쿨).
  - **적은 절대 이동하지 않음. 스폰 위치에 고정.** 근접 적은 플레이어가 105px 안에 오면 그 자리에서 공격(쿨 1.3s). 원거리 적(40% 확률, 웨이브 첫 마리는 제외)은 440px 안에서 화살 발사(쿨 2.1s, 화살 속도 330).
  - 적 간격 88px. HP바(숫자 포함)를 적 발밑에 표시. 플레이어는 HP바+실드바 2단.
  - 보스: 크기 1.7배, 뿔 달린 스킨, 3번째 공격마다 강타(x2.2, 화면 흔들림+경고 텍스트). 접근 시 "⚠️ BOSS ⚠️" 경고 + 효과음.
  - 실드: `maxSh = maxHp*0.8`(기본). 피해는 실드 먼저 소모. ~~**웨이브(보스 제외) 전멸 시 실드 완전 충전.**~~ **⚑ 주인 지시(2026-09-02): 웨이브 전멸 실드 충전 폐지.** 실드는 공짜로 재충전되지 않는다 — 충전 수단은 **특전뿐**(🔰 처치 시 충전, 🔋 피격 시 충전, 💠 초과 회복 등). 시작 시 가득 찬 실드가 닳으면 특전 없이는 그대로 끝. sim.js 에도 웨이브 전멸 충전이 있으면 제거 — **밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «실드무료충전폐지 반영» 표시.**
  - 회피/방어/반격/흡혈/치명타 모두 스크린샷 게임과 동일 개념.
- 연출(이미 구현됨, 유지): 타격 스파크, 크리 화면흔들림, 처치 연기+코인 텍스트, 데미지 숫자 팝(크리는 크고 금색), 캐릭터 런지/스쿼시, 레벨업 팝업 회전광선+배너드롭+카드 순차등장.

### 2.4 챕터 구조 (1~20)
- 챕터 수: ~~**20개.**~~ **⚑ 주인 지시(2026-09-02, §11 도입에 따름): 300개로 확장** (밸런스 과녁 — 앵커 A «신화 0강+슬롯 15렙 = 챕터 90 겨우», 앵커 B «신화 +9강+슬롯 50렙 = 챕터 300 겨우». §11.7). 클리어 시 다음 챕터 해금(순차), 300 클리어 후 "모든 챕터 클리어" 처리. 벽: 기존 10·15 유지 + **90 대형 벽 + 300 최종 벽**(중간 벽 추가 여부는 T6/T1 튜닝 몫).
- 레이아웃은 **챕터 번호 시드 기반 결정적 생성** (`sim.js`의 `chapterLayout(c)` 그대로 사용):
  - 웨이브 수: 4~5개. **웨이브당 적 10마리 또는 12마리.**
  - 웨이브 사이 이벤트: rest(쉼터)/devil(악마)/angel(천사)를 가중치(45/30/25%)로 배치하되 **쉼터 최소 1회 보장** (2~3회 나올 수도 있고, 악마나 천사 중 하나만 나올 수도 있음 — 유저 요구사항).
  - 마지막은 보스 1마리.
- 난이도: `sim.js`의 `enemyStats(c,w)` 사용. **챕터 10부터 벽(배수 점프)이 있어 강화 노가다 없이는 클리어가 빡세야 함.** 15에도 2차 점프. 정확한 수치는 시뮬 튜닝 결과를 따름 (섹션 7).
- 이벤트 상세:
  - **쉼터 🏕️**: 선택지 2개 — "❤️ 체력 N 회복" (최대체력의 40%를 **실제 숫자로 표시**) vs "🌟 경험치 +10". (레벨업 선택지가 아님에 주의 — 유저가 바꿈)
  - **악마 😈**: **지불하기 전에 어떤 특전인지 카드로 먼저 보여줌.** 전설 확정, 15% 확률로 신화. 선택지: "최대 체력의 30% 지불(-N)하고 위 특전 획득" vs "지나간다". 수락 시 특전 획득 연출(광선).
  - **천사 😇**: "공격력 +5% (무료)" vs "📺 광고 보고 +15%" (광고는 3초 카운트다운 모의 광고). 버프는 해당 런 한정.
- 경험치: **일반 적 처치 +3, 보스 +9.** 레벨업 필요량: `4+2*Lv` (Lv1→6, Lv2→8, Lv3→10 …, **레벨마다 +2씩 증가**).
- **⚑ 주인 지시(2026-09-02): 레벨업의 효과는 특전 선택지 하나뿐이다.** 레벨업 시 체력/실드 회복 금지, 최대 체력/최대 실드 증가도 금지 — 어떤 스탯 보정도 없이 **특전 3택(전지의 눈 4택)만** 띄운다. (현 index.html 이 레벨업마다 풀충전되는 구버전 동작 — 주인 관측. 제거 대상. 특전 자체의 회복/최대치 증가 효과 ❤️/🔷/🦾 등은 무관하게 유지.) sim.js 에 같은 동작이 있는지 확인해 있으면 제거 — **밸런스에 유의미한 영향이므로 반영 전 회차 점수는 무효, 회차 로그에 «레벨업무보정 반영» 표시.**
- **⚑ 주인 지시(2026-09-02): 챕터를 끝내는 보스 처치 시에는 특전 선택지를 띄우지 않는다.** 보스 경험치(+9)는 그대로 받되, 그걸로 레벨업이 됐어도 레벨업 팝업/특전 3택 없이 **바로 클리어 화면으로** 간다 (어차피 런이 끝나므로). sim.js 도 동일하게(클리어 확정 후 특전 굴림 없음).
- 골드: 처치 `round((0.6+0.10*챕터)*1.22^(챕터-1)*rand(1,1.8)*goldMul)`, 클리어 보너스 `3*챕터*1.22^(챕터-1)`
  (**T6 신설 `goldGrowth`, 현행값 1.22** — 챕터 300 확장 + 슬롯 강화 비용이 3.5^렙 으로 오르므로 수입도 지수로 따라가야 한다). 사망/포기해도 번 골드는 가져감.
  (**⚑ T16**: 이 두 숫자는 T1 회차마다 바뀌는 **경제 노브**다 — 이력은 T6 1.185 → R07 1.22, 슬롯 비용 T6 6.9^렙 → R06 5.5^렙 → R07 4.2^렙. 변경 시 §6·§11.5-a 와 함께 여기도 고칠 것. 게이트: `node tools/verifyPlanConst.js`)
  (초기 스펙은 `(2+챕터)` / `40*챕터` — R02 튜닝에서 하향, 근거 `docs/balance/round-02/`. §7 «골드 드랍» 노브)
- 레벨업 → 특전 3개 중 1개 선택(🔮 전지의 눈 보유 시 4개), 무료 새로고침 1회(🎲 보유 시 +1).

---

## 3. 특전 시스템 최종 스펙 — **총 102종** (유저 승인 완료)

### 3.0 공통 규칙
- **R03 특전 수치 조정 기록** (실험1·2 근거, PLAN §7 위임 범위): 아래 표의 수치는 튜닝으로 바뀐 값이다.
  ① **희귀 등급 전반 상향** — 실험1에서 «일반 > 희귀» 역전(19.7% vs 8.3%)이 재현돼, 희귀 소환 3종(도끼·화살·검기)의
  발동 확률·계수와 희귀 발동형 버프의 확률·수치를 올렸다. 조정 후 사다리 정상화(73.0 < 89.7 < 97.7 < 100).
  ② **지속력 아웃라이어 하향** — 주인 지시 «웨이브 전멸 실드 충전 폐지» 이후 «처치 시 회복/실드 충전» 계열이
  실험2 등급 내 최상위를 독점(전설 l_killShield10 97%)해, 해당 계열과 m_revive·l_perkHp·m_time 을 낮췄다.
  (되돌림용 원본 수치는 git 이력 `docs/balance/round-03/` 회차 커밋 참조)
- 등급 등장 확률: **일반 35% / 희귀 30% / 전설 25% / 신화 15%.**
- **⚑ 주인 지시(2026-09-02): 특전 선택지는 등급을 섞지 않는다.** 선택지가 뜰 때 위 확률로 등급을 **딱 1번** 굴리고, 3개(🔮 전지의 눈 보유 시 4개) **전부 그 등급에서만** 나온다 (전설이 떴으면 전설만 3개, 일반이면 일반만 3개). 🎲 새로고침 시에는 등급부터 다시 굴린다(위임 규약 기본값). 고유 제외로 해당 등급 풀이 모자라면 가능한 수만큼만 표시. 👼 전설 이상 특전 보유 시엔 등급 굴림이 전설/신화(신화 37.5%)로 제한. **sim.js 와 index.html 둘 다 이 방식이어야 하며, 밸런스 시뮬(T1)도 이 방식으로 돌려야 유효.**
- 등급 색: 일반 `#9EA3AC`, 희귀 `#4FA3E8`, 전설 `#FFB92E`, 신화 `#FF5E6C`(카드에 붉은 글로우).
- `(중첩)` = 여러 번 획득 가능(발동형은 획득 수만큼 확률 또는 수치 증가), `(고유)` = 런당 1회만 등장(이미 가진 고유 특전은 선택지에서 제외).
- **소환 공격은 도끼/화살/번개/검기/창 5종만 존재.** 다른 소환(메테오 등) 추가 금지.
- **⚑ 주인 지시(2026-09-02): 흡혈(steal) 증가 특전 금지.** 흡혈 스탯 자체를 올리는 특전은 너무 강해서 목록에서 배제한다 — 위 102종엔 이미 없고(확인 완료), **구 index.html 의 🩸 +2%/+5% · 🧛 +10% 흡혈 3종은 T2 재작성 때 제거 확정.** 향후에도 추가 금지. «공격/치명타/처치 시 n% 확률로 체력 n% 회복» 류 발동형 회복 특전은 괜찮다(유지). 흡혈을 올릴 수단이 사라지므로 스탯 그리드의 «흡혈» 항목(항상 0)도 T2 에서 제외하고 7종으로(위임 규약 기본값 — 엔진의 steal 지원 코드는 남겨둬도 무방, 표시만 제거).
- **모든 소환/추가타/반격에도 치명타 적용** (치확·치배 동일 사용).
- 🌪️ 폭풍의 힘: "n% 확률" 로 표기된 **모든 발동형 확률 2배** (엔진의 `pk()` 헬퍼로 일괄 처리).
- 트리거 종류가 골고루 분포해야 함 (공격시/치명타시/처치시/피격시/회피시/반격시/회복시 + 조건부/패시브) — 아래 목록이 이미 그렇게 설계됨.
- 버프 엔진: `buffs = {atk, aspd, critR, critF, def, evade}` 배열, 각 항목 `{t:남은시간, amt}`. 실효 스탯 = 기본 + 버프 합(atk/aspd는 곱연산 `*(1+합)`, 나머지는 덧셈). 최대중첩 초과 시 가장 짧게 남은 것 갱신.
- `sim.js`의 특전 id/엔진 키가 기준. index.html 이식 시 같은 키 사용.

### 3.1 일반 — 26종
| sim id | 표시 텍스트 | 구현 |
|---|---|---|
| c_aspdBuff | ⚡ 공격 시 30% 확률로 3초간 공격속도 +5% (최대 5중첩) | addBuff aspd 0.05 |
| c_atkBuff | 🗡️ 공격 시 30% 확률로 3초간 공격력 +5% (최대 5중첩) | addBuff atk 0.05 |
| c_atkPerm | 🌱 공격 시 10% 확률로 공격력 영구 +1% | dmg*=1.01 |
| c_critChain | 💥 치명타 시 3초간 치명타 확률 +5% (최대 5중첩) | addBuff critR 5*n |
| c_critF | 🔥 치명타 시 3초간 치명타 배율 +10% (최대 3중첩) | addBuff critF 10*n |
| c_critHeal1 | ❣️ 치명타 시 20% 확률로 체력 1% 회복 | heal 1% |
| c_killHeal2 | 🍖 처치 시 체력 0.37% 회복 | killHeal+=0.0037 (R03↓·R07↓) |
| c_killShield3 | 🔰 처치 시 실드 0.7% 충전 | sh += maxSh*0.007*n |
| c_gold30 | 🪙 처치 시 골드 +30% | goldMul+=0.3 |
| c_defHit | 🛡️ 피격 시 3초간 방어력 +3% (최대 5중첩) | addBuff def 3*n |
| c_shieldHit | 🔋 피격 시 10% 확률로 실드 5% 충전 | |
| c_hitHeal | 🩹 피격 시 15% 확률로 체력 2% 회복 | |
| c_evadeEv | 👥 회피 시 3초간 회피 +8% (최대 3중첩) | addBuff evade 8*n |
| c_evadeAspd | 💨 회피 시 2초간 공격속도 +5% (최대 3중첩) | |
| c_evadeDef | 🧱 회피 시 3초간 방어력 +5% (최대 3중첩) | addBuff def 5*n |
| c_hitCounterS | 🔁 피격 시 10% 확률로 즉시 반격 | doCounter |
| c_counterAtk | ⚔️ 반격 시 3초간 공격력 +5% (최대 3중첩) | |
| c_counterDef | 🛡️ 반격 시 3초간 방어력 +8% (최대 3중첩) | |
| c_healBoost | 💚 회복 시 20% 확률로 최대 체력의 2% 추가 회복 | |
| c_healDef | 🫧 회복 시 30% 확률로 3초간 방어력 +5% (최대 3중첩) | |
| c_healShield | 💦 회복 시 20% 확률로 실드 3% 수리 | |
| c_firstHit | 🎯 체력이 가득 찬 적에게 데미지 +20% | d*=1+0.2*n (풀피 대상) |
| c_hp12 | ❤️ 최대 체력 +12% (즉시 회복) | |
| c_sh15 | 🔷 실드 최대치 +15% | |
| c_walk20 | 🏃 이동 속도 +20% | walkMul+=0.2 |
| c_def3 | 🧱 방어력 +3% | def+=3 |

### 3.2 희귀 — 26종
| sim id | 표시 텍스트 |
|---|---|
| r_axe | 🪓 공격 시 15% 확률로 도끼 투척 (공격력의 50%, 랜덤 적 1명) |
| r_arrow | 🏹 공격 시 15% 확률로 화살 2발 (각 공격력의 65%, 랜덤 적) |
| r_wave | 🌊 공격 시 20% 확률로 검기 발사 (대상과 바로 뒤 적, 공격력의 70%) |
| r_atkBuffM | ⚔️ 공격 시 30% 확률로 4초간 공격력 +14% (최대 5중첩) |
| r_critFBuff | 🔥 치명타 시 4초간 치명타 배율 +34% (최대 3중첩) |
| r_critReset | ⏫ 치명타 시 45% 확률로 다음 공격 즉시 발동 (atkTimer=0) |
| r_critHeal3 | ❤️‍🔥 치명타 시 30% 확률로 체력 4% 회복 |
| r_aspdKill | ⚡ 처치 시 4초간 공격속도 +20% (최대 3중첩) |
| r_killCrit | 🧿 처치 시 30% 확률로 4초간 치명타 확률 +14% (최대 3중첩) |
| r_killDef | 🏰 처치 시 3초간 방어력 +10% (최대 3중첩) |
| r_defBuff2 | 🛡️ 피격 시 30% 확률로 4초간 방어력 +14% (최대 3중첩) |
| r_hitEvade | 🌫️ 피격 시 22% 확률로 3초간 회피 +14% (최대 2중첩) |
| r_hitCounter | 💢 피격 시 30% 확률로 즉시 반격 |
| r_evadeHeal | 🍀 회피 시 15% 확률로 체력 7% 회복 |
| r_evadeShield | 🔶 회피 시 15% 확률로 실드 14% 수리 (R07↓) |
| r_evadeRush | 🌀 회피 시 다음 공격 데미지 +50% (최대 +150%) — nextAtk 스택 |
| r_counterX | ⚜️ 반격 피해 +100% |
| r_counterAtkM | 🗡️ 반격 시 4초간 공격력 +14% (최대 3중첩) |
| r_counterCrit | 💥 반격 시 3초간 치명타 확률 +14% (최대 3중첩) |
| r_healShield5 | 💧 회복 시 30% 확률로 실드 8% 수리 |
| r_healAtk | 🌿 회복 시 3초간 공격력 +8% (최대 3중첩) |
| r_healAmp | 💞 모든 회복량 +20% |
| r_restHp | 🏕️ 쉼터 도착 시 최대 체력 +15% |
| r_lastStand | 🦉 체력 10% 이하일 때 회피 +40% (고유) |
| r_refresh | 🎲 특전 새로고침 횟수 +1 (고유) |
| r_def6 | 🧱 방어력 +6% |

### 3.3 전설 — 28종
| sim id | 표시 텍스트 |
|---|---|
| l_spear | 🔱 공격 시 7.5% 확률로 관통 창 (공격력의 100%, 일직선 8명 거리(88px×8) 관통) |
| l_bolt | ⚡ 공격 시 10% 확률로 번개 2회 (각 공격력의 75%, 랜덤 적) |
| l_atkBuffL | ⚔️ 공격 시 25% 확률로 5초간 공격력 +35% (최대 3중첩) |
| l_extraHit | 🗡️ 치명타 시 75% 확률로 즉시 추가타 (공격력 230%) |
| l_critAtk | 🔥 치명타 시 4초간 공격력 +15% (최대 3중첩) |
| l_critAspd | ⚡💥 치명타 시 3초간 공격속도 +25% (최대 3중첩) |
| l_killAspd | 👑 처치 시 공격속도 영구 +1% (고유) |
| l_killHeal5 | 💉 처치 시 체력 0.55% 회복 |
| l_killShield10 | 🏯 처치 시 실드 0.75% 충전 |
| l_thorns | 🌵 피격 시 60% 확률로 받은 피해의 150% 반사 |
| l_evadeHitBuff | 👥 피격 시 30% 확률로 3초간 회피 +15% (최대 2중첩) |
| l_defBuffL | 🛡️ 피격 시 20% 확률로 4초간 방어력 +15% (최대 2중첩) |
| l_evadeCrit | 🎯 회피 시 다음 공격 치명타 확정 (고유) |
| l_evadeCounter | 💫 회피 시 즉시 반격 (100%) |
| l_evadeAtk | 🍃 회피 시 5초간 공격력 +28% (최대 3중첩) |
| l_counterChain | 🔂 반격 시 반드시 한 번 더 반격 (연쇄 2회 제한) (고유) |
| l_counterHeal | ❤️‍🩹 반격 시 체력 4% 회복 |
| l_counterWave | 🌊🔁 반격 시 검기 발사 (100%) |
| l_overheal | 💠 초과 회복 시 남은 회복분의 7배만큼 실드 수리 (고유) |
| l_overBolt | 🌩️ 초과 회복 시 번개 2회 (각 75%, 내부 쿨 0.12초) (고유) |
| l_fullHpCrit | 🎯 체력이 가득 찬 적 공격 시 치명타 확률 62% (고유) |
| l_rage | 😤 실드가 없으면 공격력 +50% (고유) |
| l_backDmg | 🔙 뒤에 있는 적(맨 앞이 아닌 적)에게 데미지 +220% (고유) |
| l_execute | 🗜️ 체력 절반 이하 적에게 데미지 +120% (고유) |
| l_perkHp | 💗 특전 기회 1번마다 최대 체력 +1.8% (소급 적용 — 획득 시 지금까지의 기회 수만큼 즉시 적용, 이후 기회마다 +1.8%) (고유) |
| l_misfire | 🏹 적의 원거리 공격이 30% 확률로 아군을 맞힘 (오사 데미지 2배) |
| l_legendOnly | 👼 이후 모든 특전이 전설 등급 이상만 등장 (신화 37.5%) (고유) |
| l_def10 | 🧱 방어력 +10% |

### 3.4 신화 — 22종
| sim id | 표시 텍스트 |
|---|---|
| m_revive | ♻️ 불사조의 심장 — 사망 시 1회 부활 (체력·실드 7% 회복) (고유) |
| m_clone | 👻 그림자 분신 — 공격할 때마다 분신이 추가타 (공격력의 37%) (고유) |
| m_execKill | 🧨 처형자 — 체력 25% 이하인 적 즉시 처치 (보스 제외) (고유) |
| m_procX2 | 🌪️ 폭풍의 힘 — 모든 발동형 특전의 확률 1.22배 (고유) (R07↓) |
| m_arsenal | 🎇 무기고 — 공격 시 16% 확률로 도끼/화살/번개/검기/창 중 하나 랜덤 발동 |
| m_guard | 💎 수호의 결정 — 실드가 남아있는 동안 받는 피해 38% 감소 (고유) |
| m_autoBolt | ⚡👑 뇌신 — 2.4초마다 자동으로 번개 1회 (공격력의 75%) (중첩 시 횟수+1) |
| m_time | 🕰️ 시간 가속 — 공격속도 +21% & 이동속도 +21% |
| m_axe3 | 🪓 도끼 폭풍 — 도끼가 14개씩 발사 (고유) |
| m_arrow4 | 🏹 화살 폭풍 — 화살이 24발씩 발사 (고유) (R07↑) |
| m_spear200 | 🔱 창의 대가 — 창 데미지가 공격력의 1350% (고유) (R07↑) |
| m_bolt3 | ⚡ 번개 지배자 — 번개가 20회씩 발동 (고유) (R07↑) |
| m_wave4 | 🌊 검기의 왕 — 검기가 20명까지 관통 (사거리 340→1400) (고유) (R07↑) |
| m_gold2 | 💰 황금 기사 — 골드 획득 2배 (고유) |
| m_sage | 🌟 현자 — 처치 시 경험치 +1 (고유) |
| m_def20 | 🛡️ 불괴 — 방어력 +8%p (R07↓) |
| m_crit25 | 🎯 정밀 — 치명타 확률 +9% (R07↓) |
| m_giant | 🦾 거인화 — 최대 체력 +16% (즉시 회복) (R07↓) |
| m_lucky | 🍀 행운의 화신 — 회피 +11%p & 반격 확률 +11%p |
| m_choice4 | 🔮 전지의 눈 — 특전 선택지가 4개가 됨 (고유) |
| m_fortress | 🏯 요새 — 실드 최대치 +140% |
| m_wallBuff | 🧱👑 철벽 — 피격 시 4초간 방어력 +10%p (최대 2중첩) |

---

## 4. 엔진 구현 세부 (sim.js와 동일하게)

- `pk(chance)` — 발동 판정. `procX2`면 확률 **1.22배**(엔진 `pkk()` 실제 상수 — §3.4 `m_procX2` 와 같은 값. **T1 이 이 배수를 조정하면 §3.4 와 여기 둘 다 갱신할 것**). **모든 "n% 확률" 발동형은 이 헬퍼를 통해서만 굴릴 것.**
- 실효 스탯: `effDmg`(rage 포함), `effAspd`, `effCritR`, `effCritF`, `effDef`(상한 80), `effEvade`(lastStand 포함, 상한 90). HUD에도 실효값 표시.
- `healPlayer(amt)` — healAmp 적용 → healBoost2 → 회복시 트리거(healDef/healShield3/healShield5/healAtk) → 초과분은 overheal/overBolt 처리. 무한루프 방지: 특전 적용시 초기회복 등은 `noBoost=true`로 호출.
- `dealPlayerDamage(e, ratio, fromBasic)` — 치명 판정(fullHpCrit/nextCrit) → firstHit/execute/backDmg 배수 → 흡혈 → 치명타시 트리거들 → execKill → onKill. **기본공격에서만** nextCrit/nextAtk 소모, 분신/추가타 발동.
- `doCounter(src)` — 반격 데미지 `effDmg*0.7*(1+counterX)` + 반격시 트리거들 + counterChain(1회 제한).
- 소환 발사 함수 `fireAxe/fireArrows/fireBolts/fireWave/fireSpear` — 신화 강화(m_axe3 등) 반영. arsenal은 이 5개 중 랜덤 호출. 투사체는 게임에선 그려서 날아가고(도끼 회전/화살/창/검기 초승달, 번개는 하늘에서 지그재그), 시뮬에선 번개만 즉발.
- **⚑ 주인 지시(2026-09-02): 다연발 소환은 순차 연사 (다다다닥).** 한 발동에서 여러 발이 나가는 소환(화살 N발·번개 N회·도끼 3개 등, 특전이 모여 10발이 됐어도)은 **같은 프레임에 겹쳐 쏘지 말고 한 발씩 순차 발사하되, 아주 빠르게(다다다닥)** — 주인 보강: **발마다 구별되어 보이는 게 목적**이므로 «순차 + 존나 빠르게» 둘 다 지킬 것. 간격 50~70ms(눈으로 낱발이 구별되는 최소 수준, 배속 반영), 발마다 사운드·타겟 선정·데미지 판정도 그 발 시점에(위임 규약 기본값). 현 index.html 은 `for` 루프 동시 스폰이라 T2 에서 수정 대상. sim.js 는 즉발 유지(총합 데미지 동일 — 밸런스 영향 무시 가능, T1 회차 무효 아님).
- 랜덤 타겟: 플레이어 앞 540px 이내 생존 적 중 랜덤 (이벤트 노드 뒤 다음 웨이브는 사거리 밖이라 안전).
- 적 처치 중복 방지: `e.dead` 가드 필수 (반격+가시 동시 처치 등).
- 악마/레벨업 모두 `perkChances` 증가 → `perkHp` 소급 로직.

## 5. UI 추가 요구 (기존 index.html에 이미 있음 — 유지)

- 악마 거래 특전 미리보기 카드(섹션 2.4), 📘 Info 팝업, 레벨업 연출, 신화 카드 글로우.
- 특전 카드: 등급 태그 + 아이콘 + 설명, `<b>` 강조는 초록색.

## 6. 챕터 20개 + 벽 (시뮬 튜닝 대상)

`sim.js`의 `TUNE` 초기값 (2026-09-01 튜닝 전):
```
적 HP  = 40 * 1.35^(c-1) * (1+0.22*웨이브) * (c>=10: ×1.6) * (c>=15: ×1.5)
적 DMG =  8 * 1.26^(c-1) * (1+0.10*웨이브) * (c>=10: ×1.35) * (c>=15: ×1.25)
보스: HP ×12, DMG ×1.8, 5의 배수 챕터는 HP ×1.5 추가
```

**현재 확정값 (R06 튜닝. 시뮬 근거: `docs/balance/round-06/`):**
```
적 HP  = 40 * 1.185^(c-1) * (1+0.15*웨이브) * (c>=10: ×1.95) * (c>=15: ×0.82)
적 DMG =  8 * 1.08^(c-1)  * (1+0.08*웨이브) * (c>=10: ×1.25) * (c>=15: ×1.00)
보스: HP ×8, DMG ×1.8 (주인 확정 상수 — 아래)
```
(R05 값은 `1.15 / 1.10 / ×2.6 / ×1.06 / ×1.15` 였다 — 되돌림용 기록.
 R06 의도: **eHpG 를 올려 챕터6~9 의 무저항 구간(평균 1.3~2.7회)을 조이고**, 그만큼 `wallHp` 를 내려
 챕터10 벽의 절대 난도를 유지했다. 후반 상한 초과(챕터15~19 평균 12~18회)는 `wall2Hp` 1.06→0.82 와
 `eDmgG` 1.10→1.08 로 완화하고, 줄어든 압박은 `wallDmg` 1.15→1.25 로 되돌렸다.
 스윕 8종을 20회 반복 비교해 고른 조합이다.
 **⚠ 주의: 2026-09-02 07:5X 주인 지시로 영구강화 4종이 폐지되고 장비 시스템(§11)이 신설됐다.
 위 적 수치는 «영구강화 경제» 를 전제로 튜닝된 값이므로, T6 통합 후 반드시 재튜닝해야 한다.**)
- **⚑ T6 (§11 장비 시스템 통합) 추가분 — R06 적 수치(`eHpG 1.185` 등)는 한 자리도 바꾸지 않았다.** 챕터 확장에 필요한 항만 신설:
```
90 대형 벽  : c>=90  → HP ×3.6 · DMG ×1.30   (wall3Hp / wall3Dmg)
300 최종 벽 : c>=300 → HP ×3.6 · DMG ×1.30   (wall4Hp / wall4Dmg)
골드        : 처치·클리어 보너스에 챕터당 성장 배수 goldGrowth = 1.185 (= eHpG)   ← T6 값
```
- **⚑ R07 (T1) 경제 노브 재조정 — 적 수치(`eHpG` 등)는 여전히 한 자리도 안 바꿨다.** 바꾼 것은 경제 3개뿐:
```
goldGrowth  : 1.185 → 1.22
슬롯 비용   : 150 * 5.5^L → 600 * 4.2^L → 600 * 3.5^L   (slotCostBase / slotCostG · 마지막이 R09 확정값)
```
  근거(실험4 실측): T6 값(1.185 / 5.5)은 **챕터 90 에서 F2P·과금 둘 다 영구 정체**했고(슬롯 13 에서 멈춤, 앵커 A 는 15 필요),
  `goldGrowth` 만 1.22 로 올리면 90 은 뚫리지만 이번엔 **챕터 118 에서 40일 정체**했다.
  **T6 이 세운 «slotCostG < goldGrowth^6(=6.93)» 규칙은 틀렸다.** 올바른 점근 조건은
  «슬롯 1렙이 벌어 주는 챕터 수 = ln(slotG)/ln(eHpG) ≈ 5.8챕터, 그 동안의 골드 증가 `goldGrowth^5.8` ≥ `slotCostG`» 이다
  (1.22^5.8 = 3.2). 이 조건을 어기면 «레벨당 비용 ×5.5 vs 5.8챕터당 수입 ×3.0» 이라 회차가 갈수록 벌어져 **어떤 챕터에선가 반드시 멈춘다.**
  실측 확인: `slotCostG` 3.2 = 폭주(60일차 챕터 911) · 4.2 = 정상(30/60/90/120/180일차 = 31/125/155/173/179, 정체 0) · 4.8 = 챕터 90 정체 · 5.5 = 챕터 118 정체.
  `slotCostBase` 를 150 → 600 으로 올린 것은 비용 성장률을 낮춘 만큼 초반 슬롯이 너무 빨리 오르는 것을 되돌리기 위한 평행이동이다.
- **⚑ R09 (T1) 정정 — 위 R07 의 «4.2 = 정상» 은 관측 범위가 짧아서 생긴 오판이다 (T13·T15).**
```
슬롯 비용   : 600 * 4.2^L → 600 * 3.5^L   (slotCostG 만 변경, base 는 유지)
```
  R07 은 조건식 `goldGrowth^5.808 ≥ slotCostG` 를 **옳게 세워 놓고 `1.22^5.808 = 3.174 ≥ 4.2` 를 «만족» 이라 적었다** — 부등호가 반대인 계산 착오다.
  실제로는 매 챕터 **3.4% 씩 적에게 뒤처져**(적대비 0.9674) 실험3 이 챕터 120 부터 무너지고 실험4 가 **챕터 212 에서 영구 정체**했다.
  `slotCostG` 3.5 는 적대비 **0.990** 으로 적자가 1/3.5 로 줄어 챕터 300 완주가 된다.
  실측(워커 A T13 3런 + R09 6런 = **9런 독립 재현**): **91~299 구간 적합 100%** (R09 6런 전부) · 챕터 300 최종 벽 81~350회.
  유효 구간은 **3.4~3.6** — 3.3 은 폭주(91~300 전부 시도 1회), 3.8 은 열화 시점이 챕터 160 으로 밀릴 뿐 곡선이 같다.
  **⚠ 이 변경의 대가 — 챕터 90 대형 벽이 무너진다(R09 6런 0/6, 시도 4~28회 vs 목표 30~400회).**
  원인은 난이도가 아니라 **도달 시점 빌드**다: 챕터 90 도달 시 슬롯이 `4.2` 에서는 **14~15렙(= 앵커 A 스펙 «신화 0강·슬롯 균등 15렙» 과 정확히 일치)** 인데 `3.5` 에서는 **16렙**이 된다.
  `wall3Hp` 로는 못 막는다 — 그건 앵커 A 의 측정 대상 자체를 어렵게 만들어 실험5 앵커 A(현 3.3%, 목표 2~10%)를 2% 아래로 떨어뜨린다.
  `slotCostBase` 로도 못 막는다 — R09 스윕(각 3런, 챕터 95까지): base **1200** 은 슬롯이 아직 16렙 · **2400** 은 3런 **전부** 챕터 10~12 에서 400회 상한에 막힘 · **3000** 은 슬롯 15렙 복원에 성공하나 3런 중 1런이 챕터 18 에서 막힘.
  즉 **«앵커 A 정렬»과 «초반(1~20) 통과»가 같은 노브에서 정면충돌**하며, 이는 승인 대기 9번(초반 무성장 구간)의 직접 귀결이다 → **승인 대기 14번**.
  두 벽 배수와 §11 장비 계수는 **§11.7 앵커 3점에서 역산**했다. 실험5 실측(각 120판):
  **C 챕터30 = 13.3%(7.5회) · A 챕터90 = 5.0%(20회) · B 챕터300 = 7.5%(13.3회)** — 세 과녁 모두 «수십 회 끝에 겨우» 구간.
  근접 챕터: 28=64% / 32=1.7% · 88~89=100% / 92=0% · 298~299=100% / 301=1.7%.
  원시 출력 `docs/balance/T6/e5.txt`.
(R02 중간값은 waveHp 0.50 / waveDmg 0.26 / wallHp 2.6 이었다 — 주인 지시 «웨이브 전멸 실드 충전 폐지»
 반영으로 체감 난이도가 올라 R03 에서 웨이브 증가율을 되돌리고 벽만 2.9 로 올렸다.
 **R04 에서 보스 ×8·강화 상한 티어·하니스 재보정을 반영하고 eHpG 1.11→1.15 / eDmgG 1.14→1.10 으로 재조정했다**
 — 보스가 ×12(5배수 ×18)에서 ×8 로 약해진 만큼 챕터 성장률을 올리고, 후반 즉사 압박을 줄이려 DMG 성장률은 내렸다.)
- **⚑ 주인 확정 상수(2026-09-02, 07:3X 정정): 보스 HP = 일반몹 ×8 고정 · 보스 DMG = ×1.8 고정.** (앞서 07:2X 의 ×3 은 주인이 ×8 로 정정.) 기존 «HP ×12 · 5의 배수 챕터 ×1.5 추가» 폐기 — `bossHp = 8`, `bossDmg = 1.8`, `bigBoss` 배수 삭제, 챕터 무관 항상 동일 (기준 일반몹 = 해당 챕터 보스 웨이브 시점의 `enemyStats`, 위임 규약 기본값). **두 값 모두 주인 확정 상수라 튜닝 노브가 아니다** — 이후 밸런스는 다른 노브로 맞출 것. PROGRESS «주인 승인 대기» 1번 안건은 이 지시로 종결.
- 되돌림용 기록: 초기값 → R02 값은 위 두 블록 비교로 즉시 복원 가능.
- 근거: 초기값은 챕터10에서 400회 재도전으로도 못 뚫는 **완전 차단**이었음(비평가 2인 6.3/10).
  강화 비용이 지수(1.32^Lv)로 오르는데 골드 수입은 챕터에 선형이라 400판에 강화 +4레벨에 그쳐
  «반복하면 뚫린다»가 성립하지 않았다. 비용 성장률을 낮추고 적 챕터 성장률을 내리는 대신
  **웨이브 내 증가율(waveHp/waveDmg)을 올려** 한 판 안의 난이도를 유지했다.

## 7. 밸런스 시뮬레이션 절차 (필수 수행)

실행: 레포 루트에서 `node sim.js 1`, `node sim.js 2`, `node sim.js 3` (전부: `node sim.js`)

- **실험1 (등급 사다리)**: 챕터6, ~~강화 4렙~~ → **T6 재보정: 장비 «전설 6부위 · 슬롯 2렙»**(R07 재보정 — 실험3 5런 전부 챕터6 통과 시점 슬롯 최저 2렙)(영구강화 폐지로 하니스 축이 장비/슬롯으로 교체됨. 환경변수 `EXP1_GEAR='등급,강화,슬롯'` 로 덮어쓰기 가능) 고정(주인 위임 승인 2026-09-02 — T5 안건: 하니스의 강화 레벨은 «실험3에서 관측된 그 챕터 도달 시점의 중앙값 영구강화 레벨» 로 맞춘다. 경제 노브를 바꾼 라운드에는 재보정), 특전을 한 등급만 나오게 강제하고 300판씩. **합격 기준: 클리어율이 일반 < 희귀 < 전설 < 신화 순으로 단조증가** (역전 시 해당 등급 수치 조정).
- **실험2 (특전별 기여도)**: 챕터8에서 1200판, ~~강화 6렙~~ → ~~T6 재보정: 장비 «희귀 6부위 · 슬롯 1렙»~~ → **R07 재보정(R10 이 문서 동기화): 장비 «신화 6부위 · 슬롯 0렙»**(`EXP2_GEAR`) 고정(위와 같은 T5 승인·재보정 규칙 — 실험3 관측 «챕터8 도달 시점 = 신화 1~2부위·슬롯 1~2렙», 변별력이 최대인 클리어율 60~70% 지점. 근거 `sim.js:917` 주석), 특전별 픽 시 승률. **합격 기준: 같은 등급 내에서 승률 최상위와 최하위 차가 극단적(예: 25%p 이상)이면 아웃라이어 수치 조정.** (표본 25판 미만 특전은 무시)
- **실험3 (진행 곡선)**: 챕터 1→20, 골드로 강화 자동구매하며 클리어까지 시도 횟수 측정. **합격 기준:**
  - 챕터 1~5: 각 1~2회
  - 챕터 6~9: 각 2~5회
  - 챕터 10: **10회 이상 (벽)** — 하지만 400회 안에는 뚫려야 함
  - 챕터 11~19: 각 3~10회
  - 챕터 20: 10~30회 (최종 보스감)
- **⚑ 실험4 (장비 진행, 2026-09-02 §11 도입 후)**: 하루 다이아 2500 적립 모델로 일 단위 F2P 시뮬(뽑기→합성→슬롯강화 자동) — 진행이 막힘 없이 우상향해야 함(구체 목표 수치는 T6 이 제안·기록). 과금(12,000 다이아)은 «가속» 으로만 작동하는지 검증.
- **⚑ 실험5 (앵커 검증, 주인 확정 과녁 — §11.7)**: 앵커 C = **전설 풀셋 0강 + 슬롯 균등 10렙**으로 챕터 28~32, 앵커 A = **신화 0강 + 슬롯 균등 15렙**으로 챕터 88~92, 앵커 B = **신화 +9강 + 슬롯 균등 50렙**으로 챕터 295~301 클리어율 측정 — 각각 **30/90/300 이 수십 회 재도전 끝에 겨우 뚫리는 수준** (A·B 는 91+/301+ 가 사실상 벽).
- 조정 노브: `TUNE`의 성장률(eHpG/eDmgG), 벽 배수(wallHp/wallDmg — 90 벽 포함), ~~강화 비용 성장률~~(영구강화 폐지), 골드 드랍, **장비 공/체 계수·슬롯 강화 비용/스탯 곡선(§11)**. 특전 개별 수치는 실험1/2 결과로만 조정. 보스 ×8/×1.8, 뽑기 확률·천장, 다이아 수급량은 주인 확정 상수라 노브 아님.
  실행: 실험4 `node sim.js 4` (`EXP4_DAYS`, `EXP4_IAP=1` 로 과금 비교) · 실험5 `node sim.js 5`.
  **하루 플레이 판수 = 30판 (위임 기본값 `GT.runsPerDay`)** — 다이아 적립을 판 단위로 환산하는 기준이라 실험3 과 실험4 가 같은 경제 코어를 쓴다.
  **T6 제안 합격 기준 — 실험4**: ① 어떤 챕터에서도 «20일(600판) 연속 실패» 정체가 없을 것 ② 30·90·180일차 도달 챕터가 단조증가할 것 ③ 과금 1회(12,000다이아)는 같은 챕터를 더 빨리 도달시킬 뿐, 도달 불가 챕터를 열지 않을 것.
  **T6 제안 합격 기준 — 실험5**: 앵커 챕터(30·90·300) 클리어율 각 2~10%(기대 재도전 10~50회) · 그 다음 챕터부터 2% 이하 · 앵커 직전 2개 챕터는 50% 이상(직전이 뚫려 있어야 «그 챕터가 벽» 이라는 말이 성립한다).
- **⚑ 실험3 의 21+ 구간 목표 (T6 제안 — 채점표 v2 실험3 3점 중 21+ 몫)**:
  챕터 21~49 각 **1~20회** · 50~89 각 **1~40회** · **90 = 30~400회(대형 벽)** · 91~299 각 **1~50회** · **300 = 30~400회(최종 벽)**.
  실험3 은 챕터 100 까지만 돌려도 채점 가능하게 두었다(`EXP3_MAX=100`) — 300 전구간 완주는 수십 분이 걸린다.
  근거: `docs/balance/T6/e3.txt`. **1~20 구간은 T6 시점에 아직 목표 미달이다**(챕터1~5 가 1~15회, 6~9 가 5~110회) —
  영구강화가 사라져 조정 노브가 `goldGrowth` · `slotCostBase` · `slotCostG` 3개로 바뀌었으니 T1 이 이 3개로 다시 맞출 것.
  ※ ~~`slotCostG` 는 `goldGrowth^6`(=6.93) 보다 낮아야 한다~~ — **R07 에서 이 규칙이 틀렸음이 확인됐다.** 5.5 는 규칙을 지키고도 챕터 118 에서 정체했다.
  올바른 조건: **`slotCostG` ≤ `goldGrowth^(ln slotG / ln eHpG)`** (= 슬롯 1렙이 벌어 주는 챕터 수 동안의 골드 증가분이 다음 레벨 비용을 감당해야 한다).
  현행 값 기준 = `1.22^5.808` = **3.174** 이며, 여유가 너무 크면(3.3 이하) 반대로 폭주한다.
  ~~R07 확정값 `4.2` 는 실측으로 고른 «정체도 폭주도 없는» 구간이다.~~ → **R09 정정: `4.2` 는 이 조건을 31% 위반해 챕터 212 에서 영구 정체했다(T13·T15). 확정값은 `3.5`** (유효 구간 3.4~3.6, 근거는 §6 R09 항목).
- **조정한 수치는 반드시 이 문서(섹션 6, 3)에도 갱신해서 기록할 것.**
- 시뮬 조정 후 최종 수치를 index.html에 이식. (sim.js와 index.html의 수식이 항상 일치해야 함)

## 8. index.html 이식 시 주의

- 현재 index.html은 특전 65종 구버전 + 메테오가 들어있음 → 특전 목록/엔진 부분을 이 문서 스펙으로 교체. 메테오 완전 제거.
- 쉼터 선택지가 구버전("체력 50%" vs "즉시 레벨업")임 → 섹션 2.4로 교체.
- 챕터가 무한 + 웨이브 5마리 고정 구조임 → `chapterLayout(c)` 기반 20챕터로 교체.
- `expNeed`는 이미 `4+2*lv`로 수정되어 있음.
- 이벤트/보스/전투 루프/렌더러/로비/사운드는 재사용 가능.

## 9. 배포 (유저 요청)

1. `git add -A && git commit` (메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`) → `git push origin main`
2. GitHub Pages 활성화: 이 PC에는 gh CLI가 **없음**. 유저에게 https://github.com/kuzuni/aaaw/settings/pages 에서 Branch=main, 폴더=/(root) 로 저장해달라고 안내할 것 (1회만 하면 됨). 활성화되면 https://kuzuni.github.io/aaaw/ 로 서빙됨. (gh를 설치했다면 `gh api repos/kuzuni/aaaw/pages -X POST -f "source[branch]=main" -f "source[path]=/"` 도 가능)
3. 아티팩트 republish: Artifact 도구, `file_path=index.html`, `url=https://claude.ai/code/artifact/228cba1b-db0b-4aef-b5ef-7aa344890524`

## 10. 작업 방식 (유저가 강하게 요구)

- **새로운 콘텐츠(특전/시스템/수치 체계)를 임의로 추가하지 말 것.** 추가/변경하고 싶으면 먼저 목록·계획을 유저에게 보여주고 승인받을 것.
- 이미 승인된 것(이 문서 전체)은 그대로 구현하면 됨.
- 밸런스 튜닝(숫자 조정)은 섹션 7 기준 내에서 자율 수행 OK — 결과 요약해서 보고할 것.
- 유저는 한국어 사용. 보고도 한국어로.

---

## 11. 장비 시스템 (⚑ 주인 지시 2026-09-02 — 전체 신설)

> «(위임)» 표시는 주인이 세부를 정하지 않아 기본값으로 채운 것 — 주인이 뒤집으면 그때 되돌린다. 나머지는 주인 원문.

### 11.1 구조
- **등급 5개**: 일반 / 희귀 / 영웅 / 전설 / 신화 (이름·순서는 위임. 색: 특전 4색 재사용 + 영웅 보라 `#B96CE8` (위임)).
- **부위 6개**: 무기 / 투구 / 갑옷 / 장갑 / 신발 / 목걸이 (위임).
- **부위당 종류 3가지** (예: 신발 = 부츠·샌들·장화) → 총 18종 × 5등급.
- 모든 장비는 장착 시 **공격력·체력 상승** (수치 = 등급·강화 스케일, T1/T6 튜닝 몫).
- **계열 옵션**: 같은 부위의 3종류는 각각 **다른 계열** (주인 예시: 샌들=회피 계열 · 부츠=반격 계열 · 장화=체력 계열). 같은 종류의 상위 등급은 **하위 등급 옵션을 포함하고 하나 더 얹는다** (주인 예시: 희귀 샌들 «회피 +7» → 영웅 샌들 «회피 +7, 회피 시 10% 확률로 도끼 1개 랜덤 적 발사»).
- **옵션 개수**: 일반 0개(옵션 없음) · 희귀 1 · 영웅 2 · 전설 3 · 신화 4 (등급당 +1, 위임으로 일반화).
- **⚑ 신화 강화 옵션**: 신화 장비는 **+3강 · +6강 · +9강**에서 옵션이 1개씩 더 생긴다 (신화 +9강 = 옵션 7개, **+9강이 옵션의 끝**). 강화 자체는 무한이지만 옵션 추가는 +9강까지.
- **18계열 옵션표**: T6 이 초안을 짜서 이 자리(§11.6)에 등재 — **기존 엔진 동사만 재사용**(회피/반격/치명타/처치/피격/회복/실드/골드/소환 5종. 신규 메커니즘 발명 금지), PROGRESS «주인 검토» 에 표로 올릴 것.

### 11.2 뽑기 (가챠)
- 1회 **400 다이아** (상점에서).
- 확률: **신화 0.1% / 전설 2% / 영웅 10% / 희귀 30% / 일반 57.9%** (나머지).
- **50회 천장**: 누적 50회째에 신화 확정. 신화가 나오면(자연 획득이든 천장이든) **카운터 0으로 리셋** (주인 예: 29회째 자연 신화 → 다시 50회 뒤 확정).
- **전설 10회 피티**: 10회당 1번 전설 이상 확정. **50회 천장과 겹치면 신화가 우선, 전설 확정은 다음 뽑기로 이월** (주인 명시). 전설 이상 획득 시 그 카운터 리셋(위임).
- 부위·종류는 18종 균등 랜덤 (위임). 1회/10회(4,000) 버튼, 10연 할인 없음 (위임).

### 11.3 합성·강화
- **같은 부위 + 같은 종류 + 같은 등급 3개 → 다음 등급 1개** (부위·종류 유지. 일반→희귀→영웅).
- **전설(차상위)**: 3개 합성해도 등급업이 아니라 **본체 +1강** (재료 중 최고 강화 기준 +1, 위임). **+10강 도달 시 그 대신 신화 0강 1개로 변환.**
- **신화(최고)**: 같은 방식으로 3개 합성 = +1강, **무한 강화**, 변환 없음. (옵션은 +9강까지 — §11.1)
- **장착 중인 장비는 합성 재료에서 제외** (위임).
- **⚑ 주인 확정 제약: 신화 +0강 > 전설 +9강** (공/체 기여 기준으로 반드시 성립).

### 11.4 슬롯 강화 (영구강화의 대체)
- 장비를 끼우는 **슬롯 6개를 각각 골드로 강화**. 레벨당 공격력·체력 증가, **필요 골드는 레벨이 오를수록 증가** (곡선 = 튜닝 몫).
- **균등 보너스 (골고루 유도)**: 6슬롯 전부 5렙 이상 → 공/체 **+5%**, 전부 10렙 이상 → **+10%**, … 이런 식으로 계속 (`+5% × floor(min(슬롯 렙)/5)`, 무한 — 위임으로 일반화).
- **기존 영구 강화 4종 폐지** (§2.2 참조).

### 11.5 경제 (주인 확정 상수)
- **다이아 무료 보급: 하루 2,500개** (시뮬 가정치이자 게임 수급 기준. 게임 내 구현은 로비 일일 보상 수령, 위임).
- **IAP: ₩110,000 = 다이아 12,000개** (모의 결제 — 실결제 없음, 상점의 모의 지급 버튼, 위임).
- 상점 탭: 다이아 구매(모의) + 장비 뽑기.

### 11.5-a 장비·슬롯 수치 (T6 — 앵커 3점 역산, PLAN §7 조정 노브)

```
부위 1개의 공/체 기여 = unit / rarStep^(4-등급) * (1 + 0.12*강화) * slotG^(그 슬롯 레벨)
  atkUnit 0.5904 · hpUnit 5.904   (신화 = 최고 등급 기준값)
  rarStep 155                     (등급 1단계당 배수 — 신화 = 전설 ×155)
  plusStep 0.12                   (강화 1레벨당 +12%, 위임)
  slotG   2.68                    (슬롯 1레벨당 배수)
플레이어 = (기본 공30/체300 + 6부위 합) × 균등보너스(1 + 0.05*floor(min(슬롯)/5))
슬롯 강화 비용 = floor(600 * 3.5^레벨)   (slotCostBase / slotCostG — R09 확정값. R07 은 4.2, T6 초안은 3000 * 6.9^L 였다)
```
- **역산 근거**: `node sim.js fit` 이 «그 챕터가 클리어율 5% 가 되는 요구 전투력»을 이분법으로 뽑는다.
  현행 R06 곡선에서 기준(공30/체300) 대비 배수 k 는 **챕터30 = 36.5 · 챕터90 = 1.32e6 · 챕터300 = 1.22e22**.
  세 앵커의 성장 수단은 C→A 가 «1등급업 + 슬롯 5렙», A→B 가 «+9강 + 슬롯 35렙 + 균등보너스 1.15→1.50» 뿐이라
  `slotG^35 · 2.08 · 1.304 = k(300)/k(90)` 과 `rarStep · slotG^5 · 1.045 = k(90)/k(30)` 를 풀면 위 값이 나온다.
- **⚠ 이 설계의 성질 (주인 판단이 필요할 수 있음)**: 등급 1단계 배수가 **155**로 매우 크다.
  주인 확정 제약 «신화 +0강 > 전설 +9강» 은 하한만 정하므로 규칙 위반은 아니지만, 결과적으로
  **일반~영웅 장비의 전투력 기여는 전설의 1/155~1/3.7만**이라 사실상 «합성 재료» 로만 의미를 갖는다.
  이는 앵커 C(슬롯 10렙에 챕터 30)와 앵커 A·B(슬롯 1렙 ≈ 6챕터)의 **페이스가 서로 다르기 때문**이며,
  그 차이를 등급 격차가 흡수한 결과다. 완화하려면 앵커 C 의 슬롯 레벨을 15 쪽으로 올리거나(주인 확정값 변경),
  챕터 21+ 성장률을 낮춰야 한다(승인 대기 7번). **현 상태로도 세 앵커는 전부 성립하므로 그대로 두었다.**

### 11.6 계열 옵션표 (T6 초안 — **주인 검토 대상**)

> 작성: T6 (`sess-2221-26307` 워커 B). **기존 엔진 동사만 재사용**했다 — 새 메커니즘은 없다.
> 유일한 신규 조합은 `evadeAxe`(회피 트리거 + 도끼 발사)로, **주인이 §11.1 에서 직접 예시로 든 옵션**이다.
> 읽는 법: 상위 등급은 **왼쪽 옵션을 전부 포함하고 하나 더** 얹는다.
> 희귀=옵션1, 영웅=1~2, 전설=1~3, 신화=1~4, 신화 +3강=1~5, +6강=1~6, +9강=1~7 (+9강이 옵션의 끝). 일반은 옵션 없음.
> 구현 위치: `sim.js` 의 `GOPT` (덤프: `node sim.js table`).

| 부위 | 종류 | 계열 | 옵션1 · 희귀(1) | 옵션2 · 영웅(2) | 옵션3 · 전설(3) | 옵션4 · 신화(4) | 옵션5 · 신화+3(5) | 옵션6 · 신화+6(6) | 옵션7 · 신화+9(7) |
|---|---|---|---|---|---|---|---|---|---|
| 무기 | 대검 | 검기 | 공격력 +6% | 공격 시 20% 확률 검기 발사 | 공격 시 30% 확률 공격력 +14% 4초 | 검기 관통 20·사거리 1400 | 공격 시 25% 확률 공격력 +35% 5초 | 반격 시 검기 발사(확정) | 체력 50% 이하 적에게 피해 2.2배 |
| 무기 | 전투도끼 | 도끼 소환 | 공격력 +6% | 공격 시 15% 확률 도끼 발사 | 치명타 확률 +6 | 도끼 14개로 증가 | 도끼 발사 확률 +15%p | 처치 시 실드 충전 | 최대 체력 적 첫 타격 피해 +20% |
| 무기 | 장궁 | 화살 소환 | 공격력 +6% | 공격 시 15% 확률 화살 2발 | 치명타 배율 +30 | 화살 24발로 증가 | 치명타 시 75% 확률 추가타 | 화살 발사 확률 +15%p | 최대 체력 적에게 치명타 확률 62 |
| 투구 | 투구 | 방어 | 방어 +6 | 피격 시 방어 +3 3초(누적) | 방어 +8 | 피격 시 30% 확률 방어 +14 4초 | 피격 시 방어 +10 4초(최대 2중첩) | 피격 시 20% 확률 방어 +15 4초 | 실드가 있으면 받는 피해 38% 감소 |
| 투구 | 왕관 | 치명타 확률 | 치명타 확률 +6 | 치명타 시 치명 확률 +5 3초(누적) | 치명타 확률 +8 | 처치 시 30% 확률 치명 확률 +14 4초 | 치명타 시 45% 확률 공격 즉시 재장전 | 치명타 확률 +10 | 치명타 시 공격력 +15% 4초 |
| 투구 | 두건 | 번개 소환 | 치명타 배율 +25 | 공격 시 10% 확률 번개 2발 | 치명타 시 치명 배율 +34 4초 | 번개 20회로 증가 | 2.4초마다 번개 자동 발사 | 치명타 시 공격속도 +25% 3초 | 공격 시 16% 확률 소환 무작위 발사 |
| 갑옷 | 판금갑옷 | 체력·피격 | 최대 체력 +8% | 피격 시 15% 확률 체력 2% 회복 | 최대 체력 +10% | 피격 시 22% 확률 회피 +14 3초 | 최대 체력 +12% | 피격 시 10% 확률 실드 5% 충전 | 사망 시 1회 부활 |
| 갑옷 | 사슬갑옷 | 가시 반사 | 방어 +5 | 피격 시 60% 확률 가시 반사 | 방어 +7 | 피격 시 방어 +3 3초(누적) | 가시 반사 확률 +60%p | 최대 체력 +10% | 피격 시 30% 확률 회피 +15 3초 |
| 갑옷 | 로브 | 실드 | 최대 실드 +15% | 처치 시 실드 소량 충전 | 최대 실드 +20% | 회복 시 30% 확률 실드 8% 충전 | 처치 시 실드 충전 강화 | 최대 실드 +25% | 실드가 0일 때 공격력 1.5배 |
| 장갑 | 건틀릿 | 치명타 배율 | 치명타 배율 +30 | 치명타 시 치명 배율 +10 3초 | 치명타 배율 +40 | 치명타 시 30% 확률 체력 4% 회복 | 치명타 배율 +50 | 치명타 시 75% 확률 추가타 | 뒤쪽 적에게 피해 3.2배 |
| 장갑 | 가죽장갑 | 공격속도 | 공격속도 +8% | 공격 시 30% 확률 공속 +5% 3초 | 공격속도 +10% | 처치 시 공속 +20% 4초 | 공격속도 +12% | 처치마다 공속 영구 +1% | 공속 +35%·이동속도 +35% |
| 장갑 | 핸드랩 | 연타·분신 | 공격력 +5% | 공격 시 30% 확률 공격력 +5% 3초 | 공격력 +7% | 공격 시 10% 확률 공격력 영구 +1% | 공격력 +9% | 기본공격마다 분신 추가타 | 체력 25% 이하 적 즉사(보스 제외) |
| 신발 | 샌들 | 회피 | 회피 +7 | 회피 시 10% 확률 도끼 1개 발사 | 회피 +8 | 회피 시 공격력 +28% 5초 | 회피 시 15% 확률 체력 7% 회복 | 회피 시 회피 +8 3초(누적) | 회피 시 다음 공격 치명타 확정 |
| 신발 | 부츠 | 반격 | 반격 확률 +7 | 반격 시 공격력 +5% 3초 | 반격 확률 +8 | 반격 시 공격력 +14% 4초 | 반격 피해 +100% | 반격 시 체력 4% 회복 | 반격 시 연쇄 반격(확정) |
| 신발 | 장화 | 체력 | 최대 체력 +8% | 처치 시 체력 0.5% 회복 | 최대 체력 +10% | 회복량 +20% | 최대 체력 +12% | 처치 시 체력 +0.8% 추가 회복 | 체력 10% 이하 시 회피 +40 |
| 목걸이 | 펜던트 | 회복 | 회복량 +15% | 회복 시 30% 확률 방어 +5 3초 | 회복량 +20% | 회복 시 20% 확률 실드 3% 충전 | 회복 시 공격력 +8% 3초 | 회복 시 20% 확률 추가 회복 | 과회복분의 7배가 실드로 전환 |
| 목걸이 | 부적 | 처치 | 골드 획득 +30% | 처치 시 방어 +10 3초 | 골드 획득 +40% | 처치 시 30% 확률 치명 확률 +14 4초 | 획득 경험치 +1 | 처치 시 실드 충전 강화 | 골드 획득 2배 |
| 목걸이 | 구슬목걸이 | 창 소환 | 공격력 +5% | 공격 시 7.5% 확률 창 발사 | 치명타 확률 +6 | 창 피해 13.5배·관통 | 창 발사 확률 +7.5%p | 적 화살 30% 확률 오발 | 모든 발동 확률 1.22배 |

- **위임 표기**: 부위별 종류 이름(대검/전투도끼/장궁 …)과 계열 배정은 위임 기본값이다.
  단 **신발 3종은 주인 예시 그대로** — 샌들=회피 · 부츠=반격 · 장화=체력.
- 소환 5종(도끼·화살·번개·검기·창)이 각각 한 계열의 주축이 되도록 배치했다.

### 11.7 밸런스 과녁 (주인 확정)
- 챕터 ~~100개~~ **300개**로 확장 (§2.4 — 주인 추가 지시 2026-09-02 08:0X: 앵커 B 가 300 을 요구). 벽: 10 · 15 · **90(대형)** · **300(최종)**. 그 사이 중간 벽 추가 여부는 튜닝 몫.
- **앵커 A: 모든 부위 신화 0강 + 슬롯 6개 균등 15렙 → 챕터 90 을 «겨우» 클리어** (수십 회 재도전 수준), 91+ 는 사실상 벽.
- **앵커 B (주인 추가 2026-09-02): 모든 부위 신화 +9강 + 슬롯 6개 균등 50렙 → 챕터 300 을 «겨우» 클리어**, 301+ 는 사실상 벽 (300 클리어 시 «모든 챕터 클리어»).
- **앵커 C (주인 추가 2026-09-02 08:1X): 모든 부위 전설(차상위) 0강(위임 — 강화 언급 없음) + 슬롯 6개 균등 10렙 → 챕터 30 을 «겨우» 클리어.** 벽 지정은 아니고 곡선 고정점.
- 두 앵커 사이(91~299)는 장비 강화·슬롯 성장이 곧 진행 동력이 되도록 단조로운 곡선 (구체 목표는 T6 제안·§7 기록).
- 검증은 §7 실험4(F2P 진행)·실험5(앵커) — 비평가 2인 채점 루프(T1)에 통합.
