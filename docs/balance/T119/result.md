# T119 — 특전 풀 확장 + 등급 부활 · 사다리 8점 재적합 결과

> 주인 확정 2026-09-04 13:0X(13:2X 정정) · 워커 D `sess-1351-1681`
> 이 문서는 **T117(3택 복구) → T118(소환 계수 교체) → T119(풀 32종·등급)** 이 전부 합류한 트리에서
> 사다리 8점을 **한 번만** 잰 결과다(주인 지시 13:0X ③ — «T117·T118 은 사다리를 안 잰다»).

## 0. 무엇이 들어왔나

| 작업 | 내용 | 사다리에 미치는 방향 |
|---|---|---|
| T117 | 레벨업 특전 «순서대로 자동» → **3택 1** · 악마 «즉시 3택 1» · 시뮬 정책 «표 순서 우선» | 초반 칸에 6번 이후가 섞여 들어온다 |
| T118 | 소환 계수 화살 50 → **30%** · 도끼 30 → **50%** | 4번(화살) 약화 · 5번(도끼) 강화 — 초반 칸에서 상쇄·증폭 |
| T119 | 풀 10 → **32종** · 등급 3단 부활(60/25/15) · 시뮬 정책 «등급 우선 → 표 순서» | **전 구간 크게 쉬워진다** (전설 9종이 판을 뒤집는다) |

## 1. 착수 시점 실측 (세 작업이 다 들어갔고 난이도 노브는 안 건드린 트리)

`tools/verifyScoreCriteria.js` ⓓ (시드 11 고정 · 각 300판 · 밴드 ±5%p) 로 잰 값이다.

| 칸 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 과녁 챕터 | 5 | 15 | 28 | 40 | 70 | 150 | 380 | 420 |
| 클리어율 | 58.7% | 69.3% | 84.3% | 89.0% | 65.7% | 27.7% | 2.0% | 2.0% |

**여덟 칸이 전부 밴드 밖**이다. 1~6칸은 «너무 쉬워졌고»(과녁 10% ↔ 실측 28~89%),
7·8칸은 반대로 **너무 어렵다**(2.0%). 후반 두 칸이 안 오른 이유는 T114 가 이미 기록한 것과 같다 —
후반은 적 스탯이 지배해서 특전 한두 장이 승패를 못 가른다. 즉 **이번 변화는 앞쪽 여섯 칸을 통째로 밀어 올렸고,
그래서 사다리가 «앞은 쉽고 뒤는 어려운» 모양으로 꺾였다.**

## 2. ⚑ 재적합 전에 잡은 회귀 1건 (처치 시 번개가 챕터를 통째로 즉사시켰다)

착수 실측 도중 `verifyChapterFixed` ⓓ(챕터별 특전 개수)와 `verifyDevilPolicy` ⑧(악마 수락 40/40)이 함께 빨개졌다.
파고들어 보니 **레벨은 6까지 올랐는데 특전이 1~3장만 남은 판**이 섞여 있었다 — 챕터 1 이 **6.8초에 «클리어»** 됐다.

원인은 «보이는 적 전부에게 번개» 의 **대상 범위**였다. 처음 구현은 발동할 때마다 `frontNode()` 를 다시 불렀는데,
번개가 한 웨이브를 전멸시키는 순간 «최전방» 이 **다음 웨이브**로 넘어간다. 그 연쇄가 대기 웨이브를 차례로 지나
**보스까지 즉사**시키고, 보스가 죽으면 `G.cleared` 라 그 뒤의 레벨업은 특전을 주지 않는다.

고친 방식은 **T44 선례 그대로**다(«관통형은 발사 시점의 노드만 때린다 — 다음 웨이브 대기분은 절대 맞지 않는다»,
주인 15:2X · PLAN §2.3): 처치 시 번개의 대상을 **«죽은 적이 속한 웨이브»(`e.wave`)** 로 못 박고 함수가 그것을
인자로 받게 했다. 주인 문면 «보이는 적 = 현재 교전 중인 웨이브» 와도 이쪽이 맞는다.
게이트 `verifySummonChain` ⑤-ⓒ 가 두 엔진에서 이 성질을 단언한다(폴백으로 되돌아가면 빨개진다).

**이 수정은 밸런스를 크게 낮추므로, 위 1장의 착수 실측은 수정을 반영한 뒤 다시 잰 값이다.**

## 3. 사다리 8점 재적합

절차는 **T107 2단계·T114 3장과 같다** — ① 8칸 각각에서 «적 HP·DMG 공통 배수 `a`» 를 로그 이분탐색으로 재고
(챕터별 HP:DMG 비 보존) ② 구간률을 `r_new = r_old · (a_{k+1}/a_k)^(1/n)` 로 다시 자른다(기저는 ×a₁).
`a` 는 `eBaseHp`·`eBaseDmg` 에 같은 배수를 곱해 만든다 — 한 챕터만 재므로 전 챕터 균일 배수와 결과가 같다.

### 3-1. 국소 배수 a (로그 이분탐색 15회 · 시드 11·12·13 × 각 300판 = 900판/평가)

| 칸 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 과녁 챕터 | 5 | 15 | 28 | 40 | 70 | 150 | 380 | 420 |
| 착수 클리어율 | 52.0% | 63.2% | 78.8% | 88.9% | 57.3% | 17.7% | 2.8% | 2.9% |
| **a** | **1.3322** | **1.4095** | **1.4650** | **1.5945** | **1.3346** | **1.0757** | **0.8380** | **0.8484** |

**모양이 T114 와 다르다.** T114 의 a 는 전부 1 근방(0.987~1.124)이었는데 이번엔 **앞 다섯 칸이 1.33~1.59**,
**뒤 두 칸이 0.84** 로 **부호가 갈린다**. 뜻은 이렇다:

- **1~5칸(챕터 5~70)은 적을 33~59% 세게 만들어야 과녁으로 돌아온다** — 특전 풀이 10 → 32 로 늘고
  **전설 9종**이 들어오면서 플레이어가 통째로 세졌다. 가장 큰 칸은 **4칸(영웅+슬롯10 · 챕터 40)의 1.5945** 다.
- **7·8칸(챕터 380·420)은 반대로 적을 16% 약하게 해야 한다** — 이 두 칸은 원래도 과녁에 못 미치고 있었고
  (착수 2.8%·2.9%), 새 특전이 후반에서는 힘을 덜 낸다. **T114 가 남긴 «여유가 가장 적은 칸» 진단과 방향이 같다.**
- 그래서 **150 → 380 구간은 성장률을 낮춰야** 하고(a 가 1.076 → 0.838 로 떨어진다), 반대로
  **15 → 40 구간은 올려야** 한다. 재절단이 그 일을 한다.

### 3-2. 확정 수치

```
기저   eBaseHp  50.174688 → 66.840266   ( ×1.3322 = a₁ )
       eBaseDmg  9.330519 → 12.429661   ( ×1.3322 )
벽     10: HP ×1.50 / DMG ×1.25 · 15: ×1.00(비활성) · 90: HP ×2.00 / DMG ×1.50 · 420: ×1.00(꺼짐)
       — 벽 4종 · 챕터 상한 420 · 과녁 8점 · 적 수 곡선 · 원거리 곡선은 **한 글자도 안 건드렸다**
```

| 구간 | 재절단 배수 | HP 성장/챕터 (T114 → **T119**) | DMG 성장/챕터 (T114 → **T119**) |
|---|---|---|---|
| 1 → 5 | — | 2.9200% (불변 — 률은 두고 기저만 옮긴다) | 2.6500% (불변) |
| 5 → 15 | ×1.005661 | 0.3143% → **0.8822%** | 0.3143% → **0.8822%** |
| 15 → 28 | ×1.002973 | 7.1776% → **7.4962%** | 6.8093% → **7.1268%** |
| 28 → 40 | ×1.007088 | 7.7458% → **8.5095%** | 6.0994% → **6.8514%** |
| 40 → 70 | ×0.994086 | 5.3040% → **4.6813%** | 2.5582% → **1.9517%** |
| 70 → 150 | ×0.997308 | 2.3291% → **2.0536%** | 2.5206% → **2.2446%** |
| 150 → 380 | ×0.998915 | 2.3071% → **2.1961%** | 2.2541% → **2.1431%** |
| 380 → 420 | ×1.000308 | 1.4651% → **1.4963%** | 1.4651% → **1.4963%** |

**기저가 33% 오른 것이 이번 재적합의 본체다.** 초반 다섯 칸이 한꺼번에 쉬워졌으므로 «어디 한 구간» 이 아니라
사다리 전체가 아래로 내려앉았고, 그 대부분을 기저 한 값이 되받는다. 구간률 변화는 그 위에 얹는 미세 조정이라
전부 ±0.8%p 안이다 — 다만 **40 → 70 DMG 가 2.56 → 1.95% 로 가장 크게 내려간다**(5칸이 4칸보다 덜 세졌다).

**잔차 보정 없음 — 1차 절단으로 여덟 칸이 전부 들어왔다**(아래 3-3).
