# T90 — 악마 이벤트 2건 (주인 확정 2026-09-03) · 근거

> 세션 `sess-0636-7671` / 워커 C · 2026-09-03T06:36~07:2XZ · 기준 커밋 `040d0a4`
> 측정은 전부 `tools/regress.js`(PLAN §7 회귀 측정 규약 ①②③ — 고정 시드 11·12·13, 채점 판수 기본값).
> 원시 출력: `raw-after/`(반영 후 6벌). 반영 전은 같은 시드로 `040d0a4` 워크트리에서 돌렸다.

## 1. 무엇을 바꿨나

| 항목 | 전 | 후 |
|---|---|---|
| 거래 비용 | `p.hp = max(1, p.hp − p.maxHp*0.30)` — **현재체력** 차감 | `p.maxHp = max(1, p.maxHp − p.maxHp*DEVIL_COST)` + `p.hp = min(p.hp, p.maxHp)` — **최대치** 차감 |
| 시뮬 악마 정책 | `if(!G.noPerk && p.hp > p.maxHp*0.65)` — 체력 65% 초과일 때만 수락 | `if(!G.noPerk)` — **항상 수락** |
| 시뮬 천사 정책 | `p.dmg *= 1.05` (문서화 없음) | 같은 값 + `SIM_ANGEL_POLICY` 로 «항상 왼쪽(무료 +5%)» 명문화 |

- 비용 상수·동사는 두 엔진 공통(`DEVIL_COST` · `payDevilCost`)으로 모았다 — 값이 흩어지면 게이트가 빨개진다.
- 클램프는 `p.hp = Math.min(p.hp, p.maxHp)` 로 쓴다. 뜻이 같은 `if(p.hp>p.maxHp)p.hp=p.maxHp` 로 쓰면
  `verifyLegacyHtml` ③(«풀충전은 특전 정의 안에서만»)이 «풀충전» 으로 읽어 빨개진다 — 음성 ⑬ 이 이 모양을 고정한다.
- **게임(index.html)은 그대로 2택**이다(지불/지나감 · 무료/광고). 정책은 시뮬 전용이고, 게이트가 양방향으로 감시한다.
- 지불 버튼의 `<small>` 을 «위 특전을 획득합니다» → «최대 체력이 줄어든 채 진행 · 위 특전 획득» 으로 고쳤다
  (플레이어가 «현재 체력이 깎인다» 로 오해하지 않게 — 주인 확정 문면의 실질).

## 2. 밸런스 실측 (시드 11·12·13 · 반영 전 → 후)

**실험1 등급 과녁** (챕터 30 · 희귀 풀셋 · 300판/칸)

| 칸 | 과녁 | 전(평균) | 후(평균) | 차 | 판정 |
|---|---|---|---|---|---|
| 일반 | 10% | 9.1 | **8.7** | −0.4%p | ✓ → ✓ |
| 희귀 | 20% | 18.1 | **21.7** | +3.6%p | ✓ → ✓ |
| 전설 | 80% | 81.9 | **84.4** | +2.5%p | ✓ → ✓ |
| 혼합 | — | 25.3 | **27.6** | +2.3%p | — |

**합격 3/3 → 3/3 — 통과하던 과녁을 깨지 않았다.**

**실험5 스탯 사다리** (일반 특전만 뜨는 런 · 슬롯 0 · 1,000판/과녁 · 합격 구간 2~10%)

| 과녁 | 전(평균) | 후(평균) | 차 | 판정 |
|---|---|---|---|---|
| 노템(ch5) | 2.4 | **2.9** | +0.5%p | ✓ → ✓ |
| 일반(ch15) | 10.4 | **7.7** | −2.7%p | ✗ → **✓** |
| 희귀(ch30) | 8.4 | **8.7** | +0.3%p | ✓ → ✓ |
| 영웅(ch50) | 0.1 | **0.3** | +0.2%p | ✗ → ✗ |
| 전설(ch70) | 0.4 | **0.4** | 0.0%p | ✗ → ✗ |
| 신화(ch120) | 0.4 | **0.3** | −0.1%p | ✗ → ✗ |
| 신화+9강(ch260) | 11.8 | **11.2** | −0.6%p | ✗ → ✗ |

**합격 2/7 → 3/7** (일반 ch15 가 10.4 → 7.7 로 합격 구간 안에 들어왔다).

**해석**: 두 힘이 반대로 걸린다 — ①«항상 수락» 은 판마다 전설 특전 1장을 확정으로 얹어 **쉬워지게** 하고
②«최대치 차감» 은 그 판 내내 최대체력이 30% 줄어든 채로 두므로 **어려워지게** 한다(현재체력만 깎던 종전은
회복 특전 한 번이면 원복됐지만, 이제는 그 판 동안 되돌릴 방법이 없다 — 최대체력 증가 특전은 금지축이라 존재하지 않는다).
챕터 30 이상(실험1·실험5 희귀 이상)에서는 ①이 이겨 약간 쉬워지고, 특전을 적게 얻는 저챕터(ch15)에서는
②가 이겨 어려워진다 — 부호가 챕터별로 갈리는 것이 이 변경의 특징이다.

**⚑ 주인 조항대로 이 수치는 «반영 확인» 이지 기준선이 아니다** — 주인이 «효과 감사(T88) 후 기준선 재측정에 포함» 이라
못박았으므로, P3 재개 첫 회차가 T88·T90 을 함께 반영한 상태에서 다시 잰다.

## 3. 게이트

- **신설 `tools/verifyDevilPolicy.js` — 33항목 전부 통과** (정적 ⑥절 + 실행 단언 2절).
  - ⑦ 실행 단언(vm): 풀피 1000/1000 → 700/700 · **저체력 100/1000 → 100/700**(옛 구현이면 현재체력이 1 이 됐다) · 하한 1 유지.
  - ⑧ 실행 단언(vm 스파이): 챕터 40판 전부 완주하는 대조군에서 **악마 수락 40/40 = 100%**,
    `noPerk` 측정 10판에서는 **0회**(특전 미획득 조건이 오염되지 않는다).
- **음성 테스트 13/13 exit 1** — 조건부 수락 부활 · 현재체력 차감 복귀(양 엔진) · 상수 변조(단독·불일치) ·
  천사 배수 1.15 · 게임 선택지 삭제 2종 · 클램프 제거 · PLAN 문면 삭제 · 비용 호출 삭제 · 버튼 문면 되돌림 ·
  클램프를 풀충전 모양으로 바꿔 쓰기.
- **기존 게이트 2곳 갱신** (둘 다 «코드 모양이 바뀌면 게이트를 함께 갱신할 것» 이라고 스스로 적어 둔 자리다):
  - `verifyPerkPolicy` ⑤ — 악마 비용을 옛 리터럴 `p.hp=Math.max(1,p.hp-p.maxHp*0.30)` 에서 읽던 것을 `DEVIL_COST` 상수로.
  - `verifyPerkPolicy` ⑦ — 천사 배수 정규식이 한 줄짜리 `}else{p.dmg*=1.05;}` 를 전제하고 있었다(정책 주석이 붙어 깨짐).
- **상수/함수 배치 주의**: `verifyT2` ⑧ 의 `chapterLayout` 추출기는 «`const LAYOUT_MAXENEMY` 줄부터 처음 나오는 `}` 줄까지» 를
  잘라 vm 에 넣는다. 그 창 안에 함수를 새로 놓으면 추출이 조기 종료돼 빨개진다 — `DEVIL_COST`/`payDevilCost` 블록을
  **`LAYOUT_MAXENEMY` 주석 앞**으로 옮겨 해소했다(두 엔진 동일).
- **전 게이트 재실행: 26종 중 24종 exit 0**, 남은 2종(`verifyPerkFireHtml`·`verifyPerkEffectHtml`)은 playwright 필요분이라 리포 기본 상태에서 exit 2 다 — `PW_CORE` 를 주면 `verifyPerkFireHtml` 도 위반 0 으로 통과한다(기준 커밋에서도 같은 상태).
- **T3 헤드리스 스위트 209/209** (boot 78 · battle 48 · gear 64 · fx 19 — T89 합류 후), pageerror 0.

## 4. 파생 발견 — T91 로 등재 (승인 대기)

«일반 특전만 뜨는 런»(실험1 등급 고정 · 실험5 사다리, 둘 다 `rarityLock:0`)에서 **악마만은 등급 고정을 안 따르고
언제나 전설을 준다** — 악마 분기가 `perkPool(G, 2)` 를 직접 부르기 때문이다. 챕터마다 악마가 정확히 1개이므로
«일반만 뜨는 런» 은 사실 «일반 + 전설 1장» 런이다. T90 이 이 경로를 «가끔» 에서 **«항상»** 으로 바꿔 놓아
오염분이 상수가 됐다(자를 흔들리게 하지는 않지만 통째로 밀어 놓는다).

같은 시드(11·12·13)에서 악마도 등급 고정을 따르게 한 진단 빌드로 재측정한 값 — **엔진에는 반영하지 않았다**:

| 칸 | 현재(T90) | 악마도 등급 고정 | 차 |
|---|---|---|---|
| 실험1 일반 (과녁 10) | 8.7 ✓ | **4.6 ✗** | −4.1%p |
| 실험1 희귀 (과녁 20) | 21.7 ✓ | 19.7 ✓ | −2.0%p |
| 실험1 전설 (과녁 80) | 84.4 ✓ | 81.9 ✓ | −2.5%p |
| 사다리 노템(ch5) | 2.9 ✓ | **0.8 ✗** | −2.1%p |
| 사다리 일반(ch15) | 7.7 ✓ | **0.7 ✗** | −7.0%p |
| 사다리 희귀(ch30) | 8.7 ✓ | 4.5 ✓ | −4.2%p |
| 사다리 신화+9강(ch260) | 11.2 ✗ | 8.8 ✓ | −2.4%p |

즉 **실험1 «일반» 칸의 절반 가까이(8.7 중 4.1%p)와 사다리 저챕터 두 칸의 대부분이 «악마가 준 전설 1장» 에서 나온다.**
어느 쪽이 주인이 뜻한 자인지는 워커가 정할 문제가 아니라 **주인 확정 사항**이라(«악마 = 전설 확정» 은 게임 규칙으로
주인이 못박은 것이고, 등급 강제 실험에서 그 규칙을 어떻게 다룰지는 정해진 바 없다) 엔진은 손대지 않고 T91 로 등재했다.
선택지는 ⓐ 등급 고정 실험에서는 악마도 그 등급만 준다 ⓑ 등급 고정 실험에서는 악마 거래를 건너뛴다
ⓒ 현행 유지(전설 1장 포함이 정본) 셋이다. **P3 기준선 재측정 전에 답이 나와야 한다** — 자가 바뀌면 과녁 판정이 통째로 바뀐다.
