# T84 (P2 — 새 특전 132종 동작 검증) 근거 수치

> 워커 D / sess-2250-4337 · 2026-09-02T22:5X~23:3XZ · main 29a4fe2 (T83 P1 마감분) 위에서

## 0. 무엇이 남아 있었나

T83(P1) 이 이미 확인해 둔 것: T3 헤드리스 4스위트 197/197 · 132종 단독 보유 전수 실행(각 8판) 예외 0 ·
최악 조합 20판 완주 · 「엔진이 안 읽는 특전 0종」(정적 대조).

남은 것은 **«획득» 이 아니라 «발동»** — 특전이 배선만 되고 트리거가 영영 안 굴면(도달 불가 조건·
죽은 분기·오타난 필드) 획득 검사도 정적 대조도 초록인 채로 그 특전은 게임에서 아무 일도 하지 않는다.
이 회차는 그 자리를 **게이트 2종**으로 메웠다.

## 1. T3 스위트 재실행 (착수 전 기준선)

| 스위트 | 결과 |
|---|---|
| `tools/t3/boot.js` | **78/78** · pageerror 0 (390×844 · 360×800 · 375×667) |
| `tools/t3/battle.js` | **36/36** · pageerror 0 |
| `tools/t3/gear.js` | **64/64** · pageerror 0 |
| `tools/t3/fx.js` | **19/19** · pageerror 0 |
| 합계 | **197/197** — T83 등재값과 동일(회귀 0) |

기존 게이트 18종: **17 초록 · 1 빨강**. 빨강은 `verifyHarness` 뿐이고 내용도 T83 인계 그대로다
(실험1 +29.0%p · 실험2 +23.6%p 드리프트 = 하니스 재선정, **P3 첫 회차 몫**). P2 는 손대지 않았다.

## 2. 신설 게이트 ① `tools/verifyPerkFire.js` — sim.js 축

**원본 무수정.** 소스를 읽어 메모리에서만 계측본을 만들어 돌린다.

- **계측 137곳** = `if( … px.<id> … ) <효과>` 자동 변환 + 손 패치표.
  `__F` 를 **효과 자리 맨 앞**에 꽂으므로 «px 를 읽었다» 가 아니라 «조건이 통째로 참이라 효과가 실행됐다» 를 센다.
- 손 패치표 14줄 = if 문이 아닌 자리 9곳(삼항 6 · 대입 2 · `||` 꼬리 1) + **px 키를 아예 안 읽는 스탯 직변형 5종**
  (🍖 `killHeal` · 💞 `healAmp` · 🔧 `repairAmp` · 🔥 `critF` · 💪 `dmg`). 뒤 5종은 획득 시 스탯이 **실제로 변했을 때만**
  표식(`p.__amp`)을 남기고 그 스탯이 쓰인 자리에서 센다 — px 키만 보면 «수치를 0 으로 지워도 발동으로 잡히는» 헛계측이 된다.
- 자동 변환에서 **뺀 자리 3곳**: `px.c_backDmg||px.backDmg`(바깥 관문) · `l_auto*` 3종 묶음(바깥 관문) ·
  `!(stunned&&px.c_stunNoEvade)&&…`(조건이 뒤집혀 있다 — 참이면 «회피했다» = 특전이 안 막은 것). 셋 다 손 패치표가 따로 본다.
- 패치표 원문이 소스에 **정확히 1번** 없으면 그 자리에서 실패한다 — 엔진이 바뀌었는데 게이트만 조용히 통과하는 일이 없다.

실행: 대상 특전 + **동반 세트 9종**(소환 5종·방어막·기절·회복·수리 공급원) 보유, 레벨업 특전 획득은 끔,
장비는 실험1 하니스와 같은 희귀 풀셋·슬롯 0, 챕터 30·60 각 6판. 시드 고정이라 재현된다.

| 결과 | 값 |
|---|---|
| 실행 | 132종 × 12판 = **1,584판** · 예외 **0건** |
| **자연 굴림 발동** | **132/132** (확률에 손도 안 댐 — 2차 «확률 강제» 는 쓸 일이 없었다) |
| 등급별 | 일반 44/44 19,261회 · 희귀 46/46 19,831회 · 전설 42/42 **883,026회** |
| 최저 발동 | 6회 — `r_counterHeal` · `r_lowShield` · `r_spearCounter` (전부 실화, 도달 불가 0종) |

전설의 발동 횟수가 두 자릿수 배로 큰 것은 ⏰ 자동 소환 3종과 소환 연쇄 때문이다 —
T83 이 P3 로 넘긴 «소환 연쇄 임계 B = 2.791» 이 발동 횟수로도 그대로 보인다.

**자가검사 8/8**: 효과 분기(`c_waveAtk`·`l_thorns`·`r_wardHeal`)·삼항(`l_axeSpin`)·`||` 꼬리(`r_hitCounter`)·
스탯 수치(`c_killHeal2`·`r_repairAmp`·`r_atk50`)를 각각 지우면 그 특전이 «발동 0» 으로 잡힌다.
양성 대조(안 고친 계측본에서는 굴었는가)를 먼저 확인하므로 «둘 다 0» 인 헛통과도 막는다.

## 3. 신설 게이트 ② `tools/verifyPerkFireHtml.js` — index.html 축

같은 일을 **주인이 실제로 하는 게임**에서 한다. 두 엔진이 «수치가 같다» 는 것은 `verifyT2`·`verifyCombatConst` 가
이미 대조하지만 그건 정적 대조라 «게임 쪽 트리거가 실제로 굴었는가» 는 못 본다.

- 인라인 스크립트를 꺼내 같은 코어(`tools/perkFireCore.js`)로 계측 → 스크래치패드에 임시 HTML(**커밋 안 함**)
  → 헤드리스 크로미움. 페이지 안에서 `startChapter()` 로 판을 열고 **`update(1/30)` 을 직접 밀어** 프레임을 앞당긴다
  (rAF 를 기다리면 132종 × 여러 판이 실시간이 된다). 팝업이 뜨면 `G.paused` 로 멈추므로 드라이버가 눌러 치운다.
- **시드 고정**: 게임엔 `setSeed` 가 없어 `Math.random` 을 통째로 갈아끼웠다(sim.js 와 같은 mulberry).
  안 하면 발동 횟수가 판마다 흔들려 «어제 초록이던 게이트가 오늘 빨갛다» 가 된다 — 실제로 시드 없이 돌렸을 때
  `r_counterHeal` 이 한 번 0 으로 떨어졌다. 지금은 **두 번 돌려 총 발동 횟수가 자릿수까지 동일**하다.
- 계측 지점 **137곳** — sim.js 와 같은 수. 미커버 13종도 정확히 같은 13종이라 두 엔진이 구조까지 나란하다는 증거가 된다.

**발동 사다리** — 위 칸에서 굴면 거기서 끝낸다:

| 사다리 | 조건 | 신규 발동 |
|---|---|---|
| ① 표준 | 챕터 30·60 각 3판 | **131종** |
| ② 확대 | 챕터 20·45·90 각 8판 | **1종** — `r_evade3Dmg`(💃 3연속 회피) 6회 |
| ③ 확률 강제 | (도달 안 함) | 0 |

> ③ 이 ① 보다 항상 유리한 것은 아니라서 ② 를 앞에 뒀다: 모든 확률이 참이 되면 플레이어가 너무 세져
> **맞는 일 자체가 줄어** 피격·반격 축이 오히려 안 굴 수 있다(설계 중 실측으로 확인한 함정).

| 결과 | 값 |
|---|---|
| **발동** | **132/132** (표준 131 · 확대 1) · pageerror **0** |
| 등급별 | 일반 44/44 7,052회 · 희귀 46/46 7,553회 · 전설 42/42 443,839회 |
| 최저 발동 | 1회 `r_spearCounter` · 2회 `r_counterAtkM` · 3회 `l_counterChain`·`r_counterHeal` (반격 축이 바닥) |

**자가검사 5/5**: 효과를 지운 계측본을 따로 띄워 «발동 0» 이 되는지 확인(양성 대조 포함).

## 4. 결론 · 인계

- **죽은 특전 0종** — 두 엔진 모두에서 132종 전부가 실제로 굴었다. P1 이식은 배선까지 살아 있다.
- 게이트 로스터 **18 → 20종**(`verifyPerkFire` · `verifyPerkFireHtml`. `perkFireCore.js` 는 둘이 같이 쓰는 라이브러리).
  둘 다 **밸런스 무관** — 엔진을 한 글자도 안 고쳤으므로 P3 의 기준선은 T83 등재값 그대로다.
- **P3 로 넘어가는 관찰 1건(수치 조정 아님, 참고용)**: 두 엔진 모두 **반격 축이 발동 빈도의 바닥**이다
  (`r_spearCounter`·`r_counterAtkM`·`r_counterHeal`·`l_counterChain`·`r_counterX`). 반격 확률 20% × 근접 피격이라
  트리거 자체가 드물다 — 주인 지시 «드문 트리거 = 확률/수치 상향»(등급 내 밸런스 조항)의 1순위 후보다.
  P3 실험2 의 승률과 함께 보면 «표본이 적어서 낮게 나온 것» 과 «정말 약한 것» 을 가를 수 있다.
- `verifyPerkFireHtml` 은 playwright-core 가 필요하다(T3 스위트와 같은 전제 — 리포에 커밋 금지,
  스크래치패드에 깔고 `PW_CORE=<경로>/node_modules/playwright-core`). 벽시계 약 **100초**.
  `verifyPerkFire`(sim 축)는 순수 node 로 약 **28초**.
