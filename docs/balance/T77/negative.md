# T77 음성 테스트 — `verifyT2` ㊲ 가 되돌림을 실제로 잡는가

> 방법: 작업 트리 파일을 하나씩 훼손 → `node tools/verifyT2.js` → 즉시 원복.
> 게이트가 «통과(exit 0)» 를 내면 실패다. **9종 전부 exit 1.**

| # | 훼손 내용 | exit | 잡은 항목(대표) |
|---|---|---|---|
| 1 | `c_gold30` 을 `sim.js` 에 되돌림 | 1 | ② 개수 129 · 두 파일 괴리 · ㊲① 되살아남 |
| 2 | `m_sage` 를 `index.html` 에만 되돌림 | 1 | ② 두 파일 괴리 · ㊱① PLAN 128행 ≠ 129종 · ㊲① |
| 3 | 새 경제 특전 `c_goldNew`(골드 +50%) 두 파일 추가 | 1 | **㊲② 전투 무관 특전 1종: c_goldNew(경제(골드))** — 두 파일 모두 |
| 4 | 새 이속 전용 특전 `c_walkNew` 두 파일 추가 | 1 | **㊲② c_walkNew(이동속도)** — 두 파일 모두 |
| 5 | `m_time` 에서 공속을 빼 이속 전용으로 만듦 | 1 | **㊲② m_time(이동속도)** — 화이트리스트가 아니라 «전투 대입이 있는가» 로 판정한다는 증거 |
| 6 | 한쪽 파일(`sim.js`)에만 이속 특전 추가 | 1 | ② 두 파일 괴리 · ㊲②③ |
| 7 | 특전 문구를 «처치 시 골드 +7%» 로 되돌림 | 1 | **㊲④ index.html tx [c_killShield3]** · ㊱② PLAN 과 문구 불일치 |
| 8 | PLAN §3 에만 `c_gold30` 행을 되돌림 | 1 | **㊲① PLAN §3:c_gold30** · ㊲④ PLAN §3 [c_gold30] · ㊱① 행수 불일치 |
| 9 | 특전 1종을 지워 등급 개수를 깨뜨림 | 1 | ② 개수 127 · ㊲⑤ 등급별 개수 |

3·4·5·7·8 은 **㊲ 가 없으면 통과했을** 되돌림이다(개수가 안 변하거나 PLAN 만 바뀌는 훼손).
특히 5 번은 «m_time 을 이름으로 면제하는» 구현이었다면 놓쳤을 자리다.
