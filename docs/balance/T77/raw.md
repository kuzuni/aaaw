# T77 — 전투 무관 특전 4종 삭제 (주인 확정 2026-09-03) · 원시 측정

> 워커 C / sess-0336-20926 · 기준 커밋 = `f458380`(삭제 전) → 이 커밋(삭제 후)
> ROUTINE ⚑ «(2026-09-03 KST) 전투 무관 특전 전면 삭제 (주인 확정)» 이행.

## 1. 삭제한 4종 (주인 원문 그대로)

| id | 표시 | 등급 | 엔진 효과 | 삭제 근거 |
|---|---|---|---|---|
| `c_gold30` | 🪙 처치 시 골드 +30% | 일반 | `goldMul+=0.3` | 전투 기여 0 (T55 가 «일반 등급의 구조적 바닥» 으로 지목) |
| `c_walk20` | 🏃 이동 속도 +20% | 일반 | `walkMul+=0.2` | 이속류 |
| `m_gold2` | 💰 황금 기사 — 골드 획득 2배 (고유) | 신화 | `goldMul*=2` | 전투 기여 0 (T25 실측 61.5% = 영점 60% 선) |
| `m_sage` | 🌟 현자 — 처치 시 경험치 +1 (고유) | 신화 | `px.sage=true` | 경제(경험치)류 |

- **유지한 것 (주인 명시)**: 🎲 `r_refresh`(새로고침) · 🔮 `m_choice4`(전지의 눈) — «특전 선택의 질 = 전투력».
- **🕰️ `m_time`(공격속도 +21% & 이동속도 +21%)은 삭제 목록에 없어 유지**했다. 이속을 올리지만 공속(전투 스탯)을
  함께 주므로 «전투 무관» 이 아니다. → **주인 검토 등재**(주인이 이속 성분까지 빼라면 그때 뺀다).
- **장비 옵션(GOPT)의 골드·경험치 옵션은 건드리지 않았다** — 주인 지시는 «특전» 축이다. 그래서 `goldMul`·`px.sage`·
  `walkMul` 엔진 코드는 **죽은 코드가 아니라 장비 옵션이 계속 쓰는 산 코드**다(삭제하면 장비가 깨진다).

## 2. 반영 범위

- `sim.js` · `index.html` — 특전 4종 삭제(동시), 특전 정의 주석의 개수 갱신.
- `PLAN.md` §3 — 표 4행 삭제 · 제목/개수 문장 갱신(총 **128종**, 일반 31 · 희귀 33 · 전설 33 · 신화 31, 편차 2) ·
  §3.0 에 «경제/이속류 추가 금지» 규칙 신설(흡혈·적중률 금지 옆).
- 게이트 — `verifyT2.PERK_TOTAL` 132→128 · `verifyPerkGearDup` 개수 단언 132→128 및 죽은 KNOWN 항목
  (`bool|m_sage|amulet|5`) 제거 · `tools/t3/boot.js` 로드 개수 132→128.

## 3. 신설 게이트 — `verifyT2` ㊲ «전투 무관 특전 금지»

금지어 목록만 두면 «px 키를 새로 파서 우회하는» 되돌림을 못 잡으므로 **구조로** 판정한다:
특전 `ap` 가 **대입하는 대상 집합**이 전부 금지축(`goldMul`·`px.sage`·`walkMul`)이면 전투 무관 특전이다.

1. ① 삭제 4종이 두 엔진·PLAN §3 어디에도 없다(되살아남 감시).
2. ② 두 파일 각각 — 전투 기여 없이 금지축만 올리는 특전 0종. (대입이 아예 없는 특전은 T66 관할이라 제외)
3. ③ 금지축을 건드리는 특전 **집합**이 두 파일에서 동일(한쪽만 되돌아가는 것 방지) — 현재 `m_time` 하나.
4. ④ 특전 표시 문구·PLAN §3 표에 «골드»·«경험치» 0건.
5. ⑤ 등급별 개수 31/33/33/31.

**음성 테스트 9/9 exit 1** (`docs/balance/T77/negative.md` 요약):
① c_gold30 을 sim 에 되돌림 · ① m_sage 를 index 에만 되돌림 · ② 새 경제 특전(골드 +50%) 두 파일 추가 ·
② 새 이속 전용 특전 두 파일 추가 · ② **m_time 에서 공속을 빼 이속 전용으로 만듦**(화이트리스트가 아니라
구조 판정임을 증명) · ③ 한쪽 파일에만 이속 특전 추가 · ④ 특전 문구에 «골드» 되돌림 ·
① PLAN §3 에만 c_gold30 행 되돌림 · ⑤ 특전 1종을 지워 등급 개수 깨뜨림.

## 4. 밸런스 실측 — 삭제 전(132종) / 후(128종)

### 4-1. 실험1 «등급 고정 사다리» — 시드 1~6 짝지음 (각 300판, 챕터30 · 희귀 풀셋)

| 등급 | 전(132) | 후(128) | 짝지음 델타 | t |
|---|---|---|---|---|
| 혼합 | 48.85% | 50.55% | **+1.70%p ± 1.09** | +1.57 |
| 일반 | 16.55% | 19.07% | **+2.52%p ± 0.70** | **+3.58** |
| 희귀 | 44.67% | 43.60% | −1.07%p ± 1.93 | −0.55 |
| 전설 | 71.43% | 70.55% | −0.88%p ± 1.18 | −0.75 |
| 신화 | 80.02% | 81.55% | **+1.53%p ± 0.86** | +1.78 |

- **삭제한 4종은 일반 2 · 신화 2 인데, 오른 등급이 정확히 그 둘이다**(일반 t=3.58 로 유의). 죽은 칸이 빠지면
  같은 등급 굴림에서 «쓸모 있는 특전» 이 뽑힐 확률이 올라간다 — 예상과 부호·크기가 맞는다.
- **희귀·전설은 특전 풀이 한 종도 안 바뀌었는데 ±1%p 움직였다 — 이것이 이 측정의 잡음 척도**다
  (300판 × 6시드). 신화 +1.53 은 그 대역 언저리라 «유의» 로 주장하지 않는다.
- ⚑ **주인 확정 신규 과녁(일반 15 · 희귀 25 · 전설 35 · 신화 45%, ±5)** 기준으로는 삭제 후에도
  **일반 19.1(경계 안) · 희귀 43.6 · 전설 70.6 · 신화 81.6 — 상위 3등급이 전부 과녁 위**다. 조정은 T1 몫.

### 4-2. 실험2 «등급 내 스프레드» — 12,000판 · SEED=11 (주인 지시 «12,000판 이상»)

| 등급 | 전(132) 폭 | 후(128) 폭 | 최하 특전 (전 → 후) |
|---|---|---|---|
| 일반 | 23.8%p | 25.4%p | c_missAtk 60.5% → c_atkBuff 61.5% |
| 희귀 | 29.6%p | 27.1%p | r_healAtk 58.3% → r_stunCrit 61.4% |
| 전설 | 20.5%p | 21.4%p | l_overBolt 62.3% → 62.9% |
| 신화 | **34.0%p** | **31.7%p** | **m_gold2 59.0% → m_choice4 62.7%** |

- **신화 바닥이던 `m_gold2`(59.0%)가 사라져 신화 폭이 34.0 → 31.7%p 로 줄었다.** 다만 **풀이 안 바뀐
  희귀가 −2.5%p, 전설이 +0.9%p 움직였으므로 이 판수에서도 잡음이 ±2.5%p 대**다 — 신화 −2.3%p 를
  «유의한 개선» 으로 읽지 말 것. 확실한 것은 **«신화 최하가 더 이상 전투 무관 특전이 아니다»** 라는 사실이다.
- 삭제 전 하위 12 안에 `c_gold30`(61%) · `m_gold2`(59%)가 둘 다 있었다 — T55·T25 진단의 재현.
- 원시 출력: `exp2-before-132.md` · `exp2-after-128.md`.

### 4-3. 스탯 사다리 7점 (`node sim.js 5`, 각 200판)

| 상태 | 과녁 챕터 | 전(132) | 후(128) |
|---|---|---|---|
| 노템 | 5 | 48.0% | 50.5% |
| 일반 | 15 | 60.5% | 55.5% |
| 희귀 | 30 | 51.0% | 44.0% |
| 영웅 | 50 | 58.0% | 60.5% |
| 전설 | 70 | 45.0% | 49.5% |
| 신화 | 120 | 39.0% | 42.0% |
| 신화+9강 | 260 | 66.0% | 56.0% |

**합격(2~10%)은 전후 모두 0/7** — T73(주인 확정 기본 스탯 반영 후 곡선 재적합 필요)이 등재해 둔 상태 그대로다.
통과하던 과녁을 깨지 않았고, 200판이라 칸별 산포가 ±7%p 급이므로 개별 칸의 증감은 읽지 말 것.

## 5. 게이트 결과

- 상시 게이트 **15종 전부 exit 0** (`verifyT2` **462/462** — ㊲ 6항목 신설, `verifyPerkGearDup`·`verifyOptText`·
  `verifyPerkPolicy`·`verifyHarness` 포함).
- **`verifyHarness` 재보정 불요** — 삭제로 인한 드리프트가 허용치 안이라 기준선을 건드리지 않았다.
- **T3 헤드리스 4스위트 190/190** (boot 78 · battle 29 · gear 64 · fx 19), pageerror 0.
- 음성 테스트 **9/9 exit 1** (`negative.md`).

## 6. 인계

- **⚑ T1 재개 시 회차 로그에 «전투무관삭제 반영» 표시할 것** (이 커밋 이전 수치와 직접 비교 금지).
- **주인 검토 1건** — 🕰️ `m_time`(공격속도 +21% & 이동속도 +21%): 삭제 목록에 없어 유지했으나 이속 성분을
  갖는 유일한 특전이다. 주인이 «이속은 아예 없애라» 면 이속 항을 빼고 공속만 남기면 된다(게이트 ㊲③ 이
  그때 «금지축 특전 0» 으로 바뀐다).
- 장비 옵션의 골드 3칸·경험치 1칸은 그대로다 — 주인 지시가 «특전» 축이라 건드리지 않았다.
