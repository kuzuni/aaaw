# PROGRESS — 꼬마기사 키우기

> 갱신 규약은 `docs/ROUTINE.md` §3. 스펙은 `PLAN.md` (변경 금지).

## 작업 상태

| ID | 작업 | 상태 | 담당(SID/워커) | 비고 |
|---|---|---|---|---|
| T1 | 밸런스 튜닝 루프 (비평가 2인 각 ≥8/10) | 진행중 | sess-2113-31444 / A | R02 진행. 회차 로그는 아래 절 |
| T2 | index.html 이식 (특전 102종·챕터 20) | 대기(T1 후) | — | sim.js 와 수식 1:1 |
| T3 | 동작 검증 (헤드리스 부팅·콘솔 에러 0) | 대기(T2 후) | — | |
| T4 | 배포 준비 (주인 할 일 정리) | 대기(T3 후) | — | Pages·아티팩트는 주인/대화형 몫 |
| T5 | 실험1·2 하니스가 신 경제와 어긋남 (강화 6렙/8렙 고정) | 완료 | sess-2113-31444(발견) / 주인 승인 | **주인 승인(2026-09-02, `e1c3100`): 실험1 9렙 · 실험2 11렙, 경제 노브 변경 라운드마다 재보정.** 안건 종결 — sim.js 반영은 T1 루프에서 수행. 재보정 근거 수치는 아래 워커 D 검증 로그 참조 |

## T1 회차 로그

(형식: `RNN | 비평가A점/비평가B점 | 조정 내용 1줄 | SID 워커`)

## 검증 로그 (선점 가능 작업이 없던 세션)

- **2026-09-01T21:21Z / sess-2120-31792 · 워커 B** — T1 은 lock 중(`sess-2113-31444` 워커 A), T2~T4 는 T1 대기라 선점 불가. ROUTINE §0-5 에 따라 읽기 전용 검증만 수행.
  - `node --check sim.js` 통과.
  - **주인 지시 3건의 코드 반영 상태 대조**
    - ① **특전 선택지 등급 통일 → 미반영.** `sim.js:372` `rollPerks` 가 선택지 하나마다 `rollRarity(G)` 를 굴려 등급이 섞인다. `index.html` 의 `rollPerks` 도 같은 구조 → T2 에서 동일 수정 필요.
    - ② **챕터 종료 보스 킬 특전 스킵 → 미반영.** `sim.js:247` `gainExp` 가 조건 없이 `perkChoice(G)` 를 부른다. `index.html:899` 는 보스 처치 시 `gainExp(e.exp)` 를 먼저 호출하고 700ms 뒤 `openClear()` 라, 클리어 직전에 레벨업 팝업이 뜬다(`G.over` 가 아직 false).
    - ③ **레벨업 무보정(회복·최대치 증가 금지) → 이미 충족, 제거할 코드 없음.** `sim.js` 의 `gainExp`/`perkChoice`, `index.html` 의 `gainExp`/`openPerkChoice` 어디에도 레벨업에 기인한 회복·최대 체력/실드 증가가 없다. `p.sh=p.maxSh` 는 **웨이브 클리어(보스 제외) 실드 충전**(`sim.js:240`, `index.html:897`)이고, `perkHp`(🦾 전설 특전)의 최대체력 +5% 는 특전 자체 효과라 유지 대상. → **«레벨업무보정» 사유로 인한 T1 회차 무효화는 불필요**(①② 미반영에 따른 무효는 별개로 유효). 주인이 관측한 «레벨업마다 풀충전» 은 현재 `index.html` 이 아니라 배포된 구버전 아티팩트 동작으로 추정.
  - **추가 (지시 4번째 건 `2116ab1` 도착 후 재확인):** ④ **웨이브 전멸 실드 충전 폐지 → 미반영.** 제거 대상 위치는 위 ③ 에서 지목한 바로 그 두 줄 — `sim.js:240` (`if(e.wave.enemies.every(x=>x.hp<=0)&&!e.isBoss){p.sh=p.maxSh;}`) 과 `index.html:897` (`if(!e.isBoss){ p.sh=p.maxSh; ... }`). ③ 에서 «웨이브 클리어 충전이라 레벨업 지시와 무관» 이라 적은 것은 여전히 맞지만, 이제 ④ 에 의해 **그 줄 자체가 삭제 대상**이다(§2.3 취소선). `🔷 실드 최대치 +30%(즉시 충전)` 같은 특전 효과는 유지.
  - **주의(워커 간 타이밍):** 워커 A 의 T1 lock 은 `21:13:21Z`, 주인 지시 3건 커밋은 `21:16~21:19Z` 로 **lock 이 지시보다 앞선다.** A 가 구스펙으로 회차를 돌고 있을 수 있으니, T1 담당 세션은 다음 회차 전에 `git fetch` 로 최신 ROUTINE/PLAN 을 재확인하고 ①② 를 먼저 반영할 것.

- **2026-09-01T21:37Z / sess-2136-19077 · 워커 C** — T1 은 여전히 lock 중(`sess-2113-31444` 워커 A, 21:13:21Z — 24분 경과, 탈취 불가). T2~T4 는 T1 대기. ROUTINE §0-5 에 따라 읽기 전용 검증만 수행. 워커 B 가 검증한 지시 ①~④ 는 재확인 결과 **상태 변동 없음(전부 여전히 미반영)** 이라 중복 기록하지 않고, **신규 지시 ⑤ (인게임 UI 2건, `8af355b`) 의 구현 가능성**을 대신 점검했다.
  - **게이트 재실행**: `node --check sim.js` 통과. `node sim.js 1` 정상 완주 — 일반 3.7% / 희귀 4.3% / 전설 60.0% / 신화 98.0% (단조증가라 채점표 실험1 은 형식상 3/3 이나, **등급통일·실드무료충전폐지 미반영 상태의 수치라 T1 회차로는 무효**).
  - **특전 정합성**: `sim.js` 특전 102종 확인, 등급별 26/26/28/22 로 PLAN §3.1~3.4 와 일치. 반면 **`index.html` 은 65종뿐** — T2 에서 37종 추가 이식 필요.
  - **🆕 T2 선행 과제 발견 ①: 현 버프 엔진으로는 «발동 중 버프 아이콘»(지시 ⑤-①)을 구현할 수 없다.** `addBuff` 가 저장하는 항목이 `{t, amt}` 뿐이라 **그 버프가 어느 특전에서 왔는지 정보가 없다** (`sim.js:189-193`, `index.html:427-432`). 게다가 서로 다른 특전이 같은 키를 공유한다(예: `atk` 배열에 `c_atkBuff`·`critAtkBuff`·`counterAtkS`·`atkBuffM`·`atkBuffL` 가 뒤섞여 push 됨) — 그래서 **아이콘도, 등급색도, «중첩 수» 도 배열 길이만으로는 산출 불가.** → T2 에서 버프 항목을 `{t, amt, src:'<특전 id>'}` 로 확장하고 **호출부 전부에 특전 id 를 넘겨야 한다**(sim.js 29곳, index.html 12곳). 아이콘은 `src` 로 그룹핑해 개수를 세면 중첩 뱃지가 나온다. 단, 밸런스 계산(`bsum`)에는 영향 없는 순수 메타데이터 추가라 **T1 회차 무효 사유는 아니다.**
  - **🆕 T2 선행 과제 발견 ②: 특전 id → 버프 엔진 키가 1:1 이름 대응이 아니다.** 시간제 버프를 주는 특전은 28종이며, 일부는 이름이 어긋난다(특전 id `c_critChain` → 엔진 키 `px.critChain`, `c_critF` → `px.critFsmall`, `c_critHeal1` → `px.critHealS`). 위 ① 의 `src` 를 채울 때 **엔진 키가 아니라 특전 id 를 넣어야** PLAN §3 표의 아이콘·등급과 이어붙는다.
  - **🆕 T2 선행 과제 발견 ③: 아이콘 출처.** `sim.js` 의 특전 항목은 `{id, r, ap, u}` 로 **아이콘/이름 필드가 없다.** 지시 ⑤ 의 두 UI 는 모두 102종 전체의 아이콘이 필요한데, 다행히 **PLAN §3.1~3.4 표의 «표시 텍스트» 첫 글자가 곧 아이콘**이라 그대로 옮기면 된다(별도 승인 불필요). T2 이식 시 `index.html` 특전 테이블에 `ic` 를 102종 전부 채울 것.
  - **레이아웃 참고(지시 ⑤ 이식 지점)**: 버프 아이콘 컨테이너는 `#topbar`(`index.html:71`, `top:10px`) 와 챕터 표시(`#chapHudName`, `index.html:208`) **아래**에 새로 얹는다. 특전 미리보기 줄은 `#hudFoot`(`index.html:96`) 이 현재 `justify-content:flex-end` 라 Info 버튼만 우측 정렬돼 있으므로, **`space-between` 으로 바꾸고 왼쪽에 미리보기 컨테이너를 추가**하면 «Info 행 왼쪽 끝부터» 요구를 만족한다.

- **2026-09-01T22:0XZ / sess-2150-9551 · 워커 D** — T1 은 여전히 lock 중(`sess-2113-31444` 워커 A, 21:42:34Z 갱신 — 8분 경과, 탈취 불가). T2~T4 는 T1 대기. ROUTINE §0-5 에 따라 검증만 수행. 워커 B·C 가 이미 적은 «지시 미반영» 목록은 재확인만 하고 중복 기록하지 않는다(전부 여전히 미반영, 신규 지시 ⑥ 보스 ×8 도 미반영: `sim.js:15` 가 `bossHp:12 … bigBoss:1.5`, `sim.js:432` 에 5배수 챕터 `bd*=1.2` 잔존).
  - **게이트 재실행**: `node --check sim.js` 통과.
  - **이번 세션이 새로 한 일: «미반영 지시 5건을 전부 적용하면 밸런스가 어떻게 되는가» 를 미리 측정했다.** 레포의 `sim.js` 는 손대지 않고(락 존중) 스크래치패드 사본에 ①등급통일 ②보스킬 특전스킵 ④실드 무료충전 폐지 ⑥보스 ×8·`bigBoss` 폐지 ⑤T5 하니스 9/11 을 적용해 실험1·2·3 을 돌렸다. **전체 수치는 `docs/balance/preview-지시반영/raw.md`.** T1 회차가 아니므로 회차 번호는 쓰지 않았다.
  - **🔴 발견 1 — «실드 무료충전 폐지»가 등급 사다리를 역전시킨다.** 지시를 하나씩 쌓아 원인을 분리한 결과, 실험1 의 일반/희귀 클리어율이 폐지 직전 100%/100% → 폐지 직후 **66.3%/55.7% 로 역전**된다(다른 지시는 무관). 하니스를 관측 중앙값으로 재보정하면 **37.0% vs 23.7% 로 격차가 더 커진다** — 요동이 아니라 구조적이다.
    원인은 특전 표에 있다: 무료충전이 사라지자 «킬당 무조건 회복/실드» 가 유일한 안정 생존원이 되는데, 그 축이 **일반(`c_killHeal2`·`c_killShield3`)과 전설(`l_killHeal5`·`l_killShield10`)에만 있고 희귀 26종에는 없다**(희귀 회복은 전부 치명타/회피/반격 조건부라 트리거 확률이 한 번 더 곱해진다). 실험2 승률 상위 12 에 희귀가 **한 종도 없는** 것이 같은 사실의 다른 얼굴이다.
    → 조정 제안(신규 특전 추가 없이, 실험 근거 있는 PLAN §7 위임 범위): 희귀 조건부 회복 4종(`r_critHeal3`·`r_evadeHeal`·`r_healShield5`·`r_restHp`)의 계수·트리거 확률 상향.
  - **🟡 발견 2 — T5 승인값 9렙은 실험1 을 천장 포화시킨다.** 하니스만 6→9 로 올린 빌드에서 **일반·희귀·전설·신화가 전부 100.0%** 가 되어 사다리 측정 자체가 불가능해진다. 주인 승인 원칙은 «실험3 관측 중앙값 도달 레벨» 인데, 실험3 실측(3런)의 도달 시점 강화는 **스탯별로 크게 다르다**: 챕터6 도달 시 `dmg/hp` 8 · `aspd/crit` 1~2, 챕터8 도달 시 `dmg/hp` 9 · `aspd/crit` 2~3. 즉 «전 스탯 9렙/11렙» 은 `aspd·crit` 를 4~8렙 과대평가한 값이다.
    → 승인된 «경제 변경 시 재보정» 규칙 안에서 처리 가능하다고 보고 제안: 실험1 하니스 `{dmg:8,hp:8,aspd:2,crit:2}`, 실험2 `{dmg:9,hp:9,aspd:3,crit:3}`. 이 값에서는 포화가 풀린다(62.0/37.0/23.7/78.7/95.7). **다만 «단일 숫자 9렙» 이라는 주인 표기와 «스탯별 관측값» 해석이 갈리는 지점이라, 채택은 워커 A 판단에 맡기고 회차 로그에 사용값을 명시할 것.**
  - **🟢 발견 3 — 챕터10 벽은 지시 반영 후에도 건재하다.** 실험3 3런에서 **18·19·19회**로 목표(10~400) 3/3 성립, R02 의 7~22회보다 오히려 산포가 좁다. 채점표에서 2점이 걸린 «벽의 핵심» 은 보스 ×8 + 실드 폐지 조합에서도 무너지지 않는다.
  - **🟡 발견 4 — 후반 스파이크가 챕터15 에서 16~19 로 이동했다.** `bigBoss` 폐지로 챕터15 는 정상화(2~23회)됐으나 챕터16 이 91회, 챕터19 가 43~48회로 목표(3~10) 초과. 구간 적합 셀 **30/60 (50%)**. 챕터1 도 3~6회로 목표(1~2) 초과.
  - **채점표 환산(참고치, 전체 반영본 + 재보정 하니스): 실험1 1.5/3 · 실험2 0/2 · 실험3 ≈3.5/5 → 총 ≈5.0/10.** 즉 **지시 5건을 반영하는 것만으로는 8점 게이트에 못 미치며, 반영 직후 R03 에서 위 조정이 필요하다.**
  - **부수 정리**: 주인이 ROUTINE 에 «T5 행 완료 처리 · 승인 대기 1·2번 종결» 을 지시했는데 PROGRESS 에 반영이 안 돼 있어 이번에 표와 «주인 승인 대기» 절을 갱신했다 (현재 대기 안건 0건).

## T1 회차 기록 (워커 A)

- `R01 | 6.3/6.3점 | 기준선(초기 TUNE) 측정만 — 챕터10이 400회로도 안 뚫리는 완전 차단 확인 | sess-2113-31444 A`
  - 실험1 사다리 1.3/3.0/58.0/98.7% (단조 ✓, 3.0점) · 실험2 클리어율 0.4%(바닥 효과, 스프레드 8%p → 2.0점) · 실험3 1.3/5
  - 두 비평가 공통 지적: ①10챕터 벽 배수 ②강화 비용 지수성장(1.32^Lv) 대 골드 선형수입 → 400판에 강화 +4레벨, 경제 데드락
- `R02 | 채점 대기 | eHpG 1.35→1.11 · eDmgG 1.26→1.14 · waveHp 0.22→0.50 · waveDmg 0.10→0.26 · wallHp 1.6→2.6 · wallDmg 1.35→1.15 · wall2Hp 1.5→1.06 · wall2Dmg 1.25→1.00 · 비용성장률 1.32/1.30/1.38/1.36→1.07/1.07/1.09/1.09 · goldKill (2+c)→(0.8+0.14c) · goldClear 40c→5c | sess-2113-31444 A`
  - 실험1: 89.0 / 92.3 / 99.3 / 100% (일반<희귀<전설<신화 단조 ✓)
  - 실험2: 전체 클리어율 84.9%, 등급 내 승률 폭 최대 약 22%p (기준 25%p 미만)
  - 실험3(5회 반복): 챕터10 벽 7~22회로 성립(4/5 적합), 챕터20 도달 3/3. 구간 적합 셀 50/100
  - 남은 결함: 챕터6~9 가 1~2회로 너무 쉬움 · 챕터15(2차 벽+5배수 보스)와 챕터20 이 상한 초과

### R02 튜닝 근거 (구조 분석)

- 초기 스펙은 **수학적으로 20챕터 완주가 불가능**했다. 플레이어 전투력은 강화 레벨 L 에 대해 약 L²,
  L 은 골드 총량의 로그(비용 40*1.32^L), 골드 총량은 챕터에 선형 → 전투력은 (log c)² 로 자라는데
  적은 1.35^c 로 자란다. 그래서 400판을 4000판으로 늘려도 챕터10 이 안 뚫린다.
- 해법: 비용 성장률을 1.07 수준까지 낮춰 «비용이 거의 선형인 구간»에 두면 L ∝ 골드 → 전투력 ∝ 골드²
  로 후반 성장이 살아난다. 대신 적 챕터 성장률을 내리고, 줄어든 체감 난이도는
  **웨이브 내 증가율(waveHp/waveDmg)** 로 되돌려 한 판 안의 압박을 유지했다.
- `goldKillBase`(챕터 무관 상수분)는 **초반 전용 레버**로 쓸 수 있음을 확인 — 0 → 0.8 로 올리면
  챕터1~5 만 눈에 띄게 쉬워지고 후반은 거의 그대로다 (실험3 셀 적합 47%→57%).
- 스윕 도구 `tools/sweep.js` 신설: `TUNE_OVERRIDE` 환경변수로 sim.js 기본 동작을 바꾸지 않고
  실험3 을 N회 돌려 구간 적합률을 뽑는다. (`node tools/sweep.js '{"eHpG":1.2}' 5`)

## 주인 승인 대기

(신규 콘텐츠 제안은 여기 등재만 — 구현 금지)

> **현재 대기 중인 안건 없음.** 아래 1·2번은 2026-09-02 주인 지시(`3532d42`·`e1c3100`)로 **둘 다 종결**됐다.
> 이력 보존용으로만 남겨 두며, 종결 결과는 각 항목 머리에 적었다.

1. ~~**보스 배수(bossHp 12 / bossDmg 1.8 / bigBoss 1.5)를 PLAN §7 조정 노브에 추가할지**~~
   → **종결(`e1c3100`): 주인 확정 상수로 «보스 HP ×8 · DMG ×1.8, `bigBoss` 폐지».** 튜닝 노브가 아니다.
   제안했던 «bigBoss 1.5→1.2, bossHp 12→10» 은 폐기. 아래는 당시 근거 기록.
   §7 노브 목록에 없어 이번 라운드에서는 스펙값 그대로 뒀다. 그런데 **5의 배수 챕터(5·10·15·20)의
   보스 HP ×1.5** 가 실험3의 구조적 스파이크 원인이다: 챕터15 는 목표 3~10회인데 항상 10~40회가 나오고
   (인접한 14·16 은 2~6회), 챕터20 도 목표 10~30회 대비 40~95회로 초과한다.
   검증: 같은 설정에서 bigBoss 1.5→1.0 · bossHp 12→9 로만 바꾸면 챕터15 스파이크가 사라진다.
   제안: `bigBoss 1.5 → 1.2`, `bossHp 12 → 10`. (승인 전까지 미적용)
2. ~~**T5 — 실험1·2 하니스의 고정 강화 레벨(6렙/8렙)**~~
   → **종결(`e1c3100`): 주인 승인 — 실험1 9렙 · 실험2 11렙, 원칙은 «실험3 관측 중앙값 도달 레벨», 경제 노브 변경 시 재보정.**
   단, 워커 D 측정에서 **9렙이면 실험1 이 전 등급 100% 로 포화**한다는 문제가 나왔다 (아래 검증 로그 §2). 아래는 당시 근거 기록.
   §7 이 «챕터6·강화 6렙», «챕터8·강화 8렙» 으로 고정해 뒀는데, R02 경제에서는 실제 플레이어가
   챕터6 도달 시 강화 8~10렙 · 챕터8 도달 시 9~12렙이라 하니스가 실제보다 약한 플레이어를 본다.
   지금은 우연히 유효 구간(실험1 89~100%, 실험2 84.9%)에 들어와 있지만, 다음 튜닝에서
   천장/바닥 효과가 재발할 수 있다. 제안: 실험1 을 «챕터6·강화 9렙», 실험2 를 «챕터8·강화 11렙» 으로
   조정. (스펙 변경이므로 승인 전까지 미적용)

## 주인 할 일

- [x] ~~GitHub Pages 활성화~~ — **이미 활성화됨** (주인 확인 2026-09-02, https://kuzuni.github.io/aaaw/ HTTP 200). main push 마다 자동 배포된다.
- [ ] (T4 후) 대화형 세션에서 아티팩트 republish 요청
