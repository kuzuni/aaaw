# 루틴 작업 지시서 (병렬 워커 공용 — 유일 지시서)

> 이 문서와 `PLAN.md` 만 보고 작업한다. **PLAN.md 는 확정 스펙이라 임의 변경 금지** (밸런스 숫자 조정만 PLAN §7 범위 내 자율).
> 워커: **A :05 · B :20 · C :35 · D :50** (매시, 분 단위 슬롯 — 4개가 15분 간격으로 돈다. 전부 claude-opus-5).
> 보고·기록은 전부 **한국어**.

## ⚑ 신규 주인 지시 (위 항목이 최신)

> **⚑ 바로 아래 배너는 이력이다 (T115 · 2026-09-04 정리 — 배너를 지우지 않고 표시만 붙인다).**
> 배너가 «바로 아래 1건뿐 · 할 일은 T96 → T97 둘뿐» 이라고 가리키던 시점 뒤에 **주인이 실제로 다시 열었다** —
> 이 절의 2026-09-03 14:4X ~ 2026-09-04 03:4X 항목(T100·T102·T103·T105·T107·T108·T109·T114)이 전부 그 뒤의 주인 확정 지시이고,
> 배너가 가리키던 «바로 아래» 는 그 항목들이 위에 쌓이면서 더 이상 바로 아래가 아니다.
> **T96·T97 을 포함해 지금 작업표의 모든 행이 완료 상태다(2026-09-04T10:5XZ 확인 · 열린 작업 0건).**
> 따라서 «새로 착수하지 말 것» 은 지금 유효하지 않다 — 지금 따를 것은 **§0 세션 시작 절차 그대로**다:
> 이 절 맨 위 항목(현재 최신 = 2026-09-04 03:4X · T114)부터 읽고 `docs/PROGRESS.md` 작업표에서 미완료 행을 lock 으로 선점하되,
> **열린 작업이 없으면 §0-5 대로 마지막 완료 작업의 게이트만 재실행하고 커밋 없이 조용히 물러난다.**
> 배너 원문은 «특전 132종·등급·선택창 전제가 죽었다» 는 배경으로만 읽는다.

> **⚑⚑⚑ 2026-09-03 주인 지시 «헷갈리지 않게 나머지 todo 빼» — 지금 유효한 지시는 바로 아래 «특전 전면 폐지 → 고정 10종» 1건뿐이고, 할 일은 **T96 → T97** 둘뿐이다. 그 아래의 모든 지시·이 문서 §2 의 작업 목록·`docs/PROGRESS.md` 의 미착수 행(T92~T95)과 주인 승인 대기는 **전부 이력이거나 보류**다 — 읽어서 배경은 알되 **새로 착수하지 말 것.** 특전 체제가 통째로 바뀌므로 옛 지시의 «특전 132종·등급·선택창» 전제는 대부분 이미 죽었다. T97 이 끝나면 주인이 다시 연다.**

- **(2026-09-04 KST · 19:2X) ⚑⚑⚑ 주인 확정 — 장비 계열 재설계: 18계열 폐지 → «치명 · 체력실드 · 회피» 3세트 × 6부위. 옵션 사다리 8단(일반 1 · 희귀 2 · 영웅 3 · 전설 4 · 신화 5 · +3강 6 · +6강 7 · +9강 8). 신규 작업 T124 (T123 뒤).** 주인 원문 요지: «장비 계열을 치명 / 체력+실드 / 회피로. 예: 치명 부츠는 일반~신화9까지 있고 옵션은 공유(상위 등급이 하위 옵션 포함). 일반부터 옵션 1개, 등급마다 1개씩 증가. 세트마다 옵션 6개를 부위 전부 같게 하고, 1~6번은 부위마다 순서 셔플. 7번째부터는 공격력 +10% 로 고정.» §11.6 의 T6 초안 옵션표(18계열 × 7)는 **폐지**.

  **① 세트 옵션 (세트 안 6부위 공통 · 부위 하나 기준)**

  | 세트 | 옵션 a | b | c | d | e | f |
  |---|---|---|---|---|---|---|
  | **치명** | 치명타 확률 +5 | 치명타 피해 **+20** (⚑ 19:5X — 15 에서) | 반격률 +10 | 치명타 확률 +5 (⚑ 19:4X — «반격 시 도끼 1» 교체) | 치명타 피해 **+25** (⚑ 19:5X — «반격 데미지 +25%» 교체) | 치명타 시 50% 확률로 도끼 1 |
  | **체력실드** | 최대 체력 +8% | 최대 실드 +10% | 방어력 **+12%** (⚑ 19:3X 정정 — 6% 에서) | 최대 실드 +10% | 실드가 있을 때 가시갑옷 +12% | 최대 실드 +10% |
  | **회피** | 회피 +8 | 회피 시 50% 확률로 도끼 1 | 체력 50% 미만일 때 회피 시 30% 확률로 체력 10% 회복 | 최대 체력 +8% | 최대 실드 +10% | 최대 실드 +10% |

  **옵션 7 (신화 +6강)·옵션 8 (신화 +9강) = 공격력 +10%** — 세 세트·전 부위 공통 고정.

  **② 부위별 순서 셔플 (위임 기본값 — 라틴 방진: 부위마다 순서가 다르고, 여섯 부위를 모으면 각 옵션이 자리마다 한 번씩)**

  | 부위 | 1 일반 | 2 희귀 | 3 영웅 | 4 전설 | 5 신화 | 6 +3강 |
  |---|---|---|---|---|---|---|
  | 무기 | a | b | c | d | e | f |
  | 투구 | b | c | d | e | f | a |
  | 갑옷 | c | d | e | f | a | b |
  | 장갑 | d | e | f | a | b | c |
  | 신발 | e | f | a | b | c | d |
  | 목걸이 | f | a | b | c | d | e |

  세 세트 모두 같은 표를 쓴다(예: 치명 투구 일반 = 치피 +15 · 회피 신발 일반 = 최대 실드 +10%). 주인이 다른 순서를 원하면 이 표만 바꾼다.

  **③ 규칙·위임 기본값**
  - 종류 이름 = «세트명 + 부위명»(치명 무기 · 치명 투구 … 회피 목걸이) — 18종 그대로이되 계열이 3개로 준다. `GT.types` 순서는 **[치명, 체력실드, 회피]** 로, 색·아이콘은 세트별 3색(위임).
  - **같은 옵션이 여러 부위에 있으면 각각 따로 합산·발동**한다 — 풀셋 +3강 이상이면 «치명타 시 50% 도끼» 를 6번 굴린다(같은 스탯 옵션 둘은 그냥 합산 — 치확 +5 두 칸 = 부위당 +10). 공격력 +10% 는 **가산 합산**(풀셋 +9강 = 12개 × 10% = +120% · 곱연산 아님 — 위임).
  - 가시갑옷 옵션은 특전 가시갑옷과 가산(실드 > 0 일 때만 +12%씩). «체력 50% 미만일 때 회피 시 회복» 은 회피 성공 순간 체력 비율로 판정.
  - 등급별 공/체/실 기여(§11.5-a)·강화 19/9·슬롯·뽑기·합성·«신화 +3/+6/+9 에서 옵션 +1» 규칙은 그대로 — 바뀌는 것은 **옵션의 내용과 개수(일반도 1개)** 뿐. `GT.optCount` = 등급+1(일반 1 … 신화 5) + (+3/+6/+9 각 +1) = 최대 8.
  - **풀셋 +9강 합산(참고)**: 치명 세트 치확 **+60** · 치피 **+270** · 반격률 +60 · 치명 시 도끼 50%×6 · 공격력 +120% / 체력실드 세트 체력 +48% · 실드 +180% · 방어 +72% · 실드 있을 때 가시 +72% · 공격력 +120% / 회피 세트 회피 +48 · 체력 +48% · 실드 +120% · 회피 시 도끼 50%×6 · 공격력 +120%.

  **④ 밸런스 하니스** — 사다리 기준 빌드는 `mkBuild(rar, plus, slot, typeIdx=0)` 라 **`types[0]` = 치명 세트 풀셋**이 기준 장비가 된다(종전 대검·투구·판금·건틀릿·샌들·펜던트 혼합에서 바뀜). **재적합 없음** — `base10` 8점을 기록만 하고 벗어난 칸을 올린다(T120 규칙). 3택 참고표도 같이.

  **⑤ 이행·마감** — 두 엔진 `GT.types/typeName/GOPT` 재작성(옵션 동사는 전부 기존 엔진 축: 치확·치피·반격·도끼 소환·최대 체력/실드·방어·가시·회복·공격력) · `GT.optCount` 갱신 · PLAN §11.1 «옵션 개수» 문면 + §11.6 표 전면 교체 + §11.1 «샌들=회피…» 예시 갱신 · index.html 장비 탭·세부 팝업·합성 화면의 종류 이름/옵션 문구/색 · 게이트 `verifyOptText`(옵션 문구 ↔ 엔진)·`verifyGearEcon`·`verifyT2`·`verifyPierceScope`(도끼 소환 호출부) 갱신 + 음성 · T3 gear 스위트(뽑기·장착·세부 팝업 옵션 1~8 노출). `docs/balance/T124/result.md` 에 base10 8점 기록 + 풀셋 합산표 실측(엔진에서 찍은 값). PROGRESS T124 행 + 커밋 SHA.

- **(2026-09-04 KST · 18:5X) ⚑⚑⚑ 주인 확정 — 플레이어 기본 스탯 4종을 0 으로: 치명타 확률 20 → 0% · 반격 확률 20 → 0% · 방어력 20 → 0% · 회피 20 → 0%. 신규 작업 T123 (T121 뒤 · 작다).** 주인 원문: «치확 0% 반격 0% 방어력 0% 회피 0% 로 하기». 공 25 / 체 150 / 실 250 / 공속 1.0 / 치명타 피해 150% 는 그대로. 이 넷은 PLAN §2.3 «플레이어 기본 스탯 (주인 확정 · 노브 아님)» 표의 값이라 주인만 바꿀 수 있는 상수다 — 이 지시로 바뀐다.

  **① 이행** — 두 엔진 `TUNE.pCrit0 / pCounter0 / pDef0 / pEvade0` **20 → 0**(같은 이름·같은 값) · PLAN §2.3 표(치명타 확률 0% · 반격 확률 0% · 방어력 0 · 회피 0) · §11.7/ROUTINE 의 «기본 스탯» 언급 문면 · `verifyCombatConst` ① (PLAN 표 ↔ 두 엔진 대조) 값 갱신 · index.html 스탯 그리드 초기 표시(0 이 «특전으로 오른 값(초록)» 규칙과 어긋나지 않는지) · T3 boot 스탯 표시 단언.

  **② 귀결 (주인이 알고 계실 것 — 등재만)** — 기본 회피·반격·치명이 0 이면 **특전을 얻기 전엔 회피/반격/치명 트리거가 한 번도 안 터진다.** 특전으로만 자란다: 회피 = 일반 +8 · 희귀 +16 · 스택형 등, 반격 = +8/+16, 치확 = +8/+16/수집가/치명 스택. 즉 «회피 시 ○○»·«반격 시 창»·«치명 시 ○○» 계열은 그 스탯 특전과 같이 있어야 값을 한다 — 3택에서 조합을 만드는 재미가 되는 쪽이다. 방어력 0 은 받는 피해 감소가 특전 +8/+16/+24% 뿐이라는 뜻.

  **③ 밸런스** — `base10` 기준 플레이어(일반 1~10)가 크게 약해진다(회피 20→8 · 반격 20→8 · 치확 20→8 · 방어 20→8). **재적합 없음** — T120 규칙대로 8점을 **기록**만 하고(`docs/balance/T123/result.md` · 3택 참고표 포함) ±2%p 를 벗어난 칸을 적어 올린다. 주인이 «맞춰라» 하면 별도.

  **④ 마감** — 두 엔진 상수 · PLAN §2.3 · 게이트·T3 초록 · 8점 기록 · PROGRESS T123 행 + 커밋 SHA.

- **(2026-09-04 KST · 16:0X) ⚑⚑⚑ 주인 확정 — 특전 29종 추가 (일반 14 · 희귀 8 · 전설 7 → 풀 61종 — ⚑ 등재 직후 정정: «치피 +30» 은 신규가 아니라 수정이라 일반 신규는 14) + 기존 일반 9번 «치명타 피해 +50» 을 **+30** 으로. 신규 작업 T121 (T120 완료 뒤).** 주인 원문은 아래 표에 그대로 옮겼다. 등급 확률 60/25/15 · 3택 · 시뮬 정책(참고표 = 등급 우선 → 표 순서 · 판정 = `base10`)은 그대로.

  **① 신규 특전 (표 순서 = 등급 안 시뮬 우선순위 · 기존 것 뒤에 붙는다)**

  | 등급 | 특전 | 효과 (주인 원문) | 위임 기본값 |
  |---|---|---|---|
  | 일반 | 처치 시 회피 버프 | 킬 시 2초간 회피율 +40% (스택 아님) | 재발동 시 **시간만 갱신**(중첩 없음 · `buffs.evade` 단일 항목 교체) |
  | 일반 | 수집가·공격 | 보유 특전 하나당 공격력 +4% | 자기 자신 포함 · 특전을 얻을 때마다 재계산 · 곱연산 `×(1+0.04·n)` |
  | 일반 | 수집가·치명 | 보유 특전 하나당 치명타 확률 +2% | 가산 |
  | 일반 | 처치 시 공격력 스택 | 킬 시 33% 확률로 공격력 +1% (이 판 동안 · 스택형) | 무한 누적 · 곱연산 |
  | 일반 | 처치 시 회피 스택 | 킬 시 33% 확률로 회피율 +1% (이 판 동안 · 스택형) | 무한 누적 · 가산(상한 90 은 엔진 규칙) |
  | 일반 | 처치 시 회복 | 킬 시 33% 확률로 체력 6% 회복 | 최대 체력 기준 · 회복 증폭 적용 |
  | 일반 | 수집가·체력 | 보유 특전 하나당 최대 체력 +7% (10/100 → 10/107 — 최대치만) | 현재 체력 불변 · 곱연산 · 실드 무관 |
  | 일반 | **(기존 9번 수정)** 치명타 피해 증가 | +50 → **+30** | 기존 id 그대로 값만 |
  | 일반 | 치명 스택 | 한 대 때릴 때마다 치명확률 +1%, 치명타 뜨면 초기화 후 다시 쌓음 | **평타 적중**마다 +1 · 치명타 발생 시 0 · 소환·반격 제외 |
  | 일반 | 공격 시 공속 버프 | 공격 시 공격속도 +7% 7초 (스택형 · 중첩) | 기존 버프 엔진(무한 중첩) |
  | 일반 | 회피 시 즉사 | 회피 시 5% 확률로 공격한 적 사망 | 근접·원거리 모두 · **보스 포함**(옛 확정 «사신의 낫» 관례 — 아니면 정정) · 처치 판정이라 처치 트리거 발동 |
  | 일반 | 치명타 시 기절 | 치명타 시 10% 확률로 타겟 3초 기절 | 보스는 1/3(엔진 규칙) |
  | 일반 | 2타 화살 | **2타**에 한 번씩 랜덤 적에게 화살 1 (⚑ 17:3X 정정 — 3타에서) | «N타» = **평타 횟수**(빗나감 포함 · 반격·소환 제외) · 대상 = 현재 웨이브 생존 적 무작위 |
  | 일반 | 3타 도끼 | **3타**에 한 번씩 랜덤 적에게 도끼 1 (⚑ 17:3X 정정 — 4타에서) | 〃 |
  | 일반 | 3타 번개 | **3타**에 한 번씩 랜덤 적에게 번개 1 (⚑ 17:3X 정정) | 〃 |
  | 희귀 | 회복 시 수리 | 힐 시 같은 양만큼 실드도 수리 | 수리 증폭 적용 |
  | 희귀 | 처치 시 수리 | 킬 시 66% 확률로 실드 6% 수리 | 최대 실드 기준 |
  | 희귀 | 치명타 피해 증가 II | 크리 데미지 +60 | 가산(150 → 210) |
  | 희귀 | 회피 시 즉사 II | 회피 시 10% 확률로 적 사망 | 일반과 같은 규칙 |
  | 희귀 | 치명타 시 기절 II | 치명타 시 20% 확률로 3초 기절 | 〃 |
  | 희귀 | 2타 화살 II | **2타**마다 랜덤 적 화살 2 (⚑ 17:3X) | 〃 |
  | 희귀 | 3타 도끼 II | **3타**마다 랜덤 적 도끼 2 (⚑ 17:3X) | 〃 |
  | 희귀 | 3타 번개 II | **3타**마다 랜덤 적 번개 2 (⚑ 17:3X) | 〃 |
  | 전설 | 거인의 힘 | 공격력 +200% 대신 공속 2/3 | 공격력 ×3 · 공속 ×(2/3) · 둘 다 곱연산 |
  | 전설 | 회피 시 즉사 III | 회피 시 15% 확률로 적 사망 | 〃 |
  | 전설 | 치명타 시 기절 III | 치명타 시 30% 확률로 3초 기절 | 〃 |
  | 전설 | 2타 화살 III | **2타**마다 랜덤 적 화살 3 (⚑ 17:3X) | 〃 |
  | 전설 | 3타 도끼 III | **3타**마다 랜덤 적 도끼 3 (⚑ 17:3X) | 〃 |
  | 전설 | 3타 번개 III | **3타**마다 랜덤 적 번개 3 (⚑ 17:3X) | 〃 |
  | 전설 | 3타 창 | **3타**에 한 번씩 창 1 (⚑ 17:3X) | 일직선 관통 8 |

  같은 이름의 I/II/III(즉사·기절·N타 소환)은 **서로 다른 특전**이라 한 판에 여럿 얻을 수 있고 각각 따로 굴린다(즉사 5+10+15 는 독립 굴림 · N타 소환은 각자 카운터). «N타» 카운터는 특전마다 따로 센다.

  **② 기존 9번 «치명타 피해 +50 → +30» 과 밸런스** — 주인 괄호 원문 «이거는 밸런스 그렇게 맞추라는 말임». 9번은 **사다리 기준 플레이어(`base10`)의 특전**이라 기준 자체가 약간 약해진다. 이 세션은 «수치를 그렇게 두라» 로 읽고 **적 스탯 재적합은 넣지 않았다**(T120 규칙 — 주인이 «맞춰라» 라고 명시할 때만). T121 은 `base10` 조건 8점을 **기록**만 하고(T120 과 같이), ±2%p 밖이면 그 사실을 올린다. 주인이 재적합을 원하면 한 줄로.

  **③ 이행** — 두 엔진 `mkPerks()` 에 29종 추가(id `p_*` · 등급 · 표 순서) + 9번 상수 `PERK_CRITF_A 50 → 30` · 새 엔진 축: 보유 특전 수 기반 스탯(획득 시 재계산) · 평타 카운터(2타/3타 — ⚑ 17:3X 주인 정정 «3타 화살 시리즈는 2타로, 4타 시리즈는 3타로») · 치명 스택 · 즉사(처치 판정) · 최대 체력 증가(현재 체력 유지). PLAN §3.1 표 61종 · §3.0 «금지축» 중 **«최대 체력 증가» 는 주인이 이 지시로 해제**(수집가·체력) — 문면 갱신. 게이트 `verifyPerkOrder`(61 · 29/16/16 · 키·수치·두 엔진) + 음성 · `verifySummonChain`(N타 소환·즉사가 처치 트리거를 낳는 연쇄 B 재계산 — 넘으면 승인 대기) · `verifyNumClean`(33/66·+7%·2/3 등재) · `verifyOptText` · T3 battle(카드·인포 61종 노출). **T120(T114 값 복구·base10 자) 완료 뒤에 잡는다** — 같은 자(尺)를 쓰기 때문.

  **⑤ ⚑ 주인 추가 정정 (16:2X) — 기존 일반 4종 하향 + 일반 1종·희귀 4종 신규 → 풀 66종 (일반 30 · 희귀 20 · 전설 16).** 주인 원문 그대로:

  | 등급 | 특전 | 변경/신규 | 엔진 |
  |---|---|---|---|
  | 일반 (기존 8번) | 치명타 확률 증가 | +10 → **+8** | `PERK_CRITR_A 10 → 8` |
  | 일반 (기존 6번) | 반격률 증가 | +10 → **+8** | `PERK_COUNTER_A 10 → 8` |
  | 일반 (기존 2번) | 공격력 증가 | +20% → **+15%** (⚑ 16:3X 재정정 — 처음 +10% 에서) | `PERK_ATK_M 1.20 → 1.15` |
  | 일반 (기존 3번) | 회피율 증가 | +10 → **+8** | `PERK_EVADE_A 10 → 8` |
  | 일반 (신규) | 5타 회복 | **5타**마다 체력 **6%** 회복 (⚑ 18:0X 재정정 — 3타 5% 에서) | 평타 카운터 · 최대 체력 기준 · 회복 증폭 적용 · 자기 카운터 |
  | 희귀 (신규) | 치명타 확률 증가 II | 치명타 확률 **+16** | 가산 |
  | 희귀 (신규) | 반격률 증가 II | 반격률 **+16** | 가산 |
  | 희귀 (신규) | 공격력 증가 II | 공격력 **+30%** (⚑ 16:3X 재정정 — 처음 +20% 에서) | 곱연산 |
  | 희귀 (신규) | 회피율 증가 II | 회피율 **+16** | 가산 |

  | 일반 (신규 · ⚑ 16:5X) | 회피 시 스턴 | 회피 시 **30%** 확률로 공격한 적 3초 스턴 | 근접·원거리 모두(원거리는 쏜 적) · 보스 1/3 · `applyStun` |

  | 일반 (신규 · ⚑ 17:0X) | 반격 치명 | 반격 시 그 반격이 치명타일 확률 **+20%** | 반격 타격에만 치확 +20 가산 |
  | 일반 (신규 · ⚑ 17:0X) | 반격 강화 | 반격 데미지 **+30%** | 반격 타격 데미지 곱연산 ×1.30 |
  | 희귀 (신규 · ⚑ 17:0X) | 처치 시 대시 | 적 처치 시 **다음 적까지 바로 대시** (대시 데미지 없음 · «빨리 가서 공격하는 느낌») · **웨이브 마지막 적 → 다음 웨이브 첫 적으로는 대시하지 않는다** | 같은 웨이브 안에 살아 있는 다음 적이 있을 때만 · 이동 속도 ×5(`DASH_MUL` 두 엔진 같은 상수 · 위임)로 사거리(74px)까지 · 데미지·무적 없음 · 원거리 적 화살은 그대로 맞는다 |
  | 희귀 (신규 · ⚑ 17:0X) | 버서커 | 적 처치 시 스택 +1 · **한 번 때릴 때마다 스택 1 소모하며 그 공격 +100%** (8스택이라고 +800% 를 한 번에 쓰는 게 아니라 8번의 공격이 각각 +100%) | 무한 누적 · **평타에만** 소모·적용(반격·소환은 소모 안 함 — 위임) · 곱연산 ×2 |
  | 희귀 (신규 · ⚑ 17:0X) | 반격 치명 II | 반격 시 치명타일 확률 **+40%** | 가산 |
  | 희귀 (신규 · ⚑ 17:0X) | 반격 강화 II | 반격 데미지 **+60%** | 곱연산 ×1.60 (I 과 같이 얻으면 ×1.30 × ×1.60) |

  | 일반 (신규 · ⚑ 17:2X) | 처치 시 확정 치명 | 적 처치 시 **다음 공격은 치명타 확률 +100%** (스택 아님) · **광전사(치확 0 고정) 상태에서도 그 한 방은 0% → 100%** 가 된다 — 주인 명시 | 플래그 1개(켜져 있으면 다시 처치해도 그대로) · **다음 평타**에서 소모 · 광전사의 «치확 0 고정» 보다 우선(그 공격만) · 치명타 트리거(스턴 등)도 정상 발동 |

  | 일반 (신규 · ⚑ 17:4X) | 관통 베기 | 공격 시 **33%** 확률로 **바로 뒤 적**도 같은 데미지를 받는다 | «바로 뒤» = 타겟 다음으로 가까운 살아 있는 적(같은 웨이브) · 데미지 = 그 평타가 타겟에게 준 값 그대로(치명타면 치명 값) · 뒤 적의 회피 10% 는 따로 굴림 · 뒤 적 처치도 «처치» 판정 |
  | 희귀 (신규 · ⚑ 17:4X) | 관통 베기 II | 공격 시 **66%** 확률로 바로 뒤 적도 데미지 | 〃 |
  | 전설 (신규 · ⚑ 17:4X) | 관통 베기 III | 공격 시 **항상** 바로 뒤 적도 데미지 | 〃 |
  | — | (셋의 관계) | **전부 따로 굴린다** — 셋 다 있으면 셋 다 터져 **바로 뒤 적이 한 공격에 최대 3번** 데미지를 받는다 (주인 명시) | 평타에만 · 각 발동은 뒤 적 1마리에게 1회씩(뒤의 뒤로 번지지 않음) · 소환·반격은 대상 아님 |

  | 일반 (기존 4번 수정 · ⚑ 17:5X) | 회피 시 화살 | 회피 시 화살 1 (100%) → **33% 확률로** 화살 1 | T108 의 «100%» 는 이 두 칸에서 되돌아간다 — `PERK_SUMMON_CH` 단일 상수를 **특전별 확률**로 쪼갠다(반격 시 창은 100% 그대로 · 주인 언급 없음) |
  | 일반 (기존 5번 수정 · ⚑ 17:5X) | 피격 시 도끼 | 피격 시 도끼 1 (100%) → **33% 확률로** 도끼 1 | 〃 |
  | 희귀 (신규 · ⚑ 17:5X) | 회피 시 화살 II | 회피 시 **66%** 확률로 화살 1 | |
  | 희귀 (신규 · ⚑ 17:5X) | 피격 시 도끼 II | 피격 시 **66%** 확률로 도끼 1 | |
  | 전설 (신규 · ⚑ 17:5X) | 치명 시 창 | 치명타 시 **66%** 확률로 창 1 | 치명타 트리거 · 광전사면 안 뜸(확정 치명 한 방은 예외) |
  | 전설 (신규 · ⚑ 17:5X) | 치명 시 번개 | 치명타 시 **66%** 확률로 보이는 적 전부에게 번개 | 〃 |
  | 전설 (신규 · ⚑ 17:5X) | 회피 시 화살 III | 회피 시 화살 1 (100%) | I/II/III 전부 따로 굴림 |
  | 전설 (신규 · ⚑ 17:5X) | 피격 시 도끼 III | 피격 시 도끼 1 (100%) | 〃 |
  | 전설 (신규 · ⚑ 17:5X) | 회피 시 창 | 회피 시 **33%** 확률로 창 1 | |
  | 전설 (신규 · ⚑ 17:5X) | 피격 시 창 | 피격 시 **33%** 확률로 창 1 | |

  | 희귀 (신규 · ⚑ 18:0X) | 회피 시 회복 II | 회피 시 **15%** 확률로 체력 6% 회복 (⚑ 18:1X 정정 — 20% 에서) | 일반 «회피 시 회복»(10%)과 별개 특전 · 따로 굴림 · 회복 증폭 적용 |
  | 희귀 (신규 · ⚑ 18:0X) | 회피 시 수리 | 회피 시 **15%** 확률로 실드 6% 수리 (⚑ 18:1X 정정) | 최대 실드 기준 · 수리 증폭 적용 |
  | 전설 (신규 · ⚑ 18:0X) | 회피 시 수리 II | 회피 시 **25%** 확률로 실드 6% 수리 (⚑ 18:1X 정정) | 〃 · 희귀와 따로 굴림 |

  | 일반 (기존 1번 수정 · ⚑ 18:1X) | 회피 시 회복 | 회피 시 10% → **8%** 확률로 최대 체력 6% 회복 | `PERK_EVHEAL_CH 0.10 → 0.08` (`base10` 기준 특전 — 재적합 없음·기록만) |

  | 일반 (기존 10번 수정 · ⚑ 18:2X) | 방어력 증가 | +10% → **+8%** | `PERK_DEF_M 1.10 → 1.08` (`base10` 기준 특전 — 재적합 없음·기록만) |
  | 희귀 (신규 · ⚑ 18:2X) | 방어력 증가 II | 방어력 **+16%** | 곱연산(상한 80 은 엔진 규칙) |
  | 전설 (신규 · ⚑ 18:2X) | 방어력 증가 III | 방어력 **+24%** | 〃 |
  | 일반 (신규 · ⚑ 18:2X) | 피해 무시 | 피격 시 **20%** 확률로 그 피격 데미지 무시 | 회피 판정 «뒤»·방어막 «뒤» 에 굴림 · 무시된 공격은 «피격» 이 아니다(피격 트리거·가시갑옷 발동 없음 · 위임) |
  | 일반 (신규 · ⚑ 18:2X) | 실드 없을 때 공격력 | 실드가 0 인 동안 공격력 **+50%** | 상시 조건부 · 곱연산 · 실드가 다시 생기면 꺼짐 |
  | 일반 (신규 · ⚑ 18:2X) | 실드 없을 때 공속 | 실드가 0 인 동안 공격속도 **+30%** | 〃 |
  | 전설 (신규 · ⚑ 18:2X) | 실드 방벽 | **실드가 있으면** 피격 시 **50%** 확률로 피격 데미지 무시 | 실드 > 0 조건 · 일반 «피해 무시» 와 따로 굴림 |
  | 전설 (신규 · ⚑ 18:2X) | 실드 반사 | **실드가 있으면** 피격 시 **50%** 확률로 그 데미지를 공격한 적에게 반사 | 반사량 = 그 공격이 준 피해(방어 후 · 실드로 받은 양 포함) × 100% · 근접·원거리(쏜 적) 모두 · 피해는 그대로 받는다(무시와 별개) · 가시갑옷과 중복 가능(둘 다 되갚음) · 위임 |
  | 일반 (신규 · ⚑ 18:2X) | 피격 시 방어막 | 피격 시 **10%** 확률로 방어막 1장 (**스택형** — 장수 무제한) | 방어막 = 적 공격 1번을 완전히 막는 1회용 막(엔진 `ward` 축) · 판정 순서 **회피 → 방어막 → 피해 무시 → 피해** · 방어막으로 막은 공격은 «피격» 아님(트리거·가시갑옷 없음) |
  | 희귀 (신규 · ⚑ 18:2X) | 피격 시 방어막 II | 피격 시 **20%** 확률로 방어막 1장 | 〃 · I/II/III 따로 굴림(같이 있으면 한 피격에 최대 3장) |
  | 전설 (신규 · ⚑ 18:2X) | 피격 시 방어막 III | 피격 시 **30%** 확률로 방어막 1장 | 〃 |

  | 희귀 (신규 · ⚑ 18:4X) | 치명 시 창 | 치명타 시 **33%** 확률로 창 1개 | 전설 «치명 시 창»(66%)과 별개 · 따로 굴림 |

  기존 4종은 **사다리 기준 플레이어(`base10`)의 특전**이라 기준이 더 약해진다 — ②와 같은 처리: **재적합 없음**, `base10` 8점은 기록만(주인이 «맞춰라» 하면 별도). `verifyPerkOrder` 의 상수 단언(PERK_ATK_M·EVADE_A·COUNTER_A·CRITR_A·CRITF_A) 전부 새 값으로. 위 ①③④ 의 «61종·29/16/16» 은 **99종 · 39/32/28** 으로 읽는다(18:4X 희귀 «치명 시 33% 창» 포함 · 18:2X 방어 II/III·피해 무시·실드 없을 때 2종·실드 방벽/반사·방어막 I/II/III 포함 · 18:0X 회피 시 회복 II·회피 시 수리 I/II 포함 · 16:5X «회피 시 스턴» · 17:0X 반격 2종·희귀 4종 · 17:2X «처치 시 확정 치명» · 17:4X «관통 베기» I/II/III · 17:5X 회피/피격/치명 소환 8종 포함). ⚑ 기존 4·5번이 100% → 33% 로 내려가므로 `base10` 기준 플레이어가 또 약해진다 — 마찬가지로 재적합 없음·기록만. ⚑ 반격 치명/강화는 «반격 타격» 을 엔진에서 구분해 굴린다(`doCounter` 경로) — 새 축 2개(반격 치확 가산 · 반격 배율)와 대시·버서커 스택은 두 엔진 같은 이름.

  **⑥ ⚑ 주인 지시 (16:4X) — 용어는 «기절» 이 아니라 «스턴» 으로 쓴다 («스턴으로 해야 이해하기 쉬울 것 같다»).** 유저에게 보이는 글자 전부 — 특전 이름·설명(«치명타 시 스턴» · «치명타 시 30% 확률로 타겟 3초 스턴»), 카드·인포 팝업·미리보기 툴팁, 장비 옵션 문구에 «기절» 이 있으면 그것도, PLAN §3.0/§3.1 문면(«기절 지속은 3초 또는 6초만» → «스턴 지속은…»). 엔진 함수·변수 이름(`applyStun`·`e.stun`·`STUN_BOSS_MUL`)은 이미 stun 이라 그대로. 게이트 `verifyOptText`·`verifyPerkOrder` 의 문구 대조와 `verifyNumClean` 등재분의 «기절» 도 «스턴» 으로. 한 파일에 두 표기가 섞이지 않게 정적 단언 1개(«유저 노출 문자열에 ‹기절› 0건»)를 T121 게이트에 넣는다.

  **④ 마감** — 두 엔진 61종 · PLAN §3 · 게이트·T3 초록 · `base10` 8점 기록 + 3택 참고표 + 특전별 픽률·B 값 → `docs/balance/T121/result.md` · PROGRESS T121 행 + 커밋 SHA.

- **(2026-09-04 KST · 15:3X) ⚑⚑⚑ 주인 확정 — 밸런스 기준은 «전(前) 기준» 그대로다. T119 가 3택·새 특전 조건으로 적 스탯을 다시 맞춘 것은 주인이 시키지 않은 일이다 → 되돌린다. 신규 작업 T120.** 주인 원문: «맞추라 한 적이 없는데 왜 맞췄노. 전에 그 기준대로 밸런스 되야 하는데.» (재적합 조항은 대화형 세션이 T117~T119 등재문에 위임 기본값으로 붙인 것이다 — 주인 지시가 아니었다.)

  **① 밸런스 기준 (정본 · 이것만 판정 자다)** — 사다리 8점(노템 5 · 일반 15 · 희귀+슬롯5 28 · 영웅+슬롯10 40 · 전설+슬롯15 70 · 신화+슬롯25 150 · 신화9강+슬롯50 380 · 신화9강+슬롯100 420 · 각 클리어율 10% ±2%p)을 **«기준 플레이어» = 기존 일반 10종을 §3.1 옛 순서(회복 → 공격력 → 회피율 → 화살 → 도끼 → 반격률 → 창 → 치확 → 치피 → 방어력)대로 «되는 만큼» 자동 획득 · 3택 없음 · 신규 22종·등급 없음** 조건으로 잰다 — T114 가 8/8 을 낸 바로 그 조건이다. 3택과 희귀·전설 특전은 **기준 위에 얹히는 유저 보너스**라 사다리 측정에 들어오지 않는다. 시뮬 하니스에 이 조건을 **`perkMode:'base10'`** 로 고정하고 `verifyScoreCriteria` ⓓ·실험1·`regress.js` 가 전부 이 모드로 잰다(실제 게임 동작은 3택 그대로 — 자(尺)만 고정하는 것).

  **② 되돌림** — 적 스탯(`eBaseHp/eBaseDmg`·`eHpSeg/eDmgSeg`)을 **T114 완료 커밋 `f0ae9e0` 의 값으로 복구**(두 엔진 · PLAN §6/§11.7 표도 그 값으로). T119 가 올린 기저 ×1.3322·구간률 재절단은 폐기. 벽·챕터 상한·적 수·원거리 곡선·장비·경험치는 T114 이후 안 바뀌었으니 그대로.

  **③ 측정은 기록용만 — 재적합 없음 (⚑ 15:4X 주인 확인 «예전 기준 그대로 가져와서 쓰니까 재측정은 안 한다»)** — 복구한 뒤 ①의 기준 조건으로 8점을 한 번 찍어 `result.md` 에 **기록만** 한다(시드 3벌 × 1,000판). T118(도끼 50%·화살 30%)이 기준 10종의 4·5번에 걸려 T114 수치와 조금 다를 수 있지만 **±2%p 를 벗어나도 적 스탯을 손대지 않는다** — 그 사실만 적어 올리고 재적합 여부는 주인이 정한다. `verifyScoreCriteria` ⓓ 의 밴드(±5%p)는 «곡선이 통째로 어긋났나» 만 보는 자리라 그대로 두되, 그것마저 빨개지면 게이트를 손보지 말고 주인 승인 대기에 올린다. 3택·새 특전 조건의 클리어율도 **참고표**로만 같이 찍는다(판정 아님).

  **④ ⚑ 상시 규칙 (지금부터)** — 특전·3택·등급 등 «플레이어 쪽» 이 바뀌어도 루틴은 적 스탯을 **임의로 재적합하지 않는다**. 재적합은 주인이 «맞춰라» 라고 한 경우에만, 그리고 언제나 ①의 기준 조건으로만 한다. 등재문에 재적합 조항을 붙이는 것도 주인 말이 있을 때만(대화형 세션 포함).

  **⑤ 마감** — 두 엔진 T114 값 복구 · PLAN §6/§11.7 · 하니스 `perkMode:'base10'` + 게이트(`verifyScoreCriteria`·`verifySeedProtocol`·`regress.js`) 갱신 · 8점 기록(기준 조건 · 판정은 하되 재적합 없음) + 참고표(3택 조건) → `docs/balance/T120/result.md` · PROGRESS T120 행 + 커밋 SHA.

- **(2026-09-04 KST · 13:0X) ⚑⚑⚑ 주인 확정 — 특전 풀 확장 + 등급 부활: 기존 10종 = 일반 · 신규 22종(일반 5 · 희귀 8 · 전설 9) · 출현 확률 일반 **60%** / 희귀 **25%** / 전설 **15%**(⚑ 13:2X 주인 정정 — 처음 50/30/20 에서). 신규 작업 T119 — T117(3택)·T118(계수) 과 합쳐 사다리 8점은 한 번만 잰다.** 주인 원문 요지: «일단 기존 10개는 일반 특전으로 넣기. 특전 출현 확률 일반 50% 희귀 30% 전설 20%(→ 13:2X 정정 «일반 60% 희귀 25% 전설 15% ㄱㄱ»). 전설/희귀/일반 특전 아래 목록으로 추가.» 예전 133종(`docs/perk-redesign.md`)은 **폐지 확정**(주인 12:3X «예전 꺼는 폐지») — 이 목록이 §3.1 의 새 정본이다.

  **① 새 특전 풀 — 32종 (등급 · 표 순서 = 등급 안 시뮬 우선순위)**

  | 등급 | # | 특전 | 효과 | 비고 |
  |---|---|---|---|---|
  | 일반 | 1~10 | (기존 10종 그대로) | 회피 시 회복 · 공격력 +20% · 회피율 +10 · 회피 시 화살 1 · 피격 시 도끼 1 · 반격률 +10 · 반격 시 창 1 · 치확 +10 · 치피 +50 · 방어력 +10% | 수치 불변 |
  | 일반 | 11 | 처치 시 창 | 처치 시 **33%** 확률로 창 1개 | |
  | 일반 | 12 | 처치 시 번개 | 처치 시 **33%** 확률로 **보이는 적 전부**에게 번개 1회씩 | |
  | 일반 | 13 | 처치 시 화살 | 처치 시 **33%** 확률로 화살 **3개** | |
  | 일반 | 14 | 처치 시 도끼 | 처치 시 **33%** 확률로 도끼 **2개** | |
  | 일반 | 15 | 가시갑옷 | 가시갑옷 **+100%** | 정의는 ② |
  | 희귀 | 1 | 풀피 적 강타 | 체력이 가득 찬 적 공격 시 데미지 **+100%** | |
  | 희귀 | 2 | 수리 증폭 | 실드 수리량 **+100%** | |
  | 희귀 | 3 | 회복 증폭 | 체력 회복량 **+100%** | |
  | 희귀 | 4 | 가시갑옷 | 가시갑옷 **+200%** | |
  | 희귀 | 5 | 처치 시 창 | 처치 시 **66%** 확률로 창 1개 | |
  | 희귀 | 6 | 처치 시 번개 | 처치 시 **66%** 확률로 보이는 적 전부에게 번개 | |
  | 희귀 | 7 | 처치 시 화살 | 처치 시 **66%** 확률로 화살 3개 | |
  | 희귀 | 8 | 처치 시 도끼 | 처치 시 **66%** 확률로 도끼 2개 | |
  | 전설 | 1 | 처치 시 창 | 처치 시 창 1개 (100%) | |
  | 전설 | 2 | 처치 시 번개 | 처치 시 보이는 적 전부에게 번개 (100%) | |
  | 전설 | 3 | 오버킬 회복 | 처치 시 **남은 데미지(초과분)만큼** 체력 회복 | 100% |
  | 전설 | 4 | 처치 시 화살 | 처치 시 화살 3개 (100%) | |
  | 전설 | 5 | 처치 시 도끼 | 처치 시 도끼 2개 (100%) | |
  | 전설 | 6 | 광전사 | 공격력 **300%** 가 되는 대신 치명타 확률 **0%** | |
  | 전설 | 7 | 귀족의 눈 | **다음 특전부터 최소 희귀 이상**만 나온다 | |
  | 전설 | 8 | 창의 화신 | 내가 쏘는 **모든 화살이 창**으로 바뀐다 | |
  | 전설 | 9 | 가시갑옷 | 가시갑옷 **+300%** | |

  **② 규칙·위임 기본값 (다르면 한 줄로 정정)**
  - **가시갑옷** (주인 정의): «근접 적이 나를 때리면 그 적이 때린 데미지만큼 자기가 맞는다. 100% = 1배.» 위임: 대상은 **근접 공격만**(원거리 화살 제외) · 기준 데미지 = 그 공격이 **나에게 실제로 준 피해**(방어 적용 후, 실드로 받은 양 포함 · 회피했으면 0) · 반사 = 그 값 × 배율 · 반사는 «공격» 이 아니다(치명타·소환 트리거 없음) · **가산 중첩**(+100 +200 +300 = 최대 +600%) · 보스에게도 적용.
  - **처치 시 트리거**는 «내가 처치했을 때» — 평타·반격·소환 적중으로 죽인 경우 전부(소환 적중 = 공격 판정 §3.0). 그래서 **처치 시 소환이 다시 처치를 낳는 연쇄**가 생긴다 — `verifySummonChain` 의 임계 B ≤ 0.8 을 이 풀로 다시 계산해 **넘으면 값을 임의로 깎지 말고 주인 승인 대기에 등재**(주인이 수치를 정했다).
  - **보이는 적 전부** = 현재 교전 중인 웨이브의 살아 있는 적 전부(적은 고정 배치라 «화면에 보이는» = 그 웨이브). 번개 1회씩(각 공격력 75%).
  - **오버킬 회복** = 처치한 타격의 초과분 100% 만큼 체력 회복(최대치 초과분 버림 · 실드 안 채움 · 회복 증폭의 영향을 받는다 — «힐» 이므로).
  - **광전사** = 공격력 ×3(장비 합산 뒤 곱연산) · 치명타 확률을 0 으로 **고정**(치확 +10 특전·버프·장비 옵션이 있어도 0 · 치명타 트리거 특전은 발동 안 함).
  - **귀족의 눈** = 얻은 다음 레벨업부터 카드 3장의 등급 굴림에서 일반을 뺀다(희귀 62.5% · 전설 37.5% 로 재정규화 — 25:15). 희귀·전설이 다 떨어지면 일반으로 되돌아간다. (⚑ 13:2X 정정으로 재정규화는 희귀 25/40 = **62.5%** · 전설 15/40 = **37.5%**)
  - **창의 화신** = `fireArrows(n)` 이 `fireSpear(n)` 이 된다(화살 3개 → 창 3개 · 회피 시 화살 → 창 · 장비 화살 옵션도 포함). 창 데미지·8관통 그대로.
  - 소환 계수는 T118 값(도끼 50% · 화살 30% · 창 100% · 번개 75%).
  - **출현 굴림**: 카드 3장 **각각** 등급을 **60/25/15** 로 굴리고 그 등급의 «아직 안 얻은» 특전 중 무작위 1개. 같은 카드 3장 안에서 중복 금지. 그 등급이 비었으면 남은 등급들로 재정규화해 다시 굴린다. 악마 «즉시 3택 1» 도 같은 굴림.
  - **카드 표시**: 등급 테두리 색 부활(위임: 일반 회색 · 희귀 파랑 · 전설 금색) + 등급 이름. 인포 팝업·미리보기 줄에도 등급색.
  - **시뮬 측정 정책 (재현성)**: 가상 플레이어는 제시 3장 중 **등급이 높은 것 우선, 같은 등급이면 위 표 순서 우선**(실제 유저가 좋은 것을 고르는 것의 근사). 굴림은 판 난수(시드 하니스에서 결정적).
  - 한 런 획득 수는 «되는 만큼»(6~9) 그대로 · 한 특전은 한 번만 · 보스 무특전 · 시간 정지 규약 그대로.
  - 종전 규칙과 충돌하는 곳: «확률 10% 단위(5% 예외)» 는 주인이 33/66 을 직접 정했으므로 이 풀에는 적용하지 않는다(`verifyNumClean` 에 주인 확정으로 등재). «한 종이 과녁을 ±5%p 넘게 못 바꾼다»(17:0X 파워 밴드)도 전설 6번·9번 등에 성립하지 않는다 — **남는 강함은 적 난이도로 흡수**(T82 방식 · 사다리 재적합).

  **③ 이행·순서** — 두 엔진 `mkPerks()` 를 «등급 · id · 효과 · 표 순서» 로 재구성(`PERK_GRADE_RATE = [60,25,15]` · 신규 id 는 `p_*`) · `offerPerks` 는 T117 의 3택 위에 등급 굴림을 얹는다 → **T117 → T118 → T119 를 한 워커가 순서대로 잡고(lock 셋) 사다리 8점은 T119 끝에서 한 번만** 잰다(T117·T118 은 사다리를 안 잰다). PLAN §3.0(공통 규칙 — 등급·출현 확률·가시갑옷 정의·연쇄 규칙)·§3.1(32종 표) 재작성(옛 문장 취소선 금지). 게이트: `verifyPerkOrder`(풀 32 · 등급별 개수 15/8/9 · 확률 · 시뮬 정책 · 두 엔진 동일 · 각 특전의 엔진 키·수치 대조) + 음성 · `verifySummonChain`(B 재계산) · `verifyNumClean`(33/66 등재) · `verifyOptText` · `verifyT2`(카드 등급색·인포) · T3 battle(카드 3장·등급 표시·선택). 사다리 8점 재측정·재적합 → `docs/balance/T119/result.md`(8점 O/X · 구간률 · **특전별 픽률·기여도 진단**(시뮬 정책 하에서 어떤 특전이 몇 % 뽑히나) · B 값).

  **④ 마감** — 두 엔진 32종 · PLAN §3 · 게이트·T3 초록 · 사다리 8/8 · result.md · PROGRESS T119 행 + 커밋 SHA.

- **(2026-09-04 KST · 12:4X) ⚑⚑⚑ 주인 확정 — 소환 데미지 계수 교체: 화살 50% → 30% · 도끼 30% → 50%. 신규 작업 T118 — T117 의 사다리 재측정 «앞» 에 합류(T108·T109 가 T107 에 붙은 것과 같은 방식).** 주인 원문: «화살 → 공격력 30%, 도끼 → 공격력 50% 로 일단 바꾸셈.»

  **① 무엇이 바뀌나** — `R_ARROW` **0.50 → 0.30** · `R_AXE` **0.30 → 0.50**(두 엔진 같은 이름·같은 값 · sim.js/index.html 의 `const R_AXE=…, R_ARROW=…` 한 줄). 검기 50% · 번개 75% · 창 100%/8관통은 그대로. «주인 확정 상수 — 튜닝 금지» 지위는 그대로이고 값만 주인이 바꾼 것이다.
  - 영향 범위: 특전 4번 «회피 시 화살»(약해짐) · 5번 «피격 시 도끼»(세짐) · 장비 계열 옵션 중 화살 소환(장궁 계열)·도끼 소환(전투도끼 계열) 전부 — 호출부는 계수를 상수에서 읽으므로 코드 변경은 그 한 줄뿐이다.
  - 문면: PLAN §3.0 «소환 데미지 = 주인 확정 상수 … 도끼 30% · 화살 50%» → **도끼 50% · 화살 30%** · §3.1 표 4·5 행 괄호 · §11.6 옵션표의 «(공격력의 N%)» 표기 · `docs/perk-redesign.md` 머리의 공통 규칙 줄은 이력이라 손대지 않는다. index.html 특전 카드·장비 옵션 표시 문구에 «공격력의 N%» 가 박혀 있으면 상수에서 읽게 하거나 같이 고친다.
  - 게이트: `verifyCombatConst`·`verifyPierceScope`·`verifyOptText`·`verifyPerkOrder` 중 R_* 를 대조하는 단언을 새 값으로(음성 검사 포함). `verifyNumClean` 등재분에 «30%/50%» 문구가 있으면 정리.

  **② 순서·밸런스** — 한 줄 상수지만 사다리 8점을 흔든다(초반 칸: 5번 도끼는 «되는 만큼» 6개 안에 들어 세지고, 4번 화살은 약해진다). **T117(3택 복구)의 사다리 재측정은 T118 을 먼저 반영한 트리에서 한 번만** 낸다 — T117 워커가 T118 을 같이 잡는 것을 권장. 다른 워커가 T118 만 먼저 끝내도 되고, 그때는 사다리를 재지 않고 T117 에 인계한다.

  **③ 마감** — 두 엔진 상수 · PLAN §3.0/§3.1/§11.6 · 표시 문구 · 게이트 초록 · PROGRESS T118 행 + 커밋 SHA. 사다리 8점 O/X 는 T117 `result.md` 가 낸다.

- **(2026-09-04 KST · 12:3X) ⚑⚑⚑ 주인 확정 — 레벨업 특전을 «3개 중 1개 선택» 으로 되돌린다 (밸런스가 잡혔으므로). 특전은 주인이 새로 추가할 예정 — 지금은 «선택창 복구» 만. 신규 작업 T117.** 주인 원문: «이제 밸런스 다 맞췄으니까 특전 3개 중 하나 선택하는 거로 돌려줘. 특전은 내가 새로 더 추가하겠다.» T96 이 폐지한 «선택창» 이 돌아온다 — 다만 **등급·등장 확률·무료 새로고침·전지의 눈은 돌아오지 않는다**(풀에 등급이 없다). 풀은 당분간 §3.1 의 10종 그대로이고, 주인이 추가 특전을 확정하면 별도 지시로 풀만 늘린다(ROUTINE 17:0X «특전 확장 방향» 규칙 — 파워 밴드 ±5%p · 한 런 획득 수는 «되는 만큼» 그대로).

  **① 규칙 (위임 규약 기본값 — 다르면 한 줄로 정정)**
  - 레벨업마다 **아직 안 얻은 특전 중 3개를 무작위로** 뽑아 카드 3장으로 보여주고 **하나를 고른다**. 남은 것이 3개 미만이면 남은 만큼만, 0개면 팝업 없이 레벨업만. 한 특전은 한 번만(중복 금지 그대로).
  - 팝업이 떠 있는 동안 시간 정지(§2.4 불변 규약 · `openOverlay`) · 📘 보유 특전 버튼(T89) 복구 · 새로고침 없음 · 카드에 등급 테두리 없음(등급 폐지 상태).
  - **악마**: «다음 순번 앞당김» → **«즉시 3택 1»**(같은 팝업, 비용 = 최대 체력 30% 차감 그대로). 남은 특전이 없으면 거래 불성립(비용 없음) 그대로.
  - 보스 처치 레벨업은 특전 없음 · «되는 만큼»(T107 표 6/7/8/9) · `PERK_PICKS` 의미(«한 런 획득 상한»)는 그대로.
  - **시뮬 측정 정책 (재현성)**: 가상 플레이어는 **제시된 3장 중 §3.1 표 순서가 가장 앞선 것**을 고른다(ROUTINE 17:0X «특전 확장 방향» ② 에서 미리 정해 둔 정책). 제시 3장은 챕터 시드가 아니라 **판 난수**(전투 스트림)로 뽑는다 — 시드 하니스에서는 결정적.
  - 두 엔진 같은 함수(`offerPerks(taken) → 3장` · `pickPerk`), 같은 상수 `PERK_OFFER = 3`.

  **② 밸런스** — 고정 순서가 «제시 3장 중 표 순서 우선» 으로 바뀌므로 평균 획득 순서가 달라진다(초반에 6번 이후가 섞여 들어올 수 있다). **T114 사다리 8점을 같은 절차로 재측정하고 어긋나면 재적합**한다(시드 3벌 × 1,000판 · ±2%p). 주인이 특전을 추가하면 그때 한 번 더 잰다 — 이번 재측정은 «선택창 복구» 자체의 영향만 기록한다.

  **③ 게이트·문면** — `verifyPerkOrder`: «순서 획득» 단언을 «제시 3 · 중복 0 · 남은 것만 · 시뮬 정책(표 순서 우선) · 두 엔진 동일 · 악마 = 3택» 으로 교체 + 음성 검사. `verifyT2` ㊵(보유 특전 버튼)·㊴(시간 정지) 부활. T3 `battle.js`: 레벨업 → 카드 3장 노출 → 선택 → 팝업 닫힘 · 시간 정지 · 보유 특전 버튼 복귀. PLAN §2.4 «레벨업 → 특전을 순서대로 하나 자동 획득» 과 §3.0 «획득 방식» 을 «3택 1» 로 재작성(옛 문장 취소선 금지 — 게이트가 문장을 읽는다) · 악마 조항 갱신.

  **④ 순서** — `index.html` 을 T116(UI 루프)과 같이 건드리므로 **T116 회차 lock 이 없는 틈에 잡고**, 잡은 동안 T116 은 대기(같은 파일 · lock 하나 규약). 마감: 두 엔진 · PLAN · 게이트·T3 초록 · 사다리 8점 재측정 결과(`docs/balance/T117/result.md`) · PROGRESS T117 행 + 커밋 SHA.

- **(2026-09-04 KST · 11:5X) ⚑⚑⚑ 주인 지시 — UI 를 레퍼런스 스크린샷(`docs/ref/*.jpg`)의 형태에 맞추는 루프. 비평가 2인이 각각 10점 만점에 8점 이상 줄 때까지 회차를 돈다. 신규 작업 T116 (회차형 · 한 회차 = 한 lock).** 주인 원문: «이미지 원본 레퍼런스 UI 형태 그거랑 맞춰서 루프 돌려야 할 듯, 비평가 2명 껴서 8점 될 때까지». 종전 T1 밸런스 루프(§2 T1 «비평가 2인 · 8점 게이트»)와 같은 운영 방식이고, 대상만 «밸런스 수치» 에서 «화면 형태» 로 바뀐다.

  **① 대상·범위** — `index.html` 의 **화면(UI·그림)만** 손댄다. `sim.js`·`TUNE`·장비/특전/챕터 수치·게임 규칙·시뮬 정책은 **한 글자도 안 건드린다**(밸런스 영향 0 — `verifyT2` TUNE 대조·정적 게이트 18종·T3 4스위트가 매 회차 초록이어야 한다). 외부 라이브러리·외부 이미지 금지(PLAN §0 — Canvas 직접 그리기·Google Fonts Jua 유지). 레퍼런스는 **형태·배치·비율·색감·분위기를 따라 그리는 것**이지 이미지를 복사해 넣는 것이 아니다(레퍼런스 jpg 를 게임에 싣지 않는다).

  **② 레퍼런스 ↔ 화면 대응 (위임 기본값)**

  | 레퍼런스 (`docs/ref/`) | 게임 화면 | 배점 |
  |---|---|---|
  | `메인로비.jpg` | 로비(상단 바·챕터 카드·START·하단 5탭) | 2 |
  | `메인 게임화면.jpg` · `메인 게임화면_적발견.jpg` | 인게임 HUD·전투 장면(상단 HUD·진행 바·하단 스탯 패널·캐릭터/적/배경 그림체·보스 경고) | 2 |
  | `캐릭터 장비.jpg` · `장비 세부팝업.jpg` | 장비 탭 + 장비 세부 팝업 | 2 |
  | `상점 (1).jpg` · `상점 (2).jpg` | 상점 탭(다이아·뽑기) | 1 |
  | `장비 합성 업글창.jpg` | 대장간/합성 화면 | 1 |
  | `perks.jpg` · `perks 뭐뭐 있는지.jpg` | 특전 인포 팝업·획득 연출·미리보기 줄 | 1 |
  | (전체) | 공통 스타일 — 폰트·색·버튼·테두리·아이콘의 카툰 일관성, 모바일 폭(390×844)에서 깨짐 없음 | 1 |

  기준은 **«같은 게임의 다른 스킨처럼 보이는가»** — 픽셀 동일이 아니라 레이아웃·비율·구성 요소·색감·그림체다. 레퍼런스에 있고 게임에 없는 요소(예: 상점의 상품 카드 구성)는 **형태만** 재현하고 기능은 잠금(🔒) 그대로 둔다 — 새 기능·새 시스템은 금지축(§1).

  **③ 한 회차 절차 (T1 §2 절차의 UI 판)**
  1. **스크린샷 채취** — Playwright(T3 하니스와 같은 크로미움 · `tools/t3/shots.js` 신설)로 390×844 뷰포트에서 위 7묶음의 화면을 PNG 로 찍는다. **저장은 스크래치패드·`T3_OUT`(레포 밖)** — PNG 커밋 금지(§1). 팝업·전투 장면은 하니스가 상태를 만들어 찍는다(장비 3종 장착 · 전투 중 적 발견 · 특전 3개 획득 등 — 재현 가능하게 스크립트에 고정).
  2. **비평가 2명을 Task(Agent) 도구로 각각 독립 실행**(서로의 결과를 보여주지 말 것 · 매 회차 새 에이전트). 각 비평가에게 주는 것: ②의 채점표 · 레퍼런스 jpg 경로 · 이번 회차 스크린샷 경로(둘 다 이미지를 Read 로 직접 본다). 받는 것: **항목별 점수 + 총점(10점) + 감점 사유 + «가장 효과 큰 수정 2개»**(구체적으로 — 어느 화면의 무엇을 어떻게).
  3. 두 비평가 **모두 총점 ≥ 8** 이면 합격 → T116 완료. 아니면 두 비평가의 제안을 합쳐 `index.html` 화면을 고친다(감점 큰 항목부터).
  4. 게이트 전수(`tools/verify*.js` 18종) + T3 4스위트 초록 확인 → 커밋·push. **회차 기록**: `docs/ui/round-NN/` 에 두 비평가의 채점표·사유·제안을 **md 로**(이미지 없이) 남기고, PROGRESS «UI 회차 로그» 절에 한 줄(`U01 | 5.5 / 6.0 | 로비 상단 바·하단 5탭 재구성 | sess-… A`).
  5. 다음 회차는 다음 워커(또는 같은 세션)가 lock 을 새로 잡는다. 세션 90분 규약 그대로.

  **④ 안전장치** — ⓐ 3회차 연속 두 비평가 총점 합이 안 오르면 멈추지 말고 «주인 승인 대기» 에 비평가가 지목한 구조적 걸림돌(예: Canvas 손그림으로는 못 내는 원화 수준)을 등재하고 계속 돈다. ⓑ 비평가가 «기능 추가» 를 제안하면 형태만 채택하고 기능은 잠금(§1 새 콘텐츠 금지). ⓒ 회차마다 `verifyT2` 의 TUNE·특전·챕터 대조가 초록인지로 «밸런스 무수정» 을 증명한다. ⓓ 한 회차 안에서 스크린샷을 «고친 뒤 다시 찍어» 비평가에게 재채점을 요구하지 않는다 — 재채점은 다음 회차다(자 조작 방지).

  **⑥ ⚑ 주인 보강 (12:0X · 12:1X 재확인) — «레이아웃이 똑같은 게 중요하다. 비율·비례 같은 것. 껍데기가 똑같을 필요는 없고, UI 배치·비율·비례 — 레이아웃이 중요. 그거대로 비평해야 함.»** 따라서 **채점은 배치·비율·비례만으로 한다 — 10점 전부 레이아웃이다.** 색·폰트·아이콘·그림체(껍데기)는 **점수에 넣지 않는다**(비평가는 «참고 메모» 로만 적을 수 있고 그 메모로 감점하지 않는다 · ②의 «공통 스타일 1점» 은 «공통 레이아웃 1점 — 프레임 여백·바 두께·버튼 규격·팝업 규격의 일관성 + 모바일 폭에서 깨짐 없음» 으로 바꾼다). 레이아웃·비례가 뜻하는 것:
  - **요소의 위치와 크기를 프레임 대비 %** 로 본다 — 상단 바 높이, 챕터 카드의 세로 위치·폭·높이, START 버튼 폭·높이·y, 하단 탭 바 높이, 인게임 하단 패널 높이·스탯 그리드 칸 비율, 팝업 폭·모서리·여백, HP/EXP 바 두께 등. 레퍼런스 jpg 에서 픽셀로 재서 %로 환산한 값과 게임 스크린샷의 같은 요소 %를 **표로 나란히** 적는다.
  - 허용 오차 **±3%p(프레임 기준)** 안이면 그 요소는 «맞음». 묶음의 레이아웃 점수 = 맞은 요소 비율로 준다(비평가 주관이 아니라 표에서 나온다).
  - 요소의 **순서·개수·정렬**(예: 하단 탭 5개 등간격, 스탯 그리드 4×2, 장비 슬롯 6칸 배치 형태)도 레이아웃이다.
  - 그래서 ③-1 스크린샷 하니스(`tools/t3/shots.js`)는 PNG 와 함께 **주요 요소의 `getBoundingClientRect` 를 프레임 % 로 환산한 json** 을 같이 낸다(비평가가 자로 재지 않아도 되게). 레퍼런스 쪽 %는 **첫 회차 워커가 jpg 를 보고 한 번 재서 `docs/ui/ref-layout.md` 에 표로 등재**하고(화면별 요소 10~15개), 이후 회차는 그 표를 자로 쓴다. 표가 틀렸다고 보이면 고치되 회차 로그에 적는다.
  - 색·폰트·아이콘 그림체는 **채점 밖**이다 — 레이아웃이 맞으면 그림체가 달라도 8점이 나오고, 레이아웃이 안 맞으면 그림체가 좋아도 안 나온다. 워커도 회차에서 **배치·크기·비례부터** 고치고, 그림체는 레이아웃이 8점을 넘긴 뒤 여력이 있을 때만 손댄다.

  **⑤ 마감 조건** — 두 비평가 각 ≥ 8 인 회차의 채점표(`docs/ui/round-NN/`) · PROGRESS T116 행에 회차 수·최종 점수·커밋 SHA · 게이트·T3 초록. 아티팩트 republish 는 루틴 금지(§1) — «준비 완료» 표시까지.

- **(2026-09-04 KST · 03:4X) ⚑⚑⚑ 주인 확정 — 원거리 마릿수 곡선: 챕터 4 까지 0 → 5 부터 1마리씩 증가 → 30% 에 닿으면 «30% ± 2» 로 묶는다 (자리는 챕터 시드 고정 그대로). 신규 작업 T114 — 곡선 반영 + 사다리 8점 재측정까지.** (⚑ 등재 시 T113 으로 적혔다가 루틴이 그 번호를 PROGRESS 정리에 먼저 써서 **T114 로 정정**) 주인 원문: «챕터 4까지는 원거리 아예 없고 5부터 원거리 1마리씩 추가하고 30퍼 비율 될 때까지 한 마리씩 늘린 다음에 30퍼에서 플러스 마이너스 2로 묶으까?» — 배경: T105 고정 뒤 원거리 «마릿수» 가 챕터 시드 운이라 420챕터 중 186챕터가 앞 챕터보다 줄고(최소 2·최대 26), 챕터 4(8마리)가 6(3마리)보다 어려운 역전이 전 구간에 있었다. 주인은 «완만한 오르내림은 두되 되돌아가는 구간은 없앤다» 로 결정했다.

  **① 마릿수 공식 (위임 규약 기본값 — 다르면 한 줄로 정정)**
  - 굴림 대상 `E(c)` = 일반 적 − 웨이브 첫 마리 5 = `N(c) − 6` (종전과 같은 모집단 · 첫 마리 비원거리 규칙 유지). ⚑ 주인이 «30%» 를 «일반 적 전체 기준» 으로 뜻하셨다면 `E = N − 1` 로 바꾸면 된다(상한 챕터에서 13 → 15마리).
  - 기준값 `B(c) = round(0.30 · E(c))` — 챕터 1~5 = 3 · 15 = 6 · 28 = 10 · 38 이후 = **13**(E 44).
  - 흔들림 `j(c) ∈ {−2,−1,0,+1,+2}` 를 **챕터 시드로** 고른다(이벤트 셔플 «뒤», 원거리 자리 굴림 «앞» 에서 소비 — 이벤트 순서 골든 지문 불변).
  - **원거리 수 `R(c) = c ≤ 4 ? 0 : min(c − 4, max(0, B(c) + j(c)))`** — 챕터 5 = 1, 6 = 2, 7 = 3 … 램프가 «30% ± 2» 를 따라잡는 지점(챕터 8~11 근방)부터 `B ± 2`. 램프 구간은 정확히 +1/챕터 단조이고, 그 뒤는 «앞 챕터보다 최대 4마리 적을 수 있음»(주인이 ±2 로 승인).
  - **자리**: 그 마릿수만큼을 굴림 대상 중에서 챕터 시드로 뽑는다(같은 챕터 = 같은 자리, T105 그대로). 종전 «각 적 40% 독립 굴림» 은 폐기 — 비율 상수 `RANGED_RATE 0.40` 은 **0.30** 으로 바뀌고 «마릿수 결정 → 자리 추첨» 두 단계가 된다.
  - 두 엔진 같은 이름 한 벌: `RANGED_CURVE = {zeroUntil:4, rate:0.30, jitter:2}` + `chapterRangedCount(c)` (`chapterLayout` 안에서만 호출).

  **② 게이트** — `verifyChapterFixed` 의 원거리 단언(T105 ⓔ~ⓘ)을 이 공식으로 갈아끼운다: ⓔ 같은 챕터 두 번 = 같은 자리 ⓕ 두 엔진 전수 동일 ⓖ 첫 마리 비원거리 ⓗ **챕터 1~4 원거리 0 · 5~램프 끝 정확히 +1 · 그 뒤 `|R − B| ≤ 2`** ⓘ 각 적 독립 굴림(`<0.4`·`<0.3`) 잔재 0. 음성 검사 갱신. PLAN §2.3 «원거리 적(40% 확률, 웨이브 첫 마리 제외)» 문면을 이 공식으로 재작성(옛 숫자 취소선 금지 — 게이트가 문장에서 숫자를 읽는 습관), §2.4 레이아웃 항목에 «원거리 마릿수 곡선» 추가. T3 battle «같은 챕터 두 번 → 원거리 자리 동일» 은 그대로 + «챕터 1~4 원거리 0» 실측 1항목.

  **③ 밸런스 · 순서** — 원거리가 통째로 준다(비율 40 → 30% · 초반 4챕터 0) → 초반 칸부터 쉬워진다. **T107 2단계와 같은 절차(칸별 국소 배수 a → 구간률 재절단)로 사다리 8점을 다시 잡아 8/8** 까지가 T114 이다. 측정 조건은 T107 그대로(«되는 만큼» 특전 · 시드 3벌 × 1,000판 · ±2%p). T110 의 실험3 1~40 진단도 같은 표로 한 번 더 붙일 것(판정 아님).

  **④ 마감 조건** — 두 엔진 공식·상수 · PLAN §2.3/§2.4 · `verifyChapterFixed` 갱신 + 음성 검사 · `verifyT2`·T3 초록 · 사다리 8점 8/8(`verifyScoreCriteria` ⓓ) · `docs/balance/T114/result.md`(챕터 1~12 와 대표 챕터의 원거리 수 표 · «앞 챕터보다 줄어드는 챕터 수» 전수 집계 · 8점 O/X · 구간률 · 실험3 1~40 진단) · PROGRESS T114 행 + 커밋 SHA.

- **(2026-09-03 KST · 18:5X) ⚑⚑⚑ 주인 확정 — 특전 순서 교체: 2·3번 ↔ 6·7번. 신규 작업 T109 — T108 과 같이 T107 의 사다리 재측정 «앞» 에 넣는다.** 주인 원문: «2번 3번을 6번 7번과 위치를 교체». 효과·수치는 한 글자도 안 바뀌고 **순서만** 바뀐다(T104 와 같은 종류의 작업).

  **① 새 순서 (이 표가 §3.1 정본이 된다)**

  | # | 특전 | 효과 | 종전 순번 |
  |---|---|---|---|
  | 1 | 회피 시 회복 | 회피 시 10% 확률로 최대 체력 6% 회복 | 1 |
  | 2 | 공격력 증가 | 공격력 +20% | 6 |
  | 3 | 회피율 증가 | 회피율 +10 | 7 |
  | 4 | 회피 시 화살 | 회피 시 화살 1개 (⚑ T108 로 100%) | 4 |
  | 5 | 피격 시 도끼 | 피격 시 도끼 1개 (⚑ T108 로 100%) | 5 |
  | 6 | 반격률 증가 | 반격률 +10 | 2 |
  | 7 | 반격 시 창 | 반격 시 창 1개 (⚑ T108 로 100%) | 3 |
  | 8 | 치명타 확률 증가 | 치명타 확률 +10 | 8 |
  | 9 | 치명타 피해 증가 | 치명타 피해 +50 | 9 |
  | 10 | 방어력 증가 | 방어력 +10% | 10 |

  **② 이행** — 두 엔진 `mkPerks()` 배열 순서 교체(id·효과·상수 불변) · PLAN §3.1 표 재정렬 + «T104 순서 … 폐기» 문면에 이번 교체 추가 · `verifyPerkOrder` 의 WANT 순서·«델타 단언 순번» 갱신 · 이 문서 17:0X 이전 항목 ①표는 이력이므로 손대지 않는다. 악마 «다음 순번 앞당김» · 보스 레벨업 무특전 · `PERK_PICKS 10` 은 그대로.

  **③ 순서·밸런스** — T107 의 «되는 만큼» 표에서 초반 챕터(1~5 = 6개)가 얻는 것이 «회피 시 회복 → 반격률 → 창 → 화살 → 도끼 → 공격력» 에서 **«회피 시 회복 → 공격력 → 회피율 → 화살 → 도끼 → 반격률»** 로 바뀐다 — 초반 칸이 세지고, 반격·창은 6·7번이라 챕터 15(7개)부터 들어온다. 따라서 **T107 의 사다리 재측정은 T108·T109 가 모두 합류한 트리에서 한 번만** 낸다. T107 워커(현재 워커 B · lock 보유)가 T108·T109 를 먼저 반영하고 곡선을 넣은 뒤 재는 것을 권장(둘 다 작은 작업이라 lock 을 같이 잡는다). 다른 워커가 T109 만 먼저 끝내도 되고, 그때는 사다리를 재지 않고 T107 에 인계한다.

  **④ 마감 조건** — 두 엔진 순서 · PLAN §3.1 · `verifyPerkOrder` 초록(음성 검사 포함) · T3 boot «특전 10종 로드» 초록 · PROGRESS T109 행 + 커밋 SHA.

- **(2026-09-03 KST · 18:4X) ⚑⚑⚑ 주인 확정 — 소환 특전 3종(반격 시 창 · 회피 시 화살 · 피격 시 도끼) 발동 확률 50% → 100%. 신규 작업 T108 — T107 의 사다리 재측정 «앞» 에 넣는다.** 주인 원문: «저거 창 화살 도끼 다 확률 100퍼로 해줘야함».

  **① 무엇이 바뀌나** — §3.1 의 3번 «반격 시 50% 확률로 창 1개» · 4번 «회피 시 50% 확률로 화살 1개» · 5번 «피격 시 50% 확률로 도끼 1개» 가 전부 **«… 시 창/화살/도끼 1개»(확정 발동)** 이 된다. 엔진 상수 `PERK_SUMMON_CH` **0.50 → 1.00**(두 엔진 같은 이름·같은 값). 소환 데미지(도끼 30%·화살 50%·창 100%·관통 8마리)·«1개 = 1발·쿨다운 없음»·트리거 자리는 그대로. 소환 연쇄 B = 0 은 트리거가 피격/회피/반격이라 100% 여도 그대로 성립(소환 적중이 새 소환을 낳지 않는다).
  - 표시 텍스트: `index.html` 특전 카드·인포 문구와 `sim.js` `d:` 문구에서 «50% 확률로» 를 걷어낸다(«반격 시 창 1개» 식). `PLAN.md` §3.1 표 3·4·5 행과 «엔진 상수» 줄(`PERK_SUMMON_CH 0.50`)·§3.0 «3·4·5 의 소환은…» 문면 갱신.
  - 게이트: `verifyPerkOrder`(상수·문구 대조 — `PERK_SUMMON_CH` 단언을 1.00 으로) · `verifyOptText`/`verifyNumClean`(«50%» 등재분이 있으면 정리) · T3 `battle.js` 에 «반격 1회 → 창 1개 확정» 실측이 있으면 갱신.

  **② 순서 (밸런스 영향 큼 — 초반 칸부터 플레이어가 세진다)** — 한 줄짜리 상수 변경이지만 사다리 8점을 통째로 흔든다. **T107(적 수 곡선)의 사다리 재측정은 T108 이 합류한 트리에서** 낸다: T107 워커가 lock 을 잡을 때 **T108 을 먼저 반영하고(둘 다 lock)** 곡선을 넣은 뒤 한 번만 재측정하는 것이 가장 싸다. 다른 워커가 T108 만 먼저 끝내도 된다 — 그때는 T108 은 사다리를 재지 않고 «T107 이 잰다» 로 인계한다(중간 실측을 두 번 하지 않는다).

  **③ 마감 조건** — 두 엔진 `PERK_SUMMON_CH 1.00` · 표시 문구 3종 · PLAN §3.0/§3.1 · 게이트 초록 · PROGRESS T108 행 + 커밋 SHA. 사다리 8점 O/X 는 T107 `result.md` 가 낸다.

- **(2026-09-03 KST · 17:3X) ⚑⚑⚑ 주인 확정 — 챕터별 적 수 곡선 (1~5챕터 17마리 → 6챕터부터 +1 → 상한 50) + 특전은 «되는 만큼». 신규 작업 T107.** (⚑ 등재 시 T106 으로 적혔다가 루틴이 그 번호를 «T103 재측정» 에 먼저 써서 **T107 로 정정** — 18:2X) 주인 원문: «그 적수를 1챕터~5까지 17마리, 6챕터부터 1 올라갈 때마다 1개씩 추가, 그리고 적 개수 50개 되면 더이상 안 올리기(처음 76 이라 했다가 «50마리 넘으면 피로하다» 로 50 확정). 특전은 걍 되는 만큼으로 하셈 — 3개 할 정도였으면 순서대로 3개 정도만. 특전 사실 꼭 10까지 안 떠도 되긴 할듯.» **T100 의 «전 챕터 76 · 완주 = 특전 10개» 고정 구성은 이 지시로 폐기**된다.

  **① 적 수 공식 (보스 포함 · 위임 기본값으로 «17» 은 보스를 포함한 총 수)**
  `N(c) = c ≤ 5 ? 17 : min(50, 12 + c)` — 챕터 6 = 18, 7 = 19, … **챕터 38 부터 50 고정**. 일반 적 = N − 1(보스 1은 항상 마지막). `LAYOUT_MAXENEMY` 80 → **50**(주인 상한).
  - 웨이브는 **5개 유지**, 일반 적을 **최대한 균등 분배·나머지는 앞 웨이브부터**(16 → 4·3·3·3·3, 49 → 10·10·10·10·9) — 위임 기본값. 쉼터 2·악마 1·천사 1·배치 순서 시드 셔플은 그대로(T105 의 원거리 고정도 이 위에 그대로 얹힌다 — `ranged[]` 길이가 웨이브 크기를 따라간다).
  - 적 «수» 는 여전히 난이도 노브가 아니다 — 이 공식은 주인 확정 상수이고, 난이도는 적 스탯(구간률·벽)으로만 만든다.

  **② 특전 = 그 챕터의 경험치가 주는 만큼** — 처치 3 · 보스 9 · 쉼터 26×2 = 52 · `expNeed 5·Lv+1` 은 전부 **불변**. 레벨업마다 §3.1 순서대로 하나씩, 악마는 다음 순번 앞당김, 보스 처치 레벨업은 특전 없음 — 규칙도 그대로. «완주 = 정확히 10개» 항등식만 사라진다. `PERK_PICKS = 10`·풀 10종은 그대로 두되, **10번(방어력 +10%)은 50 상한에서는 어느 챕터에서도 안 나온다**(49×3 + 52 = 199 < 9레벨 234) — 주인이 «꼭 10까지 안 떠도 된다» 로 승인했으므로 그대로 둔다(빼거나 순서를 바꾸는 것은 주인 몫).

  | 챕터 | 적 수 | 경험치(보스 전) | 특전 (레벨업 + 악마 1) |
  |---|---|---|---|
  | 1~5 | 17 | 16×3+52 = 100 | **6** (1~6번: 회피 시 회복 → … → 공격력) |
  | 15 | 27 | 130 | **7** |
  | 28 | 40 | 169 | **8** |
  | 38 이후 | 50 | 199 | **9** (치명타 피해까지 · 최대) |

  **③ 이행 (두 엔진 동시)** — `chapterLayout(c)` 의 웨이브 크기를 공식으로(두 엔진 같은 함수·같은 상수 `ENEMY_CURVE = {early:17, from:6, cap:50}` 급 이름으로 한 곳에). «10개 다 얻으면 그만» 판정·`PERK_PICKS` 는 손대지 않는다. PLAN §2.4 «웨이브 5개 · 웨이브당 15마리» 와 «고정 챕터 구성 … 적 76마리 … 완주 = 특전 10개» 문면을 이 공식으로 **재작성**(게이트 `verifyCombatConst` ① 이 그 문장에서 숫자를 읽으므로 옛 숫자를 취소선으로 남기지 말 것 — T100 이 같은 이유로 적어 둔 규칙). §7.3 «적 수는 노브가 아니다» 문면은 «공식 고정» 으로 갱신.
  - 게이트 `verifyChapterFixed` 재작성: ⓐ 챕터 1~상한 전수 `N(c)` 공식 일치(두 엔진) ⓑ 단조 비감소·상한 50 ⓒ 웨이브 5·쉼터 2·악마 1·천사 1·보스 마지막 ⓓ **특전 실측** = 위 표(1~5 = 6 · 15 = 7 · 28 = 8 · 38+ = 9, 한 판 완주 시) — «10개» 단언은 폐기 ⓔ T105 의 원거리 단언은 그대로. 음성 검사 갱신. `verifyT2` 레이아웃 전수 대조·T3 «챕터 1 클리어» 실측은 새 마릿수로 통과해야 한다(`fx.js` 의 적 총수 단언 갱신).

  **④ 밸런스 영향 · 순서** — 초반 4칸(노템 5 · 일반 15 · 희귀 28 · 영웅 40)은 특전이 6~9개라 플레이어가 약해지고, 후반 4칸도 적이 76 → 50 이라 한 판이 짧아진다(회복·소환 누적이 줄어 클리어율이 움직인다). **T103·T105·T106 은 이미 완료**(18:1XZ · 사다리 8/8)이므로 T107 은 그 위에서 ① 적 수 곡선을 넣고 ② **T106 과 같은 절차(칸별 국소 배수 a 실측 → 구간률 재절단)로 사다리 8점을 다시 잡아 8/8 로 되돌리는 것까지** 한 작업이다 — 측정 조건 «특전 10종 전부 획득» 은 «그 챕터가 주는 만큼(위 표)» 으로 바뀐다. `chapterLayout` 은 T105 의 `ranged[]` 시드 굴림이 들어 있으니 웨이브 크기가 바뀌어도 «이벤트 셔플 뒤 원거리 굴림» 순서를 유지할 것(원거리 패턴은 마릿수가 바뀌므로 어차피 새로 굳는다 — T106 이 겪은 «분산이 사라져 드러난 오차» 가 한 번 더 온다). 초반 골드 수입이 처치 수에 비례해 1~20챕터에서 1/4~1/2 로 줄어드니 `docs/balance/T107/result.md` 에 실험3(진행 곡선) 챕터 1~40 구간의 시도 횟수를 진단 표로 붙일 것(판정 아님 · 슬롯 정체가 보이면 보고만).

  **⑤ 마감 조건** — 두 엔진 반영 · PLAN §2.4·§7.3 문면 · `verifyChapterFixed` 재작성 + 음성 검사 · `verifyT2`·T3 4스위트 초록 · **사다리 8점 재측정 8/8**(`verifyScoreCriteria` ⓓ 초록 · 시드 3벌 × 1,000판) · `docs/balance/T107/result.md`(8점 O/X · 구간률 · 대표 챕터 표 · 챕터별 적 수 5점(1·6·15·28·38)·특전 개수 실측 · 실험3 1~40 진단) · PROGRESS T107 행 + 커밋 SHA.

- **(2026-09-03 KST · 17:0X) ⚑⚑⚑ 주인 확정 — 챕터 배치 전부 «챕터별 고정». 같은 챕터 = 같은 원거리 자리. 신규 작업 T105.** 주인 원문: «챕터별로 고정으로 해야함. 같은 챕터 같은 원거리 자리고. 쨌든 쉼터, 악마, 천사, 적들 배치가 전부 챕터별로 고정이어야함.»

  **① 지금 상태 (무엇이 이미 고정이고 무엇이 아닌가)** — 쉼터 2·악마 1·천사 1 의 **배치 순서**, 웨이브 5×15·보스 1, 적 간격은 `chapterLayout(c)` 가 챕터 시드로 정해 **이미 챕터별 고정**이다. 그런데 **적의 원거리 여부만 매판 새로 굴린다** — 두 엔진 모두 노드를 만들 때 `Math.random()<0.4 && j>0` 으로 굴리므로(sim.js `runChapter` 웨이브 생성부 · index.html `startChapter` 웨이브 생성부) 같은 챕터라도 판마다 원거리 자리·마릿수가 다르다. **이것을 챕터 시드로 옮기는 것이 T105 의 전부다.**

  **② 이행 (T105 · 두 엔진 동시 · 위임 규약 기본값)**
  - 원거리 여부를 `chapterLayout(c)` 안에서 **챕터 시드 RNG 로 굴려 웨이브 노드에 싣는다**: 웨이브 노드 `{t:'wave', size, ranged:[bool×size]}`. 확률 **40% · 웨이브 첫 마리 제외**는 그대로(주인이 바꾼 것은 «자리의 고정» 이지 비율이 아니다). 보스는 원거리 아님 그대로.
  - **시드 스트림 소비 순서**: 이벤트 셔플을 먼저 끝낸 «뒤» 에 원거리를 굴린다 — 그래야 지금 챕터마다 정해져 있는 쉼터·악마·천사 순서가 한 챕터도 안 바뀐다(`verifyT2` 레이아웃 전수 대조가 그대로 초록이어야 한다).
  - 두 엔진의 웨이브 생성부는 `Math.random()` 대신 노드의 `ranged[j]` 를 읽는다. 스킨·첫 공격 타이머·전투 난수(치명·회피·소환)는 **연출·전투 난수라 그대로 매판 굴린다**(주인 «배치» 범위 밖 — 위임).
  - 게이트 `tools/verifyChapterFixed.js` 확장(음성 검사 포함): ⓔ 같은 챕터를 두 번 생성하면 `ranged` 패턴이 완전히 같다 ⓕ sim.js ↔ index.html 의 `ranged` 패턴이 챕터 1~상한 전수 동일 ⓖ 웨이브 첫 마리는 전 챕터 원거리 아님 ⓗ 전체 원거리 비율이 40% 근방(전 챕터 합산 36~44%) ⓘ 두 엔진 웨이브 생성부에 `Math.random()<0.4` 가 남아 있지 않다(정적). T3 `battle.js` 에 «같은 챕터 두 번 시작 → 원거리 자리 동일» 실측 1항목.
  - PLAN §2.3 «원거리 적(40% 확률, 웨이브 첫 마리는 제외)» 문면에 «⚑ 챕터별 고정 — 챕터 시드로 굴린다(T105)» 를 붙이고, §2.4 «레이아웃은 챕터 번호 시드 기반 결정적 생성» 항목에 원거리 자리도 포함됨을 명시.

  **③ T103 과의 순서 (밸런스 영향 있음 — 방향이 아니라 «분산»)** — 원거리를 고정하면 판 간 흔들림은 줄지만 **챕터마다 원거리 마릿수가 다른 값으로 굳는다**(예: 챕터 28 은 35마리, 29 는 25마리). 그래서 사다리 과녁 챕터(5·15·28·40·70·150·380·420)의 클리어율은 **그 챕터의 고정 패턴 위에서** 재야 한다. 규칙: **T105 는 T103 과 다른 파일(`chapterLayout`·게이트)만 건드리므로 다른 워커가 병렬 선점 가능**하고, **T103 의 최종 실측(`result.md`·8점 O/X)은 반드시 T105 가 합류한 트리에서 낸다.** T103 워커가 T105 를 자기가 먼저 하고 재는 것도 된다(그때는 T105 lock 도 같이 잡는다). T105 없이 낸 T103 실측은 참고치로 격하하고 result.md 에 «T105 반영 전» 을 표시한다. ⚠ 고정 패턴 탓에 **인접 챕터 난이도 역전**(T28 이 적수 변동으로 겪었던 것)이 원거리 축에서 되살아날 수 있다 — T103 은 대표 챕터 표에 «원거리 마릿수» 열을 하나 더 두고, 역전이 보이면 result.md 에 적어 올린다(임의로 비율·패턴을 손보지 말 것).

  **④ 마감 조건** — 두 엔진 반영 · `verifyChapterFixed` 확장 5항목 초록 + 음성 검사 · `verifyT2` 레이아웃 전수 대조 초록(이벤트 순서 불변 증명) · T3 battle 실측 1항목 · PLAN §2.3·§2.4 문면 · PROGRESS T105 행에 «전 챕터 원거리 마릿수 최소/최대/평균» 기록 · 커밋 SHA.

- **(2026-09-03 KST · 14:4X) ⚑⚑⚑ 주인 정정 — 난이도 사다리 8점의 맨 아랫줄: 신화9강+슬롯50 = 380 (그대로) · 신화9강+슬롯100 = ~~600~~ → 420. T103 은 이 값으로 한다.** 주인 원문: «신화9강+슬롯50 -> 380, 신화9강+슬롯100 -> 420 정도로 하자 챕터 밸런스». 바로 아래 지시의 ②표 8번 칸·③ 챕터 600·④ «최종 벽 600»·⑤·⑥ 대표 챕터 목록은 **이 항목으로 갱신**된다(나머지 ①·②의 1~7번 칸·④의 10·15·90 벽 규정은 그대로).

  **① 사다리 8점 (갱신본 · 전부 «특전은 순서 획득 · 클리어율 10% ±2%p»)** — ⚑ 17:3X T107 로 특전 개수는 «그 챕터가 주는 만큼»(노템 5 = 6개 · 일반 15 = 7 · 희귀 28 = 8 · 영웅 40 = 9 · 전설 70 이후 = 9)이 됐다.

  | # | 빌드 | 목표 챕터 |
  |---|---|---|
  | 1~6 | 노템 5 · 일반 풀셋 15 · 희귀+슬롯5 28 · 영웅+슬롯10 40 · 전설+슬롯15 70 · 신화+슬롯25 150 | (불변) |
  | 7 | 신화 +9강 풀셋 · 전 부위 슬롯 50 | **380** (불변) |
  | 8 | 신화 +9강 풀셋 · 전 부위 슬롯 100 | **420** (종전 600 폐기) |

  «정도» 라 하셨지만 과녁은 **420 정확히**, 허용 오차는 ±2%p 그대로다(자를 흔들지 않는다).

  **② 왜 420 인가 — 아래 지시 ⑤ 가 짚은 «380→600 평평» 문제의 주인 답(⑤ⓑ)이다.** 슬롯 50→100 의 파워 ×1.78 을 40챕터에 담으면 챕터당 `1.78^(1/40) = 1.0145` 로 **150→380 구간(1.0146)과 정확히 이어진다.** 즉 후반 곡선은 챕터 150 부터 420 까지 한 률로 간다 — 380~600 의 «난이도가 안 자라는 220챕터» 가 사라진다.

  **③ 챕터 수·최종 벽 위치 (위임 규약 기본값 — 주인이 챕터 수를 따로 말하지 않았다. 다르면 한 줄로 정정)**: T102(500)·T104(600) 선례대로 **«맨 아랫줄 과녁 = 챕터 수 = 최종 벽 위치»** 를 따라 `TUNE.maxChapter` **600 → 420** · `TUNE.wall4At` **600 → 420**. 이유 — 420 위는 슬롯 150(만렙, 슬롯 100 대비 파워 ×1.40)으로도 1.0145/챕터 률에서 **≈445 가 한계**라 445~600 은 아무도 못 깨는 죽은 콘텐츠가 된다. 주인이 600 을 유지하고 싶으시면 «챕터는 600 유지» 한 줄이면 된다 — 그때는 420→600 을 «슬롯 150·과금 장기 영역» 으로 두고 률만 잇는다.

  **④ 최종 벽 배수 재산정 규칙 (위임 기본값)** — 380→420 구간률은 **150→380 률을 그대로 잇고**, 과녁 420 이 10% 가 되게 하는 **잔차를 `wall4Hp/Dmg` 로** 둔다(= 마지막 챕터가 벽). 잔차가 1 아래로 나오면 벽을 끄고(1.0) 률만으로 잇는다. 90 벽은 2.0/1.5 를 유지하되 70→150 예산 안에서(그 구간률이 1 아래로 내려가면 배수를 낮춘다). 10 벽 1.5/1.25 · 15 벽 비활성은 그대로.

  **⑤ 착수 전 진단 실측 (대화형 세션이 잰 참고값 — 근거로 쓰지 말고 재측정할 것 · 시드 11 · 300판)** — 현행 곡선(T104 HEAD)에서 8칸 클리어율 = 노템 **0.0** · 일반 **0.0** · 희귀+5 **0.0** · 영웅+10 **0.0** · 전설+15 **0.0** · 신화+25(ch150) **89.0** · 신화9강+50(ch380) **100.0** · 신화9강+100(ch420) **100.0**(ch600 도 100.0 · 슬롯 150 도 100.0). **초반 5칸은 적이 너무 세고 후반 3칸은 너무 약하다** — T102 의 신화 ×6·+9강 ×20 이 들어간 뒤 난이도 노브를 아무도 안 만졌기 때문이다. 적 HP·DMG 를 **같은 배수 m** 으로 곱해 10% 가 되는 m 을 이분탐색하면 노템 ch5 **m=0.571** · 일반 ch15 **m=0.238** · 희귀+슬롯5 ch28 **m=0.331** (나머지 5칸은 미측정). **권장 절차**: 8칸 각각에서 m_k 를 재고, 과녁 사이 구간률을 `((m_{k+1}/m_k) · D_old(c_{k+1})/D_old(c_k) ÷ 구간 안 벽배수)^(1/n)` 으로 다시 자르면 한 번에 8점이 잡힌다(같은 m 을 HP·DMG 에 곱하므로 챕터별 HP:DMG 비가 보존된다). **구간 경계를 과녁 챕터 5·15·28·40·70·150·380·420 에 맞춰 새로 자를 것** — 지금 경계(15/20/30/50/70/120/260)는 옛 사다리 7점 것이라 과녁과 어긋난다. 1→5 는 옛 률을 두고 기저만 ×m_5. ⚠ 적 HP·DMG 정수 반올림 때문에 노템(적 dmg 8 → 5 수준)·일반 칸은 계단이 크다 — T97 이 적은 «0.5% 단위보다 잘게 움직이지 말 것» 이 그대로 적용된다.

  **⑥ 600 이 박힌 자리 전부 (T104 가 심은 것 — 420 으로 같이 옮길 것)**: `sim.js`·`index.html` TUNE(`maxChapter`·`wall4At`·`wall4Hp/Dmg`·구간표·기저) · `index.html` 세이브 정규화 리터럴 `Math.min(…,600)`(TDZ 라 리터럴) · 로비 «해금: 1 ~ N» · PLAN §2.4 «600개로 확장» 문면(게이트가 이 문장에서 숫자를 읽는다)·§6 확정값 블록·§7(과녁 2점 → **사다리 8점으로 재작성**)·§11.7 사다리 표·구간표 · `tools/verifyScoreCriteria.js`(WANT A·B → 8점, 자기검사 케이스 포함) · `verifyPlanConst.js` SEG 목록(구간 경계가 바뀌면 9줄 색인이 통째로 바뀐다) · `verifyT2.js`(«챕터 상한 600»·«최종 벽 = 600» 단언 · 레이아웃 1~600 루프) · `verifySeedProtocol.js` 탐침(«신화 / 챕터 120» 은 재적합 뒤 세 시드가 0 이나 100 에 겹칠 수 있다 → 사다리 칸 «신화+슬롯25 / 150» 으로) · `tools/regress.js` parse1·parse5 행 정규식(칸 이름이 바뀐다) · `tools/fitLadder.js`(슬롯 미반영 — `mkBuild(rar,plus,slot)`) · `sim.js` `EXP1_TARGETS`(2점 → 8점 · `slot` 필드) 와 `LADDER`(옛 7점 · `want` 스탯이 T102 이전 값이라 낡았다 — 8점 한 벌로 통일) · T3 `tools/t3/fx.js`(«챕터 600 진입·클리어») · `boot.js`(«챕터 상한 600» · 대형 수치 표기 `[40, 90, 600]`).

  **⑦ 마감 조건** — 아래 지시 ⑥ 그대로이되 대표 챕터 목록은 **1·5·15·28·40·70·150·250·380·420** 으로. `result.md` 에 ③의 «챕터 420 = 상한» 을 위임 기본값으로 택했음을 한 줄 명시할 것(주인이 600 을 원하면 되돌릴 자리).

- **(2026-09-03 KST) ⚑⚑⚑ 주인 확정 — 특전 순서 재정렬 + 난이도 사다리 8점 (승률 10% 기준) + 챕터 600 확장. T102·T103 은 이 값으로 한다.**

  **① 특전 획득 순서 (10종 그대로 · 순서만 교체 — 주인 지시)**

  | # | 특전 | 효과 |
  |---|---|---|
  | 1 | 회피 시 회복 | 회피 시 **10%** 확률로 **최대 체력의 6%** 회복 (⚑ 주인 정정 — 종전 «생명 흡수: 준 피해의 10% 회복» 폐기) |
  | 2 | 반격률 증가 | 반격률 **+10** |
  | 3 | 반격 시 창 | 반격 시 **50%** 확률로 창 1개 |
  | 4 | 회피 시 화살 | 회피 시 **50%** 확률로 화살 1개 |
  | 5 | 피격 시 도끼 | 피격 시 **50%** 확률로 도끼 1개 |
  | 6 | 공격력 증가 | 공격력 **+20%** |
  | 7 | 회피율 증가 | 회피율 **+10** |
  | 8 | 치명타 확률 증가 | 치명타 확률 **+10** |
  | 9 | 치명타 피해 증가 | 치명타 피해 **+50** |
  | 10 | 방어력 증가 | 방어력 **+10%** |

  종전 순서(공격력 → 회피 → 반격 → 치확 → 치배 → 방어 → 흡수 → 도끼 → 화살 → 창)는 폐기.
  **⚑ 1번 특전은 효과까지 바뀌었다 (주인 정정)** — «생명 흡수(준 피해의 10% 회복)» → **«회피 시 10% 확률로 최대 체력의 6% 회복»**. 나머지 9종은 순서만 바뀌고 수치는 그대로다.
  - 위임 기본값: **6% = 최대 체력 기준**(현재 체력 아님) · 최대치 초과분은 버림 · **실드는 안 채운다** · 트리거는 «내가 적 공격을 회피한 순간»(4번 «회피 시 화살» 과 같은 자리) · 확률 10% 는 회피 성공마다 독립 굴림.
  - 부수 효과: 특전에서 **흡혈(steal) 축이 사라진다**. 엔진의 `steal` 스탯은 남지만 특전이 쓰지 않으므로, 게이트의 «흡혈» 단언은 이 특전 기준으로 갈아끼울 것.

  **② 난이도 사다리 8점 (주인 확정 · 전부 «클리어율 10%» 기준 · 허용 오차 ±2%p)** — 이것이 유일한 밸런스 기준이다. 종전 과녁 A(희귀 = 15)·B(노장비 = 4 에서 30%)·C(신화9강 = 500 벽)는 **이 표로 대체**된다.

  | # | 빌드 (특전 10종 전부 획득) | 목표 챕터 |
  |---|---|---|
  | 1 | 노템 (장비 0 · 슬롯 0) | **5** |
  | 2 | 일반 풀셋 (슬롯 0) | **15** |
  | 3 | 희귀 풀셋 · 전 부위 슬롯 **5** | **28** |
  | 4 | 영웅 풀셋 · 전 부위 슬롯 **10** | **40** |
  | 5 | 전설 풀셋 · 전 부위 슬롯 **15** | **70** |
  | 6 | 신화 풀셋 · 전 부위 슬롯 **25** | **150** |
  | 7 | 신화 +9강 풀셋 · 전 부위 슬롯 **50** | **380** |
  | 8 | 신화 +9강 풀셋 · 전 부위 슬롯 **100** | ~~**600**~~ → **420** (⚑ 주인 정정 14:4X — 위 항목) |

  각 칸의 실제 총 스탯(새 장비 표 + 슬롯·균등 보너스 적용, 전투력 = 공×8+(체+실)×1.5):
  ```
  ch  5 노템            25 / 150 / 250            전투력      800
  ch 15 일반            50 / 250 / 400            전투력    1,375   (파워 ×1.72 · 챕터 +10)
  ch 28 희귀+슬롯5      109 / 543 / 869           전투력    2,990   (×2.17 · +13)
  ch 40 영웅+슬롯10     239 / 1,194 / 1,909       전투력    6,567   (×2.20 · +12)
  ch 70 전설+슬롯15     525 / 2,619 / 4,189       전투력   14,409   (×2.19 · +30)
  ch150 신화+슬롯25   3,742 / 18,703 / 29,922     전투력  102,875   (×7.14 · +80)
  ch380 신화9강+슬롯50 106,912 / 533,475 / 853,125 전투력 2,935,199 (×28.5 · +230)
  ch600 신화9강+슬롯100 190,050 / 948,300 / 1,516,500 전투력 5,217,598 (×1.78 · +220)
  ```

  **③ 챕터 600 확장** — `TUNE.maxChapter` **300 → 600**(종전 지시 «500» 은 이 표로 대체) · PLAN §2.4 문면 · 로비 챕터 선택 상한 600. **⚑ 위 항목(14:4X 정정)③ 으로 600 → 420 (위임 기본값).**

  **④ 벽** — 10 벽은 그대로, 15 벽은 계속 비활성(과녁 «일반 = 15» 에 직접 곱해져 예산을 갉아먹는다). **90 벽과 최종 벽은 T103 이 이 8점에 맞춰 위치·배수를 재산정**한다(90 은 과녁 70~150 사이라 그 구간 예산을 쓰고, 최종 벽은 마지막 과녁 600 에 건다). 종전 «최종 벽 500» 지시는 폐기.

  **⑤ ⚑ T103 이 먼저 알아야 할 구조 (위 파워 배수에서 바로 나온다)**
  - 챕터당 필요 성장률이 구간마다 **10배 이상 벌어진다**: 70→150 은 챕터당 약 **1.0247**(파워 7.14배/80챕터), 150→380 은 **1.0146**, **380→600 은 1.0026** 로 거의 평평하다.
  - 즉 **챕터 380~600 은 난이도가 거의 자라지 않는 구간**이 된다(슬롯 50 → 100 이 파워를 1.78배밖에 안 올리는데 챕터를 220개 더 가야 하므로 — 산술적 귀결이다).
  - 그게 의도가 아니면 손댈 곳은 둘이다: ⓐ 8번 칸을 «슬롯 150(만렙)» 으로 바꾼다(파워 ×2.5 → 조금 낫다) ⓑ 8번 칸의 목표 챕터를 낮춘다(예: 600 → 480). **어느 쪽도 주인 확정 없이는 건드리지 말고, T103 은 표 그대로 맞춘 뒤 결과를 `result.md` 에 이 사실과 함께 적어 올릴 것.**

  **⑥ 마감 조건** — T103 이 `docs/balance/T103/result.md` 에 **8점 전부의 실측 O/X** · 구간별 성장률 · 대표 챕터(1·5·15·28·40·70·150·250·380·500·600) 난이도 표 · 챕터 구성 확인(적 76 · 완주 = 특전 10개) · 측정 조건·커밋 SHA 를 남긴다. 메일은 보내지 않는다.


- **(2026-09-03 KST) ⚑ 주인 방향 — 특전은 나중에 «한 런 획득 10개 고정 + 풀만 확장» 으로 간다. 지금 그 준비만 해 둔다(구현은 나중).**
  - **왜 지금 적어 두나**: 지금 잡는 난이도(T102·T103)는 «특전 10개를 다 얻은 캐릭터» 기준이다. 나중에 특전을 30종으로 늘려도 **한 런에서 얻는 수가 10개면 파워 총량이 그대로**라 난이도를 다시 잡을 필요가 없다. 반대로 획득 «개수» 를 늘리면 챕터 경험치·적 수(T100)부터 다시 설계해야 한다.
  - **⚑ T102 가 미리 해 둘 것 (밸런스 영향 0)**: `PERKS.length` 를 «풀 크기» 와 «한 런 획득 수» 두 뜻으로 같이 쓰지 말고 **상수를 분리**한다 — `PERK_PICKS = 10`(한 런에 얻는 수 · 챕터 레벨업 횟수와 같은 값)을 신설하고, «10개를 다 얻으면 그만» 판정·게이트·문면이 그 상수를 보게 한다. 지금은 풀도 10이라 **동작은 한 글자도 안 바뀐다**(게이트로 그 동일성을 단언할 것).
  - **나중에 풀을 늘릴 때의 규칙 (미리 정해 둔다)**: ① 새 특전도 **파워 밴드** 안에서 만든다 — 한 종이 단독으로 과녁 클리어율을 **±5%p 넘게** 못 바꾼다(옛 132종 체제가 «같은 등급 안에서 99% ↔ 60%» 로 벌어져 잡지 못한 실패를 반복하지 않기 위한 규칙). ② 시뮬 측정 정책은 **«남은 것 중 표 순서로 첫 번째»** 로 고정한다(쉼터 «항상 경험치»·악마 «항상 수락» 과 같은 축 — 재현성). 게임의 선택 UI 는 그때 정한다.

- **(2026-09-03 KST) ⚑⚑⚑ 주인 확정 — 장비 등급 배수 재설계 + 챕터 500 확장 + 최종 벽을 500 으로. 그리고 밸런스 재적합. (T102 → T103. 진행 중이던 T101 은 이 지시로 무효)** 주인 원문: «일반에서 전설까지는 공체실 각각 2배씩 뛰고, 전설→신화는 6배(주인 정정 — 처음 4배에서 상향), 신화→신화9강은 20배로 띄게 해줘. 그리고 챕터 간 밸런스 다시 잡아봐라. 그리고 신화 9강 풀셋은 500챕터에서 벽 생기게 바꿔줘 — 신화9강 부분만 벽 위치 바꾸는 것.»

  **① 새 장비 표 (풀셋 총 스탯 · 0강 · 슬롯 0렙)** — 기본치(노템 25/150/250)는 불변이다.

  | 등급 | 공격력 | 체력 | 실드 | 앞 등급 대비 |
  |---|---|---|---|---|
  | 노템 | 25 | 150 | 250 | — |
  | 일반 | 50 | 250 | 400 | (현행 유지) |
  | 희귀 | 100 | 500 | 800 | **×2** |
  | 영웅 | 200 | 1,000 | 1,600 | **×2** |
  | 전설 | 400 | 2,000 | 3,200 | **×2** |
  | 신화 | 2,400 | 12,000 | 19,200 | **×6** |
  | 신화 +9강 | 48,000 | 240,000 | 384,000 | **×20** |

  **부위 1개당 기여 = (풀셋 − 기본치) ÷ 6** — 일반 4.167/16.667/25 · 희귀 12.5/58.333/91.667 · 영웅 29.167/141.667/225 · 전설 62.5/308.333/491.667 · 신화 395.833/1975/3158.333.

  **② ⚑ 강화 계수를 함께 바꿔야 한다 (산수)** — 지금 «+9강» 은 `1 + 0.13×9 = ×2.17` 이라 ×20 이 안 나온다. **`plusStep` 을 `19/9`(≈2.1111)로 바꾼다** → +9강 = **정확히 ×20**. 선형을 유지하는 이유는 신화 무한 강화 때문이다(복리 1.4^강 으로 하면 신화+20강이 836배로 폭주한다 — 선형이면 43배). PLAN §11.5-a 의 `plusStep 0.13` 문면도 같이 갱신할 것.

  **③ 챕터 500 확장 + 최종 벽 이동**
  - `TUNE.maxChapter` **300 → 500** · PLAN §2.4 «챕터 수» 문면 갱신 · 로비 챕터 선택 상한도 500.
  - **최종 벽(`wall4`)을 챕터 300 → 500 으로 옮긴다.** 10·15·90 벽은 **그대로 둔다**(주인 «신화9강 부분만 벽 위치 바꾸는 것»).
  - 벽 배수(`wall4Hp/Dmg`)는 T103 이 과녁 C 에 맞춰 재산정한다.

  **④ 과녁 — 2점 → 3점 (C 신설)**
  | 과녁 | 조건 | 목표 |
  |---|---|---|
  | **A** | 표준 장비(희귀 풀셋 6부위·0강·슬롯 0렙) + 특전 10종 | **챕터 15 클리어율 10%** |
  | **B** | 노장비(장비 0·슬롯 0) + 특전 10종 | **챕터 4 클리어율 30%** |
  | **C** | **신화 +9강 풀셋(슬롯 0렙)** + 특전 10종 | **챕터 500 = 벽** — 클리어율 **2~10%**(겨우 뚫린다 · 위임 기본값) |
  허용 오차 A·B 는 **±2%p** 그대로 · 측정은 과녁당 1,000판 이상 · 고정 시드 3벌.

  **⑤ 순서** — **T102**(표·강화 계수·챕터 500·벽 이동 = 구현) → **T103**(밸런스 재적합 = 과녁 3점). **T101 은 이 지시로 무효**다 — 재적합 대상 곡선 자체가 바뀐다. lock 을 쥔 워커는 **현 작업을 버리고 lock 을 해제**할 것(측정값도 폐기 — 새 표에서 다시 잰다).

  **⑥ 마감 조건** — T103 을 끝내는 워커가 `docs/balance/T103/result.md` 를 «완료» 커밋과 같이 남긴다: 과녁 3점 실측 O/X · 등급별 도달 챕터 표(노템~신화+9강·슬롯) · 대표 챕터 난이도 표(1·4·10·15·30·50·100·200·300·400·500) · 챕터 구성 확인(적 76 · 완주 = 특전 10개) · 측정 조건·커밋 SHA. 메일은 보내지 않는다.


- **(2026-09-03 KST) ⚑⚑⚑ 주인 확정 — 경험치 시스템 개정 → 챕터 재설계 → 밸런스 재적합. 이 순서로 셋을 다 한다. (T100 → T101)** 주인 원문: «경험치 6으로 시작해야 하고 레벨업당 필요 경험치가 5씩 증가. 적 죽이면 경험치 3씩. 그러니까 챕터들 자체가 잘못 설정됨. 경험치 시스템 바뀐 대로 챕터당 특전 10개 될 수 있게 한 다음에, 특전 순서대로 고정으로 밸런스 다시 테스트.»

  **① 경험치 곡선 (주인 확정)** — `TUNE.expNeed = lv => 5*lv + 1` (Lv1→2 에 **6**, 그다음부터 **+5** 씩: 6·11·16·21·26·31·36·41·46·51). 종전 `4+3*lv` 폐기. **적 처치 경험치 3 은 그대로**(`expKill:3`).

  **② 챕터 재설계 — «완주 = 특전 10개» 를 새 곡선으로 다시 맞춘다**
  - 10레벨 누적 필요 = Σ(5·L+1), L=1..10 = **285**
  - 확정 구성(위임 기본값): **웨이브 5 × 15마리 = 75 + 보스 1 = 적 76마리** · 쉼터 **2** · 악마 1 · 천사 1 (전 챕터 동일 고정, 순서만 셔플 — T96 방식 그대로)
  - 공급 = 75×3 + 9(보스) + 2×26(쉼터) = **286** ≥ 285 → **완주하면 정확히 10레벨**. 11레벨은 341 이 필요해 **구조적으로 불가능**(초과 획득 없음).
  - **⚠ 주인 이전 지시 «적 최대 50마리» 는 이 지시로 갱신된다** — 새 곡선에서 50마리로는 산술적으로 10개를 못 채운다(적 49 = 공급 205 < 285). 주인이 «특전 10개 될 정도의 적 개수로 바꾸라» 고 명시했으므로 **적 수를 76으로 올리는 쪽**을 택했다. 적 수를 다시 줄이고 싶으시면 보스 경험치(9)나 쉼터 경험치(26)를 올리는 것이 대안이다 — 한 줄로 정정해 주시면 그쪽으로 바꾼다.
  - `LAYOUT_MAXENEMY` 은 **80** 으로 올린다(구성 76 + 여유). 게이트의 «적 ≤ 상한» 단언은 그대로 유지.

  **③ 밸런스 재적합 (T101)** — 특전은 **순서 고정 획득 그대로**(3택 1 선택창은 주인이 취소했다 — 만들지 말 것). 과녁은 **바뀌지 않았다**:
  - **A** 표준 장비(희귀 풀셋 6부위·0강·슬롯 0렙) + 특전 10종 = **챕터 15 클리어율 10% ±2%p**
  - **B** 노장비(장비 0·슬롯 0) + 특전 10종 = **챕터 4 클리어율 30% ±2%p**
  - 노브는 적 스탯(구간 성장률·벽 배수)뿐. 측정은 고정 시드 3벌 × 과녁당 1,000판 이상.
  - **⚑ T97 이 맞춘 값은 무효다** — 챕터당 적이 49 → 76 으로 늘고 레벨업 곡선이 바뀌어 두 과녁이 통째로 움직인다. T97 의 «구간 분할(1→5 / 5→15) + 후반 보정(15→120)» 수법은 그대로 재사용하되 **숫자는 새로 잡는다.**

  **④ 취소된 것 (등록하지도 말 것)** — 직전 대화에서 나왔던 **특전 3택 1 선택창 · 하단 네비 디버그 팝업 · 챕터 자유 설정**은 **주인이 취소**했다(«방금 한 말 취소»). 특전 획득은 **순서 고정** 그대로다.

  **⑤ 끝나면** — T101 을 마치는 워커가 `docs/balance/T101/result.md` 를 T101 행을 «완료» 로 바꾸는 **같은 커밋**에 남긴다(과녁 2점 실측 O/X · 챕터 구성 확인 «적 76 · 완주 = 특전 10개» 실측 · 대표 챕터 10칸 난이도 표 · 측정 조건·커밋 SHA). 메일은 보내지 않는다.


- **(2026-09-03 KST) ⚑ T97(챕터 밸런스) 마감 조건 — 결과 요약 파일을 남긴다. (메일 보고는 주인 지시로 취소 — 알림 작업도 지웠다. 주인이 파일을 직접 본다.)**
  - T97 을 마치는 워커는 **`docs/balance/T97/result.md`** 를 반드시 남긴다(회차 원시 근거와 별개인 «보고용 요약»). 이 파일이 그대로 주인 메일 본문이 된다.
  - 담을 것: ① **과녁 2점 실측** — 표준 장비(희귀 풀셋·슬롯 0)+특전 10종 순서 획득 = 챕터 15 클리어율(목표 10% ±2%p) · 노장비 = 챕터 4 클리어율(목표 30% ±2%p), 각각 달성 O/X ② **챕터 구성 확인** — 챕터당 적 마릿수(상한 50)와 «완주 = 특전 10개» 성립 여부 실측 ③ **대표 챕터 난이도 표** — 챕터 1·4·10·15·20·30·50·100·200·300 클리어율 ④ **측정 조건**(시드·판수)·근거 문서 경로·반영 커밋 SHA.
  - **숫자는 실측만 적는다** — 없으면 «측정 기록 없음» 이라고 쓴다(지어내면 그대로 주인에게 메일로 나간다).
  - **메일·알림은 보내지 않는다**(주인 지시). **PROGRESS T97 행을 «완료» 로 바꾸는 커밋에 이 파일을 같이** 넣기만 하면 된다 — 주인이 그 파일로 결과를 본다.

- **(2026-09-03 KST) ⚑⚑⚑ 주인 확정 — «특전 10개를 얻을 정도의 적 개수로만 챕터를 구성해라. 이게 우선돼야 밸런스를 제대로 잡는다.» → 챕터 구성을 전 챕터 동일 고정으로 바꾼다.** **T96 이 T97 보다 먼저 이것부터 끝낸다.**

  **① 원칙** — 한 챕터를 완주하면 **정확히 10레벨 = 특전 10개**가 되도록 챕터의 적 수와 경험치 곡선을 맞춘다. 난이도는 **적의 스탯(구간 성장률·벽 배수)으로만** 만들고, **적 «수» 는 더 이상 난이도 노브가 아니다.**

  **② 산수 (현행 값으로는 불가능하다 — 이게 이번 지시의 핵심)**
  - 10레벨까지 필요한 누적 경험치 = Σ(4+4·L), L=1..10 = **260**.
  - 적 50 상한에서 한 챕터가 줄 수 있는 최대 = 일반 48마리 ×3 + 보스 9 + 쉼터 4×26 = **257**.
  - **최대치로도 260 에 못 닿는다** → 적 50 상한을 지키는 한 지금 곡선으로는 10개를 절대 못 모은다. 그래서 경험치 곡선을 함께 바꾼다.

  **③ 확정 구성 (위임 기본값 — 숫자가 딱 맞아떨어지게 고른 값이다)**

  | 항목 | 값 | 비고 |
  |---|---|---|
  | 웨이브 | **4 × 12마리 = 48** | 전 챕터 동일 |
  | 보스 | **1** | 총 적 **49마리** (주인 상한 50 이내) |
  | 쉼터 | **2 (고정)** | 종전 1~4 변동 폐지 |
  | 악마 · 천사 | 각 **1** | 종전 그대로 |
  | 레벨업 요구 경험치 | **`4 + 3·Lv`** | 종전 `4+4·Lv` |

  - 공급 = 48×3 + 9(보스) + 2×26(쉼터) = **205**
  - 필요 = Σ(4+3·L), L=1..10 = 40 + 165 = **205**
  - **딱 떨어진다** — 완주하면 정확히 10번 레벨업하고, 마지막 특전이 보스 근처에서 들어온다. 중간에 죽으면 그만큼 덜 얻는다(의도).

  **④ 덤으로 없어지는 문제** — 챕터마다 적 수·쉼터 수가 제비뽑기로 흔들리던 것이 사라진다. 그 흔들림이 **인접 챕터의 난이도 역전**(챕터 28 = 62% 인데 29 = 9.5%)과 실험3 곡선의 요철을 만들던 주범이었다(T28·승인 대기 10·21번). 주인 말씀대로 이걸 먼저 고정해야 난이도 곡선을 제대로 잰다.

  **⑤ 이행 (T96 · 지금 잡고 있는 워커가 이어서)**
  - `sim.js`·`index.html` 의 `chapterLayout` 을 **제비뽑기 없이 고정 구성**으로 바꾼다(시드 셔플은 «순서» 에만 남긴다 — 악마·천사·쉼터의 배치 순서는 계속 섞어도 된다).
  - `TUNE.expNeed` 를 **`lv=>4+3*lv`** 로. PLAN §2.4·§7 문면 동시 갱신.
  - `LAYOUT_MAXENEMY` 는 **50** 을 그대로 두되(주인 상한), 실제 구성은 49 로 고정.
  - **게이트 신설**: 챕터 1~300 전수로 ⓐ 적 ≤ 50 ⓑ 구성이 전 챕터 동일(48+보스·쉼터 2·악마 1·천사 1) ⓒ **«완주 = 특전 10개» 실측**(두 엔진에서 한 판 완주시켜 획득 특전 수 = 10 · 9 이하면 빨강) ⓓ 두 엔진 동일. 음성 검사 포함.
  - 그 다음 **T97** 이 이 고정 구성 위에서 과녁 2점(챕터 15 = 10% · 노장비 챕터 4 = 30%)에 난이도를 맞춘다.

  **⑥ 주의** — 쉼터 «항상 경험치» 정책(시뮬)과 악마 «항상 수락» 은 그대로다. 악마가 주는 것은 «다음 순번 특전 앞당김» 이라 총 개수는 10 그대로다(앞당길 것이 없으면 아무것도 주지 않는다 — 위임 기본값).


- **(2026-09-03 KST) ⚑⚑⚑ 주인 확정 — «방금 말한 게 기준이고 나머지 기준은 다 폐기».** 밸런스 판정 기준은 **아래 2점뿐**이고, 이전의 모든 기준은 **폐기**한다. PLAN §7 머리와 §11.7 머리에 같은 내용을 박아 뒀다.

  | 과녁 | 조건 | 목표 |
  |---|---|---|
  | **A** | 표준 장비(희귀 풀셋 6부위 · 슬롯 0렙) + 특전 10종 순서 획득 | **챕터 15 클리어율 10%** |
  | **B** | 노장비(장비 0 · 슬롯 0) + 특전 10종 순서 획득 | **챕터 4 클리어율 30%** |

  허용 오차 **±2%p** · 과녁당 **1,000판 이상 · 고정 시드 3벌** · 노브는 **구간별 성장률 · 벽 배수 · 적 수**.

  - **폐기 목록 (판정에 쓰지 말 것)**: 스탯 사다리 **7점**(5·15·30·50·70·120·260) · **채점표 v1~v5 전부**와 실험1~5 배점 · **«비평가 2인 각 ≥8/10» 합격 게이트** · 실험1 등급 과녁(10/20/80%) · 실험2 등급 내 폭(<25%p) · 실험3 진행 곡선 목표(챕터별 시도 횟수) · 실험4 기준 ①②③ · 앵커 A·B·C(이미 폐기됨).
  - **러너는 남긴다** — `node sim.js 1~5` 와 `tools/regress.js` 는 **점수 없는 진단 도구**로만 쓴다. 회귀 측정 위생(고정 시드 3벌 · 판수 · 원시 출력 그대로 복사)은 그대로 유효하다.
  - **⚑ T97 의 첫 번째 할 일**: PLAN §7·§11.7 을 위 2점 기준으로 **재작성**하고, `tools/verifyScoreCriteria.js` 를 새 기준으로 갈아엎고(옛 채점표 문면 단언 전부 제거), `sim.js` 의 죽은 채점 코드(등급 과녁·폭·실험3 점수 환산)를 정리한다. **그 전까지는 옛 채점 출력이 찍혀도 무시할 것 — 판정에 쓰지 않는다.**
  - **T96 은 이 정리를 하지 않는다** — 특전 전환에만 집중한다(순서·수치·적 50·레이아웃). 기준 재작성은 T97 몫이다.


- **(2026-09-03 KST) ⚑⚑⚑ 주인 확정 — 특전 전면 폐지 → «고정 10종 · 순서 획득» 으로 전환. 그리고 챕터 난이도 재적합이 최우선.** 지금까지의 특전 체제(132종·등급·선택창)는 **통째로 버린다.** 진행 중이던 특전 관련 작업·자·게이트도 같이 버린다(아래 ⑨). **주인 원문 그대로: «지금까지 하던 거 그냥 버리고, 특전 싹 다 폐지하고, 아래 10개를 순서대로 선택하는 걸로 고정하고, 시뮬 돌려서, 장비랑 스탯은 원래 그 상태로, 적 최대 50마리, 챕터 다 만들고, 15챕터 정도에서 승리 확률 10%, 노장비로는 4스테이지 정도까지가 승리 확률 30%. 어쨌든 챕터 난이도 조절 먼저.»**

  **① 새 특전 = 아래 10종이 전부다 (등급 없음 · 이 순서 고정)**

  | # | 특전 | 효과 |
  |---|---|---|
  | 1 | 공격력 증가 | 공격력 **+20%** |
  | 2 | 회피율 증가 | 회피율 **+10** |
  | 3 | 반격률 증가 | 반격률 **+10** |
  | 4 | 치명타 확률 증가 | 치명타 확률 **+10** |
  | 5 | 치명타 피해 증가 | 치명타 피해 **+50** |
  | 6 | 방어력 증가 | 방어력 **+10%** |
  | 7 | 생명 흡수 | 준 피해의 **10%** 회복 |
  | 8 | 피격 시 도끼 | 피격 시 **50%** 확률로 도끼 **1개** |
  | 9 | 회피 시 화살 | 회피 시 **50%** 확률로 화살 **1개** |
  | 10 | 반격 시 창 | 반격 시 **50%** 확률로 창 **1개** |

  **② 획득 방식** — 레벨업할 때마다 **위 순서대로 하나씩 자동 획득**한다. **선택창·등급 굴림·새로고침·전지의 눈은 전부 사라진다.** 10개를 다 얻은 뒤의 레벨업은 특전을 주지 않는다(**위임 기본값** — 반복 획득을 원하시면 한 줄로 정정해 주십시오).

  **③ 적 최대 50마리** — `LAYOUT_MAXENEMY` **100 → 50**. 챕터 1~300 레이아웃을 그 상한으로 다시 만든다(악마 1·천사 1·쉼터 규칙은 그대로).

  **④ 장비·스탯은 지금 그대로 둔다** — 장비 18계열·옵션표·슬롯·가챠·경제·플레이어 기본 스탯·표준 하니스(희귀 풀셋) 전부 **불변**. 이번 전환에서 손대는 것은 **특전과 챕터 난이도뿐**이다.

  **⑤ 난이도 과녁 2점 (이번 국면의 1순위 · 주인 확정)**
  - **표준 장비(희귀 풀셋·슬롯 0) + 특전 순서 획득 → 챕터 15 클리어율 ≈ 10%**
  - **노장비(장비 0·슬롯 0) + 특전 순서 획득 → 챕터 4 클리어율 ≈ 30%**
  - 허용 오차 **±2%p** · 측정 판수 **과녁당 1,000판 이상** · 고정 시드 3벌 (**위임 기본값**, 기존 회귀 측정 규약 그대로).

  **⑥ «흡혈 증가 금지»(2026-09-02 07:1X) 는 폐기됐다** — 위 7번이 주인 최신 확정이다. `sim.js`·`index.html` 주석과 게이트의 금지 축에서 흡혈을 뺀다. **적중률 금지는 그대로 유효**하다.

  **⑦ 수치 해석 (위임 기본값 — 다르게 뜻하셨으면 한 줄로 정정해 주십시오)**
  - 확률형(회피·반격·치명타 확률)은 **+10%p**(기본 20 → 30), 치명타 피해는 **+50%p**(기본 150 → 200).
  - 공격력 +20% · 방어력 +10% 는 **기본치에 곱연산**(장비 합산 뒤 적용).
  - 생명 흡수 = **준 피해의 10%** 를 체력으로 회복(초과분은 버림 · 실드는 안 채움).
  - 8·9·10 의 소환은 **기존 도끼·화살·창 소환 엔진 그대로**(1개 = 1발, 쿨다운 없음). 셋 다 «공격 시» 축이 아니라 소환 연쇄 기대값(≤ 0.8) 규칙과 충돌하지 않는다 — 게이트로 실측 확인할 것.

  **⑧ 순서 (주인 지시 «챕터 난이도 조절 먼저»)** — **T96(체제 전환) → T97(챕터 난이도 재적합)** 이고, T97 이 이 국면의 본진이다. T96 은 T97 을 재려면 반드시 먼저 끝나야 하는 준비 작업이다(특전이 안 바뀌면 잴 대상이 없다).

  **⑨ 이 지시로 죽는 것들 — 새로 손대지 말 것**
  - **T85(P3 밸런스 루프) 국면 종료** — 실험1(등급 과녁)·실험2(등급 내 폭)은 **등급이 사라져 측정 대상 자체가 없다**. 채점표 v5 도 보류(새 과녁 2점이 대신 판정한다). 회차 기록 R01~R04 는 이력으로 보존.
  - **T88(132종 전수 효과 감사) 중단** — 감사 대상이 사라졌다. lock 은 90분 경과분이라 해제한다.
  - **T92 ①(무료 새로고침 sim 구현) 폐기** — 선택창이 사라져 새로고침이 존재하지 않는다. **②(합성 재료에서 장착 장비 제외)는 살아 있다.**
  - **T94 ③(이미 켜진 효과는 선택지에 안 뜨게) 폐기** — 선택창이 없다. **①②④(장비 옵션표 2건 + 숫자 정리)는 살아 있다.**
  - **승인 대기 42번(악마도 등급 고정)·40번 ⓓ 는 전제 소멸** — 등급이 없다. 악마의 거래가 무엇을 주는지는 T96 이 정한다(**위임 기본값: 다음 순번 특전을 하나 앞당겨 준다** — 특전이 순서 고정이라 «전설 확정» 이 의미를 잃었다).
  - **T93·T95 는 그대로 유효**(실험4 정체 감지기 · 소환 연쇄 «적중» 문면 · 연사 총 시간 상한).

- **(2026-09-03 KST) ⚑⚑ 주인 위임(«알아서 해») — 승인 대기 재고 23건 → 3건으로 정리. 신규 작업 T92~T95 등재.** 주인이 «승인 대기가 왜 이렇게 많냐» 고 물어 전수 검토했고, 물어볼 필요가 없어진 것·루틴 재량인 것·애초에 승인 사안이 아니었던 것을 전부 걷어냈다. **판단 근거는 각 항목 머리(`docs/PROGRESS.md` §주인 승인 대기)에 적었고, 아래는 워커가 알아야 할 이행분만 적는다.**
  - **종결 20건** — 전제 소멸 8(3·4·5·6·10·22·29·34) · 스펙 위반이라 승인 불요 2(20·28) · 자·측정 규칙이라 루틴 재량 5(21·23·30·31·33) · 장비 옵션표 재량 4(12·15·19·35) · 연출 1(8).
  - **⚑ 남은 주인 몫은 3건뿐**: **13**(무과금이 300챕터를 5개월에 소진 — 의도하신 속도인지) · **27**(같은 등급·강화·슬롯인데 계열 조합만으로 과녁이 21.6배 — 밸런스 설계 방향) · **36**(특전·장비의 `px` 키 분리 = 구조 변경). **이 셋은 루틴이 대신 정하지 않는다.** 27번의 «측정 조건» 만은 고정해 뒀다 — 사다리·실험1 은 계열 고정(`typeIdx` 0)으로 재고, 계열 간 편차는 별도 지표로 본다.
  - **신규 작업 4건 (선점 순서 = 아래 그대로)**
    - **T92 — 스펙 준수 수정 2건**(무료 새로고침 1회를 `sim.js` 에 구현 · 합성 재료에서 장착 장비 제외). **⚑ P3 기준선 재측정 «전» 에 들어가야 한다** — 새로고침은 챕터8 하니스에서 +8.2%p 다(T25 실측). 시뮬 정책은 **«뜬 등급이 일반이면 1회 새로고침, 아니면 안 쓴다»**(쉼터 «항상 경험치»·악마 «항상 수락» 과 같은 축 · 위임 기본값).
    - **T94 — 장비 옵션표 3건 + 숫자 정리**(사슬갑옷 옵5 배율화 · 건틀릿 옵6 2.3→3.5배 · **이미 켜진 효과는 특전 선택지에 안 뜨게** · 장비 옵션 33칸 숫자 정리). **앞 셋도 재측정 «전» 에.**
    - **T93 — 자·문면 2건**(실험4 정체 감지기 = 600판 + 벽 챕터 예외 + 전 구간 스캔 · 소환 연쇄 임계는 «적중» 기준 명시). 엔진 무수정.
    - **T95 — 다연발 연사 총 시간 상한**(발수는 안 건드린다). 연출이라 밸런스 무관.
  - **⚑ P3 재개 첫 회차의 순서가 이걸로 바뀐다**: ⓐ 채점표 v5 → ⓑ 승인 42번 ⓐ(악마도 등급 고정) → **ⓒ T92·T94 반영(밸런스 영향분을 여기서 다 넣는다)** → ⓓ **기준선 재측정 한 번** → ⓔ 사다리 회복. **재측정 전에 넣을 것을 다 넣어라 — 두 번 재지 않는다.**

- **(2026-09-03 KST) ⚑⚑ 주인 답 2건 — 승인 40번(«잔여 판단») = ⓓ · 승인 42번(악마가 등급 고정을 깨는 자 오염) = ⓐ.** 주인이 **«알아서 해»** 로 위임했고, 위임 범위 안에서 아래로 확정한다. **둘 다 자(尺)·채점 규칙이고 엔진 수치·게임 동작은 한 줄도 안 바뀐다.**

  **① 승인 40번 = ⓓ (실험2 폭은 «측정·보고만»), 그 배점 1점은 실험5 로 이관 — 채점표 v4 → v5.**
  - **실험2 등급 내 폭(<25%p)은 채점에서 뺀다.** 측정·표시는 그대로 계속한다(희귀·전설·일반 전부 폭을 찍되 «측정·보고 — 채점 제외» 표기). 근거: R03·R04 가 «수치로는 도달 불가» 를 구조로 실측했다 — 바닥은 «전체 클리어율 −5~10%p» 에 고정(고유 특전이라 약한 칸을 쓴 손해가 일정)이고, 위를 내리면 다음 것이 그 자리를 물려받으며(두더지 잡기 3회 실측), 폭 25%p 는 «어떤 특전도 승률 82~85% 를 넘지 않는다» 를 뜻하는데 그것은 최우선인 실험1 등급 과녁을 즉시 깬다(계수 0.10 단독 적용 시 희귀 21.7 → 9.0).
  - **배점 재조정(«배점 재조정은 주인 몫» 조항 이행) — 실험2 의 1점을 실험5(스탯 사다리)로 옮긴다.** 새 배점 **실험1 2 · 실험2 0(측정·보고) · 실험3 3 · 실험4 2 · 실험5 3 = 총 10점**. 주인 확정 게이트 «두 비평가 각 ≥8/10» 문면은 **그대로 둔다**(만점을 9로 낮추면 게이트가 ≥8/9 로 조여져 확정 문면이 깨진다 — 삭감이 아니라 이관이다). 실험5 판정은 **과녁 7점 각 2~10%, 합격 1점당 3/7점**.
  - **왜 실험5 로 옮기나** — 주인 확정 과녁은 사다리 7점뿐이고(2026-09-02 15:1X), 승인 41번 우선순위(실험1 > 실험5 > 실험3)에서 실험1 다음이며, R04 인계대로 **챕터 30 «위» 구간 성장률(30→50·50→70·70→120·120→260)이라는 실험1·2 하니스를 한 칸도 안 건드리는 깨끗한 노브**가 남아 있다. 실험1 은 이미 만점이라 그쪽으로 옮기면 점수 인플레만 된다.
  - **되돌림(1줄)**: v5 → v4 = 배점 2/1/3/2/2 복귀 + 실험2 채점 대상 `EXP2_SCORE_RAR=[1,2]` 판정 복원.

  **② 승인 42번 = ⓐ (등급 고정 실험에서는 악마도 그 등급만 준다).**
  - 자가 이름대로 «일반 특전만» 이 되어야 한다. 지금은 실험1 «일반» 칸 8.7% 중 **4.1%p** 와 사다리 저챕터 두 칸의 대부분이 «악마가 준 전설 1장» 에서 나온다 — 측정 대상(일반 특전 강도)이 아닌 것을 재고 있으므로 자 파손으로 본다. ⓒ(현행 유지)는 그 오염을 정본으로 굳히는 것이라 기각, ⓑ(악마 건너뛰기)는 최대체력 차감까지 함께 빠져 **실제 플레이와 두 번째 축에서 어긋나므로** 기각한다. ⓐ 는 챕터 구조(악마 1개·항상 수락·최대체력 차감)를 실제 플레이와 똑같이 두고 **«악마가 주는 특전의 등급만» 고정을 따르게 하는 최소 변경**이다.
  - **실제 게임 동작(악마 = 전설 확정)은 그대로다** — `rarityLock` 이 걸린 측정 런에서만 적용된다. 게이트에 «게임 축은 불변» 단언을 함께 넣을 것.
  - **과녁 재적합은 P3 몫** — ⓐ 적용 시 실험1 3/3 → 2/3(일반 8.7 → 4.6) · 사다리 3/7 → 2/7 로 내려가는 것이 정상이다. **이것은 퇴보가 아니라 자를 고친 결과**이므로 회차 로그에 그대로 적고, 재적합은 기준선 재측정 뒤 시작한다.

  **③ 이행 순서 (P3 재개 첫 회차 · T88 효과 감사 완료 후)** — ⓐ 채점표 v5 반영 → ⓑ **42번 ⓐ 적용(악마도 등급 고정)** → ⓒ **기준선 재측정 한 번**(효과 감사 수리분 + 악마 최대체력 차감 + 자 개정이 전부 들어간 뒤 · 회차 로그에 «효과감사 반영»·«자 개정 반영» 표시) → ⓓ 그 위에서 사다리 회복(30→50·50→70·70→120 완만 · 120→260 가파르게 — 전부 챕터 30 «위» 라 실험1·2 하니스 불변). **재측정을 두 번 하지 말 것 — 자 개정을 먼저 다 넣고 한 번만 잰다.**
  - 이행 범위: `PLAN.md` §7 채점표 v5·실험2·실험5 절 문면 · `sim.js` 실험2 출력 문면과 `rarityLock` 악마 분기 · `tools/verifyScoreCriteria.js` 단언 교체(실험2 «채점 제외» · 실험5 배점 3점 · 악마 등급 고정, 각각 음성 검사) · 승인 대기 40·42번 종결 처리.
- **(2026-09-03 KST) 경험치 바에 «레벨» 쓰지 말 것 (주인 지시 — 이행 완료, T98)** — 전투 HUD 경험치 바의 `Lv«레벨»` 캡을 참고 스크린샷대로 **«EXP»** 로 교체했다. 레벨 시스템 자체(필요 경험치 `4+4*Lv` · 특전 3택 · 레벨업 팝업)는 그대로다 — 없앤 것은 표시뿐이다. `index.html` 만 수정(**`sim.js` 무수정 — T1 회차 무효 사유 아님 · 밸런스 영향 0**). 상세 PLAN §2.4.

- **(2026-09-03 KST) 주인 실플레이 관측 기록 (지금 조치 말 것 — 재측정 대조용)** — 배포 빌드에서 **노템 + 혼합 특전으로 챕터 9까지 수월하게 클리어됨**. 사다리 기준(노템 = 챕터 5 «겨우», 일반 특전만 자)과 어긋나 보이는 관측. 주인 결정: **지금 난이도 조정 금지, 냅둔다** — 효과 감사·오버킬 계수·악마 최대체력 차감이 반영된 뒤의 **기준선 재측정에서 이 관측과 사다리를 대조**하고, 그때도 노템이 물러 있으면 P3 가 초반 곡선을 조인다. 혼합 특전 캐리분(사다리는 일반만 자)을 감안해 판정할 것.

- **(2026-09-03 KST) 처형 특전 판정 정정 (주인 확정)** — 전설 🩸 «체력 40% 이하인 적 즉시 처치(보스 포함)» 는 **«40% 미만이 되는 순간 자동으로 죽는» 오라가 아니라, «내가 공격해서 적중한 순간 그 적의 체력이 40% 미만이면 그 타격이 즉사»** 다. 판정 위치 = 타격 적중 처리 안(피해 적용 직전 체력 기준 — 위임). 소환 적중도 공격 판정이므로 포함(위임). 텍스트도 «체력 40% 미만인 적을 공격하면 즉시 처치» 로 갱신. sim·게임 대조 + 게이트 단언(«비접촉 자동 처형 0건»). 효과 감사(진행 중)에 이 판정 검사 포함.

- **(2026-09-03 KST) 악마 이벤트 2건 (주인 확정)**
  ① **거래 비용 = 최대체력 차감**: 현재체력에서 깎는 게 아니라 **최대체력의 30%를 최대치에서 깎는다** (그 판 동안 최대체력이 줄어든 채 진행. 현재체력이 새 최대치를 넘으면 최대치로 클램프 — 위임). sim.js·index.html 동시 반영, PLAN §2.4 악마 문면 갱신, 게이트 단언 추가. **밸런스 영향 있음 — 효과 감사 후 기준선 재측정에 포함.**
  ② **시뮬 이벤트 정책 확정**: 밸런스 시뮬의 가상 플레이어는 **악마 = 항상 수락 · 천사 = 항상 왼쪽(무료 공격력 +5%)** 선택 (측정 조건 통일 — 쉼터 «항상 경험치» 와 같은 축). 게이트 단언 추가. **승인 대기 32번(시뮬 악마 수락 정책) 이 답으로 종결.** — **✅ 완료 (2026-09-03T06:36~07:4XZ · T90 · sess-0636-7671 / 워커 C): 두 엔진 동시 반영.** ① 비용을 두 엔진 공통 상수 `DEVIL_COST=0.30` · 공통 동사 `payDevilCost(p)` 로 모았다 — `p.maxHp = max(1, p.maxHp − p.maxHp*DEVIL_COST)` 뒤 `p.hp = min(p.hp, p.maxHp)` **내림 클램프**. **실질 차이는 저체력에서 드러난다** — 체력 100/최대 1000 이면 옛 구현은 현재체력이 **1** 이 됐고 지금은 **100/700** 이다(현재체력은 한 점도 안 깎인다). 최대 체력 증가는 금지축이라 그 판 동안 되돌릴 방법이 없다. 게임 지불 버튼 안내도 «최대 체력이 줄어든 채 진행 · 위 특전 획득» 으로 고쳤다. ② 악마 수락 조건 `if(!G.noPerk && p.hp>p.maxHp*0.65)` → `if(!G.noPerk)` 로 **항상 수락**, 천사는 값(1.05) 그대로 두고 `SIM_ANGEL_POLICY` 로 «항상 왼쪽» 명문화. `noPerk` 측정은 여전히 건너뛴다. **게임은 악마 2택·천사 2택 그대로** — 정책은 시뮬 전용이고 게이트가 양방향으로 감시한다. 게이트 `tools/verifyDevilPolicy.js` 신설 **33/33**(정적 6절 + vm 실행 단언 2절 — 저체력 100/1000 → 100/700 실측 · 40판 수락 40회 · noPerk 10판 수락 0회), **음성 13/13 exit 1**. 옛 코드 모양을 물고 있던 `verifyPerkPolicy` ⑤⑦ 도 함께 갱신. PLAN §7 에 시뮬 이벤트 정책 조항 추가. **밸런스 실측(시드 11·12·13 · 전→후): 실험1 등급 과녁 일반 9.1 → 8.7 · 희귀 18.1 → 21.7 · 전설 81.9 → 84.4 = 합격 3/3 → 3/3(과녁을 깨지 않았다) · 실험5 사다리 2/7 → 3/7**(일반 ch15 10.4 → 7.7 로 합격 구간 진입). 두 힘이 반대로 걸린다 — 항상 수락이 판마다 전설 1장을 얹어 쉬워지게 하고, 최대치 차감이 그 판 내내 무겁게 한다(저챕터는 후자가, 챕터 30 이상은 전자가 이긴다). **⚑ 주인 조항대로 이 수치는 «반영 확인» 이지 기준선이 아니다 — P3 재개 첫 회차가 T88 과 함께 반영된 상태에서 기준선을 다시 잰다.** **⚑ 파생 발견 1건 → T91 · 승인 대기 42번**: 등급 고정 실험(실험1·실험5)에서 **악마만은 등급 고정을 안 따르고 언제나 전설을 준다** — «일반만 뜨는 런» 이 사실 «일반 + 전설 1장» 이다. 진단 측정에서 실험1 일반 8.7 → **4.6**, 사다리 일반 ch15 7.7 → **0.7** 로 내려간다. 자(尺) 변경이라 엔진은 손대지 않았고 **P3 기준선 재측정 전에 주인 답이 필요하다**. 근거 `docs/balance/T90/raw.md`.

- **(2026-09-03 KST) ⚑⚑ 주인 순서 지시: 아래 132종 효과 감사를 먼저 끝내고 나서 밸런스(P3) — P3 는 감사 완료까지 신규 회차 금지** (진행 중 회차는 마감만). 효과 미적용이 있는 채로 잰 측정은 못 믿으므로, **감사에서 수리가 1건이라도 나오면 P3 재개 첫 회차는 기준선 재측정부터** (회차 로그 «효과감사 반영» 표시).

- **(2026-09-03 KST) 🔴 주인 버그 보고 — 🃏🔁 «보유 특전 1개당 반격 확률 +2%» 가 적용 안 되는 느낌 (배포 빌드).** 다음 빈 번호로 등재, **최우선 착수** (위 순서 지시에 따라 P3 보다 먼저):
  ① **재현 먼저** — index.html 에서 해당 특전 획득 후 특전 수가 늘 때 실효 반격 확률이 실제로 +2%/개 오르는지 계측 (sim.js 도 동일 확인).
  ② 의심 지점: 수집가 4종(공/회피/반격/방어)은 «보유 특전 수» 를 실시간 참조해야 하는 동적 패시브라, 획득 시점 1회 계산으로 굳거나·반격 굴림이 기본치만 읽거나·HUD 만 갱신 안 되는 세 갈래 중 하나일 것. **4종 전부** 같은 경로 점검.
  ③ 수리 후 게이트: «수집가 4종 — 특전 N개 보유 시 실효치 = 기본 + 2N» 실행 단언 신설 (두 엔진 대조).
  ④ HUD 스탯 그리드가 실효치(버프·수집가 포함)를 보여주는지도 확인 — 주인이 «느낌» 으로 알아챈 것 자체가 표시 문제일 수 있다.
  ⑤ **⚑⚑ 주인 추가 지시 — 132종 전수 «효과 적용» 감사**: 기존 발동 게이트(verifyPerkFire)는 «트리거가 굴렀나» 만 본다 — 수집가 건이 보여주듯 **굴렀는데 효과가 안 먹는 경로**는 못 잡는다. 132종 각각에 대해 **효과가 실제로 적용됐는지를 실측**하라: 스탯형 = 실효치 변화량, 데미지형 = 데미지 증분, 회복형 = 체력/실드 변화, 기절형 = 적 공격 정지 시간, 방어막형 = 장수 증감·피해 무효, 소환형 = 투사체 생성+피해 적용. 두 엔진(sim.js·index.html) 모두, 안 먹는 것 전부 수리 + **«효과 적용» 상시 게이트로 승격** (한 종이라도 무효화되면 빨강). 규모가 크면 다단계 이어받기 허용.

- **(2026-09-03 KST) 특전 선택창 «보유 특전» 버튼 (주인 지시 — UI, 다음 빈 번호로 등재)** — 레벨업 특전 선택 화면 **오른쪽 하단에 보유 특전 정보 버튼**(📘 아이콘류) 추가: 누르면 이번 런에서 얻은 특전 목록(기존 Info 팝업 내용)을 보여주고, 닫으면 선택창으로 복귀 — **선택지를 고르기 전에 내가 뭘 갖고 있는지 확인**하는 용도. 선택창의 카드/새로고침과 겹치지 않는 위치, 목록이 열려 있는 동안에도 시간 정지 유지. sim 무관(UI 전용, 밸런스 영향 0 — P3 회차와 병행 가능하나 **1워커 규약 준수: P3 lock 이 있으면 대기**). index.html 만 수정 + T3 에 확인 항목 추가. — **✅ 완료 (2026-09-03T06:2X~07:0XZ · T89 · sess-0626-20423 / 워커 B): `index.html` 한 파일만 수정(`sim.js`·`PLAN.md` 무수정 = 밸런스 영향 0).** ① 선택창 맨 아래 오른쪽 정렬 줄(`.ov-foot`)에 **📘 보유 특전 N** 버튼 — **흐름 배치**라 카드·새로고침과 «구조적으로» 안 겹친다(절대 배치로 띄우면 좁은 화면에서 스크롤된 카드 위에 올라탄다. 실측 390×844 에서 오른쪽 여백 2px·카드 아래). ② **핵심 설계 = 그리기(paint)와 굴리기(roll)의 분리** — 책에서 돌아올 때 다시 굴리면 선택지가 바뀌어 «보고 나서 고른다» 가 성립하지 않고 무료 무한 새로고침이 된다. `rollPerks` 는 `roll()` 안에서만 불린다(실측: 복귀 후 3장 문면·새로고침 잔여 1 그대로). ③ 책은 `openOverlay` 로 **내용만** 갈아끼운다 — `closeOverlay` 를 안 거치므로 **`G.paused` 가 한 프레임도 안 풀린다**(T79 규약. 실측 `G.t` 16.199 → 16.199 불변). 복귀는 «← 특전 선택으로» 버튼 또는 배경 탭. ④ **T3 실측으로 버그 1건 잡음 — 버튼 클릭이 핸들러 «뒤» 오버레이까지 거품처럼 올라가, 책이 방금 깔아 둔 `ov.onclick`(=복귀)에 걸려 열자마자 선택창으로 되돌아갔다**(목록 0장 관측). `stopPropagation` 으로 수리, 게이트 ③ 이 이 한 줄을 상시 감시. ⑤ HUD 📘·미리보기 줄로 여는 «평소의 책» 은 종전 그대로(탭하면 닫힘) — 복귀 모드는 `typeof back === 'function'` 으로만 갈린다. 게이트 `verifyT2` **㊵** 신설 9항목(음성 **12/12** exit 1) + T3 `battle.js` **실측 10항목** 추가(36 → 48 · 4스위트 합 197 → **209/209**). 정적 게이트 22종 exit 0 · 헤드리스 `verifyPerkFireHtml` 위반 0.

- **(2026-09-03 KST) ⚑⚑⚑ 주인 답 — 승인 38·39·40·41번 전부 결정. P3 R04 착수 가능.**
  - **40번 = ⓐ+ⓔ 승인 (주인 원문 «조건 발생 빈도 바꾸고 계수 허용도 하면 되지, ㄱㄱ»)**: ①**구조형 특전에 계수 부착 허용** — 오버킬 회복 2종(🩸·🔧⚡)에 «초과 데미지의 n%» 계수, 기타 구조형(도끼 3회전 수, 반격 연쇄 등)에도 노브 부착 가능 ②**바닥 특전의 조건 발생 빈도 조정 허용** — 트리거 조건 완화(예: «5회마다»→«3회마다», «3연속 회피»→«2연속», 기절 지속·발동 확률 상향 등). 단: **효과의 정체성(문장 골격·컨셉) 유지 · 깔끔한 숫자 규칙 준수 · 일반 44종 동결 불변**(조건 빈도 조정도 희귀·전설만). 이렇게 해도 폭 25%p 에 못 닿으면 잔여는 ⓓ(측정·보고만)로 접는다.
  - **38번 = A안 승인**: 스탯 사다리를 «일반 특전만 뜨는 런» 으로 재정의 (실험1 과 자 공유). PLAN §7·§11.7 문면 갱신.
  - **39번 = ⓐ 승인**: 일반 등급 폭은 채점에서 제외, 실험2 1점을 희귀·전설 각 1/2점으로 재배분 (동결 불변).
  - **41번 = 등급 과녁(실험1) 최우선 + 실험3 은 벽 배수(국소 노브)로 회복**: 벽 배수는 벽 챕터에만 걸려 챕터30 측정과 독립 — 이걸로 벽 시도수를 되살린다. 그래도 상충이 남으면 실험1 > 실험5 > 실험3 순.
  - 이로써 P3 차단 해제 — R04 부터 위 승인 반영, 회차 로그에 «승인38-41 반영» 표시.

- **(2026-09-03 KST) 일반 44종 동결 게이트 신설 지시 (주인 재확인)** — P1 이식 완료 시 **게이트 신설: `docs/perk-redesign.md` 일반 44종의 표시 텍스트·수치 ↔ PLAN §3.1 ↔ sim.js ↔ index.html 4자 대조, 한 글자라도 다르면 exit 1.** 밸런스 회차가 일반 특전을 만지면 이 게이트가 빨개진다. perk-redesign.md 의 일반 절은 주인만 고칠 수 있다(워커 수정 금지).

- **(2026-09-03 KST) ⚑ 밸런스 국면 전역 1워커 규약 (주인 확정)** — **P3(밸런스 루프) 동안은 동시 작업 1개만**: 다른 워커의 lock 이 하나라도 살아 있으면, 새로 깬 워커는 **곁다리 작업(검증·게이트 신설·신규 등재 포함) 일절 없이 즉시 조용히 종료**한다. 앞 세션이 회차를 완전히 마감하고 lock 을 지운 뒤에만 다음 워커가 이어받는다 (루틴 4개의 15분 슬롯은 «빈자리 확인 주기» 역할). 90분 사망 lock 탈취 규칙은 유지(죽은 세션 복구용). P1(이식)·P2(검증)는 기존 규약(단계 lock + 다단계 이어받기) 그대로 — 순차 회차가 생명인 밸런스에만 적용.

- **(2026-09-03 KST) ⚑⚑⚑ ▶ 재가동 — 새 특전 132종 체제로 전환, 비평가 8점까지 (직전 ⏸ 전면 정지 해제).**
  - **특전 정본 = `docs/perk-redesign.md` (일반 44 · 희귀 46 · 전설 42, 신화 폐지·새로고침/전지의눈 삭제).** 작업 순서 (각 단계 = 별도 작업 번호로 등재·lock 선점, **앞 단계 PROGRESS 완료 표시 전 다음 단계 선점 금지**):
    1. **P1 — PLAN §3 교체 + 구 132종 → 신 132종 전면 이식** (sim.js·index.html 동시, 신화 등급·등급 확률·악마 이벤트(전설 확정으로)·👼 계열 정리, 선택지 3개 고정·무료 새로고침 1회 유지, 게이트 전면 갱신. 규모 크므로 T2 방식 다단계 이어받기 허용) — **✅ 완료 (2026-09-02T21:3X~23:0XZ · T83 · sess-2129-19196 / 워커 C, 4단계 한 세션).** PLAN §3 전면 교체(정본 문장 그대로 132/132) → `sim.js` → `index.html` → 게이트. **핵심 설계 = 네임스페이스 분리** — 구 128종이 쓰던 짧은 px 키를 장비 계열 옵션(`GOPT`)이 68종 그대로 쓰므로 지우지 않고 남기고, 새 특전은 **px 키 = 특전 id** 를 쓴다. 덕분에 특전 교체가 장비 밸런스를 건드리지 않고, 종전 «특전↔장비 중복 사장 18종» 도 0 이 됐다. 반영: 등급 3단 45/35/20 · 획득 중복 금지(전 특전 고유) · 버프/방어막 무한 중첩 · 선택지 3개 고정(🔮 삭제) · 악마 전설 확정 · 소환 데미지 주인 확정 상수 전역 적용(도끼 .30/화살 .50/검기 .50/번개 .75/창 1.00) · 소환 발사 함수가 발수 인자를 받는다. **주인 지시 «일반 44종 동결 게이트» 도 이 단계에서 신설**(`tools/verifyCommonFreeze.js` — 4자 대조, 음성 7/7). **게이트 18종 중 17 초록**, 남은 빨강은 `verifyHarness` 뿐이고 이는 **P3 첫 회차가 재선정할 하니스 드리프트**다. **⚑ P3 인계 실측**: 실험1 과녁 **0/3**(일반 78.3 ↔ 과녁 10 · 희귀 72.7 ↔ 20 · 전설 98.3 ↔ 80) · 실험5 사다리 **0/7** — **난이도가 통째로 모자라다.** 근거 `docs/balance/T83/raw.md`. **⚠ 주인 판단 대기 2건**: ① **소환 연쇄 임계 B ≤ 0.8(T78 주인 확정)을 새 확정 목록이 구조적으로 어긴다** — 실측 B **2.791**(「공격 시」 소환 6종 + 개조 3종의 합. 132종 보유 시 **평타를 한 번도 안 치고** 자동 소환만으로 챕터가 끝난다). 다만 새 과녁 «전설만 = 80%» 와 방향이 같아 임계를 폐기할지·확률을 내릴지는 주인 몫이다(게이트는 기준선 2.90 으로 회귀만 막는다). ② **일반 13 🛡️⚔️ «실드가 남아있는 동안 공격력 +20%» 와 희귀 22 🎇 «실드가 있으면 공격력 +20%» 가 효과 완전 동일** — 정본대로 둘 다 구현했고 동시 보유 시 곱으로 쌓인다.
    2. **P2 — 동작 검증** (T3 스위트 재실행 + 신 특전 전수 발동 확인)
    3. **P3 — 밸런스 루프**: 위 «새 132종 밸런싱 방법» 그대로 — ①일반 44 동결 상태에서 적 노브로 일반 10% 영점 ②난이도 동결, 희귀 20%·전설 80% 를 특전 수치로 ③등급 내 폭 <25%p(희귀·전설만 조정) ④사다리 7과녁·진행곡선·F2P 재검증 → **비평가 2인 독립 채점 각 ≥8/10 까지 회차 반복** (채점표는 새 과녁 기준으로 P3 첫 회차가 개정·등재)
  - **동시 작업 규약 재강조 (주인 지시 «서로 안 꼬이게»)**: lock 규약(docs/claims/) 엄수 — 선점 없이 작업 금지, P단계 간 선점 게이트 준수, sim.js·index.html·PLAN 을 만지는 작업은 반드시 단독 lock, 검증-전용 세션은 커밋 없이 종료. push 경쟁 시 rebase 후 자기 lock 생존 확인.

- **(2026-09-03 KST) ⚑⚑⚑ 새 특전 132종 밸런싱 방법 확정 (주인) — 일반이 절대 기준.**
  - **일반 44종은 내용·수치 전부 동결 (주인 확정)** — 밸런스 튜닝으로 일반 특전을 건드리는 것 금지. 킬힐 5% 기준점 조항을 이 조항이 확대 대체한다.
  - **등급 강제 클리어율 과녁 개정: 일반만 = 10% · 희귀만 = 20% · 전설만 = 80%** (종전 15/25/35/45 폐기 — 신화 없음. 전설 80 은 «전설 뜨면 판이 뒤집힌다» 는 주인 의도).
  - **순서**: ① 일반 특전만 뜨는 기준 플레이어(희귀 풀셋)가 해당 챕터에서 **10%** 가 되도록 **적 밸런스(난이도 노브)로만** 영점 조정 (적 ×10 시작점 조항 유지) → ② 난이도 동결 후 **희귀 20% · 전설 80%** 를 각 등급 **특전 수치로만** 달성 → ③ 등급 내 밸런스(폭 <25%p — 일반은 동결이라 측정·보고만, 조정은 희귀·전설만). 노브 교차 금지 유지.
  - 이 지시는 «재적합 순서 확정» 및 «과녁 15/25/35/45» 조항을 대체한다. **실행은 여전히 보류** — 새 132종의 PLAN 반영·구현(T2급) 완료 후, 주인이 밸런스 시작을 지시할 때 착수.

- **(2026-09-03 KST) ⏸ 전면 정지 — 주인이 특전을 대화형 세션에서 직접 재작성 중.** 루틴 4개 비활성화됨. 진행 중이던 세션은 현 작업만 마감하고 lock 해제 후 종료. **T1·특전 수치 반영 작업 전부 보류** — 특전 목록이 통째로 바뀔 예정이라 지금 튜닝은 낭비다. 주인 확정 규칙들(킬힐5 기준·적 ×10 시작점·중복금지·무한중첩·재적합 순서 등)은 유효하며 새 목록 확정 후 그 위에서 재개한다.

- **(2026-09-03 KST) ⚑⚑ 특전 획득 중복 금지 + 버프 중첩 무한 (주인 확정 2건)**
  - **① 한 번 선택한 특전은 그 런에서 다시 등장 금지** — 사실상 전 특전 고유화. «(중첩) = 여러 번 획득» 메커니즘 폐지(획득 수 n 배율 로직 제거, 표기에서 (중첩) 삭제). 풀이 모자라면 가능한 수만큼만 표시(기존 고유 규칙 준용).
  - **② 전투 중 버프 중첩 상한 전부 삭제 — 무한 중첩** («최대 3중첩/5중첩» 전부 제거. 발동될 때마다 계속 쌓임, 각자 자기 시간 끝나면 빠짐). 방어막 장수 상한(5장)도 삭제 — 무한(위임, 같은 취지). ⚠ 이에 따라 신화 📚👑 «축적의 대가(중첩 상한 +5)» 는 대상 소멸 — **삭제**(신화 30종이 됨, 편차 3 문제없음. 대체 신설은 주인이 원할 때만).
  - 획득 중복 금지(하향 요소)와 무한 중첩(상향 요소)이 상쇄 방향이라 **같은 회차에 묶어 반영** 후 측정. sim·게임 동시, 게이트 갱신, 회차 로그 «중복금지·무한중첩 반영».

- **(2026-09-03 KST) 킬힐5기준 재적합 순서 확정 (주인 합의)** — ① **일반 영점**: 특전 체급 재설정 + 적 ×10 시작값으로, «일반만 뜨는 기준 플레이어(희귀 풀셋·챕터30) = 15%» 를 **난이도 노브로만** 맞춘다 ② 난이도 동결 후 **희귀 25 → 전설 35 → 신화 45** 를 각 등급 **특전 확률·수치로만** 순차 달성 ③ 각 등급 내 폭 재분배(<25%p). 한 단계 통과 전에 다음 단계 노브 건드리지 말 것 (노브 교차 금지 — 꼬임 방지). 각 단계 통과를 회차 로그에 표시.

- **(2026-09-03 KST) 킬힐5기준 난이도 재적합 시작점 (주인 지시)** — «적 스탯 10배로 늘려볼까» → **적 HP·DMG 기본치 ×10 을 첫 시도값**으로 놓고 사다리 7과녁(§11.7) 재적합을 시작하라. 10배는 주인 확정 상수가 아니라 **출발점** — 사다리가 성립하는 값으로 자유 조정(과하면 내리고 모자라면 더 올림), 최종 채택값과 10배 대비 어디로 갔는지를 회차 로그에 명시. 특전 읽히는 체급 재설정(킬힐 5% 기준)과 같은 회차에서 묶어서 진행할 것 — 한쪽만 먼저 반영된 상태로 채점 금지.

- **(2026-09-03 KST) ⚑⚑ 밸런싱 방향 전환 (주인 확정) — «처치 시 체력 5% 회복» 을 기준점으로, 특전을 깎지 말고 난이도를 올려라.** 주인 원문: «처치 시 체력 회복 5%로 하고 그거 기준으로 맞추쇼. 그러려면 챕터 난이도를 위로 올려야 하는데 그렇게 해줘야 함. 지금(0.37% 따위) 너무 극단적으로 구림.»
  - **🍖 `c_killHeal2` = «처치 시 체력 5% 회복» 고정 (주인 확정 상수 — 튜닝 노브 아님).**
  - **원칙 전환**: 특전이 세서 밸런스가 안 맞으면 **특전 수치를 소수점(0.37%·0.5%·0.55%)으로 깎는 것 금지** — 특전은 읽히는 체급(수치 5% 단위·확률 10% 단위·기절 3/6초)을 유지하고, 남는 강함은 **적 난이도(구간 성장률·기본치·벽 배수)를 올려서** 흡수한다. 난이도 노브는 §7 위임 그대로.
  - 기존 소수점 특전들(실드 0.5%·전설 0.55%/0.75% 등)도 킬힐 5% 체급을 기준 삼아 **읽히는 값으로 재설정** (전설은 일반보다 좋게 — 확률·수치로 차등).
  - 스탯 사다리 7과녁(§11.7)은 불변 — 난이도 상향은 사다리를 지나는 곡선 재적합으로. 반영 회차부터 «킬힐5기준 반영» 표시, 반영 전 회차 점수는 참고치로 격하.
  - **✅ 1단계 완료 (2026-09-02T20:5XZ · T82 · sess-2036-499 / 워커 C) — 수치 재설정·게이트·실측까지. 난이도 재적합은 T1 몫으로 남았다(T1 lock 이 살아 있어 §7 노브를 건드리지 않았다).** 🍖 `c_killHeal2` 0.37 → **5%** 확정 상수 · 🔰 `c_killShield3` 0.5 → **5%** · 💉 `l_killHeal5` 0.55 → **10%** · 🏯 `l_killShield10` 0.75 → **10%**(전설 = 일반의 2배). 두 엔진·PLAN §3 동시. 게이트 `verifyCombatConst` **⑧** 신설(102항목) — 확정 상수·5% 배수·등급 차등·두 엔진·표시 텍스트 + **`onKill` 실행 단언**, 음성 9/9 exit 1. `verifyNumClean` 래칫 동결분에서 이 4종을 빼 소수점 회귀가 곧장 빨개진다. 실측: 실험1 일반 10.9 → **45.9%** · 전설 64.1 → 76.2 · 사다리 신화 120 이 1.2 → 5.3 으로 **합격 진입(1/7)**. 하니스 재보정(실험1 38.3 → 55.0 · 실험2 62.7 → 70.3, 재선정 불요). **⚑ 남은 절반 = 난이도 상향인데, 균일 상향으로는 과녁이 안 잡힌다** — 처치 회복 축이 일반 2종·전설 2종뿐이고 **희귀 0종**이라 기저 ×1.2 에서 일반 25.3 ↔ 희귀 4.7 로 희귀가 먼저 죽는다. 해법 3안을 **주인 승인 대기 37번**에 등재(추천 = 희귀에 같은 축 1종 신설, 신규 콘텐츠라 승인 전 구현 금지). T1 은 다음 회차부터 «킬힐5기준 반영» 표시. 근거 `docs/balance/T82/raw.md`.**

- **(2026-09-03 KST) 주기형 회복 금지 (주인 확정)** — **«N초마다 체력 회복/실드 수리» 류 시간 경과형 회복은 특전·장비 옵션에 존재 금지** (현행 132종엔 원래 없음 — 예방 조항. 뇌신처럼 시간형 «공격» 은 허용). 게이트 금지어 검사에 추가. 아울러 «이벤트/팝업 열림 중 게임 시간 완전 정지» 는 불변 규약으로 승격 — 회귀 시 게이트가 빨개지게 단언 추가(쉼터 무한 대기 악용 원천 차단). — **✅ 완료 (2026-09-02T19:XXZ · T79 · sess-0336-20926 / 워커 C): 둘 다 «이미 그렇게 돌고 있었다» 를 먼저 확인하고(주기형 회복 0건 — 주기 블록 4개가 전부 뇌신·위압·적 공격 = 허용 축 / 팝업 중 정지도 이미 동작) **예방·규약 게이트만 신설**했다. **엔진 무수정 = 밸런스 영향 0.** `verifyT2` **㊳** — 낱말이 아니라 구조로 본다: 주기 패턴 `X-=dt; if(X<=0){…}` 의 **몸통에 회복·실드 충전이 들어가면 빨개진다**(문구 축은 태그를 걷어낸 뒤 검사 — «3초마다 체력 `<b>`3%`</b>` 회복» 우회 차단). `verifyT2` **㊴** — 오버레이를 켜는 자리가 `openOverlay` 하나뿐이고 거기서 정지 · 루프가 정지 중 `update` 를 안 부름 · `G.t` 를 미는 자리 1곳. T3 `battle.js` **실측 7항목** — 팝업을 띄운 채 800ms 동안 48프레임이 그려져도 시계·체력·실드·쿨다운·적 좌표가 전부 불변, 닫으면 다시 흐른다. 음성 정적 8/8 + **실행 1/1**(루프 가드를 지우면 실측이 빨개진다). PLAN §2.4·§3.0 에 규칙 기재. 근거 `docs/balance/T79/`.**

- **(2026-09-03 KST) 소환 연쇄 임계 규칙 (주인 우려 → 밸런스 강제 조건)** — 연쇄 메커니즘(소환 적중 = 공격 판정)은 유지하되, **«소환 1발의 적중이 낳는 신규 소환 기대값 ≤ 0.8» 을 어떤 특전 조합에서도 유지**해야 한다 (기대값 = Σ 보유 소환 특전의 발동확률 × 발수. 1.0 넘으면 기하급수 폭주 — 도끼폭풍 14개 × 15% = 2.1 이 «신화 뜨면 게임 끝» 의 원인). T1 은 신화 발수 특전(도끼14·화살24·번개20 등)을 이 조건과 신화 과녁 45% 안으로 하향하고, **게이트 신설: 최악 조합(모든 소환+발수 특전 동시 보유) 기대값 ≤ 0.8 단언**. 상세는 이 항목이 정본.

- **(2026-09-03 KST) 전투 무관 특전 전면 삭제 (주인 확정)** — 132종에서 다음 4종 삭제: **🪙 c_gold30(처치 골드 +30%) · 💰 m_gold2(골드 2배) · 🌟 m_sage(처치 경험치 +1) · 🏃 c_walk20(이동속도 +20%)**. sim.js·index.html 동시 삭제, 죽은 코드·게이트 항목 정리. 향후 경제/이속류 특전 추가 금지(흡혈·적중률 금지와 같은 축, 게이트 감시). ※ 🎲새로고침·🔮전지의 눈은 특전 선택의 질 = 전투력이라 유지(위임 — 주인이 빼라면 뺌). 등급별 개수가 일반 31·희귀 33·전설 33·신화 31 로 변함(편차 2, 문제없음). 삭제 후 측정치로만 T1 진행, 회차 로그 «전투무관삭제 반영» 표시. — **✅ 완료 (2026-09-02T19:XXZ · T77 · sess-0336-20926 / 워커 C): 4종을 두 엔진·PLAN §3 에서 동시에 삭제해 **132 → 128종**(일반 31 · 희귀 33 · 전설 33 · 신화 31, 편차 2). `goldMul`·`px.sage`·`walkMul` 은 **장비 옵션이 계속 쓰는 산 코드**라 남겼다(주인 지시는 «특전» 축). 게이트 `verifyT2` **㊲** 신설 — 금지어가 아니라 «특전 ap 가 대입하는 대상이 전부 금지축이면 전투 무관» 이라는 **구조 판정**이라 px 키를 새로 파는 우회도 잡는다(음성 9/9 exit 1, 그중 «m_time 에서 공속을 빼 이속 전용화» 포함). **밸런스 실측**: 실험1 6시드 짝지음 일반 16.55 → **19.07%**(+2.52 ± 0.70) · 신화 80.02 → **81.55**(+1.53 ± 0.86) — 삭제분이 일반 2·신화 2 인데 오른 등급이 정확히 그 둘. 실험2 12,000판 **신화 폭 34.0 → 31.7%p**, 바닥이 `m_gold2` 59.0% → `m_choice4` 62.7% 로 교체(«신화 최하 = 전투 무관 특전» 상태 해소). 사다리 7점은 전후 모두 0/7 로 불변. 게이트 15종 exit 0 · T3 190/190. **주인 검토 1건: 🕰️ `m_time`(공속+이속)은 삭제 목록에 없어 유지 — 이속 성분까지 뺄지 판단 요청.** 근거 `docs/balance/T77/`.**

- **(2026-09-03 KST) ⚑⚑⚑ 주인 최종 결정 — 특전 재설계 전면 폐기, 기존 132종으로 회귀. T1 재개, 1순위 = 등급 내 밸런스.**
  - **아래 «재설계» 계열 지시(신화 등급 폐지·3등급 과녁·73종 조율 15:5X~20차 전부)는 폐기.** 특전은 **현행 132종(신화 33 포함, 신화 11종 삭제 지시도 폐기)** 그대로가 정본 — 코드는 안 바꿨으므로 되돌릴 것도 없다. sim.js·index.html 현행 유지.
  - **T1 즉시 재개, 회차 1순위 = 실험2 «등급 내 밸런스»** (주인 지시: «등급 안 밸런스 맞춰서 하자»): ①측정은 12,000판 이상(노이즈 규명 반영) ②영합 재분배(등급 내 강한 것↓ 약한 것↑, 전체 클리어율 유지) — **신화부터**(폭 35.9%p 최대) ③전투 무관 특전(🪙골드·🎲새로고침)은 바닥 고정이 구조적이므로 수치가 아니라 «전투 보너스를 곁들이는» 소폭 개조 제안까지 허용(주인 검토 등재) ④트리거 빈도 보정(드문 트리거 = 확률/수치 상향).
  - **실험1 과녁 개정(4등급)**: 등급 강제 클리어율 **일반 15% · 희귀 25% · 전설 35% · 신화 45%** (±5%p. 신화 45 는 위임 — 기존 실측 70%는 주인이 «신화 너무 쎔» 이라 했으므로 낮춤. 주인이 바꾸면 그 값으로). 이 과녁이 «단조증가» 기준을 대체.
  - **표기 규칙은 유지**: %p 금지·소수점 금지(0.37% 류는 효과 재설계로 정리)·확률 5~10% 단위·추상 표현 금지. 수치 정리 시 밸런스 등가 유지.
  - 밸런스 루틴 4개 재가동(대화형 세션이 enabled 처리).

- **(2026-09-03 KST) 재설계 20차 — 주인 선택 + 자작 16종 추가**:
  **일반 +5**: 🛡️실드 있는 동안 공격력 +10% · 😤실드 없는 동안 회피 +10% · 🔢4번째 공격마다 공격력 +100% · 🍖회복할 때마다 3초간 방어력 +10% · 💨빗나가면 실드 3% 충전.
  **희귀 +3**: ⛓️기절 끝난 적 3초간 공격속도 -50% · 🎇실드 있으면 공격력 +20% · 😤👑실드 없는 동안 공격력 +50%(주인 원문 «없어지면» — 없는 동안으로 해석, 위임).
  **전설 +8**: 🗡️🌀평타 광역화(대상+바로 뒤) · 🩸체력 20% 이하 적 즉시 처치(**보스 포함** — 주인 명시) · 🦔피격 반사 확정 100% · 🔱⚔️공격 2회마다 창 1개 · 🎭2연속 회피 시 다음 공격 3배 · 🪓🌪️모든 도끼 3회전 3타 · 🎯👑풀피 적에게 데미지 +150% · 🏕️🛡️쉼터 쉴 때마다 방어막 5장(전설 배치 — 위임).
  합계: 일반 24 · 희귀 22 · 전설 27 = **73종** (편차 5). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 19차 — 주인 승인 추가 9종**: 희귀 +4 (🧲 기절 적 데미지 +100% · 🥊 같은 적 연타마다 +10% 누적(대상 바꾸면 초기화) · 🛡️→⚔️ 방어막 소모 시 다음 공격 +100% · 🪞 도끼 1회 튕김) / 전설 +5 (🕳️ 기절 적 데미지 3배 · ⚔️⚔️ 평타 2연타(2타는 50%) · 🌊🔱 검기 8마리 관통화 · 🥶 모든 적 공격속도 -30% 상시 · 🃏 보유 특전 1개당 공격력 +2%). 베이스 48종 + 9 = 57종 (일반 19 · 희귀 19 · 전설 19). 추가 아이디어 90개(등급당 30) 주인 검토 중.

- **(2026-09-03 KST) 재설계 18차 — 주인 자필 초안 확정 베이스 (48종)**: 주인이 직접 쓴 일반 19 · 희귀 15 · 전설 14 목록이 확정 베이스다 (대화형 세션 기록이 정본, 최종 확정 시 PLAN §3 교체). 주인 확인 3건: ①희귀 버프 0개 의도 맞음 ②사신의 낫 = 일반 · 회피 시 20% · **보스 포함** ③전설 «치확 0% 대신 공격력 300%» = 3배. 신규 요소: 오버킬 힐/수리 · 회복↔수리 동기화 · 2초마다 자동 소환 3종 · 화살→창 변환 · 실드 보유 시 50% 데미지 무시 · 힐/수리 효과 +100%. 추가 아이디어(희귀·전설 10개씩)는 주인 검토 중.

- **(2026-09-03 KST) 재설계 주인 수정 17차 — 일반 버프 추가**: «⚡👟 처치 시 2초간 회피 +40%» 를 일반 버프에 추가 (주인 원문 그대로 — 확률 조항 없음 = 처치마다 무조건 발동, 지속 2초로 짧게. 주인이 확률 원하면 추가). 일반 = 16개(버프 4), 희귀·전설 15개, 총 46종. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 16차 — 사신의 낫은 일반 등급**: ☠️🌾(회피 시 10% 즉사, 보스 제외)를 전설 → **일반 기타**로 이동 (일반의 처치 방어막 획득과 맞교환 — 방어막 획득은 전설로 올려 «회피 시 30% 확률로 방어막 1장»). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 15차 — 버프 5→3개씩**: 등급당 = 소환 5 · 버프 3 · 회복 2 · 기타 5 = **15개, 총 45종**. 버프 9개는 트리거·스탯 겹침 최소(엇갈림 유지). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 14차 — 기타 카테고리 발동 조건 전면 재배치**: 기타 15개(3등급×5)의 트리거를 등급 안 5개 전부 다르게 + 전체에서도 같은 트리거 최대 2회(주인 필수 빗나감 2종 제외)로. 기절 사다리 = 공격(일)/치명타(희)/처치(전) — 전설 기절은 «처치 시 가장 가까운 적 기절» 로 이동. 반격 발동 = 화살피격(일)/피격(희). 방어막 획득 = 처치(일)/공격(희). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 13차 — 회복·기타도 트리거 골고루**: 회복 6개(3등급×2)는 트리거 전부 다르게(처치/피격/치명타/회피/반격 + 처치 1회 중복만 허용), 기타도 등급 안에서 트리거 집중 금지(일반 기타가 피격×4였던 것 해소 — 기절은 공격(일)/치명타(희)/피격(전) 사다리로, 방어막 획득은 회피(일)/공격(희)로, 반사는 화살 피격으로 이동). 빗나감 필수 2종이 전설 기타에 같이 있는 것은 주인 지시 산물이라 유지. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 12차** — ① **전설 소환도 전부 1발/1개/1회** (도끼 3개·화살 4발·번개 2회 폐지) — 등급 차이는 **발동 확률**로만 (전설 = 높은 확률·확정). 화살 발수 차등(일1/희2/전2~4)은 이 지시로 개정: 일반 1발·희귀 2발·전설 1발(고확률). ② 💢 빗나감 +100% 스택의 **최대 5개 상한 삭제** (무제한 적립, 적중 1타당 1개 소모는 유지). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 11차 — 버프는 엇갈림 배치**: 공격형 트리거(공격/치명타/처치) → 방어형 스탯(회피/방어) 상승, 방어형 트리거(피격/회피/회복) → 공격형 스탯(공격력/공속/치확/치명뎀) 상승, «공격 시 → 회피 상승» 식 크로스 매칭. 10차의 «조건·스탯 골고루 + 조합 중복 금지» 는 유지. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 10차 — 버프 골고루 매트릭스**: 등급당 버프 5개는 **발동 조건 5개 전부 다르게 + 오르는 스탯 5개 전부 다르게**, 등급 간에도 조합이 겹치지 않게 회전 배치. 스탯 풀 = 공격력/공격속도/치명타 확률/치명타 데미지/방어력/회피 6종, 트리거 풀 = 공격/치명타/처치/피격/회피/반격/빗나감/회복 8종. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 9차 — 최대 체력 증가 특전 전면 삭제**: ❤️최대 체력 +10% 등 최대 체력을 늘리는 특전 금지 (실드 최대치 증가류도 이미 목록에서 빠짐). 빈 자리는 🎯 «체력 가득 찬 적에게 데미지 +20%» 로 대체(위임 — 주인이 바꾸면 교체). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 8차 — 버프도 전부 «n% 확률로» 형식**: 트리거만 있고 확률 없이 무조건 발동하던 버프 특전 전부에 확률 명시(기본 30%). «치명타 시 3초간 +5%» → «치명타 시 30% 확률로 3초간 +5%» 식. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 6차 — 카테고리 구조 확정**: 등급당 = **소환 5 · 버프 5 · 회복 2 · 기타 5 = 17개** (주인 7차 수정: 회복 5→2). 소환 5 = 5종 각 1개(트리거 제각각). 기타 = 기절/반격/반사/방어막/특수. 총 51종. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 5차 — 등급당 20개로 축소 (비율 유지)**: 각 등급 정확히 **20개** = 소환 7 + 기타 13 (직전 소환:기타 ≈ 1:2 비율 유지). 총 60종. 트리거 축 커버리지는 유지하며 중복·유사 효과부터 잘라냄. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 4차 — 소환 특전 3배 확대**: 등급당 소환 특전 **15개(5종 × 트리거 3개씩)**. 같은 소환이라도 트리거가 다르면 별개 특전. 등급 차이는 확률·발수·확정성으로(데미지 고정 불변). **화살 발수도 등급 차등(주인 보강): 일반 1발 · 희귀 2발 · 전설 2~4발.** 등급당 총수가 44~46개로 늘어남(등급 간 골고루 유지). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 3차 — 소환물 배치 개편**: ① 소환 특전이 «공격 시» 에 몰려 있으면 안 됨 — **트리거(공격/치명타/처치/피격/회피/반격/빗나감/원거리피격)에 분산 배치** ② **일반 등급에도 소환 5종(도끼·화살·번개·검기·창) 전부 등장** — 등급 차이는 데미지(고정)가 아니라 발동 확률·트리거 질·발수로. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 2차** — ① «영구» 단어 금지(게임 끝나고도 적용될 것 같은 오해 — 효과는 그 판 한정 그대로, 표기만) ② **시간제 버프는 중첩 상한 없음(무한 중첩)** — «(N중첩)» 표기·로직 전부 삭제 (횟수 소모형 스택(빗나감 +100% 등)의 상한은 유지) ③ 처치 회복/충전은 확정 소량 대신 **«10% 확률로 10%» 식 한방형**으로 (일반: 처치 시 10% 확률 체력/실드 10%, 전설: 20% 확률 15%) ④ 전설 처치 공속도 «10% 확률로 +10%» 식으로 ⑤ 검기 텍스트에서 «대상+뒤 적» 설명 삭제 ⑥ **방어막 장수 상한 삭제(무한)** — 밸런스는 획득 확률로 ⑦ 오발 특전의 «데미지 2배» 삭제 ⑧ 전설 피격 기절 확률 50%→30% ⑨ **삭제 2종**: 방어막 10장+획득 2배 · 기절 지속 2배 ⑩ 희귀 «공속 10중첩» 특전은 무한 중첩 도입으로 무의미해져 삭제. 확정본은 대화형 세션에서 계속 조율 — 최종 시 PLAN §3 교체.

- **(2026-09-03 KST) 재설계 초안 주인 수정 3건 + 개수 명시 규칙** — ① 💠 초과회복 특전: «남는 회복량의 5배 실드 수리» → **«남는 회복량만큼(1배) 실드 수리»** (배수 놀음 금지). ② 🌊🔁 반격 검기: **«검기 1개 발사»** 로 개수 명시 — **일반화 규칙: 모든 소환 특전은 발사 개수를 텍스트에 정확히 쓴다** (도끼 1개·화살 2발·번개 2회·검기 1개·창 1개). ③ 👼: «이후 전설만 등장» → **«이후 희귀 이상만 등장»**. (특전 재설계 확정본은 대화형 세션에서 계속 조율 중 — 최종 확정 시 PLAN §3 전면 교체.)

- **(2026-09-03 KST) ⚑⚑ 특전 재설계 확정 규칙 6개 (주인 확정 — 재설계 지시에 합침).**
  ① **골드 관련 특전 전부 삭제** (🪙 처치 골드 +30% 등).
  ② **즉시 회복 딸린 것 전부 제거** — 최대 체력이 늘어나는 특전은 **최대치만 늘고 회복은 안 됨** (❤️+12%, 💗특전기회마다 등 전부).
  ③ **이동속도 특전 전부 삭제** (🏃 등).
  ④ **소환 데미지 = 원본 스펙 고정 (주인 확정 상수)**: 도끼 = 공격력의 **30%** · 화살 = 2발 각 **50%** · 검기 = **50%** · 번개 = 각 **75%** · 창 = 마리당 **100%**. 튜닝으로 데미지 계수 변경 금지 — 밸런스는 발동 확률(10% 단위)로만 조정.
  ⑤ **기절 지속 = 3초 또는 6초만** (1초/1.5초/2초/2.5초류 전부 3초로 통일. 6초는 «기절 지속 2배» 류 상위 효과의 결과로만).
  ⑥ **방어력·회피 = % 표기·% 의미 (주인 확정)**: 방어력 20% = 받는 데미지 20% 감소, **방어력 100% = 모든 데미지 0**. 회피도 «회피 +40%» 식 % 표기. «방어력 +10» «회피 +40» 같은 단위 없는 표기 전면 금지. ~~기본 스탯(방어 20%·회피 20%)과 동일 스케일~~ **⚑ T123 — 기본 스탯이 방어 0%·회피 0% 가 됐다. 스케일 규칙(«% 표기·% 의미»)은 그대로이고 출발점만 0 이다.** 상한은 밸런스 몫(위임 — 현행 방어 80·회피 90 유지).
  구현·게이트: sim·게임·PLAN·특전 텍스트 전부 이 규칙으로. 위반 검출을 숫자 청결 게이트에 합칠 것.

- **(2026-09-03 KST) ⚑⚑ 플레이어 기본 스탯 주인 확정 + 밸런스 기준점 + 깔끔한 숫자 규칙.**
  - **플레이어 기본 스탯 (주인 확정 상수 — 노브 아님)**: 공격속도 **1.0**/s · ~~치명타 확률 **20%**~~ · 치명타 데미지 **150%** · ~~반격 확률 **20%**~~ · ~~방어력 **20**~~ · ~~회피 **20**~~. (기존 치확5·반격10·방어5·회피8·치배200 전면 교체 — T27 이 지적한 미문서 상수 4종이 이걸로 전부 문서화됨. 공/체/실 기본치는 §11.7 노템 25/150/250 유지.) PLAN §2.3 에 표로 기재, sim·게임 동시 반영, `verifyCombatConst` 갱신.
    **⚑⚑⚑ T123 (2026-09-04 18:5X) 로 넷이 0 이 됐다** — 치명타 확률 **0%** · 반격 확률 **0%** · 방어력 **0** · 회피 **0**. 공격속도 1.0 과 치명타 데미지 150% 만 이 항목의 값 그대로다. 지금 유효한 표는 이 문서 최상단 18:5X 항목과 PLAN §2.3 이다.
  - **밸런스 기준점 (주인 확정)**: 실험 하니스의 표준 장비 = **희귀 풀셋** (공 100/체 500/실 800). 특전 실험(실험1·2)은 «위 기본 스탯 + 희귀 풀셋» 플레이어로 측정하는 것이 정본. 측정 챕터는 변별점 규칙대로.
  - **깔끔한 숫자 규칙 (주인 확정 — 표기·튜닝 공통)**: 특전·장비 옵션의 **확률은 10% 단위**(10/20/30…%, 예외적으로 5% 허용) · 지속시간 0.5초 단위 · 수치 계수 5% 단위 · **소수점 금지** (0.37% 같은 값 금지 — 밸런스가 안 맞으면 소수점으로 깎지 말고 효과 설계 자체를 바꿔라). 게이트에 «숫자 청결» 검사 추가(전 특전·옵션 텍스트 파싱). 재설계 초안·이후 튜닝 모두 이 단위로만 움직인다.

- **(2026-09-03 KST) ⚑⚑⚑ 특전 신화 등급 전면 폐지 + 등급별 클리어율 과녁 확정 (주인 확정 — 재설계 지시에 합침).**
  - **특전 등급 = 일반/희귀/전설 3개.** 신화 특전 33종 전부 삭제(11종 부분 삭제 지시를 흡수·확대). **장비의 신화 등급은 별개 — 유지**(스탯 사다리 체계 불변).
  - **⚑ 실험1 과녁 (주인 확정, «단조증가» 기준 대체)**: 한 등급만 뜨게 강제했을 때 클리어율이 **일반 15% · 희귀 25% · 전설 35%** 수준이어야 함 (허용 오차 ±5%p — 위임). **등급 간 격차에 상한이 생겼으므로 «신화 뻥튀기» 류 인플레 튜닝은 구조적으로 불가.**
  - 연쇄 정리(위임 기본값): 등급 등장 확률 3단 재정의 **일반 45% / 희귀 35% / 전설 20%** · 악마 이벤트 = 전설 확정(신화 15% 조항 삭제) · 👼 전설이상만 특전 = 전설 확정으로 개정 또는 삭제(재설계 초안에서 결정) · 등급통일 굴림도 3단.
  - **주인 필수 특전 중 신화에 있던 것들(사신의 낫·방어막 10장·기절 지배 등)은 삭제가 아니라 전설로 강등 배치 후보** — 재설계 초안에서 전설 목록에 넣어 주인 검토.
  - 일반 등급은 주인과 대화형 세션에서 정리 중(기존 유지 골자) — 재설계 초안은 대화형 세션이 주인과 확정한 것을 받아 문서화·구현한다.

- **(2026-09-03 KST) ⚑⚑⚑ T1 밸런스 루프 전면 정지 — 특전 전체 재설계 먼저 (주인 지시: «밸런스 루프 그만 돌리고 특전부터 싹 다 다시»).**
  - **T1 즉시 정지**: 진행 중인 R05 는 측정만 마감하고 lock 해제. 특전 재설계가 끝나고 주인이 승인할 때까지 **T1 신규 회차 금지** (특전이 다 바뀔 거라 지금 튜닝은 낭비다).
  - **신규 최우선 작업 (다음 빈 번호로 등재): 특전 132종 전면 재설계 초안.** 직전 «신화 대숙청» 지시(11종 삭제·대체 11종)를 **흡수 확대** — 신화만이 아니라 **4등급 전부 백지에서 다시 짠다.**
    - **유지할 것**: 주인이 직접 시킨 필수 특전들(스턴 2종 · 빗맞음 확정크리/+100%스택 · 반사 30% · 원거리 피격 반사/번개/창 · 공속 10중첩 · 횟수형 방어막 · 회피 즉사 사신의 낫) + 트리거 축 구조(공격/치명타/처치/피격/회피/반격/회복/빗맞음/원거리피격) + 등급당 개수 골고루(30~40종, 편차 ≤6).
    - **버릴 것**: 주인이 자른 부류 — 소환 개수 뻥튀기(도끼14개류) · 경제류(골드/경험치) · 부활 · 즉시 회복 붙은 스탯 특전. 유사 계열도 초안에 넣지 말 것.
    - **작성 규칙**: 전부 **게임에 뜰 문장 그대로 + 구체 수치** («%p»·«대폭»·«조금» 금지). 등급이 오르면 같은 축의 수치가 왜 그만큼 좋아지는지 한눈에 보이게(축별 사다리형 설계 권장).
    - **절차: 초안 = 문서만.** PLAN §3 은 건드리지 말고 `docs/perk-redesign.md` 에 전체 목록 초안을 쓰고 PROGRESS «주인 검토» 에 등재 → **주인 승인 후에만** PLAN·sim·게임에 반영. 승인 전 엔진 구현 금지 (직전 지시의 «신화 11종 삭제» 는 이미 확정이므로 그 삭제만 선행 실행 OK).
  - 재설계 승인·구현 후 T1 재개 (그때 회차는 R01 부터 다시, «특전재설계 반영» 표시).

- **(2026-09-03 KST) ⚑⚑ 신화 특전 대숙청 (주인 지시 — «신화 대부분이 좆같네»).**
  - **삭제 11종 (즉시)**: `m_revive`(부활) · `m_clone`(분신) · `m_procX2`(폭풍의 힘) · `m_axe3`(도끼 폭풍) · `m_arrow4`(화살 폭풍) · `m_spear200`(창의 대가) · `m_bolt3`(번개 지배자) · `m_wave4`(검기의 왕) · `m_gold2`(황금 기사) · `m_sage`(현자) · `m_fortress`(요새). sim.js·index.html 동시 삭제, PLAN §3.4 표 갱신, 관련 엔진 훅·게이트 항목 정리(죽은 코드 남기지 말 것).
  - **`m_giant`(거인화) 수정**: «즉시 회복» 부분 제거 — 최대 체력 증가만 (회복은 사기라서 주인이 뺌).
  - **남는 신화 21종** → 등급 균형(≤6 편차) 위반 상태가 되므로, **신화 대체 특전 ~11종 초안을 최우선 작성해 PROGRESS «주인 검토» 에 등재** (승인 전 구현 금지 — 삭제만 먼저). 초안 방향: 이미 승인된 축(스턴·빗맞음·방어막·반사·원거리 피격·고중첩 버프·즉사류) 안에서 «빌드가 보이는» 것들로, 삭제된 것 같은 «소환 개수 뻥튀기»·«경제(골드/경험치)»·«부활» 류는 금지. **모든 수치는 구체적으로**.
  - **표기 규칙 (주인 지시)**: «%p» 표기 전면 금지. 방어력·회피·반격·치확 같은 가산 스탯은 평문으로 («방어력 +8» + 첫 등장 시 «받는 피해를 깎는 수치» 식 짧은 설명). «대폭 증가» 같은 추상 표현도 금지 — 특전 텍스트·문서·보고 전부 구체 수치로. 게이트의 금지어 검사에 «%p» 추가.
  - 밸런스: 삭제 후 사다리·하니스 재측정, 회차 로그 «신화숙청 반영» 표시.

- **(2026-09-02 17:2X KST) 횟수형 방어막 특전 추가 (15:5X 확장에 합침)** — «공격 시 10% 확률로 **적 공격 1회를 완전히 막아주는 방어막** 1장 획득» 필수 포함. **횟수형 스택** — 5장 쌓였으면 5번 막음(피격 1회당 1장 소모, 그 타격 데미지 완전 무효 — 수치형 실드와 별개 축). 스택 상한은 넉넉히(주인: «존나 쌓여서» — 위임 기본값 상한 5, 등급 변형으로 10까지 루틴 초안). 버프 아이콘에 남은 장수 뱃지 표시 + 막을 때 전용 이펙트(위임). 상세 PLAN §3.0. — **✅ 완료 (T48 3단계 · sess-0906-23482 / 워커 A): 플레이어 `ward`(장수) 신설 — 피격 1회당 1장 소모하고 그 타격 데미지를 통째로 무효화한다(방어력·실드·체력을 아예 안 거친다). 막은 타격도 «피격 시» 트리거는 굴린다(주인 원문이 «그 타격 데미지 완전 무효» 라 «맞은 사건» 은 일어난 것으로 봤다 — 위임). 획득 4트리거(공격 10%·치명타 12%·피격 8%·회피 10%)가 전부 `gainWard` 한 곳을 거쳐 상한·신화 배수가 한 자리에서 걸린다. 상한 5장 · 신화 🛡️👑 이 10장 + 획득 확률 2배. 버프 줄 맨 앞에 남은 장수 뱃지(`.ward-ic`) + 막을 때 전용 이펙트. 게이트 `verifyT2` ㉕ · `verifyCombatConst` ⑥, 음성 10/10.**

- **(2026-09-02 17:1X KST) 쉼터 선택지 개정 (주인 확정)** — «최대체력 40% 회복 vs 경험치 +10» → **«체력 260 회복(고정값) vs 경험치 +26»**. sim.js·index.html 동시 반영(시뮬은 여전히 항상 경험치 선택), 게이트 갱신, 회차 로그 «쉼터260/26 반영». 상세 PLAN §2.4.

- **(2026-09-02 17:0X KST) 레벨업 필요 경험치 개정 (주인 확정)** — `expNeed = 4+2*Lv` → **`4+4*Lv`** (레벨마다 +4씩 증가. Lv1→8, Lv2→12 …). sim.js·index.html 동시 반영 + 게이트 상수 갱신. 밸런스 유의미(특전 획득 속도 하락) — 회차 로그에 «경험치4증가 반영» 표시. 상세 PLAN §2.4.

- **(2026-09-02 16:5X KST) 회피 즉사 특전 추가 (15:5X 확장에 합침)** — «회피 시 10% 확률로 공격한 그 적 즉사» 필수 포함(보스 제외 위임). **사신의 낫 전용 연출 필수** — 낫이 베어 죽이는 이펙트, 일반 처치 연기와 구별. 등급 상위(신화급 권장) 배치. 상세 PLAN §3.0. — **✅ 완료 (T48 3단계 · sess-0906-23482 / 워커 A): 신화 ☠️🌾 `m_reaper` — 회피 분기 안에서 10%, 보스 제외(🧨 처형자 선례). 사신의 낫 전용 연출 = 큰 낫이 호를 그리며 훑고 → 붉은 참격선 → 검은 재. 일반 처치 연기(`poof`)를 안 부르므로 확실히 구별되고, 게이트가 «reapFx 안에 poof 없음» 을 단언해 되돌리면 빨개진다.**

- **(2026-09-02 16:4X KST) 시뮬 쉼터 정책 확정** — **모든 밸런스 시뮬(실험 1~5·사다리·하니스 포함)에서 가상 플레이어는 쉼터에서 항상 «🌟 경험치» 를 선택**한다 (체력 회복 선택 금지 — 측정 조건 통일). sim.js 의 쉼터 선택 로직을 고정하고 게이트에 단언 추가. 실제 게임(index.html)은 당연히 유저가 자유 선택 — 시뮬 정책일 뿐. 반영 시점을 회차 로그에 «쉼터경험치 반영» 으로 표시(반영 전후 수치 섞지 말 것). PLAN §7 에도 기재.

- **(2026-09-02 16:2X KST) 특전 예시 추가 (15:5X 확장에 합침)** — ① «공격 시 n% 확률 4초 공속 +5%, **최대 10중첩**» 류 고중첩 상위 변형 ② 빗맞음 축 필수 2종: «빗나갈 시 다음 공격 확정 치명타» · «빗나갈 시 **데미지 +100%** 스택 — 적중 1타당 1개 소모, 한 타에 +100% 한 번만(잔여는 다음 타 순차 소모)» **(주인 정정 16:3X: ×2 배수 아님 — 가산 +100%, 다른 데미지 보너스와 합연산)**. 상세 PLAN §3.0. — **✅ 완료: 빗맞음 2종은 T48 1단계(전설 `l_missCrit`·`l_missStack`, 가산 풀로 이행 — 풀피와 겹치면 2.2배·곱이면 2.4배를 게이트가 실측 단언), 공속 10중첩은 T48 2단계(희귀 `r_aspdStack10` 25%·4초·+5%·10중첩 + 신화 `m_stackMaster` 모든 중첩 상한 +5). 워커 A / sess-0906-23482.**

- **(2026-09-02 16:1X KST) 원거리 피격 트리거 신설 (15:5X 특전 확장에 합침)** — 적 원거리 공격(화살)에 맞았을 때 발동하는 축. 주인 예시 3종 필수: 반사 30% · 번개 30% · 창 30%. 일반 피격 트리거와 별개 축(원거리 피격은 둘 다 굴림 — 위임). 상세 PLAN §3.0. — **✅ 완료 (T48 2단계 · sess-0906-23482 / 워커 A): `procOnRanged` 신설, 화살 피격 한 자리에서만 호출. 일반 «피격 시» 트리거를 전부 굴린 «뒤» 에 추가로 굴린다(둘 다 발동 — 게이트가 두 파일 순서를 대조). **회피에 성공하면 굴지 않는다**(주인 원문 «맞았을 때»). 주인 필수 3종 전부 반영 — `r_rangeThorns`(반사 30%) · `l_rangeBolt`(번개 30%) · `m_rangeSpear`(창 30%). 게이트 `verifyT2` ㉓ + `verifyCombatConst` ⑤, 음성 8/8.**

- **(2026-09-02 16:0X KST) 특전 확장 보강 (위 15:5X 지시에 합침)** — ① **반사 계열 확장**: 주인 예시 «피격 시 30% 확률로 해당 적에게 데미지 반사» 필수 포함(기존 🌵 축의 등급별 변형). ② **특전 총수 확장 허용**: 등급당 30~40개까지 OK, 단 **등급 간 개수 골고루**(일반만 많은 편중 금지 — 등급별 개수 편차 게이트 감시, 위임: 최다−최소 ≤6종). 상세 PLAN §3.0. — **✅ ① 완료 (T48 2단계 · sess-0906-23482 / 워커 A): 반사 축이 3단이 됐다 — 일반 🌿 `c_thornsS` 30%/50%(주인 예시 그 자리) · 전설 🌵 60%/150%(기존) · 신화 🌵👑 확정/300%. ② 도 완료 (T48 3단계) — **33/33/33/33 = 132종, 편차 0.** 편차 ≤6 은 `verifyT2` ② 가 상시 감시.**

- **(2026-09-02 15:5X KST) ⚑ 신규 특전 축 2개 — 스턴 · 빗맞음 (주인 지시 = 승인 완료된 신규 콘텐츠)** — ① **스턴 메커니즘 신설**(적 공격 정지 n초, 기절 표시, 보스 지속 1/3 은 위임 기본값) + 주인 예시 2종 필수: «피격 시 적 3초 스턴» «치명타 시 3초 스턴» ② **빗맞음 트리거 신설** — 플레이어 공격이 적 회피 10% 로 빗나갔을 때 발동하는 특전 축(적중률 금지 규칙과 공존 — 빗맞음에서 이득을 얻는 쪽). **루틴이 특전 목록 초안(등급 배분 포함)을 짜서 PLAN §3 표에 추가 + PROGRESS «주인 검토» 등재.** sim·게임 동시 반영, 게이트 갱신. 상세 PLAN §3.0. — **✅ 1단계 완료 (2026-09-02T09:XXZ · T48 · sess-0906-23482 / 워커 A) — 이 지시의 두 축(스턴·빗맞음)만. 같은 T48 에 묶인 나머지 6건(16:0X~17:2X 보강)은 2·3단계 몫으로 남아 있다.** ① **스턴 신설** — 적 객체에 `stun`(남은 초), 스턴 중이면 그 틱 공격을 통째로 건너뛰고 **공격 타이머도 멈춘다**(흐르게 두면 스턴이 «막았다» 가 아니라 «미뤘다» 가 된다 — 위임 근거). 재적용은 **더 긴 쪽만**(합산이면 일반 특전 연타로 영구 스턴락). `applyStun` 한 곳으로 모으고 보스 지속 1/3, 게임은 적 머리 위 💫 + 남은 초. ② **빗맞음(onMiss) 신설** — `procOnMiss` 를 빗맞음이 실제로 일어나는 두 자리(`dealDmg`·`doCounter` 의 회피 분기 = «MISS» 팝이 뜨는 곳)에서만 부른다. ③ **주인 정정(16:3X «×2 배수 아님, 가산 +100%») 이행** — 스택을 `c_firstHit` 과 같은 **가산 보너스 풀**에 넣었다(풀피와 겹치면 2.2배, 곱이면 2.4배 — 게이트가 실측으로 단언). 소모는 적중 1타당 1장. ④ **신규 특전 15종**(일반 3·희귀 3·전설 4·신화 5), **주인 필수 4종 전부 포함**. 총 **102 → 117종**, 등급별 29/29/32/27 = **편차 5**(주인 기준 ≤6). ⑤ **밸런스 실측(사다리 7점 200판, 전→후): 영웅 50 = 7.5→9.0%(합격 유지) · 전설 70 = 21.0→25.5 · 나머지 5점 불변. 합격 1/7 → 1/7 — 통과하던 과녁을 깨지 않았다.** ⑥ 하니스 **재보정 불요**(실험1 39.7→39.0 · 실험2 55.0→56.0, 둘 다 드리프트 허용치 안 · `verifyHarness` 7/7). ⑦ 게이트 2곳 신설(`verifyT2` **㉒** 28항목 · `verifyCombatConst` **④** vm 실행 단언 9항목), **음성 15/15 exit 1**, 전 게이트 12종 exit 0, T3 헤드리스 **125/125** pageerror 0. ⑧ 게이트 보강 — `verifyOptText` 가 «이름 붙은 상수»(`MISS_STACK_CAP` 등)를 값으로 못 풀던 것을 고쳤다. **⚑ T1 재개 시 회차 로그에 «스턴빗맞음 반영» 표시할 것.** 근거 `docs/balance/T48/raw.md`.**

- **(2026-09-02 15:4X KST) 적 회피 10% 고정 + 적중률 스탯 금지 (주인 확정)** — 모든 적은 플레이어 타격을 10% 회피(주인 확정 상수, 노브 아님. 보스 포함·소환/반격에도 적용은 위임 기본값 — MISS 팝 표시). **적중률(명중) 옵션은 게임에 존재 금지** — 특전·장비·버프 어디에도 추가하지 말 것(흡혈 금지와 같은 축, 게이트에 단언 추가). sim.js·index.html 동시 반영 — 실질 DPS −10% 라 밸런스 유의미: 반영 후 수치로만 측정, T1 회차 로그에 «적회피 반영» 표시. 상세 PLAN §2.3. — **✅ 완료 (2026-09-02T08:4XZ · T43 · sess-0820-31023 / 워커 B): ① `ENEMY_EVADE=0.10` 을 두 엔진 최상위 const 로 신설 — `TUNE` 밖에 뒀다(주인 확정 상수 = 노브 아님, 게이트가 «TUNE 안으로 들어오면 빨개짐» 을 상시 감시). ② 주인 명시 3종 전부 적용 — 기본공격·소환 5종은 `dealDmg`/`dealPlayerDamage` 한 곳으로 모이므로 그 자리, 반격은 `doCounter`. 게임은 회피 지점마다 «MISS» 팝. ③ 판정을 치명타 굴림 «뒤» 에 둬서 빗맞아도 `nextCrit`·`nextAtk` 가 함께 소모되게 했다(앞에 두면 두 버프가 서로 다르게 동작한다 — 위임 판단, 근거 PROGRESS 검증로그). **회피 제외 2경로(위임): 가시 반사·오발 화살** — 플레이어가 겨눈 타격이 아니다. ④ **적중률 금지 게이트** `verifyT2` **⑳** 신설 — 상수 일치·적용 3지점·MISS 팝(함수 본문별)·**금지어 전수 검사**(특전 102종 tx · 장비 126칸 d · px 키 · PLAN 표 행에서 «명중/적중률/적중 확률/회피 무시/accuracy/hitRate» 0건)·회피 제외 경로 양쪽 일치, 211/211. `verifyCombatConst` 에 «적 회피율» PLAN↔엔진 대조 추가(24/24). 음성 테스트 **14종 전부 exit 1**(판정 제거 4 · 값 변조 3 · MISS 팝 제거 2 · 금지 특전/옵션 추가 2 · 가시 반사 부착 1 · PLAN 괴리 1 · TUNE 이동 1). ⑤ **실측 회피율 10.246%**(38,189 판정 / 3,913 빗맞음). ⑥ **밸런스 실측(사다리 7점 200판, 전→후): 전설 70 = 15.0% → 5.5% 로 합격 구간(2~10%)에 진입 — 합격 0/7 → 1/7.** 신화 120 = 98.0→94.5 · 영웅 50 = 0.5→1.0 · 나머지 4점 불변. **⚑ T1 재개 시 회차 로그에 «적회피 반영» 표시할 것.** ⑦ `verifyHarness` 는 직전 T31 이 박은 기준선(챕터 6 = 52.7%)이 −10% DPS 로 39.7%(−13.0%p)가 되며 DRIFT_TOL 을 넘겨 빨개졌다 — 정본 ④ 지시대로 `--rebase`(6: 39.7 / 8: 55.0). **하니스 재선정은 하지 않았다**(변별력 ①②③ 5항목 전부 통과). ⚠ 실험2 55.0% 는 목표 밴드 60~70% 의 허용치 하단 경계라 **다음 대형 엔진 변경 때는 기준선 갱신이 아니라 하니스 재선정(일반+7 → +8)** 이 맞다 — T1 인계. **게이트 12종 전부 초록.****

- **(2026-09-02 15:3X KST) ⚑⚑ 소환 적중도 «공격» 이다 — 트리거 연쇄 허용 (주인 확정, 기존 «기본공격에서만» 규칙 개정).** 주인 원문: «공격 시 50% 창이면, 창이 적 때렸을 때도 그 50% 적용. 치명타 시 창이면 창이 치명 뜨면 창 또 나가야 함.» 즉:
  - **소환(창/도끼/화살/번개/검기)의 적중도 «공격 시» 트리거를 굴리고, 소환이 치명타면 «치명타 시» 트리거도 굴린다** — 창이 창을 부르는 연쇄 허용. 특전·장비 옵션 공통.
  - PLAN §4 의 «기본공격에서만 분신/추가타 발동» 규칙은 이 지시로 개정: **분신·추가타·nextCrit/nextAtk 소모만 기본공격 전용 유지(위임 기본값)**, 그 외 공격시/치명타시 트리거는 소환 적중에도 적용.
  - 확률·연쇄에 인위적 제한 금지 (연쇄가 세면 그건 밸런스 수치로 잡는다 — T1 몫). 단 **성능 가드만 허용**: 동시 활성 투사체 상한(예: 200, 초과분은 즉발 판정으로 대체) 같은 프레임 보호 장치(위임).
  - sim.js·index.html 동시 반영, 게이트에 «소환 적중 → 트리거 발동» 단언 추가. **밸런스 영향 큼 — T1 은 반영 후 수치로만 측정** (어차피 R01 재시작). PLAN §4 갱신할 것. — **✅ 완료 (2026-09-02T09:1XZ · T45 · sess-0807-26153 / 워커 A): ① 진단 — «치명타 시» 는 데미지 함수 안에 있어 **소환 적중에도 이미 걸려 있었다**. 빠진 건 «공격 시» 하나뿐(`procOnAttack` 이 기본공격에서만 불렸다). ② 두 파일에 `summonHit()` 신설 — 소환 적중 5경로(번개 즉발·자동번개·관통·단일 투사체) 전부가 트리거를 굴린다. 투사체 생성은 `pushProj()` 로 모았다. 기본공격 전용은 위임대로 nextCrit/nextAtk·분신·추가타 셋만 잔존. ③ 성능 가드 2종 — `PROJ_CAP=200`(초과분 즉발 판정 대체) · `PROC_TICK_CAP=200`(틱당 트리거 예산; 번개 즉발 연쇄의 기대 분기수가 2.0 이라 필요). PLAN §4 에 값·감시처 기재. ④ **밸런스 영향(T45 단독 A/B): 영웅 50 1.5→7.5%(합격 진입) · 전설 70 13.0→41.5% · 신화 120 97→100 → 합격 0/7 → 1/7.** T43·T44 병합 후 현재는 영웅 6.0%✓ · 전설 27.0 · 신화 98.0 (합격 1/7). ⑤ 하니스 재보정(정본 ④) — 같은 날 T43(회피 10%)와 반대 방향으로 밀려 실험2 «일반+7» 이 66.3→55.0(T43)→77.7(T45)→**62.3%(둘 다 반영, 밴드 정중앙)** 로 돌아왔다. 하니스는 일반+7 유지, 기준선만 갱신. ⑥ 게이트 2곳 신설(`verifyCombatConst` ③ 실행 단언 · `verifyT2` ⑳ 두 파일 대조), 음성 7/7 exit 1, 전 게이트 12종 exit 0, 헤드리스 pageerror 0. **⚑ T1 재개 시 회차 로그에 «소환트리거 반영» 표시할 것.** 근거 `docs/balance/T45/raw.md`.**

- **(2026-09-02 15:2X KST) 창 관통 규칙 주인 확정 보강 (T34 후속)** — 창은 **«필드 위에 현재 존재하는(스폰된) 적» 중 일직선 최대 8마리** 관통. **아직 생성/활성화되지 않은 적(다음 웨이브 대기분 등)은 절대 맞지 않는다.** T34 의 `pierce:8` 은 유지하되, 관통 대상 필터가 «활성 스폰 적만» 인지 sim.js·index.html 양쪽에서 확인하고(사거리 88×8 안에 다음 웨이브가 들어오는 경우 포함), 게이트에 «미스폰/대기 적 피격 0» 단언을 추가할 것. 검기 등 다른 관통형도 같은 규칙.

- **(2026-09-02 15:1X KST) ⚑⚑ 승인 25·26번 종결 — 주인 확정: «성장률 표는 필요 없고, 사다리 기준으로».** §11.7 의 **스탯 사다리 7점(챕터·공/체/실 절대값)만 주인 확정 과녁**이고, **구간별 성장률 표는 확정 지위 해제 → T1 자유 튜닝 노브**(T35 초기값 취급). T1 은 사다리 7점이 전부 합격 구간(클리어율 2~10%)에 들어올 때까지 성장률·벽 배수를 조정하라. 승인 25번(하니스 규칙 충돌)은 워커 추천안(변별점 규칙 정본화) 채택으로 함께 종결 — 주석·문서를 실제 규칙에 맞춰 정리할 것. PLAN §11.7 갱신 완료. — **✅ 승인 25번 이행 완료 (2026-09-02T08:5XZ · T31 · sess-0807-26153 / 워커 A): ① 정본 «변별점 규칙» 5개조를 PLAN §7 에 신설하고 `sim.js` 주석 2곳·게이트를 거기에 맞췄다(T5 의 «도달 시점 중앙값» 기준은 개정, 재보정 조항만 존속). ② 정리하다 보니 **개편 후 두 하니스가 모두 클리어율 100.0% 천장 포화 — 실험1·2 는 T35 이후 한 번도 유효하게 측정된 적이 없었다.** 규칙대로 재보정(실험1 «전설·슬롯1» → «일반+2·슬롯0» 52.7% · 실험2 «신화·슬롯0» → «일반+7·슬롯0» 66.3%). ③ 재측정에서 **실험1 사다리가 개편 이후 처음 단조증가**(11.3/39.3/77.7/87.0), 실험2 도 특전 지목이 가능해졌다(T1 R13 «특전 개별 수치» 자료). ④ `verifyHarness` 3축 재편 후 **7/7 통과**(직전엔 위반 5로 빨간 상태), 음성 4/4 exit 1, 전 게이트 11종 exit 0. 근거 `docs/balance/T31/recal.md`. **⚑ T1 은 성장률·벽 배수를 움직인 회차마다 이 하니스를 재보정하고(정본 ④) 사용값을 회차 로그에 적을 것 — 게이트가 ±12%p 드리프트로 감시한다.** (26번·T1 자유 튜닝 몫은 T3 통과 후 T1 이 이어받는다.)**

- **(2026-09-02 15:0X KST) 승인 24번(T34) 종결 — 창 관통 상한은 스펙 준수 수정으로 즉시 진행.** 주인과 확인: 창 = 맞은 적 «한 마리당» 공격력 100% 는 스펙 그대로 유지, 문제는 관통 마릿수뿐. **PLAN §3.3 `l_spear` 원문 «일직선 8명 거리(88px×8) 관통» 의 8마리 상한을 sim.js·index.html 양쪽 엔진에 강제**하라(신화 `m_spear200` 은 데미지 200% 로만 작동, 관통 수 불변). 신규 너프가 아니라 누락된 스펙 구현이므로 승인 불요 처리. 게이트에 «창 관통 ≤8» 항목 추가. — **✅ 완료 (2026-09-02T08:2XZ · T34 · sess-0750-146 / 워커 D): ① `sim.js`·`index.html` 양쪽 `fireSpear` 에 `pierce:8` 을 실었다. ② 관통 판정을 `pr.type==='wave'&&…` 에서 «타입 무관 `pr.hit.size>=pr.pierce`» 로 바꿔 창에도 상한이 걸리게 했다(`m_spear200` 은 데미지 13.5배만, 관통 수 불변 — 게이트가 상시 감시). ③ **추가 파손 1건 발견·보정 — `index.html` 은 관통 판정에 리터럴 `>=2` 를 박아 둬서 `pierce` 를 아예 안 봤다. 신화 «🌊 검기의 왕»(m_wave4, 20명 관통)이 배포 빌드에서 통째로 죽어 있었다**(sim 은 20 으로 도는데 게임은 2 — sim↔게임 괴리). 같은 수정으로 함께 해소. ④ 게이트 2종 신설: `verifyCombatConst` «창 관통 상한»(PLAN §3.3 «일직선 8명» ↔ 엔진 3자 대조, 23항목 전부 통과) + `verifyT2` **⑱** (두 파일 pierce 일치 · `m_spear200` 관통 불변 · 관통 판정이 리터럴 아닌 `pr.pierce` · 창·검기 양쪽 적용, 194/194). 음성 테스트 6종 전부 exit 1(pierce 제거 2 · 리터럴 2 되돌림 · PLAN 불일치 · 두 파일 불일치 · combatConst 교차확인). ⑤ **밸런스 영향 실측(`node sim.js 5` 사다리 7점, 수정 전/후):** 전설 70 **17.0% → 11.5%**(합격 2~10% 쪽으로 이동) · 일반 15 0.5→0.0 · 신화 120 96.5→98.0(200판 노이즈) · 나머지 4점 불변. **합격 점수는 전후 모두 0/7 — 통과하던 과녁을 깨지 않았다.** l_spear 가 전설 특전이라 전설 구간이 가장 크게 움직인 것이 예상과 맞다. **⚑ T1 재개 시 회차 로그에 «창관통상한 반영» 표시할 것.**

- **(2026-09-02 14:4X KST) ⚑ 긴급 핫픽스 — 모바일에서 UI/글씨 축소 문제 (viewport 메타 부재).** 주인이 핸드폰 크롬에서 관측. 현 `index.html` 의 `<head>` 에 `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, viewport-fit=cover">` 추가 — **다음 빈 번호로 등재 후 즉시 선점, 한 줄 핫픽스**(T32 선례). 추가 후 9:19 프레임이 모바일에서 꽉 차는지 CSS 확인(깨지면 그 보정까지가 핫픽스 범위). T2 재작성본에도 필수 포함 + T3 에 모바일 폭 검증 항목 추가. 상세 PLAN §2.1. — **✅ 완료 (2026-09-02T06:1XZ · T40 · sess-0608-25655 / 워커 A): ① `index.html` 최상단에 확정 문자열 그대로 viewport 메타 추가 ② CSS 확인에서 **추가 파손 1건 발견·보정** — `#frame` 이 `100vh` 뿐이라 모바일 크롬에서 주소창 높이만큼 하단 5탭이 화면 밖으로 잘린다(vh = 주소창 숨김 기준 높이). `100dvh` 를 병기(구형 브라우저는 `100vh` 로 폴백). ③ 회귀 방지 게이트 `verifyT2.js ⑫` 신설 — 메타 필수 3항목 + `#frame` 의 `100dvh` 병기를 상시 검사하므로 **T2 재작성본에서 이 한 줄이 사라지면 게이트가 빨개진다**. ④ T3 에 모바일 폭 검증 항목 등재(아래 T3 절). 프레임 폭 실측(계산): 390×844 → 390px(꽉 참) · 360×800 → 360 · 412×915 → 412 · 430×932 → 430. 375×667(SE, 16:9)만 316px 로 좌우 여백이 남는데 9:19 프레임이 16:9 화면에 들어갈 수 없어 정상이다.**

- **(2026-09-02) ⚑⚑⚑ 밸런스 근간 전면 교체 — 주인이 «등급별 절대 스탯» 을 직접 확정. PLAN §11.5-a·§11.7·§2.3·§2.4·§11.4 갱신 완료. 필독.** *(주인 재확인 2026-09-02 14:5X, 대화형 세션 — «내가 지시한 거 맞음». 다른 세션발 지시지만 정본이다. 앵커 A·B·C 폐기 포함 전부 유효.)*
  - **왜**: 종전 설계(앵커 C·A·B 역산)는 신화 풀셋 공격력이 **86억**, 일반 장비가 신화의 **1/5.8억** 이었다. 주인이 원한 건 «신화 풀셋 공격력 1,200» 수준의 읽히는 숫자다. R05~R12 가 실험3 초반 점수를 못 올린 근본 원인(승인 대기 17번)도 이것이다.
  - **① 스탯 사다리 7점이 유일한 과녁** (앵커 A·B·C 폐기): 노템 5 · 일반 풀셋 15 · 희귀 30 · 영웅 50 · 전설 70 · 신화 120 · 신화+9강 260 챕터. 공/체/실 절대값은 PLAN §11.7 표.
  - **② 실드 = 독립 스탯** (`maxSh = maxHp*0.8` 파생 폐지). 장비가 공격력·체력·**실드** 3스탯을 기여한다.
  - **③ 노템 기본치 공30/체300 → 공 25 / 체 150 / 실드 250.**
  - **④ 등비수열 `rarStep 155` 폐기** → 등급별 명시 기여표(PLAN §11.5-a). `plusStep 0.12 → 0.13`.
  - **⑤ 슬롯 `slotG 2.68` 폐기** → **1레벨당 공/체/실 각 +1%** (`1 + 0.01*L`), **레벨 상한 150**. 균등 보너스(5의 배수마다 +5%)는 그대로.
  - **⑥ 난이도 단일 상수 `eHpG 1.185`·`eDmgG 1.08` 폐기 → 구간별 성장률** (PLAN §11.7 표. 7.18% → 0.55%). 승인 대기 7번 종결.
  - **⑦ 벽 배수(10·15·90·300)는 주인이 튜닝에 위임.** 종전 ×3.6 은 새 곡선에서 83챕터분이라 성립 불가 — T1 에서 재산정. 챕터 300 콘텐츠가 아직 없으므로 급하지 않다.
  - **⚠ 신화 체력만 주인 표의 2000 → 2385 로 조정했다.** 2000 이면 «전설 +9강» 체력이 «신화 0강» 을 따라잡아 주인 확정 제약(T29)이 깨진다. 근거 PLAN §11.5-a.
  - **⚠ T1 회차 R01~R12 전부 무효** — 재개 시 **R01 부터 다시** 센다. 승인 대기 **7·9·11·14·16·17·18번은 전제 소멸** — 재등재 필요 여부부터 판단할 것.
  - **⚠ T35 완료 전까지 `node tools/verifyPlanConst.js` 는 실패한다** (불일치 1 = `plusStep` PLAN 0.13 ≠ 엔진 0.12 · 미검출 3 = `atkUnit`/`hpUnit`/`rarStep` — 셋 다 이번 개편으로 **폐기된 상수**라 PLAN 에서 사라진 것이 정상이다). **이걸 «PLAN 이 틀렸다» 로 보고 PLAN 을 엔진에 맞춰 되돌리지 말 것** — 엔진을 PLAN 에 맞추는 것이 T35 다. T35 가 게이트의 해당 3항목을 «등급별 표 대조» 로 교체하면서 해소한다. — **✅ 해소 (2026-09-02T05:5XZ · T35 · sess-0520-18737 / 워커 B): `verifyPlanConst` 74/74 통과.** `atkUnit`/`hpUnit`/`rarStep`/`slotG` 4항목을 제거하고 «등급별 기여표 15칸 + 구간별 성장률 12칸» 표 대조를 신설했다. `plusStep` 은 엔진이 0.13 으로 올라가 일치. **다만 T35 완료 게이트인 «사다리 7점» 은 1/7 만 통과했다 — 승인 대기 26번**(확정 성장률 표가 «플레이어 힘 선형» 전제라 후반이 물러진다). `verifyHarness` 는 이 개편으로 빨개졌고(T36) T1 이 `eBase*` 재적합 후 `--rebase` 할 몫이다.
  - **⚠ T2 진행 중인 워커는 그대로 계속하라** (구조 우선·수치 임시라는 14:0X 지시 그대로). 새 수치는 **T35 → T1** 이 넣는다.

- **(2026-09-02 14:2X KST) 챕터 레이아웃 확정 제약** — 챕터당 **적 총 수 ≤100 · 쉼터 1~4 · 악마 =1 · 천사 =1 (정확히 하나씩)**. 기존 가중치(45/30/25) 배치 폐기 — 악마1·천사1 선배치 후 남는 슬롯 쉼터, 순서만 시드 셔플(위임). `chapterLayout` 수정 대상: sim.js 와 T2 이식분 둘 다. 매 챕터 악마·천사가 보장되므로 밸런스에 유의미 — T1 재개 시점엔 이미 반영돼 있어야 하고(현재 T1 정지 중이라 회차 무효 이슈 없음), 반영 커밋을 PROGRESS 에 기록. 상세 PLAN §2.4. — **✅ 완료 (2026-09-02T05:5XZ · T2 2단계 · sess-0508-7315 / 워커 A): `sim.js`·`index.html` 양쪽 `chapterLayout` 동시 반영 — 가중치(45/30/25) 삭제, 악마1·천사1 선배치 + 남는 슬롯 전부 쉼터(`clamp(waveCount-3,1,4)`) + 시드 셔플, 적 상한 `LAYOUT_MAXENEMY=100`. 챕터 1~300 전수 위반 0(적 40~60마리·쉼터 1~2·악마 1·천사 1), 두 파일 노드열 전수 동일. 게이트 `verifyT2.js` ⑧ 신설로 상시 감시. ⚑ T1 재개 시 회차 로그에 «레이아웃제약 반영» 표시할 것 — 실험1 바닥이 올라갔다(일반 9.0/희귀 44.3/전설 79.7/신화 86.7%).**

- **(2026-09-02 14:0X KST) ⚑⚑⚑ 작업 순서 전면 변경 — 구현·동작 검증이 먼저, 밸런스는 그 다음 (주인 원문: «특전을 반영 안 하고 어떻게 밸런스를 잡나. 쉴드도 그렇고. 동작 검증까지 하고 밸런스 잡아야지»).** 시뮬만 앞세우고 게임 파일을 뒤로 미룬 기존 순서를 폐기한다. **새 순서: T2 → T3 → T1.**
  - **T2 즉시 선점 가능 (지금 최우선)** — «T1 완료 후» 조건 삭제. **⚑ 주인 보강(14:1X): T2 내부 착수 순서도 «챕터 300 구조부터»** — 챕터 1~300 존재·해금·클리어 처리(시드 생성 상한 300)를 먼저 넣고, 그 다음 특전/장비/UI 순으로. 난이도 수치는 이식 시점 sim.js 값 임시 탑재(곡선 확정은 T1 재개 후 몫). **현시점 sim.js 수치 그대로**(T6 통합본) index.html 전면 이식: 특전 102종·장비/뽑기/합성/슬롯/상점·챕터 300·모든 ⚑ 지시(등급통일·보스킬 특전스킵·실드 폐지·보스 ×8·버프 아이콘·미리보기 줄·흡혈 제거·순차 연사)·`docs/ref/` 스크린샷 기준 UI. 규모가 크므로 **여러 세션에 걸쳐 이어받기 허용** — T2 lock 을 잡은 세션은 진행 상황을 PROGRESS T2 행에 남기고 lock 해제, 다음 워커가 이어서.
  - **T3 — T2 완료 즉시** 동작 검증 (기존 스펙 + 장비/상점/연출 확인 항목 추가).
  - **T1 은 T3 통과 후 재개.** 재개 후 규칙: **회차에서 확정한 수치는 같은 커밋에서 index.html 에도 동시 반영** — sim 과 게임이 다시는 벌어지지 않게. 공유 상수 동기화 게이트(`tools/verifySync.js` 급) 신설(위임). 이후 밸런스는 «실제 게임과 항상 일치하는 sim» 위에서 잡는다.
  - 진행 중인 T1/측정 회차는 현 회차만 마감하고 lock 해제. 기존 R01~R12 결과·게이트·발견(T24 등)은 폐기가 아니라 T2 이식 검증 자료로 쓴다.

- **(2026-09-02 08:5X KST) ⚑⚑ 긴급 핫픽스 — 구버전 index.html 실드 무료충전 즉시 제거 (T2 기다리지 마라).** 주인이 현 배포 빌드에서 웨이브 전멸 실드 충전을 또 관측했다(지시 `2116ab1` 은 T2 대기 중이라 게임 파일 미반영). **다음 빈 번호로 PROGRESS 등재 후 즉시 선점, T1 보다 우선.** 내용: ① `index.html:897` 부근 웨이브 클리어 `p.sh=p.maxSh` 제거(워커 B 가 위치 지목해 둠) ② 구버전에 레벨업 풀충전이 있으면 그것도 제거 ③ 그 외 구버전 대개편은 하지 말 것(T2 몫) — 이 두 줄만. push 시 GitHub Pages 자동 배포로 주인이 바로 확인 가능. 완료 시 이 항목에 완료 표시. — **✅ 완료 (2026-09-02T04:5XZ · T32 · sess-0448-26595 / 워커 B): ① `index.html:897` 웨이브 전멸 `p.sh=p.maxSh` (+사운드·«실드 충전!» 문구) 제거 ② 레벨업 풀충전은 구버전에 **원래 없었다**(전수 확인 — `gainExp`/`openPerkChoice`/`takePerk`/`afterPerk` 어디에도 회복·최대치 보정 없음). 남은 `p.sh=p.maxSh` 2곳은 특전(«실드 최대치 +30% 즉시 충전» · 부활 50%)이라 유지. sim.js 와 동일 상태(sim 에는 웨이브 충전 로직 없음).**

- **(2026-09-02 08:3X KST) 다연발 소환 = 순차 연사 (구별되되 존나 빠르게)** — 화살/번개/도끼 등 여러 발 나가는 소환은 동시 스폰 금지, 50~70ms 간격으로 다다다닥 순차 발사(발마다 낱발이 구별돼 보여야 함 — 주인 보강. 발마다 판정·사운드). T2 연출 스펙 — sim 무관, 회차 무효 없음. 상세 PLAN §4.

- **(2026-09-02 08:2X KST) UI 참고 스크린샷 등록 — `docs/ref/*.jpg` 10장 (원본 이름 그대로).** T2/T6 의 모든 UI 는 이 스크린샷을 참고해 같은 구도·스타일로 만들 것: 로비(`메인로비.jpg`) · 인게임(`메인 게임화면.jpg`, `메인 게임화면_적발견.jpg`) · 특전(`perks.jpg`, `perks 뭐뭐 있는지.jpg`) · **장비/상점 신규 4장**(`캐릭터 장비.jpg`, `장비 세부팝업.jpg`, `장비 합성 업글창.jpg`, `상점 (1).jpg`, `상점 (2).jpg`) — §11 장비 탭·합성·상점 UI 는 이 4~5장이 기준. 이미지를 Read 도구로 직접 열어 보고 작업하라. **`docs/ref/` 는 주인이 넣은 참고 자료라 «캡처 PNG 커밋 금지» 규칙의 예외다 — 워커는 이 폴더에 추가·수정·삭제 금지.**

- **(2026-09-02 08:1X KST) 앵커 C 추가** — **전설(차상위) 풀셋 0강(위임) + 슬롯 균등 10렙 → 챕터 30 겨우 클리어.** 벽 아님, 곡선 고정점. 실험5 는 이제 C(30)·A(90)·B(300) 3점 측정. 상세 PLAN §7·§11.7.

- **(2026-09-02 08:0X KST) 앵커 B 추가 — 챕터 300 확장** — **신화 풀셋 +9강 + 슬롯 균등 50렙 → 챕터 300 겨우 클리어**(301+ 벽). 챕터 수 100→**300**, 벽 10·15·90·300. 실험5 는 앵커 A(90)·B(300) 둘 다 측정. T6 통합 시 처음부터 300 기준으로 구현할 것. 상세 PLAN §2.4·§7·§11.7.

- **(2026-09-02 07:5X KST) ⚑⚑ 장비 시스템 전면 신설 — PLAN §11 (필독).** 등급 5·부위 6·종류 3(18종), 뽑기(400다이아, 신화 0.1%·50회 천장·전설 10회 피티), 합성(3→1 등급업, 전설은 +강, +10강→신화, 신화 무한강+3/6/9강 옵션), 슬롯 강화(골드, 균등 보너스), **영구강화 4종 폐지**(§2.2 — 07:4X 강화상한 티어 지시도 대상 소멸로 폐기), 챕터 100 확장·90 벽, 다이아 2500/일·IAP 12,000. 이에 따라:
  - **T6 신설 (T1 보다 우선, lock ID = T6)**: ① sim.js 에 장비·뽑기·합성·슬롯·다이아 경제·챕터 100 통합 ② 18계열 옵션표 초안 작성 → PLAN §11.6 기입 + PROGRESS «주인 검토» 등재 (기존 엔진 동사만 재사용, 신규 메커니즘 금지) ③ §7 실험4·5 하니스 구현 ④ 완료 후 T1 재개 조건 충족.
  - **T1 은 T6 완료까지 신규 회차 시작 금지** (영구강화 폐지로 현 경제 튜닝 축이 소멸 — 강화 비용 성장률 계열 결과는 폐기물이다. 진행 중 세션은 현 회차만 마감하고 lock 해제).
  - **T1 채점표 v2 (T6 후 적용, 총 10점·두 비평가 모두 ≥8 합격)**: 실험1 사다리 2점 · 실험2 등급 내 1점 · 실험3 진행 곡선(1~20 구간 기존 기준 유지, 21+ 구간 목표는 T6 이 제안해 §7 에 기록) 3점 · 실험4 장비 진행 2점 · 실험5 챕터 90 앵커 2점.
  - **T2 범위 확대**: 장비 탭(장착/합성/슬롯)·상점 탭(다이아 모의 구매·뽑기 연출)·저장 포맷 교체 포함.

- **(2026-09-02 07:4X KST) 영구 강화 상한 티어 시스템** — 강화 4종 각각 max 10렙, 4종 전부 상한 도달 시 상한 +10 (10→20→30…, 무한). 수식 `상한 = 10*(1+floor(min(4종 레벨)/10))`. sim.js 실험3 자동구매가 이 상한을 따라야 함 — **반영 전 T1 회차 무효, 로그에 «강화상한 반영» 표시.** 저장 포맷 `up:{}` 은 그대로(레벨만 저장, 상한은 파생값). T2: 강화 팝업에 MAX·해제 조건 표시. 상세 PLAN.md §2.2.

- **(2026-09-02 07:3X KST) T5 승인 (주인 위임)** — 실험1 하니스 강화 6렙→**9렙**, 실험2 8렙→**11렙**. 원칙: 하니스 강화 레벨 = 실험3 관측 «그 챕터 도달 시점 중앙값 영구강화 레벨», 경제 노브를 바꾼 라운드마다 재보정하고 사용값을 회차 로그에 기록. 승인 대기 2번(T5) 안건 종결 — T5 행 완료 처리. 상세 PLAN.md §7.

- **(2026-09-02 07:3X KST) 보스 = HP ×8 · DMG ×1.8 고정 (주인 확정 상수 — 직전 ×3 지시를 주인이 ×8 로 정정)** — `bossHp 12 → 8`, `bossDmg 1.8 유지(고정 승격)`, 5배수 챕터 `bigBoss ×1.5` 폐지. 둘 다 튜닝 노브 아님. 승인 대기 1번 안건 종결. **밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «보스8배 반영» 표시 후 재채점** («보스3배 반영» 으로 이미 돌린 회차가 있으면 그것도 무효). 상세 PLAN.md §6. T2 이식 시 index.html 도 동일.

- **(2026-09-02 07:1X KST) 흡혈 증가 특전 금지** — 흡혈 스탯을 올리는 특전은 전면 배제(발동형 «n% 확률 체력 회복» 류는 유지). 102종 목록엔 원래 없음 — sim.js 밸런스 영향 0, **T1 회차 무효 아님.** 실 작업은 T2: 구 index.html 의 흡혈 특전 3종(🩸🩸🧛, 대략 `index.html:482,491,512`) 제거 + 스탯 그리드 «흡혈» 항목 제외(7종). 상세 PLAN.md §3.0.

- **(2026-09-02 07:0X KST) 인게임 UI 2건 — T2 스펙 (sim 무관, 회차 무효 없음)**: ① **발동 중 버프 아이콘** — 왼쪽 위(챕터 아래)에 발동 중인 시간제 버프의 특전 아이콘 + 등급색 테두리 + 중첩 수 뱃지(아이콘 우상단). ② **얻은 특전 미리보기 줄** — Info 버튼 행 왼쪽 끝부터 획득 순서대로 아이콘 나열(등급색 포함, 중복은 개수 뱃지). 상세 PLAN.md §2.3. T2 이식 시 구현하고 T3 에서 두 UI 의 표시/소멸 확인 항목 추가. — **✅ 완료 (2026-09-02T06:2X~07:0XZ · T2 4단계 · sess-0624-31490 / 워커 B): ① `#buffBar` 신설 — `addBuff` 에 표시 전용 6번째 인자(`src` = 그 버프를 준 px 키)를 더하고 호출부 28건 전부를 자기 가드 키로 넘기게 고쳐 «어느 특전의 버프인가» 를 알게 했다(px→특전 매핑은 `takePerk` 의 ap 전후 px 비교로 자동 생성). 챕터 표시 아래·왼쪽에 세로로, 등급색 테두리 + 중첩 2 이상일 때 오른쪽 위 개수 뱃지, 오래된 것이 위, 만료 시 제거. 장비 옵션발 버프는 특전이 아니므로 스탯 기본 아이콘으로 폴백. ② `#perkStrip` 신설 — Info 행 왼쪽 끝(실측 `strip.left = hudFoot.left = 12px`), 획득 순서·등급색·중복 개수 뱃지, 폭을 실측해 넘치면 최신 것만 남기고 «+N»(390px 20종 → «+14 + 6칩»). 천사의 축복도 같은 줄. **`sim.js` 무수정 — T1 회차 무효 아님.** 게이트 `verifyT2.js ⑬` 신설(114/114), 음성 테스트 18종 전부 exit 1, 헤드리스 pageerror 0. 같은 단계에서 **T36(챕터 300 이동 UI)도 해소** — ◀▶ 길게누르기 가속(1.6초에 19챕터) + 제목 탭 → 이동 팝업(숫자 입력·±10·최신 N).**

- **(2026-09-02 06:5X KST) 웨이브 전멸 실드 충전 폐지** — 구 스펙 «웨이브(보스 제외) 전멸 시 실드 완전 충전» 을 주인이 폐지했다(PLAN §2.3 취소선 처리). 실드 충전은 특전으로만. **sim.js 의 해당 로직 제거 — 반영 전 T1 회차 점수 무효, 로그에 «실드무료충전폐지 반영» 표시.** T2 이식 시 index.html 도 동일.

- **(2026-09-02 06:4X KST) 레벨업 = 특전 선택뿐** — 레벨업 시 체력·실드 풀충전 금지(주인이 현 빌드에서 관측한 구버전 동작), 최대 체력·최대 실드 증가도 금지. 특전 선택지만 띄운다. 상세 PLAN.md §2.4. **sim.js 에 레벨업 회복/보정이 있으면 제거 — 밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «레벨업무보정 반영» 표시.** T2 이식 시 index.html 도 동일.

- **(2026-09-02 06:3X KST) 챕터 종료 보스 킬 = 특전 스킵** — 보스 경험치 +9 는 유지하되, 챕터를 끝내는 보스 처치로 레벨업이 되어도 특전 선택지·레벨업 팝업 없이 바로 클리어 화면으로. 상세 PLAN.md §2.4. sim.js·index.html(T2) 둘 다 반영 (밸런스 영향은 미미 — T1 회차 무효 아님, 반영 시점만 로그에 표시).

- **(2026-09-02 06:2X KST) 특전 선택지 등급 통일** — 특전 선택지가 뜰 때 등급을 1번만 굴리고 3개(전지의 눈 4개) 전부 같은 등급으로. 섞어서 금지. 상세는 PLAN.md §3.0 에 반영해 둠. **T1 진행 세션은 다음 회차 전에 sim.js 에 즉시 반영할 것** (선택지 분포가 바뀌므로 반영 전 회차의 점수는 무효 — PROGRESS 회차 로그에 «등급통일 반영» 표시). T2 이식 시 index.html 도 동일 로직.

## 0. 세션 시작 절차 (모든 워커 공통)

1. `git fetch && git checkout -B main origin/main` (pull --rebase 금지, 로컬 잔재 위에서 작업 금지)
2. SID 발급: `sess-HHMM-$RANDOM` (예: sess-0512-23481)
3. `PLAN.md` → `docs/ROUTINE.md` → `docs/PROGRESS.md` → `docs/claims/` 순서로 읽는다.
4. 아래 [2. 작업 목록]에서 **선점 가능한 가장 앞 작업**을 lock 으로 선점한다 (규약: `docs/claims/README.md`).
5. 선점할 작업이 없으면(전부 lock 또는 전부 완료): 마지막 완료 작업의 게이트를 재실행해 검증만 하고, 이상 없으면 **커밋 없이 조용히 종료**한다. 이상이 있으면 PROGRESS 에 등재하고 종료.

## 1. 절대 규칙

- **새 콘텐츠(특전/시스템/수치 체계) 임의 추가 금지.** 추가하고 싶으면 PROGRESS 에 «주인 승인 대기» 로 등재만 하고 넘어간다. (위임 규약: 밸런스 *숫자* 조정은 PLAN §7 범위 내에서 즉시 채택·진행하되 되돌림 가능하게 기록)
- 승인 프롬프트가 뜨는 명령을 피하고, 대화형 편집기(`git rebase -i` 등) 금지.
- 캡처 PNG·대용량 바이너리 커밋 금지. 증거는 md 의 수치로.
- 작업이 끝나면 lock 삭제 → PROGRESS 갱신 → 커밋 → push. **push 실패 시 `git pull --rebase=false` 말고 `git fetch && git rebase origin/main`** 후 재push (자기 lock 이 사라졌으면 진 것 — 작업 버리고 종료).
- 아티팩트 republish 는 **루틴 금지** (대화형 세션 전용 — PLAN §9-3). GitHub Pages 활성화도 주인 몫(PLAN §9-2). 루틴은 PROGRESS 에 «준비 완료» 표시까지만.

## 2. 작업 목록 (순서 고정 — lock ID = 아래 번호)

> **⚠ 이 절은 이력이다 (2026-09-03).** 아래 T35·T1 절차는 특전 132종·등급 체제를 전제로 쓰였고 그 전제가 폐기됐다. ~~지금 선점할 작업은 최상단 지시의 T96 → T97 뿐~~ **⚑ T115 정정 (2026-09-04) — 그 두 작업은 완료됐고 주인이 그 뒤로 T100~T114 를 다시 열었다가 그것도 전부 끝났다. 선점할 작업은 이 절이 아니라 언제나 «최상단 ⚑ 항목 + `docs/PROGRESS.md` 작업표의 미완료 행» 에서 찾는다 — 그것이 비어 있으면 §0-5(게이트 재실행 후 조용히 종료)로 간다.** 이 절의 «한 회차 절차»·lock 규약·세션 운영 규칙(90분 등)은 그대로 유효하다.

### T35 — sim.js 를 주인 확정 스탯 체계로 이식 (T1 재개 전 선행 · T2/T3 과 병렬 선점 가능 — sim.js 만 건드린다)
PLAN §11.5-a·§11.7 확정값을 `sim.js` 에 그대로 옮긴다. **역산·재해석 금지 — 표의 숫자를 그대로 쓴다.**
1. 기본치 `pAtk0 30 → 25` · `pHp0 300 → 150` · **`pSh0 250` 신설** (실드가 `maxHp*0.8` 파생이던 곳 전부 제거 — 독립 스탯).
2. `GT.atk/hp` 의 `rarStep` 등비 생성 폐지 → **등급별 명시 배열 3종**(공/체/실, PLAN §11.5-a 표) 하드코딩. `plusStep 0.12 → 0.13`.
3. `GT.slotMul = L => 1 + 0.01*L` (`slotG^L` 폐기) · **슬롯 레벨 상한 150** 강제 · 균등보너스를 실드에도 적용.
4. `TUNE.eHpG`/`eDmgG` 단일 상수 폐기 → **구간별 성장률**(PLAN §11.7 표)을 쓰는 `enemyStats(c,w)` 로 교체.
5. **벽 배수는 임시값**으로 두되(종전 3.6 은 새 곡선에서 83챕터분이라 그대로 쓰면 안 됨) 고른 값과 근거를 PROGRESS 에 남긴다. 확정은 T1 몫.
6. 완료 게이트: `node sim.js 5` 를 **스탯 사다리 7점**(노템 5 · 일반 15 · 희귀 30 · 영웅 50 · 전설 70 · 신화 120 · 신화+9강 260)으로 갈아끼워, 각 과녁 챕터 클리어율 2~10% 를 표로 기록. 못 맞추면 그 수치를 PROGRESS 에 등재.
7. `tools/verify*.js` 를 새 상수 체계에 맞게 갱신 — 특히 `verifyPlanConst`(PLAN↔엔진 대조)와 `verifyGearEcon`(T29 제약을 «등비 조건식» 이 아니라 «등급별 표 대조» 로 교체).

### T1 — 밸런스 튜닝 루프 (비평가 2인 · 8점 게이트) ← ~~최우선~~ **순서 변경(2026-09-02 ⚑⚑⚑): T3 통과 후 재개**
> **⚠ 스탯 사다리 개편으로 R01~R12 전면 무효 — 회차는 R01 부터 다시 센다. 선행 작업 T35 완료가 전제.** 이 루프에 **벽 배수(10·15·90·300) 재산정**이 포함된다(주인 위임).
목표: `sim.js` 밸런스를 **비평가 2명이 각각 10점 만점에 8점 이상** 줄 때까지 조정.

한 회차(라운드) 절차:
1. `node sim.js 1` / `node sim.js 2` / `node sim.js 3` 실행, 원시 출력 저장 (`docs/balance/round-NN/` 에 md 로. NN = 회차 번호).
2. **비평가 2명을 Task(Agent) 도구로 각각 독립 실행** (서로의 결과를 보여주지 말 것). 각 비평가에게 주는 것: ①아래 채점표 ②이번 회차 시뮬 원시 출력 전문. 받는 것: 항목별 점수 + 총점(10점 만점) + 감점 사유 + 구체적 조정 제안.
3. 두 비평가 모두 **총점 ≥ 8** 이면 합격 → T1 완료 처리. 아니면:
4. 감점 사유를 근거로 `sim.js` 의 `TUNE` / 특전 수치 / 강화 비용 성장률 / 골드 드랍을 조정 (PLAN §7 의 노브만. 특전 개별 수치는 실험1·2 근거로만).
5. **조정한 수치를 PLAN.md §6·§3 에도 갱신 기록** (PLAN 의 요구사항).
6. 회차 결과를 PROGRESS 의 T1 행에 기록 (회차 번호 · 두 점수 · 무엇을 얼마로 바꿨는지 1줄씩) 하고 커밋·push. 다음 회차로.
- 세션 시간이 다 되어가면: 현재 회차까지 커밋·push, lock 삭제 후 종료 (다음 워커가 이어받는다).
- 세션이 90분을 넘기게 되면 lock 내용의 타임스탬프를 갱신해 push (탈취 방지).

**채점표 (비평가용, 총 10점):**
- 실험1 등급 사다리 (3점): 클리어율이 일반<희귀<전설<신화 로 단조증가하면 3점. 인접 역전 1쌍당 −1.5점.
- 실험2 등급 내 균형 (2점): 각 등급 내 (표본 ≥25판 특전 중) 승률 최상−최하 차가 전 등급 25%p 미만이면 2점. 25%p 이상인 등급 1개당 −1점.
- 실험3 진행 곡선 (5점): 챕터1~5 각 1~2회(1점) · 6~9 각 2~5회(1점) · **10이 10회 이상 & 400회 이내(2점 — 벽의 핵심)** · 11~19 각 3~10회 & 20이 10~30회(1점). 구간별로 어긋난 챕터가 있으면 해당 점수를 비례 감점.
- 비평가는 점수 외에 «가장 효과 클 조정 노브 1~2개» 를 반드시 제안할 것.

### T2 — index.html 이식 (~~T1 완료 후에만 선점 가능~~ **순서 변경(2026-09-02 ⚑⚑⚑): 지금 최우선 — 현 sim.js 수치로 이식, 상단 지시 참조**)
- T1 이 확정한 최종 수치·특전 102종·챕터 20개 구조를 PLAN §2~§6·§8 스펙대로 `index.html` 에 전면 이식. 메테오 등 구버전 잔재 완전 제거.
- `sim.js` 와 수식이 1:1 일치해야 함 (검증: 대표 수식 5개 이상을 grep 으로 대조해 PROGRESS 에 기록).
- 완료 게이트: `node --check` 급 문법 검사 + 특전 102종 전수 존재 확인 스크립트(`tools/verifyT2.js` 신설, 통과 수 기록).

### T3 — 동작 검증 (T2 완료 후)
- 가능하면 Playwright(`npx playwright install chromium` 후)로 헤드리스 부팅 → 콘솔 에러 0 · 로비 렌더 · 전투 진입 · 레벨업 특전 3택 노출까지 확인. Playwright 불가 환경이면 jsdom/정적 검사로 대체하고 그 사실을 기록.
- **⚑ 모바일 폭 검증 (주인 지시 14:4X · T40 에서 등재)**: 뷰포트 **390×844**(및 360×800)로 열어 ① 글씨·UI 가 축소되지 않는다(로비 `CHAPTER N` 실측 폰트가 CSS 의 36px 그대로) ② `#frame` 이 화면 폭·높이를 꽉 채운다(390×844 에서 폭 390px) ③ **하단 5탭이 잘리지 않는다** — 주소창 표시 상태(작은 뷰포트)에서도 `#bottomNav` 가 뷰포트 안에 들어온다. 정적 게이트는 `node tools/verifyT2.js` ⑫ 가 대신 보지만(메타 + `100dvh` 병기), **실제 렌더 확인은 T3 몫**이다.
- **⚑ 인게임 UI 2건 표시/소멸 확인 (주인 지시 07:0X · T2 4단계에서 등재)**: ① **발동 중 버프 아이콘** — 실제 전투에서 시간제 버프가 발동하면 `#buffBar` 에 아이콘이 뜨고(등급색 테두리·중첩 2 이상이면 개수 뱃지), **지속시간이 끝나면 사라지는지**. 챕터 표시(`#chapHud`)와 겹치지 않는지도 볼 것 — 4단계에서 실제로 2px 겹쳐서 한 번 고쳤다. ② **얻은 특전 미리보기 줄** — 특전을 얻을 때마다 `#perkStrip` 에 획득 순서대로 늘어나는지, 중복은 개수 뱃지인지, 많이 얻으면 «+N» 으로 접히고 **최신 것들이 보이는지**, 좁은 폭(360px)에서 줄이 넘치지 않는지. 정적 게이트는 `verifyT2.js ⑬` 이 보지만 **실제 발동/소멸 확인은 T3 몫**이다.
- **⚑ 특전 선택창 «보유 특전» 버튼 (주인 지시 2026-09-03 · T89 에서 등재)**: 레벨업 선택창에서 **📘 보유 특전** 버튼을 눌러 ① 이번 런에서 얻은 특전 목록이 뜨는지(장수 = `G.perksTaken.length`) ② **목록이 열려 있는 동안에도 시간 정지가 유지되는지**(T79 규약 — `G.paused`·`G.t` 불변) ③ 닫으면 **선택지가 바뀌지 않은 채** 선택창으로 복귀하는지(무료 새로고침 잔여도 그대로) ④ 버튼이 카드·새로고침과 안 겹치고 오른쪽 하단에 있는지 ⑤ HUD 📘 로 연 책은 종전대로 «탭하면 닫힘» 인지. 정적 게이트는 `verifyT2.js` ㊵ 가 보지만 **실제 열림/복귀 확인은 T3 몫**이다(`tools/t3/battle.js`).
- 발견 버그는 PROGRESS 에 번호 등재(T3-1, T3-2 …) 후 이 세션 또는 다음 워커가 수리.

### T4 — 배포 준비 (T3 완료 후)
- 최종 커밋·push 확인, PROGRESS 에 «주인 할 일» 정리: ① GitHub Pages 설정(https://github.com/kuzuni/aaaw/settings/pages, Branch=main, /(root)) ② 아티팩트 republish 는 대화형 세션에 요청.

### 신규 작업 등재
- 버그·후속 작업 발견 시 PROGRESS 표에 **이미 쓰인 번호 중 가장 큰 것 +1** 로 등재 (번호 재사용 금지, 한 번호 = 한 작업).

## 3. PROGRESS.md 기록 규약

- 표의 자기 작업 행을 갱신: 상태(진행중/완료/대기) · SID · 워커 · 마지막 회차 · 핵심 수치.
- T1 은 회차 이력을 표 아래 «T1 회차 로그» 절에 한 줄씩 누적 (`R03 | 7/6.5점 | wallHp 1.6→1.75 | sess-… A`).
- 완료 시 반드시: 게이트 결과 숫자(예: 비평가 8.5/8점) + 커밋 해시.
