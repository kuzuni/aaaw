# 루틴 작업 지시서 (병렬 워커 공용 — 유일 지시서)

> 이 문서와 `PLAN.md` 만 보고 작업한다. **PLAN.md 는 확정 스펙이라 임의 변경 금지** (밸런스 숫자 조정만 PLAN §7 범위 내 자율).
> 워커: **A :05 · B :20 · C :35 · D :50** (매시, 분 단위 슬롯 — 4개가 15분 간격으로 돈다. 전부 claude-opus-5).
> 보고·기록은 전부 **한국어**.

## ⚑ 신규 주인 지시 (위 항목이 최신)

- **(2026-09-03 KST) 일반 44종 동결 게이트 신설 지시 (주인 재확인)** — P1 이식 완료 시 **게이트 신설: `docs/perk-redesign.md` 일반 44종의 표시 텍스트·수치 ↔ PLAN §3.1 ↔ sim.js ↔ index.html 4자 대조, 한 글자라도 다르면 exit 1.** 밸런스 회차가 일반 특전을 만지면 이 게이트가 빨개진다. perk-redesign.md 의 일반 절은 주인만 고칠 수 있다(워커 수정 금지).

- **(2026-09-03 KST) ⚑ 밸런스 국면 전역 1워커 규약 (주인 확정)** — **P3(밸런스 루프) 동안은 동시 작업 1개만**: 다른 워커의 lock 이 하나라도 살아 있으면, 새로 깬 워커는 **곁다리 작업(검증·게이트 신설·신규 등재 포함) 일절 없이 즉시 조용히 종료**한다. 앞 세션이 회차를 완전히 마감하고 lock 을 지운 뒤에만 다음 워커가 이어받는다 (루틴 4개의 15분 슬롯은 «빈자리 확인 주기» 역할). 90분 사망 lock 탈취 규칙은 유지(죽은 세션 복구용). P1(이식)·P2(검증)는 기존 규약(단계 lock + 다단계 이어받기) 그대로 — 순차 회차가 생명인 밸런스에만 적용.

- **(2026-09-03 KST) ⚑⚑⚑ ▶ 재가동 — 새 특전 132종 체제로 전환, 비평가 8점까지 (직전 ⏸ 전면 정지 해제).**
  - **특전 정본 = `docs/perk-redesign.md` (일반 44 · 희귀 46 · 전설 42, 신화 폐지·새로고침/전지의눈 삭제).** 작업 순서 (각 단계 = 별도 작업 번호로 등재·lock 선점, **앞 단계 PROGRESS 완료 표시 전 다음 단계 선점 금지**):
    1. **P1 — PLAN §3 교체 + 구 132종 → 신 132종 전면 이식** (sim.js·index.html 동시, 신화 등급·등급 확률·악마 이벤트(전설 확정으로)·👼 계열 정리, 선택지 3개 고정·무료 새로고침 1회 유지, 게이트 전면 갱신. 규모 크므로 T2 방식 다단계 이어받기 허용)
    2. **P2 — 동작 검증** (T3 스위트 재실행 + 신 특전 전수 발동 확인)
    3. **P3 — 밸런스 루프**: 위 «새 132종 밸런싱 방법» 그대로 — ①일반 44 동결 상태에서 적 노브로 일반 10% 영점 ②난이도 동결, 희귀 20%·전설 80% 를 특전 수치로 ③등급 내 폭 <25%p(희귀·전설만 조정) ④사다리 7과녁·진행곡선·F2P 재검증 → **비평가 2인 독립 채점 각 ≥8/10 까지 회차 반복** (채점표는 새 과녁 기준으로 P3 첫 회차가 개정·등재)
  - **동시 작업 규약 재강조 (주인 지시 «서로 안 꼬이게»)**: lock 규약(docs/claims/) 엄수 — 선점 없이 작업 금지, P단계 간 선점 게이트 준수, sim.js·index.html·PLAN 을 만지는 작업은 반드시 단독 lock, 검증-전용 세션은 커밋 없이 종료. push 경쟁 시 rebase 후 자기 lock 생존 확인.

- **(2026-09-03 KST) ⚑⚑⚑ 새 특전 132종 밸런싱 방법 확정 (주인) — 일반이 절대 기준.**
  - **일반 44종은 내용·수치 전부 동결 (주인 확정)** — 밸런스 튜닝으로 일반 특전을 건드리는 것 금지. 킬힐 5% 기준점 조항을 이 조항이 확대 대체한다.
  - **등급 강제 클리어율 과녁 개정: 일반만 = 10% · 희귀만 = 20% · 전설만 = 80%** (종전 15/25/35/45 폐기 — 신화 없음. 전설 80 은 «전설 뜨면 판이 뒤집힌다» 는 주인 의도).
  - **순서**: ① 일반 특전만 뜨는 기준 플레이어(희귀 풀셋)가 해당 챕터에서 **10%** 가 되도록 **적 밸런스(난이도 노브)로만** 영점 조정 (적 ×10 시작점 조항 유지) → ② 난이도 동결 후 **희귀 20% · 전설 80%** 를 각 등급 **특전 수치로만** 달성 → ③ 등급 내 밸런스(폭 <25%p — 일반은 동결이라 측정·보고만, 조정은 희귀·전설만). 노브 교차 금지 유지.
  - 이 지시는 «재적합 순서 확정» 및 «과녁 15/25/35/45» 조항을 대체한다. **실행은 여전히 보류** — 새 132종의 PLAN 반영·구현(T2급) 완료 후, 주인이 밸런스 시작을 지시할 때 착수.

- **(2026-09-03 KST) ⏸ 전면 정지 — 주인이 특전을 대화형 세션에서 직접 재작성 중.** 루틴 4개 비활성화됨. 진행 중이던 세션은 현 작업만 마감하고 lock 해제 후 종료. **T1·특전 수치 반영 작업 전부 보류** — 특전 목록이 통째로 바뀔 예정이라 지금 튜닝은 낭비다. 주인 확정 규칙들(킬힐5 기준·적 ×10 시작점·중복금지·무한중첩·재적합 순서 등)은 유효하며 새 목록 확정 후 그 위에서 재개한다.

- **(2026-09-03 KST) ⚑⚑ 특전 획득 중복 금지 + 버프 중첩 무한 (주인 확정 2건)**
  - **① 한 번 선택한 특전은 그 런에서 다시 등장 금지** — 사실상 전 특전 고유화. «(중첩) = 여러 번 획득» 메커니즘 폐지(획득 수 n 배율 로직 제거, 표기에서 (중첩) 삭제). 풀이 모자라면 가능한 수만큼만 표시(기존 고유 규칙 준용).
  - **② 전투 중 버프 중첩 상한 전부 삭제 — 무한 중첩** («최대 3중첩/5중첩» 전부 제거. 발동될 때마다 계속 쌓임, 각자 자기 시간 끝나면 빠짐). 방어막 장수 상한(5장)도 삭제 — 무한(위임, 같은 취지). ⚠ 이에 따라 신화 📚👑 «축적의 대가(중첩 상한 +5)» 는 대상 소멸 — **삭제**(신화 30종이 됨, 편차 3 문제없음. 대체 신설은 주인이 원할 때만).
  - 획득 중복 금지(하향 요소)와 무한 중첩(상향 요소)이 상쇄 방향이라 **같은 회차에 묶어 반영** 후 측정. sim·게임 동시, 게이트 갱신, 회차 로그 «중복금지·무한중첩 반영».

- **(2026-09-03 KST) 킬힐5기준 재적합 순서 확정 (주인 합의)** — ① **일반 영점**: 특전 체급 재설정 + 적 ×10 시작값으로, «일반만 뜨는 기준 플레이어(희귀 풀셋·챕터30) = 15%» 를 **난이도 노브로만** 맞춘다 ② 난이도 동결 후 **희귀 25 → 전설 35 → 신화 45** 를 각 등급 **특전 확률·수치로만** 순차 달성 ③ 각 등급 내 폭 재분배(<25%p). 한 단계 통과 전에 다음 단계 노브 건드리지 말 것 (노브 교차 금지 — 꼬임 방지). 각 단계 통과를 회차 로그에 표시.

- **(2026-09-03 KST) 킬힐5기준 난이도 재적합 시작점 (주인 지시)** — «적 스탯 10배로 늘려볼까» → **적 HP·DMG 기본치 ×10 을 첫 시도값**으로 놓고 사다리 7과녁(§11.7) 재적합을 시작하라. 10배는 주인 확정 상수가 아니라 **출발점** — 사다리가 성립하는 값으로 자유 조정(과하면 내리고 모자라면 더 올림), 최종 채택값과 10배 대비 어디로 갔는지를 회차 로그에 명시. 특전 읽히는 체급 재설정(킬힐 5% 기준)과 같은 회차에서 묶어서 진행할 것 — 한쪽만 먼저 반영된 상태로 채점 금지.

- **(2026-09-03 KST) ⚑⚑ 밸런싱 방향 전환 (주인 확정) — «처치 시 체력 5% 회복» 을 기준점으로, 특전을 깎지 말고 난이도를 올려라.** 주인 원문: «처치 시 체력 회복 5%로 하고 그거 기준으로 맞추쇼. 그러려면 챕터 난이도를 위로 올려야 하는데 그렇게 해줘야 함. 지금(0.37% 따위) 너무 극단적으로 구림.»
  - **🍖 `c_killHeal2` = «처치 시 체력 5% 회복» 고정 (주인 확정 상수 — 튜닝 노브 아님).**
  - **원칙 전환**: 특전이 세서 밸런스가 안 맞으면 **특전 수치를 소수점(0.37%·0.5%·0.55%)으로 깎는 것 금지** — 특전은 읽히는 체급(수치 5% 단위·확률 10% 단위·기절 3/6초)을 유지하고, 남는 강함은 **적 난이도(구간 성장률·기본치·벽 배수)를 올려서** 흡수한다. 난이도 노브는 §7 위임 그대로.
  - 기존 소수점 특전들(실드 0.5%·전설 0.55%/0.75% 등)도 킬힐 5% 체급을 기준 삼아 **읽히는 값으로 재설정** (전설은 일반보다 좋게 — 확률·수치로 차등).
  - 스탯 사다리 7과녁(§11.7)은 불변 — 난이도 상향은 사다리를 지나는 곡선 재적합으로. 반영 회차부터 «킬힐5기준 반영» 표시, 반영 전 회차 점수는 참고치로 격하.
  - **✅ 1단계 완료 (2026-09-02T20:5XZ · T82 · sess-2036-499 / 워커 C) — 수치 재설정·게이트·실측까지. 난이도 재적합은 T1 몫으로 남았다(T1 lock 이 살아 있어 §7 노브를 건드리지 않았다).** 🍖 `c_killHeal2` 0.37 → **5%** 확정 상수 · 🔰 `c_killShield3` 0.5 → **5%** · 💉 `l_killHeal5` 0.55 → **10%** · 🏯 `l_killShield10` 0.75 → **10%**(전설 = 일반의 2배). 두 엔진·PLAN §3 동시. 게이트 `verifyCombatConst` **⑧** 신설(102항목) — 확정 상수·5% 배수·등급 차등·두 엔진·표시 텍스트 + **`onKill` 실행 단언**, 음성 9/9 exit 1. `verifyNumClean` 래칫 동결분에서 이 4종을 빼 소수점 회귀가 곧장 빨개진다. 실측: 실험1 일반 10.9 → **45.9%** · 전설 64.1 → 76.2 · 사다리 신화 120 이 1.2 → 5.3 으로 **합격 진입(1/7)**. 하니스 재보정(실험1 38.3 → 55.0 · 실험2 62.7 → 70.3, 재선정 불요). **⚑ 남은 절반 = 난이도 상향인데, 균일 상향으로는 과녁이 안 잡힌다** — 처치 회복 축이 일반 2종·전설 2종뿐이고 **희귀 0종**이라 기저 ×1.2 에서 일반 25.3 ↔ 희귀 4.7 로 희귀가 먼저 죽는다. 해법 3안을 **주인 승인 대기 37번**에 등재(추천 = 희귀에 같은 축 1종 신설, 신규 콘텐츠라 승인 전 구현 금지). T1 은 다음 회차부터 «킬힐5기준 반영» 표시. 근거 `docs/balance/T82/raw.md`.**

- **(2026-09-03 KST) 주기형 회복 금지 (주인 확정)** — **«N초마다 체력 회복/실드 수리» 류 시간 경과형 회복은 특전·장비 옵션에 존재 금지** (현행 132종엔 원래 없음 — 예방 조항. 뇌신처럼 시간형 «공격» 은 허용). 게이트 금지어 검사에 추가. 아울러 «이벤트/팝업 열림 중 게임 시간 완전 정지» 는 불변 규약으로 승격 — 회귀 시 게이트가 빨개지게 단언 추가(쉼터 무한 대기 악용 원천 차단). — **✅ 완료 (2026-09-02T19:XXZ · T79 · sess-0336-20926 / 워커 C): 둘 다 «이미 그렇게 돌고 있었다» 를 먼저 확인하고(주기형 회복 0건 — 주기 블록 4개가 전부 뇌신·위압·적 공격 = 허용 축 / 팝업 중 정지도 이미 동작) **예방·규약 게이트만 신설**했다. **엔진 무수정 = 밸런스 영향 0.** `verifyT2` **㊳** — 낱말이 아니라 구조로 본다: 주기 패턴 `X-=dt; if(X<=0){…}` 의 **몸통에 회복·실드 충전이 들어가면 빨개진다**(문구 축은 태그를 걷어낸 뒤 검사 — «3초마다 체력 `<b>`3%`</b>` 회복» 우회 차단). `verifyT2` **㊴** — 오버레이를 켜는 자리가 `openOverlay` 하나뿐이고 거기서 정지 · 루프가 정지 중 `update` 를 안 부름 · `G.t` 를 미는 자리 1곳. T3 `battle.js` **실측 7항목** — 팝업을 띄운 채 800ms 동안 48프레임이 그려져도 시계·체력·실드·쿨다운·적 좌표가 전부 불변, 닫으면 다시 흐른다. 음성 정적 8/8 + **실행 1/1**(루프 가드를 지우면 실측이 빨개진다). PLAN §2.4·§3.0 에 규칙 기재. 근거 `docs/balance/T79/`.**

- **(2026-09-03 KST) 소환 연쇄 임계 규칙 (주인 우려 → 밸런스 강제 조건)** — 연쇄 메커니즘(소환 적중 = 공격 판정)은 유지하되, **«소환 1발의 적중이 낳는 신규 소환 기대값 ≤ 0.8» 을 어떤 특전 조합에서도 유지**해야 한다 (기대값 = Σ 보유 소환 특전의 발동확률 × 발수. 1.0 넘으면 기하급수 폭주 — 도끼폭풍 14개 × 15% = 2.1 이 «신화 뜨면 게임 끝» 의 원인). T1 은 신화 발수 특전(도끼14·화살24·번개20 등)을 이 조건과 신화 과녁 45% 안으로 하향하고, **게이트 신설: 최악 조합(모든 소환+발수 특전 동시 보유) 기대값 ≤ 0.8 단언**. 상세는 이 항목이 정본.

- **(2026-09-03 KST) 전투 무관 특전 전면 삭제 (주인 확정)** — 132종에서 다음 4종 삭제: **🪙 c_gold30(처치 골드 +30%) · 💰 m_gold2(골드 2배) · 🌟 m_sage(처치 경험치 +1) · 🏃 c_walk20(이동속도 +20%)**. sim.js·index.html 동시 삭제, 죽은 코드·게이트 항목 정리. 향후 경제/이속류 특전 추가 금지(흡혈·적중률 금지와 같은 축, 게이트 감시). ※ 🎲새로고침·🔮전지의 눈은 특전 선택의 질 = 전투력이라 유지(위임 — 주인이 빼라면 뺌). 등급별 개수가 일반 31·희귀 33·전설 33·신화 31 로 변함(편차 2, 문제없음). 삭제 후 측정치로만 T1 진행, 회차 로그 «전투무관삭제 반영» 표시. — **✅ 완료 (2026-09-02T19:XXZ · T77 · sess-0336-20926 / 워커 C): 4종을 두 엔진·PLAN §3 에서 동시에 삭제해 **132 → 128종**(일반 31 · 희귀 33 · 전설 33 · 신화 31, 편차 2). `goldMul`·`px.sage`·`walkMul` 은 **장비 옵션이 계속 쓰는 산 코드**라 남겼다(주인 지시는 «특전» 축). 게이트 `verifyT2` **㊲** 신설 — 금지어가 아니라 «특전 ap 가 대입하는 대상이 전부 금지축이면 전투 무관» 이라는 **구조 판정**이라 px 키를 새로 파는 우회도 잡는다(음성 9/9 exit 1, 그중 «m_time 에서 공속을 빼 이속 전용화» 포함). **밸런스 실측**: 실험1 6시드 짝지음 일반 16.55 → **19.07%**(+2.52 ± 0.70) · 신화 80.02 → **81.55**(+1.53 ± 0.86) — 삭제분이 일반 2·신화 2 인데 오른 등급이 정확히 그 둘. 실험2 12,000판 **신화 폭 34.0 → 31.7%p**, 바닥이 `m_gold2` 59.0% → `m_choice4` 62.7% 로 교체(«신화 최하 = 전투 무관 특전» 상태 해소). 사다리 7점은 전후 모두 0/7 로 불변. 게이트 15종 exit 0 · T3 190/190. **주인 검토 1건: 🕰️ `m_time`(공속+이속)은 삭제 목록에 없어 유지 — 이속 성분까지 뺄지 판단 요청.** 근거 `docs/balance/T77/`.**

- **(2026-09-03 KST) ⚑⚑⚑ 주인 최종 결정 — 특전 재설계 전면 폐기, 기존 132종으로 회귀. T1 재개, 1순위 = 등급 내 밸런스.**
  - **아래 «재설계» 계열 지시(신화 등급 폐지·3등급 과녁·73종 조율 15:5X~20차 전부)는 폐기.** 특전은 **현행 132종(신화 33 포함, 신화 11종 삭제 지시도 폐기)** 그대로가 정본 — 코드는 안 바꿨으므로 되돌릴 것도 없다. sim.js·index.html 현행 유지.
  - **T1 즉시 재개, 회차 1순위 = 실험2 «등급 내 밸런스»** (주인 지시: «등급 안 밸런스 맞춰서 하자»): ①측정은 12,000판 이상(노이즈 규명 반영) ②영합 재분배(등급 내 강한 것↓ 약한 것↑, 전체 클리어율 유지) — **신화부터**(폭 35.9%p 최대) ③전투 무관 특전(🪙골드·🎲새로고침)은 바닥 고정이 구조적이므로 수치가 아니라 «전투 보너스를 곁들이는» 소폭 개조 제안까지 허용(주인 검토 등재) ④트리거 빈도 보정(드문 트리거 = 확률/수치 상향).
  - **실험1 과녁 개정(4등급)**: 등급 강제 클리어율 **일반 15% · 희귀 25% · 전설 35% · 신화 45%** (±5%p. 신화 45 는 위임 — 기존 실측 70%는 주인이 «신화 너무 쎔» 이라 했으므로 낮춤. 주인이 바꾸면 그 값으로). 이 과녁이 «단조증가» 기준을 대체.
  - **표기 규칙은 유지**: %p 금지·소수점 금지(0.37% 류는 효과 재설계로 정리)·확률 5~10% 단위·추상 표현 금지. 수치 정리 시 밸런스 등가 유지.
  - 밸런스 루틴 4개 재가동(대화형 세션이 enabled 처리).

- **(2026-09-03 KST) 재설계 20차 — 주인 선택 + 자작 16종 추가**:
  **일반 +5**: 🛡️실드 있는 동안 공격력 +10% · 😤실드 없는 동안 회피 +10% · 🔢4번째 공격마다 공격력 +100% · 🍖회복할 때마다 3초간 방어력 +10% · 💨빗나가면 실드 3% 충전.
  **희귀 +3**: ⛓️기절 끝난 적 3초간 공격속도 -50% · 🎇실드 있으면 공격력 +20% · 😤👑실드 없는 동안 공격력 +50%(주인 원문 «없어지면» — 없는 동안으로 해석, 위임).
  **전설 +8**: 🗡️🌀평타 광역화(대상+바로 뒤) · 🩸체력 20% 이하 적 즉시 처치(**보스 포함** — 주인 명시) · 🦔피격 반사 확정 100% · 🔱⚔️공격 2회마다 창 1개 · 🎭2연속 회피 시 다음 공격 3배 · 🪓🌪️모든 도끼 3회전 3타 · 🎯👑풀피 적에게 데미지 +150% · 🏕️🛡️쉼터 쉴 때마다 방어막 5장(전설 배치 — 위임).
  합계: 일반 24 · 희귀 22 · 전설 27 = **73종** (편차 5). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 19차 — 주인 승인 추가 9종**: 희귀 +4 (🧲 기절 적 데미지 +100% · 🥊 같은 적 연타마다 +10% 누적(대상 바꾸면 초기화) · 🛡️→⚔️ 방어막 소모 시 다음 공격 +100% · 🪞 도끼 1회 튕김) / 전설 +5 (🕳️ 기절 적 데미지 3배 · ⚔️⚔️ 평타 2연타(2타는 50%) · 🌊🔱 검기 8마리 관통화 · 🥶 모든 적 공격속도 -30% 상시 · 🃏 보유 특전 1개당 공격력 +2%). 베이스 48종 + 9 = 57종 (일반 19 · 희귀 19 · 전설 19). 추가 아이디어 90개(등급당 30) 주인 검토 중.

- **(2026-09-03 KST) 재설계 18차 — 주인 자필 초안 확정 베이스 (48종)**: 주인이 직접 쓴 일반 19 · 희귀 15 · 전설 14 목록이 확정 베이스다 (대화형 세션 기록이 정본, 최종 확정 시 PLAN §3 교체). 주인 확인 3건: ①희귀 버프 0개 의도 맞음 ②사신의 낫 = 일반 · 회피 시 20% · **보스 포함** ③전설 «치확 0% 대신 공격력 300%» = 3배. 신규 요소: 오버킬 힐/수리 · 회복↔수리 동기화 · 2초마다 자동 소환 3종 · 화살→창 변환 · 실드 보유 시 50% 데미지 무시 · 힐/수리 효과 +100%. 추가 아이디어(희귀·전설 10개씩)는 주인 검토 중.

- **(2026-09-03 KST) 재설계 주인 수정 17차 — 일반 버프 추가**: «⚡👟 처치 시 2초간 회피 +40%» 를 일반 버프에 추가 (주인 원문 그대로 — 확률 조항 없음 = 처치마다 무조건 발동, 지속 2초로 짧게. 주인이 확률 원하면 추가). 일반 = 16개(버프 4), 희귀·전설 15개, 총 46종. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 16차 — 사신의 낫은 일반 등급**: ☠️🌾(회피 시 10% 즉사, 보스 제외)를 전설 → **일반 기타**로 이동 (일반의 처치 방어막 획득과 맞교환 — 방어막 획득은 전설로 올려 «회피 시 30% 확률로 방어막 1장»). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 15차 — 버프 5→3개씩**: 등급당 = 소환 5 · 버프 3 · 회복 2 · 기타 5 = **15개, 총 45종**. 버프 9개는 트리거·스탯 겹침 최소(엇갈림 유지). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 14차 — 기타 카테고리 발동 조건 전면 재배치**: 기타 15개(3등급×5)의 트리거를 등급 안 5개 전부 다르게 + 전체에서도 같은 트리거 최대 2회(주인 필수 빗나감 2종 제외)로. 기절 사다리 = 공격(일)/치명타(희)/처치(전) — 전설 기절은 «처치 시 가장 가까운 적 기절» 로 이동. 반격 발동 = 화살피격(일)/피격(희). 방어막 획득 = 처치(일)/공격(희). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 13차 — 회복·기타도 트리거 골고루**: 회복 6개(3등급×2)는 트리거 전부 다르게(처치/피격/치명타/회피/반격 + 처치 1회 중복만 허용), 기타도 등급 안에서 트리거 집중 금지(일반 기타가 피격×4였던 것 해소 — 기절은 공격(일)/치명타(희)/피격(전) 사다리로, 방어막 획득은 회피(일)/공격(희)로, 반사는 화살 피격으로 이동). 빗나감 필수 2종이 전설 기타에 같이 있는 것은 주인 지시 산물이라 유지. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 12차** — ① **전설 소환도 전부 1발/1개/1회** (도끼 3개·화살 4발·번개 2회 폐지) — 등급 차이는 **발동 확률**로만 (전설 = 높은 확률·확정). 화살 발수 차등(일1/희2/전2~4)은 이 지시로 개정: 일반 1발·희귀 2발·전설 1발(고확률). ② 💢 빗나감 +100% 스택의 **최대 5개 상한 삭제** (무제한 적립, 적중 1타당 1개 소모는 유지). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 11차 — 버프는 엇갈림 배치**: 공격형 트리거(공격/치명타/처치) → 방어형 스탯(회피/방어) 상승, 방어형 트리거(피격/회피/회복) → 공격형 스탯(공격력/공속/치확/치명뎀) 상승, «공격 시 → 회피 상승» 식 크로스 매칭. 10차의 «조건·스탯 골고루 + 조합 중복 금지» 는 유지. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 10차 — 버프 골고루 매트릭스**: 등급당 버프 5개는 **발동 조건 5개 전부 다르게 + 오르는 스탯 5개 전부 다르게**, 등급 간에도 조합이 겹치지 않게 회전 배치. 스탯 풀 = 공격력/공격속도/치명타 확률/치명타 데미지/방어력/회피 6종, 트리거 풀 = 공격/치명타/처치/피격/회피/반격/빗나감/회복 8종. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 9차 — 최대 체력 증가 특전 전면 삭제**: ❤️최대 체력 +10% 등 최대 체력을 늘리는 특전 금지 (실드 최대치 증가류도 이미 목록에서 빠짐). 빈 자리는 🎯 «체력 가득 찬 적에게 데미지 +20%» 로 대체(위임 — 주인이 바꾸면 교체). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 8차 — 버프도 전부 «n% 확률로» 형식**: 트리거만 있고 확률 없이 무조건 발동하던 버프 특전 전부에 확률 명시(기본 30%). «치명타 시 3초간 +5%» → «치명타 시 30% 확률로 3초간 +5%» 식. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 6차 — 카테고리 구조 확정**: 등급당 = **소환 5 · 버프 5 · 회복 2 · 기타 5 = 17개** (주인 7차 수정: 회복 5→2). 소환 5 = 5종 각 1개(트리거 제각각). 기타 = 기절/반격/반사/방어막/특수. 총 51종. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 5차 — 등급당 20개로 축소 (비율 유지)**: 각 등급 정확히 **20개** = 소환 7 + 기타 13 (직전 소환:기타 ≈ 1:2 비율 유지). 총 60종. 트리거 축 커버리지는 유지하며 중복·유사 효과부터 잘라냄. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 4차 — 소환 특전 3배 확대**: 등급당 소환 특전 **15개(5종 × 트리거 3개씩)**. 같은 소환이라도 트리거가 다르면 별개 특전. 등급 차이는 확률·발수·확정성으로(데미지 고정 불변). **화살 발수도 등급 차등(주인 보강): 일반 1발 · 희귀 2발 · 전설 2~4발.** 등급당 총수가 44~46개로 늘어남(등급 간 골고루 유지). 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 3차 — 소환물 배치 개편**: ① 소환 특전이 «공격 시» 에 몰려 있으면 안 됨 — **트리거(공격/치명타/처치/피격/회피/반격/빗나감/원거리피격)에 분산 배치** ② **일반 등급에도 소환 5종(도끼·화살·번개·검기·창) 전부 등장** — 등급 차이는 데미지(고정)가 아니라 발동 확률·트리거 질·발수로. 확정본은 대화형 조율 중.

- **(2026-09-03 KST) 재설계 주인 수정 2차** — ① «영구» 단어 금지(게임 끝나고도 적용될 것 같은 오해 — 효과는 그 판 한정 그대로, 표기만) ② **시간제 버프는 중첩 상한 없음(무한 중첩)** — «(N중첩)» 표기·로직 전부 삭제 (횟수 소모형 스택(빗나감 +100% 등)의 상한은 유지) ③ 처치 회복/충전은 확정 소량 대신 **«10% 확률로 10%» 식 한방형**으로 (일반: 처치 시 10% 확률 체력/실드 10%, 전설: 20% 확률 15%) ④ 전설 처치 공속도 «10% 확률로 +10%» 식으로 ⑤ 검기 텍스트에서 «대상+뒤 적» 설명 삭제 ⑥ **방어막 장수 상한 삭제(무한)** — 밸런스는 획득 확률로 ⑦ 오발 특전의 «데미지 2배» 삭제 ⑧ 전설 피격 기절 확률 50%→30% ⑨ **삭제 2종**: 방어막 10장+획득 2배 · 기절 지속 2배 ⑩ 희귀 «공속 10중첩» 특전은 무한 중첩 도입으로 무의미해져 삭제. 확정본은 대화형 세션에서 계속 조율 — 최종 시 PLAN §3 교체.

- **(2026-09-03 KST) 재설계 초안 주인 수정 3건 + 개수 명시 규칙** — ① 💠 초과회복 특전: «남는 회복량의 5배 실드 수리» → **«남는 회복량만큼(1배) 실드 수리»** (배수 놀음 금지). ② 🌊🔁 반격 검기: **«검기 1개 발사»** 로 개수 명시 — **일반화 규칙: 모든 소환 특전은 발사 개수를 텍스트에 정확히 쓴다** (도끼 1개·화살 2발·번개 2회·검기 1개·창 1개). ③ 👼: «이후 전설만 등장» → **«이후 희귀 이상만 등장»**. (특전 재설계 확정본은 대화형 세션에서 계속 조율 중 — 최종 확정 시 PLAN §3 전면 교체.)

- **(2026-09-03 KST) ⚑⚑ 특전 재설계 확정 규칙 6개 (주인 확정 — 재설계 지시에 합침).**
  ① **골드 관련 특전 전부 삭제** (🪙 처치 골드 +30% 등).
  ② **즉시 회복 딸린 것 전부 제거** — 최대 체력이 늘어나는 특전은 **최대치만 늘고 회복은 안 됨** (❤️+12%, 💗특전기회마다 등 전부).
  ③ **이동속도 특전 전부 삭제** (🏃 등).
  ④ **소환 데미지 = 원본 스펙 고정 (주인 확정 상수)**: 도끼 = 공격력의 **30%** · 화살 = 2발 각 **50%** · 검기 = **50%** · 번개 = 각 **75%** · 창 = 마리당 **100%**. 튜닝으로 데미지 계수 변경 금지 — 밸런스는 발동 확률(10% 단위)로만 조정.
  ⑤ **기절 지속 = 3초 또는 6초만** (1초/1.5초/2초/2.5초류 전부 3초로 통일. 6초는 «기절 지속 2배» 류 상위 효과의 결과로만).
  ⑥ **방어력·회피 = % 표기·% 의미 (주인 확정)**: 방어력 20% = 받는 데미지 20% 감소, **방어력 100% = 모든 데미지 0**. 회피도 «회피 +40%» 식 % 표기. «방어력 +10» «회피 +40» 같은 단위 없는 표기 전면 금지. 기본 스탯(방어 20%·회피 20%)과 동일 스케일. 상한은 밸런스 몫(위임 — 현행 방어 80·회피 90 유지).
  구현·게이트: sim·게임·PLAN·특전 텍스트 전부 이 규칙으로. 위반 검출을 숫자 청결 게이트에 합칠 것.

- **(2026-09-03 KST) ⚑⚑ 플레이어 기본 스탯 주인 확정 + 밸런스 기준점 + 깔끔한 숫자 규칙.**
  - **플레이어 기본 스탯 (주인 확정 상수 — 노브 아님)**: 공격속도 **1.0**/s · 치명타 확률 **20%** · 치명타 데미지 **150%** · 반격 확률 **20%** · 방어력 **20** · 회피 **20**. (기존 치확5·반격10·방어5·회피8·치배200 전면 교체 — T27 이 지적한 미문서 상수 4종이 이걸로 전부 문서화됨. 공/체/실 기본치는 §11.7 노템 25/150/250 유지.) PLAN §2.3 에 표로 기재, sim·게임 동시 반영, `verifyCombatConst` 갱신.
  - **밸런스 기준점 (주인 확정)**: 실험 하니스의 표준 장비 = **희귀 풀셋** (공 100/체 500/실 800). 특전 실험(실험1·2)은 «위 기본 스탯 + 희귀 풀셋» 플레이어로 측정하는 것이 정본. 측정 챕터는 변별점 규칙대로.
  - **깔끔한 숫자 규칙 (주인 확정 — 표기·튜닝 공통)**: 특전·장비 옵션의 **확률은 10% 단위**(10/20/30…%, 예외적으로 5% 허용) · 지속시간 0.5초 단위 · 수치 계수 5% 단위 · **소수점 금지** (0.37% 같은 값 금지 — 밸런스가 안 맞으면 소수점으로 깎지 말고 효과 설계 자체를 바꿔라). 게이트에 «숫자 청결» 검사 추가(전 특전·옵션 텍스트 파싱). 재설계 초안·이후 튜닝 모두 이 단위로만 움직인다.

- **(2026-09-03 KST) ⚑⚑⚑ 특전 신화 등급 전면 폐지 + 등급별 클리어율 과녁 확정 (주인 확정 — 재설계 지시에 합침).**
  - **특전 등급 = 일반/희귀/전설 3개.** 신화 특전 33종 전부 삭제(11종 부분 삭제 지시를 흡수·확대). **장비의 신화 등급은 별개 — 유지**(스탯 사다리 체계 불변).
  - **⚑ 실험1 과녁 (주인 확정, «단조증가» 기준 대체)**: 한 등급만 뜨게 강제했을 때 클리어율이 **일반 15% · 희귀 25% · 전설 35%** 수준이어야 함 (허용 오차 ±5%p — 위임). **등급 간 격차에 상한이 생겼으므로 «신화 뻥튀기» 류 인플레 튜닝은 구조적으로 불가.**
  - 연쇄 정리(위임 기본값): 등급 등장 확률 3단 재정의 **일반 45% / 희귀 35% / 전설 20%** · 악마 이벤트 = 전설 확정(신화 15% 조항 삭제) · 👼 전설이상만 특전 = 전설 확정으로 개정 또는 삭제(재설계 초안에서 결정) · 등급통일 굴림도 3단.
  - **주인 필수 특전 중 신화에 있던 것들(사신의 낫·방어막 10장·기절 지배 등)은 삭제가 아니라 전설로 강등 배치 후보** — 재설계 초안에서 전설 목록에 넣어 주인 검토.
  - 일반 등급은 주인과 대화형 세션에서 정리 중(기존 유지 골자) — 재설계 초안은 대화형 세션이 주인과 확정한 것을 받아 문서화·구현한다.

- **(2026-09-03 KST) ⚑⚑⚑ T1 밸런스 루프 전면 정지 — 특전 전체 재설계 먼저 (주인 지시: «밸런스 루프 그만 돌리고 특전부터 싹 다 다시»).**
  - **T1 즉시 정지**: 진행 중인 R05 는 측정만 마감하고 lock 해제. 특전 재설계가 끝나고 주인이 승인할 때까지 **T1 신규 회차 금지** (특전이 다 바뀔 거라 지금 튜닝은 낭비다).
  - **신규 최우선 작업 (다음 빈 번호로 등재): 특전 132종 전면 재설계 초안.** 직전 «신화 대숙청» 지시(11종 삭제·대체 11종)를 **흡수 확대** — 신화만이 아니라 **4등급 전부 백지에서 다시 짠다.**
    - **유지할 것**: 주인이 직접 시킨 필수 특전들(스턴 2종 · 빗맞음 확정크리/+100%스택 · 반사 30% · 원거리 피격 반사/번개/창 · 공속 10중첩 · 횟수형 방어막 · 회피 즉사 사신의 낫) + 트리거 축 구조(공격/치명타/처치/피격/회피/반격/회복/빗맞음/원거리피격) + 등급당 개수 골고루(30~40종, 편차 ≤6).
    - **버릴 것**: 주인이 자른 부류 — 소환 개수 뻥튀기(도끼14개류) · 경제류(골드/경험치) · 부활 · 즉시 회복 붙은 스탯 특전. 유사 계열도 초안에 넣지 말 것.
    - **작성 규칙**: 전부 **게임에 뜰 문장 그대로 + 구체 수치** («%p»·«대폭»·«조금» 금지). 등급이 오르면 같은 축의 수치가 왜 그만큼 좋아지는지 한눈에 보이게(축별 사다리형 설계 권장).
    - **절차: 초안 = 문서만.** PLAN §3 은 건드리지 말고 `docs/perk-redesign.md` 에 전체 목록 초안을 쓰고 PROGRESS «주인 검토» 에 등재 → **주인 승인 후에만** PLAN·sim·게임에 반영. 승인 전 엔진 구현 금지 (직전 지시의 «신화 11종 삭제» 는 이미 확정이므로 그 삭제만 선행 실행 OK).
  - 재설계 승인·구현 후 T1 재개 (그때 회차는 R01 부터 다시, «특전재설계 반영» 표시).

- **(2026-09-03 KST) ⚑⚑ 신화 특전 대숙청 (주인 지시 — «신화 대부분이 좆같네»).**
  - **삭제 11종 (즉시)**: `m_revive`(부활) · `m_clone`(분신) · `m_procX2`(폭풍의 힘) · `m_axe3`(도끼 폭풍) · `m_arrow4`(화살 폭풍) · `m_spear200`(창의 대가) · `m_bolt3`(번개 지배자) · `m_wave4`(검기의 왕) · `m_gold2`(황금 기사) · `m_sage`(현자) · `m_fortress`(요새). sim.js·index.html 동시 삭제, PLAN §3.4 표 갱신, 관련 엔진 훅·게이트 항목 정리(죽은 코드 남기지 말 것).
  - **`m_giant`(거인화) 수정**: «즉시 회복» 부분 제거 — 최대 체력 증가만 (회복은 사기라서 주인이 뺌).
  - **남는 신화 21종** → 등급 균형(≤6 편차) 위반 상태가 되므로, **신화 대체 특전 ~11종 초안을 최우선 작성해 PROGRESS «주인 검토» 에 등재** (승인 전 구현 금지 — 삭제만 먼저). 초안 방향: 이미 승인된 축(스턴·빗맞음·방어막·반사·원거리 피격·고중첩 버프·즉사류) 안에서 «빌드가 보이는» 것들로, 삭제된 것 같은 «소환 개수 뻥튀기»·«경제(골드/경험치)»·«부활» 류는 금지. **모든 수치는 구체적으로**.
  - **표기 규칙 (주인 지시)**: «%p» 표기 전면 금지. 방어력·회피·반격·치확 같은 가산 스탯은 평문으로 («방어력 +8» + 첫 등장 시 «받는 피해를 깎는 수치» 식 짧은 설명). «대폭 증가» 같은 추상 표현도 금지 — 특전 텍스트·문서·보고 전부 구체 수치로. 게이트의 금지어 검사에 «%p» 추가.
  - 밸런스: 삭제 후 사다리·하니스 재측정, 회차 로그 «신화숙청 반영» 표시.

- **(2026-09-02 17:2X KST) 횟수형 방어막 특전 추가 (15:5X 확장에 합침)** — «공격 시 10% 확률로 **적 공격 1회를 완전히 막아주는 방어막** 1장 획득» 필수 포함. **횟수형 스택** — 5장 쌓였으면 5번 막음(피격 1회당 1장 소모, 그 타격 데미지 완전 무효 — 수치형 실드와 별개 축). 스택 상한은 넉넉히(주인: «존나 쌓여서» — 위임 기본값 상한 5, 등급 변형으로 10까지 루틴 초안). 버프 아이콘에 남은 장수 뱃지 표시 + 막을 때 전용 이펙트(위임). 상세 PLAN §3.0. — **✅ 완료 (T48 3단계 · sess-0906-23482 / 워커 A): 플레이어 `ward`(장수) 신설 — 피격 1회당 1장 소모하고 그 타격 데미지를 통째로 무효화한다(방어력·실드·체력을 아예 안 거친다). 막은 타격도 «피격 시» 트리거는 굴린다(주인 원문이 «그 타격 데미지 완전 무효» 라 «맞은 사건» 은 일어난 것으로 봤다 — 위임). 획득 4트리거(공격 10%·치명타 12%·피격 8%·회피 10%)가 전부 `gainWard` 한 곳을 거쳐 상한·신화 배수가 한 자리에서 걸린다. 상한 5장 · 신화 🛡️👑 이 10장 + 획득 확률 2배. 버프 줄 맨 앞에 남은 장수 뱃지(`.ward-ic`) + 막을 때 전용 이펙트. 게이트 `verifyT2` ㉕ · `verifyCombatConst` ⑥, 음성 10/10.**

- **(2026-09-02 17:1X KST) 쉼터 선택지 개정 (주인 확정)** — «최대체력 40% 회복 vs 경험치 +10» → **«체력 260 회복(고정값) vs 경험치 +26»**. sim.js·index.html 동시 반영(시뮬은 여전히 항상 경험치 선택), 게이트 갱신, 회차 로그 «쉼터260/26 반영». 상세 PLAN §2.4.

- **(2026-09-02 17:0X KST) 레벨업 필요 경험치 개정 (주인 확정)** — `expNeed = 4+2*Lv` → **`4+4*Lv`** (레벨마다 +4씩 증가. Lv1→8, Lv2→12 …). sim.js·index.html 동시 반영 + 게이트 상수 갱신. 밸런스 유의미(특전 획득 속도 하락) — 회차 로그에 «경험치4증가 반영» 표시. 상세 PLAN §2.4.

- **(2026-09-02 16:5X KST) 회피 즉사 특전 추가 (15:5X 확장에 합침)** — «회피 시 10% 확률로 공격한 그 적 즉사» 필수 포함(보스 제외 위임). **사신의 낫 전용 연출 필수** — 낫이 베어 죽이는 이펙트, 일반 처치 연기와 구별. 등급 상위(신화급 권장) 배치. 상세 PLAN §3.0. — **✅ 완료 (T48 3단계 · sess-0906-23482 / 워커 A): 신화 ☠️🌾 `m_reaper` — 회피 분기 안에서 10%, 보스 제외(🧨 처형자 선례). 사신의 낫 전용 연출 = 큰 낫이 호를 그리며 훑고 → 붉은 참격선 → 검은 재. 일반 처치 연기(`poof`)를 안 부르므로 확실히 구별되고, 게이트가 «reapFx 안에 poof 없음» 을 단언해 되돌리면 빨개진다.**

- **(2026-09-02 16:4X KST) 시뮬 쉼터 정책 확정** — **모든 밸런스 시뮬(실험 1~5·사다리·하니스 포함)에서 가상 플레이어는 쉼터에서 항상 «🌟 경험치» 를 선택**한다 (체력 회복 선택 금지 — 측정 조건 통일). sim.js 의 쉼터 선택 로직을 고정하고 게이트에 단언 추가. 실제 게임(index.html)은 당연히 유저가 자유 선택 — 시뮬 정책일 뿐. 반영 시점을 회차 로그에 «쉼터경험치 반영» 으로 표시(반영 전후 수치 섞지 말 것). PLAN §7 에도 기재.

- **(2026-09-02 16:2X KST) 특전 예시 추가 (15:5X 확장에 합침)** — ① «공격 시 n% 확률 4초 공속 +5%, **최대 10중첩**» 류 고중첩 상위 변형 ② 빗맞음 축 필수 2종: «빗나갈 시 다음 공격 확정 치명타» · «빗나갈 시 **데미지 +100%** 스택 — 적중 1타당 1개 소모, 한 타에 +100% 한 번만(잔여는 다음 타 순차 소모)» **(주인 정정 16:3X: ×2 배수 아님 — 가산 +100%, 다른 데미지 보너스와 합연산)**. 상세 PLAN §3.0. — **✅ 완료: 빗맞음 2종은 T48 1단계(전설 `l_missCrit`·`l_missStack`, 가산 풀로 이행 — 풀피와 겹치면 2.2배·곱이면 2.4배를 게이트가 실측 단언), 공속 10중첩은 T48 2단계(희귀 `r_aspdStack10` 25%·4초·+5%·10중첩 + 신화 `m_stackMaster` 모든 중첩 상한 +5). 워커 A / sess-0906-23482.**

- **(2026-09-02 16:1X KST) 원거리 피격 트리거 신설 (15:5X 특전 확장에 합침)** — 적 원거리 공격(화살)에 맞았을 때 발동하는 축. 주인 예시 3종 필수: 반사 30% · 번개 30% · 창 30%. 일반 피격 트리거와 별개 축(원거리 피격은 둘 다 굴림 — 위임). 상세 PLAN §3.0. — **✅ 완료 (T48 2단계 · sess-0906-23482 / 워커 A): `procOnRanged` 신설, 화살 피격 한 자리에서만 호출. 일반 «피격 시» 트리거를 전부 굴린 «뒤» 에 추가로 굴린다(둘 다 발동 — 게이트가 두 파일 순서를 대조). **회피에 성공하면 굴지 않는다**(주인 원문 «맞았을 때»). 주인 필수 3종 전부 반영 — `r_rangeThorns`(반사 30%) · `l_rangeBolt`(번개 30%) · `m_rangeSpear`(창 30%). 게이트 `verifyT2` ㉓ + `verifyCombatConst` ⑤, 음성 8/8.**

- **(2026-09-02 16:0X KST) 특전 확장 보강 (위 15:5X 지시에 합침)** — ① **반사 계열 확장**: 주인 예시 «피격 시 30% 확률로 해당 적에게 데미지 반사» 필수 포함(기존 🌵 축의 등급별 변형). ② **특전 총수 확장 허용**: 등급당 30~40개까지 OK, 단 **등급 간 개수 골고루**(일반만 많은 편중 금지 — 등급별 개수 편차 게이트 감시, 위임: 최다−최소 ≤6종). 상세 PLAN §3.0. — **✅ ① 완료 (T48 2단계 · sess-0906-23482 / 워커 A): 반사 축이 3단이 됐다 — 일반 🌿 `c_thornsS` 30%/50%(주인 예시 그 자리) · 전설 🌵 60%/150%(기존) · 신화 🌵👑 확정/300%. ② 도 완료 (T48 3단계) — **33/33/33/33 = 132종, 편차 0.** 편차 ≤6 은 `verifyT2` ② 가 상시 감시.**

- **(2026-09-02 15:5X KST) ⚑ 신규 특전 축 2개 — 스턴 · 빗맞음 (주인 지시 = 승인 완료된 신규 콘텐츠)** — ① **스턴 메커니즘 신설**(적 공격 정지 n초, 기절 표시, 보스 지속 1/3 은 위임 기본값) + 주인 예시 2종 필수: «피격 시 적 3초 스턴» «치명타 시 3초 스턴» ② **빗맞음 트리거 신설** — 플레이어 공격이 적 회피 10% 로 빗나갔을 때 발동하는 특전 축(적중률 금지 규칙과 공존 — 빗맞음에서 이득을 얻는 쪽). **루틴이 특전 목록 초안(등급 배분 포함)을 짜서 PLAN §3 표에 추가 + PROGRESS «주인 검토» 등재.** sim·게임 동시 반영, 게이트 갱신. 상세 PLAN §3.0. — **✅ 1단계 완료 (2026-09-02T09:XXZ · T48 · sess-0906-23482 / 워커 A) — 이 지시의 두 축(스턴·빗맞음)만. 같은 T48 에 묶인 나머지 6건(16:0X~17:2X 보강)은 2·3단계 몫으로 남아 있다.** ① **스턴 신설** — 적 객체에 `stun`(남은 초), 스턴 중이면 그 틱 공격을 통째로 건너뛰고 **공격 타이머도 멈춘다**(흐르게 두면 스턴이 «막았다» 가 아니라 «미뤘다» 가 된다 — 위임 근거). 재적용은 **더 긴 쪽만**(합산이면 일반 특전 연타로 영구 스턴락). `applyStun` 한 곳으로 모으고 보스 지속 1/3, 게임은 적 머리 위 💫 + 남은 초. ② **빗맞음(onMiss) 신설** — `procOnMiss` 를 빗맞음이 실제로 일어나는 두 자리(`dealDmg`·`doCounter` 의 회피 분기 = «MISS» 팝이 뜨는 곳)에서만 부른다. ③ **주인 정정(16:3X «×2 배수 아님, 가산 +100%») 이행** — 스택을 `c_firstHit` 과 같은 **가산 보너스 풀**에 넣었다(풀피와 겹치면 2.2배, 곱이면 2.4배 — 게이트가 실측으로 단언). 소모는 적중 1타당 1장. ④ **신규 특전 15종**(일반 3·희귀 3·전설 4·신화 5), **주인 필수 4종 전부 포함**. 총 **102 → 117종**, 등급별 29/29/32/27 = **편차 5**(주인 기준 ≤6). ⑤ **밸런스 실측(사다리 7점 200판, 전→후): 영웅 50 = 7.5→9.0%(합격 유지) · 전설 70 = 21.0→25.5 · 나머지 5점 불변. 합격 1/7 → 1/7 — 통과하던 과녁을 깨지 않았다.** ⑥ 하니스 **재보정 불요**(실험1 39.7→39.0 · 실험2 55.0→56.0, 둘 다 드리프트 허용치 안 · `verifyHarness` 7/7). ⑦ 게이트 2곳 신설(`verifyT2` **㉒** 28항목 · `verifyCombatConst` **④** vm 실행 단언 9항목), **음성 15/15 exit 1**, 전 게이트 12종 exit 0, T3 헤드리스 **125/125** pageerror 0. ⑧ 게이트 보강 — `verifyOptText` 가 «이름 붙은 상수»(`MISS_STACK_CAP` 등)를 값으로 못 풀던 것을 고쳤다. **⚑ T1 재개 시 회차 로그에 «스턴빗맞음 반영» 표시할 것.** 근거 `docs/balance/T48/raw.md`.**

- **(2026-09-02 15:4X KST) 적 회피 10% 고정 + 적중률 스탯 금지 (주인 확정)** — 모든 적은 플레이어 타격을 10% 회피(주인 확정 상수, 노브 아님. 보스 포함·소환/반격에도 적용은 위임 기본값 — MISS 팝 표시). **적중률(명중) 옵션은 게임에 존재 금지** — 특전·장비·버프 어디에도 추가하지 말 것(흡혈 금지와 같은 축, 게이트에 단언 추가). sim.js·index.html 동시 반영 — 실질 DPS −10% 라 밸런스 유의미: 반영 후 수치로만 측정, T1 회차 로그에 «적회피 반영» 표시. 상세 PLAN §2.3. — **✅ 완료 (2026-09-02T08:4XZ · T43 · sess-0820-31023 / 워커 B): ① `ENEMY_EVADE=0.10` 을 두 엔진 최상위 const 로 신설 — `TUNE` 밖에 뒀다(주인 확정 상수 = 노브 아님, 게이트가 «TUNE 안으로 들어오면 빨개짐» 을 상시 감시). ② 주인 명시 3종 전부 적용 — 기본공격·소환 5종은 `dealDmg`/`dealPlayerDamage` 한 곳으로 모이므로 그 자리, 반격은 `doCounter`. 게임은 회피 지점마다 «MISS» 팝. ③ 판정을 치명타 굴림 «뒤» 에 둬서 빗맞아도 `nextCrit`·`nextAtk` 가 함께 소모되게 했다(앞에 두면 두 버프가 서로 다르게 동작한다 — 위임 판단, 근거 PROGRESS 검증로그). **회피 제외 2경로(위임): 가시 반사·오발 화살** — 플레이어가 겨눈 타격이 아니다. ④ **적중률 금지 게이트** `verifyT2` **⑳** 신설 — 상수 일치·적용 3지점·MISS 팝(함수 본문별)·**금지어 전수 검사**(특전 102종 tx · 장비 126칸 d · px 키 · PLAN 표 행에서 «명중/적중률/적중 확률/회피 무시/accuracy/hitRate» 0건)·회피 제외 경로 양쪽 일치, 211/211. `verifyCombatConst` 에 «적 회피율» PLAN↔엔진 대조 추가(24/24). 음성 테스트 **14종 전부 exit 1**(판정 제거 4 · 값 변조 3 · MISS 팝 제거 2 · 금지 특전/옵션 추가 2 · 가시 반사 부착 1 · PLAN 괴리 1 · TUNE 이동 1). ⑤ **실측 회피율 10.246%**(38,189 판정 / 3,913 빗맞음). ⑥ **밸런스 실측(사다리 7점 200판, 전→후): 전설 70 = 15.0% → 5.5% 로 합격 구간(2~10%)에 진입 — 합격 0/7 → 1/7.** 신화 120 = 98.0→94.5 · 영웅 50 = 0.5→1.0 · 나머지 4점 불변. **⚑ T1 재개 시 회차 로그에 «적회피 반영» 표시할 것.** ⑦ `verifyHarness` 는 직전 T31 이 박은 기준선(챕터 6 = 52.7%)이 −10% DPS 로 39.7%(−13.0%p)가 되며 DRIFT_TOL 을 넘겨 빨개졌다 — 정본 ④ 지시대로 `--rebase`(6: 39.7 / 8: 55.0). **하니스 재선정은 하지 않았다**(변별력 ①②③ 5항목 전부 통과). ⚠ 실험2 55.0% 는 목표 밴드 60~70% 의 허용치 하단 경계라 **다음 대형 엔진 변경 때는 기준선 갱신이 아니라 하니스 재선정(일반+7 → +8)** 이 맞다 — T1 인계. **게이트 12종 전부 초록.****

- **(2026-09-02 15:3X KST) ⚑⚑ 소환 적중도 «공격» 이다 — 트리거 연쇄 허용 (주인 확정, 기존 «기본공격에서만» 규칙 개정).** 주인 원문: «공격 시 50% 창이면, 창이 적 때렸을 때도 그 50% 적용. 치명타 시 창이면 창이 치명 뜨면 창 또 나가야 함.» 즉:
  - **소환(창/도끼/화살/번개/검기)의 적중도 «공격 시» 트리거를 굴리고, 소환이 치명타면 «치명타 시» 트리거도 굴린다** — 창이 창을 부르는 연쇄 허용. 특전·장비 옵션 공통.
  - PLAN §4 의 «기본공격에서만 분신/추가타 발동» 규칙은 이 지시로 개정: **분신·추가타·nextCrit/nextAtk 소모만 기본공격 전용 유지(위임 기본값)**, 그 외 공격시/치명타시 트리거는 소환 적중에도 적용.
  - 확률·연쇄에 인위적 제한 금지 (연쇄가 세면 그건 밸런스 수치로 잡는다 — T1 몫). 단 **성능 가드만 허용**: 동시 활성 투사체 상한(예: 200, 초과분은 즉발 판정으로 대체) 같은 프레임 보호 장치(위임).
  - sim.js·index.html 동시 반영, 게이트에 «소환 적중 → 트리거 발동» 단언 추가. **밸런스 영향 큼 — T1 은 반영 후 수치로만 측정** (어차피 R01 재시작). PLAN §4 갱신할 것. — **✅ 완료 (2026-09-02T09:1XZ · T45 · sess-0807-26153 / 워커 A): ① 진단 — «치명타 시» 는 데미지 함수 안에 있어 **소환 적중에도 이미 걸려 있었다**. 빠진 건 «공격 시» 하나뿐(`procOnAttack` 이 기본공격에서만 불렸다). ② 두 파일에 `summonHit()` 신설 — 소환 적중 5경로(번개 즉발·자동번개·관통·단일 투사체) 전부가 트리거를 굴린다. 투사체 생성은 `pushProj()` 로 모았다. 기본공격 전용은 위임대로 nextCrit/nextAtk·분신·추가타 셋만 잔존. ③ 성능 가드 2종 — `PROJ_CAP=200`(초과분 즉발 판정 대체) · `PROC_TICK_CAP=200`(틱당 트리거 예산; 번개 즉발 연쇄의 기대 분기수가 2.0 이라 필요). PLAN §4 에 값·감시처 기재. ④ **밸런스 영향(T45 단독 A/B): 영웅 50 1.5→7.5%(합격 진입) · 전설 70 13.0→41.5% · 신화 120 97→100 → 합격 0/7 → 1/7.** T43·T44 병합 후 현재는 영웅 6.0%✓ · 전설 27.0 · 신화 98.0 (합격 1/7). ⑤ 하니스 재보정(정본 ④) — 같은 날 T43(회피 10%)와 반대 방향으로 밀려 실험2 «일반+7» 이 66.3→55.0(T43)→77.7(T45)→**62.3%(둘 다 반영, 밴드 정중앙)** 로 돌아왔다. 하니스는 일반+7 유지, 기준선만 갱신. ⑥ 게이트 2곳 신설(`verifyCombatConst` ③ 실행 단언 · `verifyT2` ⑳ 두 파일 대조), 음성 7/7 exit 1, 전 게이트 12종 exit 0, 헤드리스 pageerror 0. **⚑ T1 재개 시 회차 로그에 «소환트리거 반영» 표시할 것.** 근거 `docs/balance/T45/raw.md`.**

- **(2026-09-02 15:2X KST) 창 관통 규칙 주인 확정 보강 (T34 후속)** — 창은 **«필드 위에 현재 존재하는(스폰된) 적» 중 일직선 최대 8마리** 관통. **아직 생성/활성화되지 않은 적(다음 웨이브 대기분 등)은 절대 맞지 않는다.** T34 의 `pierce:8` 은 유지하되, 관통 대상 필터가 «활성 스폰 적만» 인지 sim.js·index.html 양쪽에서 확인하고(사거리 88×8 안에 다음 웨이브가 들어오는 경우 포함), 게이트에 «미스폰/대기 적 피격 0» 단언을 추가할 것. 검기 등 다른 관통형도 같은 규칙.

- **(2026-09-02 15:1X KST) ⚑⚑ 승인 25·26번 종결 — 주인 확정: «성장률 표는 필요 없고, 사다리 기준으로».** §11.7 의 **스탯 사다리 7점(챕터·공/체/실 절대값)만 주인 확정 과녁**이고, **구간별 성장률 표는 확정 지위 해제 → T1 자유 튜닝 노브**(T35 초기값 취급). T1 은 사다리 7점이 전부 합격 구간(클리어율 2~10%)에 들어올 때까지 성장률·벽 배수를 조정하라. 승인 25번(하니스 규칙 충돌)은 워커 추천안(변별점 규칙 정본화) 채택으로 함께 종결 — 주석·문서를 실제 규칙에 맞춰 정리할 것. PLAN §11.7 갱신 완료. — **✅ 승인 25번 이행 완료 (2026-09-02T08:5XZ · T31 · sess-0807-26153 / 워커 A): ① 정본 «변별점 규칙» 5개조를 PLAN §7 에 신설하고 `sim.js` 주석 2곳·게이트를 거기에 맞췄다(T5 의 «도달 시점 중앙값» 기준은 개정, 재보정 조항만 존속). ② 정리하다 보니 **개편 후 두 하니스가 모두 클리어율 100.0% 천장 포화 — 실험1·2 는 T35 이후 한 번도 유효하게 측정된 적이 없었다.** 규칙대로 재보정(실험1 «전설·슬롯1» → «일반+2·슬롯0» 52.7% · 실험2 «신화·슬롯0» → «일반+7·슬롯0» 66.3%). ③ 재측정에서 **실험1 사다리가 개편 이후 처음 단조증가**(11.3/39.3/77.7/87.0), 실험2 도 특전 지목이 가능해졌다(T1 R13 «특전 개별 수치» 자료). ④ `verifyHarness` 3축 재편 후 **7/7 통과**(직전엔 위반 5로 빨간 상태), 음성 4/4 exit 1, 전 게이트 11종 exit 0. 근거 `docs/balance/T31/recal.md`. **⚑ T1 은 성장률·벽 배수를 움직인 회차마다 이 하니스를 재보정하고(정본 ④) 사용값을 회차 로그에 적을 것 — 게이트가 ±12%p 드리프트로 감시한다.** (26번·T1 자유 튜닝 몫은 T3 통과 후 T1 이 이어받는다.)**

- **(2026-09-02 15:0X KST) 승인 24번(T34) 종결 — 창 관통 상한은 스펙 준수 수정으로 즉시 진행.** 주인과 확인: 창 = 맞은 적 «한 마리당» 공격력 100% 는 스펙 그대로 유지, 문제는 관통 마릿수뿐. **PLAN §3.3 `l_spear` 원문 «일직선 8명 거리(88px×8) 관통» 의 8마리 상한을 sim.js·index.html 양쪽 엔진에 강제**하라(신화 `m_spear200` 은 데미지 200% 로만 작동, 관통 수 불변). 신규 너프가 아니라 누락된 스펙 구현이므로 승인 불요 처리. 게이트에 «창 관통 ≤8» 항목 추가. — **✅ 완료 (2026-09-02T08:2XZ · T34 · sess-0750-146 / 워커 D): ① `sim.js`·`index.html` 양쪽 `fireSpear` 에 `pierce:8` 을 실었다. ② 관통 판정을 `pr.type==='wave'&&…` 에서 «타입 무관 `pr.hit.size>=pr.pierce`» 로 바꿔 창에도 상한이 걸리게 했다(`m_spear200` 은 데미지 13.5배만, 관통 수 불변 — 게이트가 상시 감시). ③ **추가 파손 1건 발견·보정 — `index.html` 은 관통 판정에 리터럴 `>=2` 를 박아 둬서 `pierce` 를 아예 안 봤다. 신화 «🌊 검기의 왕»(m_wave4, 20명 관통)이 배포 빌드에서 통째로 죽어 있었다**(sim 은 20 으로 도는데 게임은 2 — sim↔게임 괴리). 같은 수정으로 함께 해소. ④ 게이트 2종 신설: `verifyCombatConst` «창 관통 상한»(PLAN §3.3 «일직선 8명» ↔ 엔진 3자 대조, 23항목 전부 통과) + `verifyT2` **⑱** (두 파일 pierce 일치 · `m_spear200` 관통 불변 · 관통 판정이 리터럴 아닌 `pr.pierce` · 창·검기 양쪽 적용, 194/194). 음성 테스트 6종 전부 exit 1(pierce 제거 2 · 리터럴 2 되돌림 · PLAN 불일치 · 두 파일 불일치 · combatConst 교차확인). ⑤ **밸런스 영향 실측(`node sim.js 5` 사다리 7점, 수정 전/후):** 전설 70 **17.0% → 11.5%**(합격 2~10% 쪽으로 이동) · 일반 15 0.5→0.0 · 신화 120 96.5→98.0(200판 노이즈) · 나머지 4점 불변. **합격 점수는 전후 모두 0/7 — 통과하던 과녁을 깨지 않았다.** l_spear 가 전설 특전이라 전설 구간이 가장 크게 움직인 것이 예상과 맞다. **⚑ T1 재개 시 회차 로그에 «창관통상한 반영» 표시할 것.**

- **(2026-09-02 14:4X KST) ⚑ 긴급 핫픽스 — 모바일에서 UI/글씨 축소 문제 (viewport 메타 부재).** 주인이 핸드폰 크롬에서 관측. 현 `index.html` 의 `<head>` 에 `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, viewport-fit=cover">` 추가 — **다음 빈 번호로 등재 후 즉시 선점, 한 줄 핫픽스**(T32 선례). 추가 후 9:19 프레임이 모바일에서 꽉 차는지 CSS 확인(깨지면 그 보정까지가 핫픽스 범위). T2 재작성본에도 필수 포함 + T3 에 모바일 폭 검증 항목 추가. 상세 PLAN §2.1. — **✅ 완료 (2026-09-02T06:1XZ · T40 · sess-0608-25655 / 워커 A): ① `index.html` 최상단에 확정 문자열 그대로 viewport 메타 추가 ② CSS 확인에서 **추가 파손 1건 발견·보정** — `#frame` 이 `100vh` 뿐이라 모바일 크롬에서 주소창 높이만큼 하단 5탭이 화면 밖으로 잘린다(vh = 주소창 숨김 기준 높이). `100dvh` 를 병기(구형 브라우저는 `100vh` 로 폴백). ③ 회귀 방지 게이트 `verifyT2.js ⑫` 신설 — 메타 필수 3항목 + `#frame` 의 `100dvh` 병기를 상시 검사하므로 **T2 재작성본에서 이 한 줄이 사라지면 게이트가 빨개진다**. ④ T3 에 모바일 폭 검증 항목 등재(아래 T3 절). 프레임 폭 실측(계산): 390×844 → 390px(꽉 참) · 360×800 → 360 · 412×915 → 412 · 430×932 → 430. 375×667(SE, 16:9)만 316px 로 좌우 여백이 남는데 9:19 프레임이 16:9 화면에 들어갈 수 없어 정상이다.**

- **(2026-09-02) ⚑⚑⚑ 밸런스 근간 전면 교체 — 주인이 «등급별 절대 스탯» 을 직접 확정. PLAN §11.5-a·§11.7·§2.3·§2.4·§11.4 갱신 완료. 필독.** *(주인 재확인 2026-09-02 14:5X, 대화형 세션 — «내가 지시한 거 맞음». 다른 세션발 지시지만 정본이다. 앵커 A·B·C 폐기 포함 전부 유효.)*
  - **왜**: 종전 설계(앵커 C·A·B 역산)는 신화 풀셋 공격력이 **86억**, 일반 장비가 신화의 **1/5.8억** 이었다. 주인이 원한 건 «신화 풀셋 공격력 1,200» 수준의 읽히는 숫자다. R05~R12 가 실험3 초반 점수를 못 올린 근본 원인(승인 대기 17번)도 이것이다.
  - **① 스탯 사다리 7점이 유일한 과녁** (앵커 A·B·C 폐기): 노템 5 · 일반 풀셋 15 · 희귀 30 · 영웅 50 · 전설 70 · 신화 120 · 신화+9강 260 챕터. 공/체/실 절대값은 PLAN §11.7 표.
  - **② 실드 = 독립 스탯** (`maxSh = maxHp*0.8` 파생 폐지). 장비가 공격력·체력·**실드** 3스탯을 기여한다.
  - **③ 노템 기본치 공30/체300 → 공 25 / 체 150 / 실드 250.**
  - **④ 등비수열 `rarStep 155` 폐기** → 등급별 명시 기여표(PLAN §11.5-a). `plusStep 0.12 → 0.13`.
  - **⑤ 슬롯 `slotG 2.68` 폐기** → **1레벨당 공/체/실 각 +1%** (`1 + 0.01*L`), **레벨 상한 150**. 균등 보너스(5의 배수마다 +5%)는 그대로.
  - **⑥ 난이도 단일 상수 `eHpG 1.185`·`eDmgG 1.08` 폐기 → 구간별 성장률** (PLAN §11.7 표. 7.18% → 0.55%). 승인 대기 7번 종결.
  - **⑦ 벽 배수(10·15·90·300)는 주인이 튜닝에 위임.** 종전 ×3.6 은 새 곡선에서 83챕터분이라 성립 불가 — T1 에서 재산정. 챕터 300 콘텐츠가 아직 없으므로 급하지 않다.
  - **⚠ 신화 체력만 주인 표의 2000 → 2385 로 조정했다.** 2000 이면 «전설 +9강» 체력이 «신화 0강» 을 따라잡아 주인 확정 제약(T29)이 깨진다. 근거 PLAN §11.5-a.
  - **⚠ T1 회차 R01~R12 전부 무효** — 재개 시 **R01 부터 다시** 센다. 승인 대기 **7·9·11·14·16·17·18번은 전제 소멸** — 재등재 필요 여부부터 판단할 것.
  - **⚠ T35 완료 전까지 `node tools/verifyPlanConst.js` 는 실패한다** (불일치 1 = `plusStep` PLAN 0.13 ≠ 엔진 0.12 · 미검출 3 = `atkUnit`/`hpUnit`/`rarStep` — 셋 다 이번 개편으로 **폐기된 상수**라 PLAN 에서 사라진 것이 정상이다). **이걸 «PLAN 이 틀렸다» 로 보고 PLAN 을 엔진에 맞춰 되돌리지 말 것** — 엔진을 PLAN 에 맞추는 것이 T35 다. T35 가 게이트의 해당 3항목을 «등급별 표 대조» 로 교체하면서 해소한다. — **✅ 해소 (2026-09-02T05:5XZ · T35 · sess-0520-18737 / 워커 B): `verifyPlanConst` 74/74 통과.** `atkUnit`/`hpUnit`/`rarStep`/`slotG` 4항목을 제거하고 «등급별 기여표 15칸 + 구간별 성장률 12칸» 표 대조를 신설했다. `plusStep` 은 엔진이 0.13 으로 올라가 일치. **다만 T35 완료 게이트인 «사다리 7점» 은 1/7 만 통과했다 — 승인 대기 26번**(확정 성장률 표가 «플레이어 힘 선형» 전제라 후반이 물러진다). `verifyHarness` 는 이 개편으로 빨개졌고(T36) T1 이 `eBase*` 재적합 후 `--rebase` 할 몫이다.
  - **⚠ T2 진행 중인 워커는 그대로 계속하라** (구조 우선·수치 임시라는 14:0X 지시 그대로). 새 수치는 **T35 → T1** 이 넣는다.

- **(2026-09-02 14:2X KST) 챕터 레이아웃 확정 제약** — 챕터당 **적 총 수 ≤100 · 쉼터 1~4 · 악마 =1 · 천사 =1 (정확히 하나씩)**. 기존 가중치(45/30/25) 배치 폐기 — 악마1·천사1 선배치 후 남는 슬롯 쉼터, 순서만 시드 셔플(위임). `chapterLayout` 수정 대상: sim.js 와 T2 이식분 둘 다. 매 챕터 악마·천사가 보장되므로 밸런스에 유의미 — T1 재개 시점엔 이미 반영돼 있어야 하고(현재 T1 정지 중이라 회차 무효 이슈 없음), 반영 커밋을 PROGRESS 에 기록. 상세 PLAN §2.4. — **✅ 완료 (2026-09-02T05:5XZ · T2 2단계 · sess-0508-7315 / 워커 A): `sim.js`·`index.html` 양쪽 `chapterLayout` 동시 반영 — 가중치(45/30/25) 삭제, 악마1·천사1 선배치 + 남는 슬롯 전부 쉼터(`clamp(waveCount-3,1,4)`) + 시드 셔플, 적 상한 `LAYOUT_MAXENEMY=100`. 챕터 1~300 전수 위반 0(적 40~60마리·쉼터 1~2·악마 1·천사 1), 두 파일 노드열 전수 동일. 게이트 `verifyT2.js` ⑧ 신설로 상시 감시. ⚑ T1 재개 시 회차 로그에 «레이아웃제약 반영» 표시할 것 — 실험1 바닥이 올라갔다(일반 9.0/희귀 44.3/전설 79.7/신화 86.7%).**

- **(2026-09-02 14:0X KST) ⚑⚑⚑ 작업 순서 전면 변경 — 구현·동작 검증이 먼저, 밸런스는 그 다음 (주인 원문: «특전을 반영 안 하고 어떻게 밸런스를 잡나. 쉴드도 그렇고. 동작 검증까지 하고 밸런스 잡아야지»).** 시뮬만 앞세우고 게임 파일을 뒤로 미룬 기존 순서를 폐기한다. **새 순서: T2 → T3 → T1.**
  - **T2 즉시 선점 가능 (지금 최우선)** — «T1 완료 후» 조건 삭제. **⚑ 주인 보강(14:1X): T2 내부 착수 순서도 «챕터 300 구조부터»** — 챕터 1~300 존재·해금·클리어 처리(시드 생성 상한 300)를 먼저 넣고, 그 다음 특전/장비/UI 순으로. 난이도 수치는 이식 시점 sim.js 값 임시 탑재(곡선 확정은 T1 재개 후 몫). **현시점 sim.js 수치 그대로**(T6 통합본) index.html 전면 이식: 특전 102종·장비/뽑기/합성/슬롯/상점·챕터 300·모든 ⚑ 지시(등급통일·보스킬 특전스킵·실드 폐지·보스 ×8·버프 아이콘·미리보기 줄·흡혈 제거·순차 연사)·`docs/ref/` 스크린샷 기준 UI. 규모가 크므로 **여러 세션에 걸쳐 이어받기 허용** — T2 lock 을 잡은 세션은 진행 상황을 PROGRESS T2 행에 남기고 lock 해제, 다음 워커가 이어서.
  - **T3 — T2 완료 즉시** 동작 검증 (기존 스펙 + 장비/상점/연출 확인 항목 추가).
  - **T1 은 T3 통과 후 재개.** 재개 후 규칙: **회차에서 확정한 수치는 같은 커밋에서 index.html 에도 동시 반영** — sim 과 게임이 다시는 벌어지지 않게. 공유 상수 동기화 게이트(`tools/verifySync.js` 급) 신설(위임). 이후 밸런스는 «실제 게임과 항상 일치하는 sim» 위에서 잡는다.
  - 진행 중인 T1/측정 회차는 현 회차만 마감하고 lock 해제. 기존 R01~R12 결과·게이트·발견(T24 등)은 폐기가 아니라 T2 이식 검증 자료로 쓴다.

- **(2026-09-02 08:5X KST) ⚑⚑ 긴급 핫픽스 — 구버전 index.html 실드 무료충전 즉시 제거 (T2 기다리지 마라).** 주인이 현 배포 빌드에서 웨이브 전멸 실드 충전을 또 관측했다(지시 `2116ab1` 은 T2 대기 중이라 게임 파일 미반영). **다음 빈 번호로 PROGRESS 등재 후 즉시 선점, T1 보다 우선.** 내용: ① `index.html:897` 부근 웨이브 클리어 `p.sh=p.maxSh` 제거(워커 B 가 위치 지목해 둠) ② 구버전에 레벨업 풀충전이 있으면 그것도 제거 ③ 그 외 구버전 대개편은 하지 말 것(T2 몫) — 이 두 줄만. push 시 GitHub Pages 자동 배포로 주인이 바로 확인 가능. 완료 시 이 항목에 완료 표시. — **✅ 완료 (2026-09-02T04:5XZ · T32 · sess-0448-26595 / 워커 B): ① `index.html:897` 웨이브 전멸 `p.sh=p.maxSh` (+사운드·«실드 충전!» 문구) 제거 ② 레벨업 풀충전은 구버전에 **원래 없었다**(전수 확인 — `gainExp`/`openPerkChoice`/`takePerk`/`afterPerk` 어디에도 회복·최대치 보정 없음). 남은 `p.sh=p.maxSh` 2곳은 특전(«실드 최대치 +30% 즉시 충전» · 부활 50%)이라 유지. sim.js 와 동일 상태(sim 에는 웨이브 충전 로직 없음).**

- **(2026-09-02 08:3X KST) 다연발 소환 = 순차 연사 (구별되되 존나 빠르게)** — 화살/번개/도끼 등 여러 발 나가는 소환은 동시 스폰 금지, 50~70ms 간격으로 다다다닥 순차 발사(발마다 낱발이 구별돼 보여야 함 — 주인 보강. 발마다 판정·사운드). T2 연출 스펙 — sim 무관, 회차 무효 없음. 상세 PLAN §4.

- **(2026-09-02 08:2X KST) UI 참고 스크린샷 등록 — `docs/ref/*.jpg` 10장 (원본 이름 그대로).** T2/T6 의 모든 UI 는 이 스크린샷을 참고해 같은 구도·스타일로 만들 것: 로비(`메인로비.jpg`) · 인게임(`메인 게임화면.jpg`, `메인 게임화면_적발견.jpg`) · 특전(`perks.jpg`, `perks 뭐뭐 있는지.jpg`) · **장비/상점 신규 4장**(`캐릭터 장비.jpg`, `장비 세부팝업.jpg`, `장비 합성 업글창.jpg`, `상점 (1).jpg`, `상점 (2).jpg`) — §11 장비 탭·합성·상점 UI 는 이 4~5장이 기준. 이미지를 Read 도구로 직접 열어 보고 작업하라. **`docs/ref/` 는 주인이 넣은 참고 자료라 «캡처 PNG 커밋 금지» 규칙의 예외다 — 워커는 이 폴더에 추가·수정·삭제 금지.**

- **(2026-09-02 08:1X KST) 앵커 C 추가** — **전설(차상위) 풀셋 0강(위임) + 슬롯 균등 10렙 → 챕터 30 겨우 클리어.** 벽 아님, 곡선 고정점. 실험5 는 이제 C(30)·A(90)·B(300) 3점 측정. 상세 PLAN §7·§11.7.

- **(2026-09-02 08:0X KST) 앵커 B 추가 — 챕터 300 확장** — **신화 풀셋 +9강 + 슬롯 균등 50렙 → 챕터 300 겨우 클리어**(301+ 벽). 챕터 수 100→**300**, 벽 10·15·90·300. 실험5 는 앵커 A(90)·B(300) 둘 다 측정. T6 통합 시 처음부터 300 기준으로 구현할 것. 상세 PLAN §2.4·§7·§11.7.

- **(2026-09-02 07:5X KST) ⚑⚑ 장비 시스템 전면 신설 — PLAN §11 (필독).** 등급 5·부위 6·종류 3(18종), 뽑기(400다이아, 신화 0.1%·50회 천장·전설 10회 피티), 합성(3→1 등급업, 전설은 +강, +10강→신화, 신화 무한강+3/6/9강 옵션), 슬롯 강화(골드, 균등 보너스), **영구강화 4종 폐지**(§2.2 — 07:4X 강화상한 티어 지시도 대상 소멸로 폐기), 챕터 100 확장·90 벽, 다이아 2500/일·IAP 12,000. 이에 따라:
  - **T6 신설 (T1 보다 우선, lock ID = T6)**: ① sim.js 에 장비·뽑기·합성·슬롯·다이아 경제·챕터 100 통합 ② 18계열 옵션표 초안 작성 → PLAN §11.6 기입 + PROGRESS «주인 검토» 등재 (기존 엔진 동사만 재사용, 신규 메커니즘 금지) ③ §7 실험4·5 하니스 구현 ④ 완료 후 T1 재개 조건 충족.
  - **T1 은 T6 완료까지 신규 회차 시작 금지** (영구강화 폐지로 현 경제 튜닝 축이 소멸 — 강화 비용 성장률 계열 결과는 폐기물이다. 진행 중 세션은 현 회차만 마감하고 lock 해제).
  - **T1 채점표 v2 (T6 후 적용, 총 10점·두 비평가 모두 ≥8 합격)**: 실험1 사다리 2점 · 실험2 등급 내 1점 · 실험3 진행 곡선(1~20 구간 기존 기준 유지, 21+ 구간 목표는 T6 이 제안해 §7 에 기록) 3점 · 실험4 장비 진행 2점 · 실험5 챕터 90 앵커 2점.
  - **T2 범위 확대**: 장비 탭(장착/합성/슬롯)·상점 탭(다이아 모의 구매·뽑기 연출)·저장 포맷 교체 포함.

- **(2026-09-02 07:4X KST) 영구 강화 상한 티어 시스템** — 강화 4종 각각 max 10렙, 4종 전부 상한 도달 시 상한 +10 (10→20→30…, 무한). 수식 `상한 = 10*(1+floor(min(4종 레벨)/10))`. sim.js 실험3 자동구매가 이 상한을 따라야 함 — **반영 전 T1 회차 무효, 로그에 «강화상한 반영» 표시.** 저장 포맷 `up:{}` 은 그대로(레벨만 저장, 상한은 파생값). T2: 강화 팝업에 MAX·해제 조건 표시. 상세 PLAN.md §2.2.

- **(2026-09-02 07:3X KST) T5 승인 (주인 위임)** — 실험1 하니스 강화 6렙→**9렙**, 실험2 8렙→**11렙**. 원칙: 하니스 강화 레벨 = 실험3 관측 «그 챕터 도달 시점 중앙값 영구강화 레벨», 경제 노브를 바꾼 라운드마다 재보정하고 사용값을 회차 로그에 기록. 승인 대기 2번(T5) 안건 종결 — T5 행 완료 처리. 상세 PLAN.md §7.

- **(2026-09-02 07:3X KST) 보스 = HP ×8 · DMG ×1.8 고정 (주인 확정 상수 — 직전 ×3 지시를 주인이 ×8 로 정정)** — `bossHp 12 → 8`, `bossDmg 1.8 유지(고정 승격)`, 5배수 챕터 `bigBoss ×1.5` 폐지. 둘 다 튜닝 노브 아님. 승인 대기 1번 안건 종결. **밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «보스8배 반영» 표시 후 재채점** («보스3배 반영» 으로 이미 돌린 회차가 있으면 그것도 무효). 상세 PLAN.md §6. T2 이식 시 index.html 도 동일.

- **(2026-09-02 07:1X KST) 흡혈 증가 특전 금지** — 흡혈 스탯을 올리는 특전은 전면 배제(발동형 «n% 확률 체력 회복» 류는 유지). 102종 목록엔 원래 없음 — sim.js 밸런스 영향 0, **T1 회차 무효 아님.** 실 작업은 T2: 구 index.html 의 흡혈 특전 3종(🩸🩸🧛, 대략 `index.html:482,491,512`) 제거 + 스탯 그리드 «흡혈» 항목 제외(7종). 상세 PLAN.md §3.0.

- **(2026-09-02 07:0X KST) 인게임 UI 2건 — T2 스펙 (sim 무관, 회차 무효 없음)**: ① **발동 중 버프 아이콘** — 왼쪽 위(챕터 아래)에 발동 중인 시간제 버프의 특전 아이콘 + 등급색 테두리 + 중첩 수 뱃지(아이콘 우상단). ② **얻은 특전 미리보기 줄** — Info 버튼 행 왼쪽 끝부터 획득 순서대로 아이콘 나열(등급색 포함, 중복은 개수 뱃지). 상세 PLAN.md §2.3. T2 이식 시 구현하고 T3 에서 두 UI 의 표시/소멸 확인 항목 추가. — **✅ 완료 (2026-09-02T06:2X~07:0XZ · T2 4단계 · sess-0624-31490 / 워커 B): ① `#buffBar` 신설 — `addBuff` 에 표시 전용 6번째 인자(`src` = 그 버프를 준 px 키)를 더하고 호출부 28건 전부를 자기 가드 키로 넘기게 고쳐 «어느 특전의 버프인가» 를 알게 했다(px→특전 매핑은 `takePerk` 의 ap 전후 px 비교로 자동 생성). 챕터 표시 아래·왼쪽에 세로로, 등급색 테두리 + 중첩 2 이상일 때 오른쪽 위 개수 뱃지, 오래된 것이 위, 만료 시 제거. 장비 옵션발 버프는 특전이 아니므로 스탯 기본 아이콘으로 폴백. ② `#perkStrip` 신설 — Info 행 왼쪽 끝(실측 `strip.left = hudFoot.left = 12px`), 획득 순서·등급색·중복 개수 뱃지, 폭을 실측해 넘치면 최신 것만 남기고 «+N»(390px 20종 → «+14 + 6칩»). 천사의 축복도 같은 줄. **`sim.js` 무수정 — T1 회차 무효 아님.** 게이트 `verifyT2.js ⑬` 신설(114/114), 음성 테스트 18종 전부 exit 1, 헤드리스 pageerror 0. 같은 단계에서 **T36(챕터 300 이동 UI)도 해소** — ◀▶ 길게누르기 가속(1.6초에 19챕터) + 제목 탭 → 이동 팝업(숫자 입력·±10·최신 N).**

- **(2026-09-02 06:5X KST) 웨이브 전멸 실드 충전 폐지** — 구 스펙 «웨이브(보스 제외) 전멸 시 실드 완전 충전» 을 주인이 폐지했다(PLAN §2.3 취소선 처리). 실드 충전은 특전으로만. **sim.js 의 해당 로직 제거 — 반영 전 T1 회차 점수 무효, 로그에 «실드무료충전폐지 반영» 표시.** T2 이식 시 index.html 도 동일.

- **(2026-09-02 06:4X KST) 레벨업 = 특전 선택뿐** — 레벨업 시 체력·실드 풀충전 금지(주인이 현 빌드에서 관측한 구버전 동작), 최대 체력·최대 실드 증가도 금지. 특전 선택지만 띄운다. 상세 PLAN.md §2.4. **sim.js 에 레벨업 회복/보정이 있으면 제거 — 밸런스 영향 큼: 반영 전 T1 회차 점수 무효, 로그에 «레벨업무보정 반영» 표시.** T2 이식 시 index.html 도 동일.

- **(2026-09-02 06:3X KST) 챕터 종료 보스 킬 = 특전 스킵** — 보스 경험치 +9 는 유지하되, 챕터를 끝내는 보스 처치로 레벨업이 되어도 특전 선택지·레벨업 팝업 없이 바로 클리어 화면으로. 상세 PLAN.md §2.4. sim.js·index.html(T2) 둘 다 반영 (밸런스 영향은 미미 — T1 회차 무효 아님, 반영 시점만 로그에 표시).

- **(2026-09-02 06:2X KST) 특전 선택지 등급 통일** — 특전 선택지가 뜰 때 등급을 1번만 굴리고 3개(전지의 눈 4개) 전부 같은 등급으로. 섞어서 금지. 상세는 PLAN.md §3.0 에 반영해 둠. **T1 진행 세션은 다음 회차 전에 sim.js 에 즉시 반영할 것** (선택지 분포가 바뀌므로 반영 전 회차의 점수는 무효 — PROGRESS 회차 로그에 «등급통일 반영» 표시). T2 이식 시 index.html 도 동일 로직.

## 0. 세션 시작 절차 (모든 워커 공통)

1. `git fetch && git checkout -B main origin/main` (pull --rebase 금지, 로컬 잔재 위에서 작업 금지)
2. SID 발급: `sess-HHMM-$RANDOM` (예: sess-0512-23481)
3. `PLAN.md` → `docs/ROUTINE.md` → `docs/PROGRESS.md` → `docs/claims/` 순서로 읽는다.
4. 아래 [2. 작업 목록]에서 **선점 가능한 가장 앞 작업**을 lock 으로 선점한다 (규약: `docs/claims/README.md`).
5. 선점할 작업이 없으면(전부 lock 또는 전부 완료): 마지막 완료 작업의 게이트를 재실행해 검증만 하고, 이상 없으면 **커밋 없이 조용히 종료**한다. 이상이 있으면 PROGRESS 에 등재하고 종료.

## 1. 절대 규칙

- **새 콘텐츠(특전/시스템/수치 체계) 임의 추가 금지.** 추가하고 싶으면 PROGRESS 에 «주인 승인 대기» 로 등재만 하고 넘어간다. (위임 규약: 밸런스 *숫자* 조정은 PLAN §7 범위 내에서 즉시 채택·진행하되 되돌림 가능하게 기록)
- 승인 프롬프트가 뜨는 명령을 피하고, 대화형 편집기(`git rebase -i` 등) 금지.
- 캡처 PNG·대용량 바이너리 커밋 금지. 증거는 md 의 수치로.
- 작업이 끝나면 lock 삭제 → PROGRESS 갱신 → 커밋 → push. **push 실패 시 `git pull --rebase=false` 말고 `git fetch && git rebase origin/main`** 후 재push (자기 lock 이 사라졌으면 진 것 — 작업 버리고 종료).
- 아티팩트 republish 는 **루틴 금지** (대화형 세션 전용 — PLAN §9-3). GitHub Pages 활성화도 주인 몫(PLAN §9-2). 루틴은 PROGRESS 에 «준비 완료» 표시까지만.

## 2. 작업 목록 (순서 고정 — lock ID = 아래 번호)

### T35 — sim.js 를 주인 확정 스탯 체계로 이식 (T1 재개 전 선행 · T2/T3 과 병렬 선점 가능 — sim.js 만 건드린다)
PLAN §11.5-a·§11.7 확정값을 `sim.js` 에 그대로 옮긴다. **역산·재해석 금지 — 표의 숫자를 그대로 쓴다.**
1. 기본치 `pAtk0 30 → 25` · `pHp0 300 → 150` · **`pSh0 250` 신설** (실드가 `maxHp*0.8` 파생이던 곳 전부 제거 — 독립 스탯).
2. `GT.atk/hp` 의 `rarStep` 등비 생성 폐지 → **등급별 명시 배열 3종**(공/체/실, PLAN §11.5-a 표) 하드코딩. `plusStep 0.12 → 0.13`.
3. `GT.slotMul = L => 1 + 0.01*L` (`slotG^L` 폐기) · **슬롯 레벨 상한 150** 강제 · 균등보너스를 실드에도 적용.
4. `TUNE.eHpG`/`eDmgG` 단일 상수 폐기 → **구간별 성장률**(PLAN §11.7 표)을 쓰는 `enemyStats(c,w)` 로 교체.
5. **벽 배수는 임시값**으로 두되(종전 3.6 은 새 곡선에서 83챕터분이라 그대로 쓰면 안 됨) 고른 값과 근거를 PROGRESS 에 남긴다. 확정은 T1 몫.
6. 완료 게이트: `node sim.js 5` 를 **스탯 사다리 7점**(노템 5 · 일반 15 · 희귀 30 · 영웅 50 · 전설 70 · 신화 120 · 신화+9강 260)으로 갈아끼워, 각 과녁 챕터 클리어율 2~10% 를 표로 기록. 못 맞추면 그 수치를 PROGRESS 에 등재.
7. `tools/verify*.js` 를 새 상수 체계에 맞게 갱신 — 특히 `verifyPlanConst`(PLAN↔엔진 대조)와 `verifyGearEcon`(T29 제약을 «등비 조건식» 이 아니라 «등급별 표 대조» 로 교체).

### T1 — 밸런스 튜닝 루프 (비평가 2인 · 8점 게이트) ← ~~최우선~~ **순서 변경(2026-09-02 ⚑⚑⚑): T3 통과 후 재개**
> **⚠ 스탯 사다리 개편으로 R01~R12 전면 무효 — 회차는 R01 부터 다시 센다. 선행 작업 T35 완료가 전제.** 이 루프에 **벽 배수(10·15·90·300) 재산정**이 포함된다(주인 위임).
목표: `sim.js` 밸런스를 **비평가 2명이 각각 10점 만점에 8점 이상** 줄 때까지 조정.

한 회차(라운드) 절차:
1. `node sim.js 1` / `node sim.js 2` / `node sim.js 3` 실행, 원시 출력 저장 (`docs/balance/round-NN/` 에 md 로. NN = 회차 번호).
2. **비평가 2명을 Task(Agent) 도구로 각각 독립 실행** (서로의 결과를 보여주지 말 것). 각 비평가에게 주는 것: ①아래 채점표 ②이번 회차 시뮬 원시 출력 전문. 받는 것: 항목별 점수 + 총점(10점 만점) + 감점 사유 + 구체적 조정 제안.
3. 두 비평가 모두 **총점 ≥ 8** 이면 합격 → T1 완료 처리. 아니면:
4. 감점 사유를 근거로 `sim.js` 의 `TUNE` / 특전 수치 / 강화 비용 성장률 / 골드 드랍을 조정 (PLAN §7 의 노브만. 특전 개별 수치는 실험1·2 근거로만).
5. **조정한 수치를 PLAN.md §6·§3 에도 갱신 기록** (PLAN 의 요구사항).
6. 회차 결과를 PROGRESS 의 T1 행에 기록 (회차 번호 · 두 점수 · 무엇을 얼마로 바꿨는지 1줄씩) 하고 커밋·push. 다음 회차로.
- 세션 시간이 다 되어가면: 현재 회차까지 커밋·push, lock 삭제 후 종료 (다음 워커가 이어받는다).
- 세션이 90분을 넘기게 되면 lock 내용의 타임스탬프를 갱신해 push (탈취 방지).

**채점표 (비평가용, 총 10점):**
- 실험1 등급 사다리 (3점): 클리어율이 일반<희귀<전설<신화 로 단조증가하면 3점. 인접 역전 1쌍당 −1.5점.
- 실험2 등급 내 균형 (2점): 각 등급 내 (표본 ≥25판 특전 중) 승률 최상−최하 차가 전 등급 25%p 미만이면 2점. 25%p 이상인 등급 1개당 −1점.
- 실험3 진행 곡선 (5점): 챕터1~5 각 1~2회(1점) · 6~9 각 2~5회(1점) · **10이 10회 이상 & 400회 이내(2점 — 벽의 핵심)** · 11~19 각 3~10회 & 20이 10~30회(1점). 구간별로 어긋난 챕터가 있으면 해당 점수를 비례 감점.
- 비평가는 점수 외에 «가장 효과 클 조정 노브 1~2개» 를 반드시 제안할 것.

### T2 — index.html 이식 (~~T1 완료 후에만 선점 가능~~ **순서 변경(2026-09-02 ⚑⚑⚑): 지금 최우선 — 현 sim.js 수치로 이식, 상단 지시 참조**)
- T1 이 확정한 최종 수치·특전 102종·챕터 20개 구조를 PLAN §2~§6·§8 스펙대로 `index.html` 에 전면 이식. 메테오 등 구버전 잔재 완전 제거.
- `sim.js` 와 수식이 1:1 일치해야 함 (검증: 대표 수식 5개 이상을 grep 으로 대조해 PROGRESS 에 기록).
- 완료 게이트: `node --check` 급 문법 검사 + 특전 102종 전수 존재 확인 스크립트(`tools/verifyT2.js` 신설, 통과 수 기록).

### T3 — 동작 검증 (T2 완료 후)
- 가능하면 Playwright(`npx playwright install chromium` 후)로 헤드리스 부팅 → 콘솔 에러 0 · 로비 렌더 · 전투 진입 · 레벨업 특전 3택 노출까지 확인. Playwright 불가 환경이면 jsdom/정적 검사로 대체하고 그 사실을 기록.
- **⚑ 모바일 폭 검증 (주인 지시 14:4X · T40 에서 등재)**: 뷰포트 **390×844**(및 360×800)로 열어 ① 글씨·UI 가 축소되지 않는다(로비 `CHAPTER N` 실측 폰트가 CSS 의 36px 그대로) ② `#frame` 이 화면 폭·높이를 꽉 채운다(390×844 에서 폭 390px) ③ **하단 5탭이 잘리지 않는다** — 주소창 표시 상태(작은 뷰포트)에서도 `#bottomNav` 가 뷰포트 안에 들어온다. 정적 게이트는 `node tools/verifyT2.js` ⑫ 가 대신 보지만(메타 + `100dvh` 병기), **실제 렌더 확인은 T3 몫**이다.
- **⚑ 인게임 UI 2건 표시/소멸 확인 (주인 지시 07:0X · T2 4단계에서 등재)**: ① **발동 중 버프 아이콘** — 실제 전투에서 시간제 버프가 발동하면 `#buffBar` 에 아이콘이 뜨고(등급색 테두리·중첩 2 이상이면 개수 뱃지), **지속시간이 끝나면 사라지는지**. 챕터 표시(`#chapHud`)와 겹치지 않는지도 볼 것 — 4단계에서 실제로 2px 겹쳐서 한 번 고쳤다. ② **얻은 특전 미리보기 줄** — 특전을 얻을 때마다 `#perkStrip` 에 획득 순서대로 늘어나는지, 중복은 개수 뱃지인지, 많이 얻으면 «+N» 으로 접히고 **최신 것들이 보이는지**, 좁은 폭(360px)에서 줄이 넘치지 않는지. 정적 게이트는 `verifyT2.js ⑬` 이 보지만 **실제 발동/소멸 확인은 T3 몫**이다.
- 발견 버그는 PROGRESS 에 번호 등재(T3-1, T3-2 …) 후 이 세션 또는 다음 워커가 수리.

### T4 — 배포 준비 (T3 완료 후)
- 최종 커밋·push 확인, PROGRESS 에 «주인 할 일» 정리: ① GitHub Pages 설정(https://github.com/kuzuni/aaaw/settings/pages, Branch=main, /(root)) ② 아티팩트 republish 는 대화형 세션에 요청.

### 신규 작업 등재
- 버그·후속 작업 발견 시 PROGRESS 표에 **이미 쓰인 번호 중 가장 큰 것 +1** 로 등재 (번호 재사용 금지, 한 번호 = 한 작업).

## 3. PROGRESS.md 기록 규약

- 표의 자기 작업 행을 갱신: 상태(진행중/완료/대기) · SID · 워커 · 마지막 회차 · 핵심 수치.
- T1 은 회차 이력을 표 아래 «T1 회차 로그» 절에 한 줄씩 누적 (`R03 | 7/6.5점 | wallHp 1.6→1.75 | sess-… A`).
- 완료 시 반드시: 게이트 결과 숫자(예: 비평가 8.5/8점) + 커밋 해시.
