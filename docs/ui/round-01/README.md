# T116 · U01 회차 — 채점표 · 사유 · 반영 내역

- **회차**: U01 (첫 회차) · **워커**: D · **SID**: `sess-1150-20326` · 2026-09-04T11:51~12:3XZ
- **채점 대상 스크린샷**: 회차 «시작 시점» 의 `index.html`(HEAD `a92d56f` 코드 = `69bdfab` 와 동일) 을 390×844 로 찍은 9장.
  PNG 는 규약대로 레포 밖(`T3_OUT`)에만 있다 — 이 폴더에는 **수치와 글만** 남긴다(ROUTINE §1 PNG 커밋 금지).
- **자**: `docs/ui/ref-layout.md`(이번 회차가 신설) + 하니스가 낸 `layout.json`.
- **두 비평가 총점: 1.6 / 10 · 1.6 / 10** → 합격선(각 ≥ 8) 미달 → **다음 회차로 이어진다.**

## 1. 비평가 점수 (묶음별 · 각각 독립 실행 · 서로의 결과를 보지 않았다)

| 묶음 | 배점 | 비평가 1 | 비평가 2 |
|---|---|---|---|
| 로비 | 2 | 0.4 (3/16) | 0.3 (2/15) |
| 인게임 | 2 | 0.4 (4/18) | 0.5 (4/17) |
| 장비 탭 + 세부 팝업 | 2 | 0.2 (2/22) | 0.1 (1/21) |
| 상점 | 1 | 0.1 (1/12) | 0.2 (2/11) |
| 대장간/합성 | 1 | 0.1 (1/12) | 0.0 (0/10) |
| 특전 | 1 | 0.0 (0/12) | 0.0 (0/13) |
| 공통 레이아웃 | 1 | 0.4 (3/7) | 0.5 (3/6) |
| **총점** | **10** | **1.6** | **1.6** |

두 비평가는 서로 모르는 채로 **같은 총점·같은 원인 진단·같은 «가장 효과 큰 수정 2개»** 를 냈다.

## 2. 감점 사유 (두 비평가 공통 요지)

- **로비 (가장 심함)** — 세로 리듬이 통째로 어긋났다. 챕터 카드가 «폭 44.5 · 높이 13.7(가로가 1.25배)» 여야 하는데
  «폭 62 · 높이 27.3(거의 정사각)» 이라 카드 하나가 화면의 27% 를 먹고, 그 여파로 제목(y 11.6 ↔ 27.2)·밑줄·좌우
  화살표·START 가 전부 자리를 잃었다. START 는 폭 74 로 «카드와 같은 폭» 규칙을 깼다. 이벤트 배너·좌우 사이드
  아이콘 열·보조 2버튼 줄이 통째로 없어 화면 중·하단 40%p 가 비어 보인다.
- **인게임** — x·w·h 는 이미 레퍼런스에 붙어 있는데 **y 만 일제히 4.3~4.4%p 위**였다(패널 65.1 ↔ 69.5).
  즉 «자리는 맞고 높이만 틀린» 상태라 고치기 가장 쌌다. 인포 버튼이 «작은 정사각(w10)» 이 아니라 폭 22.4 의 알약.
- **장비** — 무대가 19.5%(레퍼런스 26.5)로 눌리고 슬롯이 «폭 14 정사각» 이 아니라 폭 27.8 의 넓은 가로 카드.
  **세부 팝업은 팝업이 아니었다** — 박스 경계·등급 배지 없이 화면 전체에 항목이 흩뿌려진 형태.
- **상점** — 레퍼런스의 뼈대인 **3열 상품 그리드**가 없고 1열 스택이었다.
- **대장간** — 무대가 41% → 19.7% 로 반토막이라 그 아래가 전부 16~17%p 위로 밀렸다. 자동/합성이 «좌우 끝(28/27.5)»
  이 아니라 화면을 반씩(46/46) 나눠 먹었다. 뒤로 버튼이 폭 17 이 아니라 전폭.
- **특전** — 카드 1장의 폭·높이는 거의 맞는데 y 가 6%p 아래. 하단 줄이 «가운데 큰 버튼 + 오른쪽 작은 정사각» 형태가 아님.
- **공통** — 상단 바가 **같은 게임 안에서도** 달랐다(로비 h7.6 / 나머지 h5.0 · 레퍼런스의 y3.7 띄움 없음).
  장비 탭과 대장간의 인벤 그리드 y 가 5.6%p 어긋났다.

## 3. «가장 효과 큰 수정 2개» — 두 비평가가 **똑같이** 지목했다

1. **인게임 하단 패널을 4.4%p 내린다** (`y 65.1 → 69.5 · h 34.9 → 30.5`, 바 줄·스탯 그리드·배속 버튼이 따라 내려감)
   — 한 번의 이동으로 7개 요소가 동시에 오차 안으로 들어온다(점당 비용 최소).
2. **로비 챕터 카드를 레퍼런스 비례로 되돌리고 START 를 카드에 맞춘다**
   (`카드 x27.9 y41.0 w44.5 h13.7` · `START x27.9 y70.7 w44.5 h7.0` · 제목 y27.2 · 화살표는 카드 바깥의 작은 삼각)
   — 제목·밑줄·좌우 화살표·카드·START 6개가 한꺼번에 통과한다.

## 4. 이번 회차에 실제로 고친 것 (전부 `index.html` 화면 — 게임 수치·규칙 0줄)

두 제안을 먼저 넣고, 남은 시간에 두 비평가가 «다음 순위» 로 든 것들까지 반영했다.

| # | 화면 | 고친 것 | 실측 결과(회차 종료 시점) |
|---|---|---|---|
| ① | 전체 | `#frame` 에 **`--fh`(프레임 높이) 변수** 신설 — 세로 여백을 «프레임 높이의 %» 로 쓸 수 있게 (CSS 의 margin % 는 폭 기준이라 그대로는 비례를 못 쓴다) | 이후 모든 세로 수치의 기반 |
| ② | 인게임 | 하단 패널 30.6%(69.4~100) · 바 줄 y70.2 · 스탯 그리드 y74.2 · 배속 버튼 y65.0 · 인포 버튼을 **작은 정사각**(x87.5 w9.4 h4.0) | 레퍼런스 69.5/70.5/75.0/65.0/85·10·4.0 — **7개 전부 ±1%p 안** |
| ③ | 로비 | 카드 44.5×13.7 @ y40.3 · START 44.5×6.7 @ y69.8(카드와 같은 x) · 제목 y26.5 · 밑줄 y30.4 · 화살표 6.7×4.3 @ x17.5/75.8 · 상단 바 y3.6 · 탭바 h7.2 | 레퍼런스와 **최대 1.0%p** 차 |
| ④ | 로비 | 이벤트 배너·좌우 사이드 아이콘 열(3+3)·보조 2버튼 줄을 **형태만** 신설(전부 🔒 잠금 · 기능 없음) | ref 자리 그대로 |
| ⑤ | 장비 | 무대 26.8%(y7.0) · 슬롯을 **정사각**(11.6% 폭) 좌우 끝(x8.5/79.9) · 캐릭터 29.9% · 인벤 칸 비율 1.18 · 그리드 x3 w94 @ y46.3 | ref 26.5 / 14 / 8.5·77.5 / 30 / 1.18 / 47.5 |
| ⑥ | 대장간 | **재화 줄 표시 끔**(레퍼런스에 없다) → 무대 y0 h40.0 · 액션바 y40.0 · 자동/합성을 **좌우 끝**(x2.0 w26.9 / x71.1 w26.9) · 뒤로 버튼 폭 17% · 인벤 그리드 y45.9(장비 탭과 같은 자리) | ref 41 / 42 / 2·28 / 70·27.5 / 17 / 47.5 |
| ⑦ | 상점 | 상품 그리드 **2열 → 3열** · 카드 «폭 30.0 · 높이 18.5»(0.75 비율) · 무료 보급을 한 줄 띠(y11.2 h6.3)로 · 맨 위 헤더 접음 | ref 30 / 18.5 / 12.5·6.5 |
| ⑧ | 팝업 전체 | 오버레이를 **레퍼런스의 «상자»** 로 — 폭 87.0 · 좌우 여백 6.5 대칭 · 윗변 y24.4 · 높이 43~47 | ref 장비팝업 6.5/28/87/44 · 특전 인포 6.5/23/87/52.5 |
| ⑨ | 특전 | 배너 폭 55.7(ref 60) · 부제 y30.9(31.5) · 카드 y35.3(36.5) · 보유 특전 버튼을 **작은 정사각**(w8.8 h4.0) | 카드·배너·부제 전부 ±1.5%p |
| ⑩ | 공통 | 상단 바를 **모든 탭에서 같은 자리**로(y3.7 · 로비=장비=상점) · 탭바 h 9.4 → 7.2 | ref 3.7 / 7.4 |

### 곁다리로 잡은 실제 결함 2건 (레이아웃 변경이 드러낸 것 — 둘 다 수리 완료)

- **버프 아이콘이 챕터 표시를 파고들 뻔했다** — `#chapHud` 를 고정 64px 에서 «프레임 높이 10.8%» 로 옮기자
  고정 114px 인 `#buffBar` 와의 간격이 42px 아래로 내려갔다. `#buffBar` 도 같은 식(`--fh*.108 + 46px`)에 얹어
  프레임 크기와 무관하게 «챕터 블록 아래» 가 유지되게 했다. 게이트 ⑬ 이 이 차이를 실제로 잡아냈다.
- **특전 미리보기 줄이 1px 넘쳤다** — Info 버튼이 «작은 정사각» 이 되며 줄이 69% → 82% 로 넓어지자,
  `renderPerkStrip` 의 «+N 칩도 28px» 가정이 드러났다(«+15» 는 34px). 접을 때 그 자리를 44px 로 잡게 고쳤다
  (T3 battle «줄이 자기 폭을 넘지 않는다» 가 잡았다).

### 갱신한 게이트 단언 5개 (전부 «뜻은 그대로, 리터럴만»)

레이아웃 값을 바꾸면 그 값을 **문자 그대로** 읽던 단언이 빨개진다. 아래 5개는 지키는 성질을 그대로 두고 표기만 넓혔다
— **어느 것도 «검사를 없앤» 것이 아니다**(넓힌 이유를 각 자리에 주석으로 남겼다).

| 게이트 | 종전 | 지금 | 지키는 성질(불변) |
|---|---|---|---|
| `verifyT2` ⑬ | `#chapHud{top:NNpx}` 정규식 | `top:NNpx` **와** `calc(var(--fh)*.NNN + NNpx)` 를 둘 다 px 로 환산해 비교 | 버프 아이콘이 챕터 블록보다 **42px 이상 아래** |
| `verifyT2` (7) | `.lobby-top` padding **12px** 리터럴 | 세로 여백은 자유 · `gap 6` · 좌우 `10px * --tf` 는 그대로 | 좁은 프레임에서 줄이 안 넘친다 |
| `verifyT2` (7) | `#avatar` width **52px** 리터럴 | `width:calc(… var(--tf))` | 아바타 치수가 **--tf 배율에 걸려 있다** |
| `verifyT2` ① | `.top-bar .pill` font **14px** | `1[34]px` | pill 이 --tf + `min-width:0` 을 갖는다 |
| `verifyT2` ⑬ | `slice(order.length-(cap-1))` | `slice(order.length-(?cap` | 넘칠 때 **앞쪽이 아니라 최신** 이 남는다 |
| `t3/boot` | 로비 제목 **36px** | **24px** | 좁은 폰에서 글씨가 **프레임에 연동해 줄지 않는다**(고정 px) |

## 5. 게이트 결과 (회차 종료 시점)

- 정적 게이트 **18종 전부 exit 0** (`verifyT2` **411/411**)
- T3 헤드리스 **4스위트 219/219** — boot 84 · battle 52 · gear 64 · fx 19 · **pageerror 0**
- **밸런스 무수정 증명**: `git diff --numstat sim.js` = **0줄** · `index.html` 변경분에 `TUNE`·`expNeed`·`RANGED_*`·
  `PERK_*`·`GT.*`·`eBase*`·`wall*`·`maxChapter` 를 건드린 줄이 **하나도 없다**(전부 CSS·마크업, 예외는
  위에 적은 `renderPerkStrip` 의 접기 폭 계산 1곳 = 표시 로직).

## 6. 다음 회차(U02)가 볼 것 — 남은 큰 어긋남

1. **장비 세부 팝업의 내용이 상자보다 길다** — 상자를 레퍼런스 규격(높이 ~44~48%)으로 묶었더니 기여표+슬롯강화+옵션
   7줄이 상자를 넘어 **스크롤**된다(버튼줄이 y82.7 로 상자 밖). 레퍼런스는 «스탯 2줄 + 옵션 5줄 + 비용 + 버튼» 이 상자
   안에 다 들어간다 — 줄 높이·묶음을 줄여 **상자 안에 담는 것**이 U02 의 첫 과제다.
2. **특전 획득창의 하단 버튼 y** — 레퍼런스는 y79 인데 상자 높이(43%)에 묶여 y52.6 이다. 레퍼런스의 선택창은
   **상자가 없고** 카드가 화면에 바로 놓인다(인포 팝업만 상자다) — 두 팝업의 상자 유무를 갈라야 한다.
3. **특전 획득창 카드 폭** — 상자 안쪽 여백 때문에 79.8%(레퍼런스 89). 위 2와 같은 원인.
4. **대장간 재료 줄** — 3칸(36.4%)이라 레퍼런스의 단일 슬롯(17%)보다 두 배 넓다. 칸 크기를 줄이거나 형태를 바꿀 것.
5. **인게임 적 대열** — 적이 화면 오른쪽 끝에 몰려 플레이어와 47%p 떨어져 있다(레퍼런스는 첫 적이 x27 근방).
   이것은 캔버스 그리기(카메라)라 CSS 가 아니다 — 손대려면 `drawWorld` 의 카메라 오프셋이다.
6. 아래 «주인 승인 대기» 2건은 워커가 임의로 못 넘는 벽이다.

## 7. ⚑ 주인 승인 대기 (레이아웃만으로는 못 넘는 것 — T116 ④ⓐ)

- **인게임 스탯 그리드가 4행×2열 8칸이 안 된다** — 레퍼런스는 8칸(마지막이 «Lifesteal Chance»)인데,
  게임은 **주인 지시(2026-09-02 07:1X «흡혈 특전 전면 배제 → 7종»)** 로 흡혈 행을 뺀 7칸이고 게이트 ⑰ 이 그 7을 단언한다.
  8칸을 채우려면 **주인이 흡혈 행을 되살리거나 다른 8번째 스탯을 정해 주셔야** 한다. 워커는 손대지 않았다.
- **상점 상품이 1종뿐이라 3열 그리드가 한 칸만 찬다** — 레퍼런스는 젬 6종 + 골드 3종 = 9칸이다.
  칸의 규격·열 수는 맞췄으나(폭 30.0 · 높이 18.5 · 3열) **상품을 늘리는 것은 새 콘텐츠**라 §1 금지축이다.
  주인이 «상품 종류를 늘려도 된다» 고 하시면 그 자리는 바로 찬다.
